# A8 — ⚙️ Build, config y deploy

> **Estado:** ✅ completada · **Owner/Sesión:** fable5-675614-a8 · **Inicio:** 2026-07-09 11:02 · **SHA auditado:** `61f74e5`
> **SHA de referencia del framework:** `bcc7f1e`
> **Protocolo:** seguir al pie de la letra el "Protocolo por sesión (lock atómico)" y las "Reglas de evidencia" de `docs/audits/00_INDEX.md`. Read-only sobre el código de la app: solo se escribe en este MD y en el INDEX.

---

## 1. Objetivo

Verificar que el proyecto **compila, tipa y lintea limpio**, que su **configuración declarada** (env vars, `vercel.json`, `next.config.js`, `tsconfig.json`, `drizzle.config.ts`, `package.json`) coincide con lo que el código realmente consume y con lo que la documentación promete, y que los supuestos de **deploy en Vercel Hobby** (cron cada 12h, `maxDuration = 300`) son compatibles con los límites reales del plan. Detectar defaults peligrosos, vars sin documentar, vars fantasma, y ausencia de tooling (lint/tests).

## 2. Contexto mínimo (sesión fría)

- **App:** "Party Time!" — Next.js 14 App Router + TypeScript + Drizzle ORM + Neon PostgreSQL + Resend. **Producción con datos y usuarios reales.**
- **Hosting:** Vercel plan **Hobby**, deploy **automático on push a `main`** (no hay CI intermedio: lo que pushea, se despliega).
- **Límites de Hobby relevantes a esta auditoría** (verificar contra doc oficial vigente durante la ejecución — cambian con el tiempo):
  - **Cron jobs:** Hobby históricamente limita cantidad de crons por proyecto y la **frecuencia de invocación** (documentado en su momento como "una vez al día"). El proyecto declara `0 */12 * * *` (cada 12h) — ítem 8 del checklist.
  - **Duración de funciones serverless:** Hobby tiene un default bajo y un máximo por plan; `maxDuration = 300` (5 min) está declarado en 2 endpoints — ítem 9. El fix `cc195f8` (no duplicar emails por timeout de cron) nació precisamente de un timeout, lo que sugiere que el límite efectivo fue menor a 300s en algún momento.
- **Build local:** requiere `RESEND_API_KEY` aunque sea dummy: `RESEND_API_KEY=re_dummy npm run build` (ver PRE-4).
- **Migraciones:** existe carpeta `drizzle/` con SQL generado (`0000_add_og_image_url.sql`, `0001_add_rsvp_closed.sql`) **y** scripts `db:push` (push directo sin migración) — ítem 13.

## 3. Scope

**Incluye:**
- `next.config.js`, `vercel.json`, `tsconfig.json`, `drizzle.config.ts`, `package.json` (scripts y dependencias).
- Build (`npm run build`), typecheck (`npx tsc --noEmit`), lint (`npm run lint`).
- Inventario exhaustivo `process.env.*` en `app/`, `lib/`, `scripts/` vs `.env.example` vs `SETUP_GUIDE.md`/`README.md`.
- Defaults/fallbacks de env vars con efecto en producción (URLs en emails, secrets con default).
- `app/manifest.ts` + íconos (`app/icon.tsx`, `app/apple-icon.tsx`, `app/icon-192/route.tsx`, `app/icon-512/route.tsx`).
- Compatibilidad de `vercel.json` (cron) y `maxDuration` con el plan Hobby.
- Ausencia de tests automatizados.
- Flujo real de schema DB: `db:push` vs migraciones generadas.

**NO incluye (out of scope):**
- Contenido/exactitud editorial de los MDs de root (`README.md`, `SETUP_GUIDE.md`, `ADMIN_GUIDE.md`, etc.) más allá de la tabla de env vars → **A5** (código/documentación muerta o duplicada).
- Modelo de auth, protección de endpoints y sesiones → **Fase S**. Las menciones a `CRON_SECRET` en este MD son constatación factual de existencia del check, sin evaluación.
- Lógica de negocio de emails/reminders → **A1**.

## 4. Checklist ejecutable

> Todo ítem requiere evidencia (`archivo:línea` o comando + output), también cuando PASA. Ítems no ejecutables → `⏭️ NOT RUN` + razón.
> Ejecutado 2026-07-09 11:02–11:20 sobre `61f74e5`. No existe `.env.local` en el working copy (verificado: `ls .env*` → solo `.env.example`), así que los builds locales corren sin env del proyecto salvo lo inyectado por comando.

| # | Verificación | Cómo verificar (comando exacto) | Resultado | Evidencia |
|---|--------------|--------------------------------|-----------|-----------|
| 1 | `npm run build` termina limpio (exit 0, sin errores; warnings anotarlos) con env dummy | `cd /Users/bob/TimeKast/partytime && RESEND_API_KEY=re_dummy npm run build` | 🟩 PASA | Ejecutado 2x: `✓ Compiled successfully`, 15/15 páginas estáticas, **exit 0**. Warnings: 9× `⚠️ DATABASE_URL not configured` (`lib/db.ts:9`, esperado sin `.env.local`) y 1× `⚠ Using edge runtime on a page currently disables static generation` (rutas edge de íconos/OG). Ningún error de tipos ni de compilación |
| 2 | **PRE-4:** `npm run build` SIN `RESEND_API_KEY` falla — confirmar reproduciendo y anotar el error exacto | `cd /Users/bob/TimeKast/partytime && env -u RESEND_API_KEY npm run build` (no existe `.env.local`, no hubo que renombrar nada) | 🟥 CONFIRMADO | **exit 1**. Compila OK (`✓ Compiled successfully`) y revienta en "Collecting page data": `Error: Missing API key. Pass it to the constructor \`new Resend("re_123")\`` → `Build error occurred: Failed to collect page data for /api/admin/send-bulk-email`. Causa raíz: `lib/resend.ts:7` (`new Resend(process.env.RESEND_API_KEY)` a nivel de módulo). El warn de `lib/resend.ts:3-5` sí se imprime pero no evita el crash del constructor → **A8-01** |
| 3 | `npx tsc --noEmit` limpio (tsconfig ya tiene `strict: true`, `tsconfig.json:7`) | `cd /Users/bob/TimeKast/partytime && npx tsc --noEmit` | 🟩 PASA | **exit 0**, cero output. `tsconfig.json:7` `"strict": true` confirmado |
| 4 | ¿ESLint está configurado? | `ls -a \| grep -i eslint ; grep -c '"eslint' package-lock.json ; npm run lint` con kill a 20s | 🟥 CONFIRMADO | Sin config: `ls -a \| grep -i eslint` → vacío (exit 1). `grep -c '"eslint' package-lock.json` → **0**. `grep '"eslint' package.json` → sin match (devDeps `package.json:33-41`: solo types/dotenv/drizzle-kit/tsx/typescript). `npm run lint` ejecutado: entra al **prompt interactivo** `? How would you like to configure ESLint?` (Strict/Base/Cancel) — matado sin seleccionar; verificado que no creó config (`git status` limpio, re-`ls` sin eslintrc) → **A8-02** |
| 5 | **Tabla env vars** (sección 4.1): confirmar cada fila con grep, marcar faltantes/sobrantes en `.env.example`, `README.md:49-107`, `SETUP_GUIDE.md:36-98,186-194` | `grep -rn "process\.env\." app lib scripts` + `cat .env.example` + `vercel env ls` (solo nombres) | 🟥 CONFIRMADO con hallazgos | Inventario: 15 vars distintas en código (grep completo, ver 4.1). `.env.example` tiene 7 (todas usadas — no hay sobrantes). Faltantes en `.env.example`: `CRON_SECRET`, `NEXT_PUBLIC_BASE_URL`, `ADMIN_EMAIL`, `BLOB_READ_WRITE_TOKEN` (fila nueva, ver 4.1). **Runtime (`vercel env ls`, solo nombres):** 13 vars en Vercel — incluye `BLOB_READ_WRITE_TOKEN` y las 4 legacy `GOOGLE_CLOUD_*`/`FIRESTORE_COLLECTION_NAME`; **NO** incluye `NEXT_PUBLIC_BASE_URL` ni `ADMIN_EMAIL` → **A8-05/A8-08/A8-09** |
| 6 | **Default peligroso — URLs en emails:** `NEXT_PUBLIC_APP_URL \|\| 'http://localhost:3000'` en el cancelUrl que viaja dentro de emails reales | `grep -rn "NEXT_PUBLIC_APP_URL" app lib --include="*.ts" --include="*.tsx"` | 🟥 CONFIRMADO (latente) | 6 sitios confirmados con línea exacta: `app/api/admin/send-bulk-reminder/route.ts:126`, `app/api/admin/send-email/route.ts:86`, `app/api/admin/send-bulk-email/route.ts:107`, `app/api/rsvp/route.ts:105`, `app/api/cron/send-reminders/route.ts:144`, `lib/email-template.ts:92`. **Mitigante runtime:** `NEXT_PUBLIC_APP_URL` SÍ está seteada en Vercel (Development/Preview/Production, `vercel env ls`) → el fallback NO está activo hoy; severidad 🟡 latente → **A8-06** |
| 7 | **Var duplicada / inconsistente:** `NEXT_PUBLIC_APP_URL` (fallback localhost) vs `NEXT_PUBLIC_BASE_URL` (fallback `https://party.timekast.mx`) | `grep -rn "NEXT_PUBLIC_BASE_URL" app lib ; grep -n ... .env.example README.md SETUP_GUIDE.md` | 🟥 CONFIRMADO | 4 sitios: `app/page.tsx:11`, `app/layout.tsx:5`, `app/[slug]/layout.tsx:15`, `app/api/og-image/[slug]/route.ts:119`. Ausente de `.env.example`/`README.md`/`SETUP_GUIDE.md` (grep exit 1) **y ausente de Vercel** (`vercel env ls`) → en producción los 4 sitios viven del fallback hardcoded `'https://party.timekast.mx'` (que coincide con el dominio prod — `get_project` domains: `party.timekast.mx`). No son intencionalmente distintas: mismo concepto "URL pública", fallbacks opuestos (localhost vs dominio prod) → **A8-05** |
| 8 | **Cron en Hobby:** `vercel.json` declara `0 */12 * * *` (cada 12h, UTC). ¿Hobby lo acepta? | Doc `vercel.com/docs/cron-jobs/usage-and-pricing` (fetched 2026-07-09, last_updated 2026-06-16) + estado real vía Vercel MCP + cruce A1 | 🟥 CONFIRMADO (contradicción doc/realidad) | `vercel.json:2-7` declara el cron. **Doc vigente:** Hobby = "Minimum interval: **Once per day**", precisión "Per-hour (±59 min)", 100 crons/proyecto; warning textual: *"Cron expressions that would run more frequently will fail during deployment"*. **Empírico:** el deploy de HOY con esa expresión está READY (`get_deployment dpl_HRfx3UovHTtpgNUwnHZHfCyYgvM3`, commit `9b06ced`, target production, sin errores de build) y **A1 verificó con runtime logs de producción** ejecuciones `GET /api/cron/send-reminders 200` a las **00:01 y 12:01 UTC** (`01_emails_recordatorios.md` ítem 8) → el cron corre 2×/día pese al límite documentado. Funciona hoy fuera de spec → **A8-04**. Nota CLI: `vercel crons ls` no existe en la versión instalada del CLI ("not a valid subcommand") — constatación por MCP + logs |
| 9 | **`maxDuration = 300` en Hobby:** ¿permitido o truncado? Relacionar con fix `cc195f8` | `grep -rn "maxDuration" app --include="*.ts"` + doc `docs/functions/configuring-functions/duration` + `git show cc195f8 --stat` | 🟩 PASA (con matiz) | 2 sitios confirmados: `app/api/cron/send-reminders/route.ts:17` y `app/api/admin/send-bulk-reminder/route.ts:18` (= 300). **Doc vigente (last_updated 2026-07-01):** con Fluid Compute (*"enabled by default"*) Hobby tiene Default **300s** y Maximum **300s** → declarar 300 es válido (y redundante: igual al default). El fix `cc195f8` ("prevent duplicate reminder emails on cron timeout", 2026-02-27, `git show --stat`) nació de un timeout bajo el modelo previo con límite menor; el lock anti-duplicado sigue vigente (verificado en A1 ítem 1). Sub-check "¿Fluid Compute activo en ESTE proyecto?": ⏭️ NOT RUN — no inspeccionable vía CLI/MCP disponible (`get_project` no lo expone); si estuviera OFF, el límite clásico Hobby (60s) truncaría el 300 y el riesgo de timeout persiste — mitigado por `cc195f8` |
| 10 | **Constatación factual (→ Fase S):** check de `CRON_SECRET` en el cron | `sed -n '10,40p' app/api/cron/send-reminders/route.ts` | 🟩 CONSTATADO | `app/api/cron/send-reminders/route.ts:22-37`: lee `authorization` (:22) y `x-vercel-cron-secret` (:23); **solo si** `process.env.CRON_SECRET` está definido (:24,27) valida `x-vercel-cron-secret === secret` (:28) O `Bearer <secret>` (:29); si no matchea → 401 (:31-35). `CRON_SECRET` existe en Vercel env (`vercel env ls`). El cotejo del header real ya lo hizo A1 (ítem 8 + A1-16: Vercel manda `Authorization: Bearer`; la rama `x-vercel-cron-secret` es muerta). Sin evaluación aquí → sección 6, Fase S |
| 11 | **Manifest + íconos consistentes** + endpoints 200 | Read de los 5 archivos + `npm run dev` (puerto 3789) + curl + `file` | 🟩 PASA (2 notas 🟢) | `app/manifest.ts:5-32`: name/short_name "Party Time!", `#1a0033`, `start_url:'/admin'` (:8), íconos `/icon-192` 192 maskable + `/icon-512` 512 any+maskable. Generadores: `app/icon.tsx:5-8` 32×32, `app/apple-icon.tsx:5-8` 180×180, `app/icon-192/route.tsx:109-110` 192×192, `app/icon-512/route.tsx:135-136` 512×512 — los 4 edge runtime (`runtime='edge'`), gradiente base `#1a0033→#2d0052` (el 512 agrega stop `#3d0070`, mismo tema) y emoji 🎉. **Runtime local** (dev server puerto 3789, matado al terminar): `/icon-192` → `200 image/png` 26 201 B; `/icon-512` → `200 image/png` 114 516 B; `/manifest.webmanifest` → `200 application/manifest+json`. Dimensiones reales verificadas con `file`: `192 x 192` y `512 x 512` PNG RGBA — coinciden con lo declarado. Notas: `start_url:'/admin'` → **A8-11** 🟢; íconos `purpose:maskable` con borderRadius propio → **A8-12** 🟢 |
| 12 | **Ausencia de tests automatizados** | `grep -n "jest\|vitest\|playwright\|@testing-library\|\"test\"" package.json ; ls -d __tests__ e2e tests` | 🟥 CONFIRMADO | Ambos comandos exit 1: `package.json` sin script `test` (scripts `:5-13`: dev/build/start/lint/db:*) y sin dependencia de testing; no existen dirs `__tests__`/`e2e`/`tests`. Cero tests en un repo con auto-deploy a producción → **A8-03** |
| 13 | **Flujo de schema: `db:push` vs migraciones** | `grep db: package.json ; ls drizzle ; cat drizzle/meta/_journal.json ; grep -rn "drizzle-kit" README.md SETUP_GUIDE.md COMMANDS.md` | 🟥 CONFIRMADO | Scripts: `db:generate`/`db:push`/`db:studio` (`package.json:10-12`), **NO existe `db:migrate`**. Docs instruyen `npx drizzle-kit push` (`README.md:72`, `SETUP_GUIDE.md:45`, `COMMANDS.md:27`) → **flujo real = push directo**. `drizzle/` contiene `0000_add_og_image_url.sql` + `0001_add_rsvp_closed.sql` + `meta/`, pero `meta/_journal.json` tiene **UNA sola entrada** (tag `0000_add_og_image_url`) y solo existe `0000_snapshot.json` → `0001` es SQL manual **fuera** del sistema de migraciones (sin journal ni snapshot). Reflejo en DB: ⏭️ NOT RUN directo (sin `DATABASE_URL` local — no hay `.env.local`; `vercel env pull` prohibido por reglas de esta auditoría); **indirecto:** producción usa `rsvpClosed` sin errores (`get_runtime_errors` 7d → "No runtime errors found") ⇒ las columnas de `0001` existen en la DB. Dualidad no documentada en ningún MD → **A8-07** |
| 14 | **`next.config.js` y `tsconfig.json` sanos** | Read de ambos + `grep -n "ignoreBuildErrors\|ignoreDuringBuilds" next.config.js` | 🟩 PASA | `next.config.js:1-6` completo: solo `reactStrictMode: true`; grep de flags silenciadores → exit 1 (no hay). `tsconfig.json`: `strict:true` (:7), `paths @/*` (:22-24), `skipLibCheck:true` (:6, estándar). No se usa `next/image` en `app/` ni `components/` (grep exit 1) → no se requiere `images.remotePatterns`. Sin ajustes pendientes |
| 15 | **Instanciación a nivel de módulo:** `lib/resend.ts` vs `lib/db.ts` | Read completos de ambos | 🟩 CONSTATADO | `lib/resend.ts` (9 líneas): `:3-5` warn si falta key, `:7` `new Resend(process.env.RESEND_API_KEY)` a nivel de módulo — el constructor lanza si undefined (PRE-4/**A8-01**); `:9` `FROM_EMAIL` default `'onboarding@resend.dev'` (**A8-10**). `lib/db.ts` (25 líneas): `:8-10` warn, `:13,16` degrada a `sql=null`/`db=null`, `:19-21` helper `isDatabaseConfigured()`. Asimetría confirmada: mismo problema (env ausente), estrategias opuestas — db degrada, resend revienta el build |
| 16 | **Dependencias fantasma en build:** `firebase-admin` + `lib/firestore.ts` | `grep -rn "from ... firestore" app lib scripts ; grep -rn "firebase-admin" app lib` | 🟩 CONSTATADO (→ A5) | Imports de `lib/firestore`: **CERO** en `app/`, `lib/` y `scripts/` (grep vacío) — módulo muerto; ni siquiera los scripts legacy lo usan (`scripts/migrate-firebase-to-neon.ts:12` y `scripts/add-demo-data.ts:1` importan `firebase-admin` directo). Sin impacto en el bundle de la app (nada vivo lo importa; sus `console.log` de módulo `lib/firestore.ts:4-14` no corren en runtime de app). `firebase-admin@^12` sigue en `dependencies` (`package.json:20`) instalándose en cada build de Vercel (build cache 191 MB, build logs del deploy actual). Remoción → **A5** (sección 6), incl. las 4 vars legacy aún seteadas en Vercel |

### 4.1 Tabla de env vars — código vs `.env.example` vs docs (pre-poblada al preparar este MD; confirmada fila por fila en esta ejecución)

> Columna extra **"En Vercel"** = presencia del NOMBRE en `vercel env ls` (2026-07-09; nunca se consultaron valores). Correcciones a la pre-población: líneas de `drizzle.config.ts` son `:7,16` (no `:8,17`); fila nueva `BLOB_READ_WRITE_TOKEN`.

| Var | Usada en (archivo:línea) — CONFIRMADO | `.env.example` | `README.md` | `SETUP_GUIDE.md` | En Vercel | Default en código | Nota |
|-----|--------------------------------------|----------------|-------------|------------------|-----------|-------------------|------|
| `DATABASE_URL` | `lib/db.ts:6`, `drizzle.config.ts:7,16`, `scripts/create-super-admin.ts:17`, `scripts/fix-rsvp-event-link.ts:12`, `scripts/create-legacy-event.ts:15`, `scripts/migrate-firebase-to-neon.ts:50` | ✅ (:8) | ✅ (:53,102) | ✅ (:39,189) | ✅ | ninguno (warn + db=null) | OK |
| `RESEND_API_KEY` | `lib/resend.ts:3,7` | ✅ (:21) | ✅ (:56,103) | ✅ (:71,190) | ✅ | ninguno (warn, pero build revienta — A8-01) | Marcada "OPCIONAL" (`.env.example:17`) pese a que el build la requiere → A8-08 |
| `FROM_EMAIL` | `lib/resend.ts:9` | ✅ (:22) | ✅ (:57,104) | ✅ (:72,191) | ✅ | `'onboarding@resend.dev'` | Default sandbox Resend si faltara → A8-10 |
| `CANCEL_TOKEN_SECRET` | `lib/queries.ts:189`, `lib/firestore.ts:187` | ✅ (:29) | ✅ (:63,106) | ✅ (:85,96,192) | ✅ | `'default-secret'` | **Fallback silencioso a secret fijo** — constatado factualmente; evaluación → Fase S |
| `NEXT_PUBLIC_APP_URL` | 6 sitios (ítem 6, líneas exactas ahí) | ✅ (:35) | ✅ (:60,105) | ✅ (:98,194) | ✅ | `'http://localhost:3000'` | Default peligroso latente en emails → A8-06 |
| `NEXT_PUBLIC_BASE_URL` | `app/page.tsx:11`, `app/layout.tsx:5`, `app/[slug]/layout.tsx:15`, `app/api/og-image/[slug]/route.ts:119` | ❌ | ❌ | ❌ | ❌ | `'https://party.timekast.mx'` | **No documentada Y no seteada en Vercel** — prod vive del fallback hardcoded; duplica concepto con APP_URL → A8-05 |
| `CRON_SECRET` | `app/api/cron/send-reminders/route.ts:24` | ❌ | ✅ (:66,107) | ✅ (:91,97,193) | ✅ | ninguno (check condicional) | **Falta en `.env.example`** → A8-08; factual → Fase S |
| `ADMIN_USERNAME` | `lib/auth.ts:16`, `app/api/auth/login/route.ts:26`, `lib/auth-utils.ts:106` | ✅ (:13) | ❌ | ❌ | ✅ | ninguno | Falta en listas de deploy README(:102-107)/SETUP(:189-194) → A8-09 |
| `ADMIN_PASSWORD` | `lib/auth.ts:17`, `app/api/auth/login/route.ts:27` | ✅ (:14) | ❌ | ❌ | ✅ | ninguno | Ídem → A8-09 |
| `ADMIN_EMAIL` | `app/api/auth/login/route.ts:26`, `lib/auth-utils.ts:106` | ❌ | ❌ | ❌ | ❌ | fallback a `ADMIN_USERNAME` / `'admin@env'` | **No documentada en ningún lado ni seteada** — hoy opera vía fallback → A8-09 |
| `BLOB_READ_WRITE_TOKEN` *(fila nueva)* | implícita — `@vercel/blob` la lee internamente; `app/api/admin/upload-image/route.ts:2` importa `put` de `@vercel/blob` (`package.json:17`) | ❌ | ❌ | ❌ | ✅ | ninguno (SDK lanza si falta) | Requerida en prod para subir imágenes y **no documentada en ningún lado** → A8-08 |
| `NODE_ENV` | `lib/auth-utils.ts:175,194` (cookie `secure`) | n/a | n/a | n/a | n/a | n/a | Provista por runtime; no requiere doc |
| `GOOGLE_CLOUD_PROJECT_ID` / `GOOGLE_CLOUD_PRIVATE_KEY` / `GOOGLE_CLOUD_CLIENT_EMAIL` | `lib/firestore.ts:4-6,12-14,27,35,37`, `scripts/migrate-firebase-to-neon.ts:34-42`, `scripts/add-demo-data.ts:8,16,18` | ❌ | ❌ | ❌ | ✅ (¡aún seteadas!) | ninguno | Legacy Firebase post-migración; `lib/firestore.ts` no tiene imports vivos (ítem 16) → cruce A5 |
| `FIRESTORE_COLLECTION_NAME` | `lib/firestore.ts:48`, `scripts/add-demo-data.ts:24` | ❌ | ❌ | ❌ | ✅ (¡aún seteada!) | `'rsvps'` | Ídem legacy → A5 |

## 5. Hallazgos

> Formato: **A8-XX** · severidad (🔴/🟡/🟢) · descripción · evidencia `archivo:línea` · propuesta (si aplica). Un hallazgo sin evidencia no cuenta.

### Pre-registrados (resueltos en esta ejecución)

- **PRE-4** → **CONFIRMADO** y promovido a **A8-01** 🟡 (ítem 2: reproducido con error exacto; esta vez el crash aterrizó en la page-data collection de `/api/admin/send-bulk-email` — el punto exacto varía según qué route importe `lib/resend` procese primero el build, la causa raíz es la misma).

### Confirmados en esta ejecución

| ID | Sev | Descripción | Evidencia | Propuesta |
|----|-----|-------------|-----------|-----------|
| A8-01 | 🟡 | **Build local/CI frío FALLA sin `RESEND_API_KEY`** (PRE-4): `new Resend()` a nivel de módulo lanza `Error: Missing API key` durante "Collecting page data"; el warn previo no lo evita. No rompe deploys en Vercel (var seteada) pero rompe cualquier build sin la var y **ningún MD documenta el requisito**. Asimetría con `lib/db.ts` que sí degrada (null + warn) | `lib/resend.ts:3-7`; repro ítem 2 (exit 1, error textual); `lib/db.ts:8-16` (contraste) | Lazy init: factory `getResend()` que instancie al primer uso (mismo patrón defensivo que `db.ts`), o documentar `RESEND_API_KEY=re_dummy` como requisito de build en README/SETUP |
| A8-02 | 🟡 | **ESLint no configurado:** `npm run lint` = `next lint` sin ESLint instalado → prompt interactivo (inusable en CI); cero lint gate en un repo con auto-deploy a producción on push a main (el único gate estático real es el typecheck del build) | ítem 4: `grep -c '"eslint' package-lock.json` → 0; `package.json:9,33-41`; prompt observado y matado sin escribir config | Instalar `eslint` + `eslint-config-next` como devDeps y correr `next lint` localmente/en CI antes del push (el script ya existe) |
| A8-03 | 🟡 | **Cero tests automatizados:** sin script `test`, sin framework de testing, sin directorio de tests — en producción real con auto-deploy y sin CI | ítem 12: greps exit 1; `package.json:5-13` | Mínimo viable: `vitest` como devDep + `"test": "vitest run"` + 2-3 tests de humo sobre lógica pura sin red (p.ej. `generateCancelToken` `lib/queries.ts:188-192`, selección de destinatarios de reminders, `generateConfirmationEmail` con `eventData` definido/undefined) |
| A8-04 | 🟡 | **Cron cada 12h fuera de spec del plan Hobby:** doc vigente limita Hobby a "once per day" y advierte que expresiones más frecuentes *"will fail during deployment"*; hoy el deploy pasa y el cron ejecuta 2×/día (00:01/12:01 UTC, logs verificados por A1) — funciona **fuera de garantía documentada**: un cambio de enforcement de Vercel puede rechazar deploys o degradar el schedule sin aviso. Precisión Hobby además es ±59 min | `vercel.json:2-7`; doc `cron-jobs/usage-and-pricing` (2026-06-16); `get_deployment dpl_HRfx3Uo…` READY hoy; `01_emails_recordatorios.md` ítem 8 (logs 00:01/12:01 UTC) | Decidir explícitamente: (a) upgrade a Pro si las 2 corridas/día importan, o (b) mover a `0 12 * * *` (1×/día) — el diseño de reminders (ventana "hoy" UTC, A1) debe revisarse si se elige (b) |
| A8-05 | 🟡 | **Dos vars para el mismo concepto de URL pública con fallbacks opuestos:** `NEXT_PUBLIC_APP_URL` (emails, fallback `localhost:3000`) vs `NEXT_PUBLIC_BASE_URL` (metadata/OG, fallback `https://party.timekast.mx`). `BASE_URL` no está documentada NI seteada en Vercel → los 4 sitios de metadata/OG dependen de un dominio hardcodeado en código; si el dominio cambia hay que editar 4 archivos | ítem 7: `app/page.tsx:11`, `app/layout.tsx:5`, `app/[slug]/layout.tsx:15`, `app/api/og-image/[slug]/route.ts:119`; `vercel env ls` sin `NEXT_PUBLIC_BASE_URL` | Unificar en una sola var (`NEXT_PUBLIC_APP_URL`), setearla en Vercel y usar un helper único `getBaseUrl()` |
| A8-06 | 🟡 | **Default `http://localhost:3000` en los cancelUrl que viajan dentro de emails reales** (6 sitios): si `NEXT_PUBLIC_APP_URL` faltara/se renombrara en Vercel, TODOS los emails de producción saldrían con links a localhost. Latente (la var está seteada hoy), pero el modo de fallo es silencioso: el build no avisa y los emails salen igual | ítem 6 (6 sitios con línea exacta); `vercel env ls` confirma var presente | Fallar ruidosamente: helper que lance (o loguee error y aborte el envío) si la var falta en producción, en vez de degradar a localhost |
| A8-07 | 🟡 | **Sistema de migraciones fósil e inconsistente:** flujo real y documentado es `db:push` directo (sin `db:migrate`), y `drizzle/0001_add_rsvp_closed.sql` ni siquiera está registrado en `meta/_journal.json` (1 sola entrada, tag 0000) ni tiene snapshot → la carpeta `drizzle/` no refleja el estado real y un `drizzle-kit generate` futuro partiría de un snapshot desactualizado, regenerando cambios ya aplicados. Dualidad sin documentar | `package.json:10-12`; `drizzle/meta/_journal.json` (1 entrada); `ls drizzle/` (2 SQL, 1 snapshot); `README.md:72`, `SETUP_GUIDE.md:45`, `COMMANDS.md:27` | Elegir UN flujo: si es push, borrar `drizzle/` y documentar "solo db:push"; si son migraciones, regenerar snapshot/journal coherentes y agregar `db:migrate` |
| A8-08 | 🟡 | **`.env.example` incompleto y engañoso:** faltan `CRON_SECRET` (documentada en README/SETUP pero no en el example) y `BLOB_READ_WRITE_TOKEN` (requerida en prod por upload-image, no documentada en NINGÚN lado); `RESEND_API_KEY` está bajo header "OPCIONAL" pese a que el build revienta sin ella (A8-01). Un setup fresco desde `.env.example` produce un entorno que no buildea igual que prod | `.env.example:17,21` (OPCIONAL + RESEND); grep `CRON_SECRET`/`BLOB_READ_WRITE_TOKEN` en `.env.example` → exit 1; `app/api/admin/upload-image/route.ts:2`; `vercel env ls` (ambas seteadas en prod) | Completar `.env.example` con las 2 vars faltantes y reetiquetar RESEND como requerida para build |
| A8-09 | 🟡 | **Checklists de deploy en README/SETUP omiten las credenciales admin:** `README.md:102-107` y `SETUP_GUIDE.md:189-194` listan 6 vars y omiten `ADMIN_USERNAME`/`ADMIN_PASSWORD` (sin ellas el login admin de un deploy fresco no funciona); `ADMIN_EMAIL` no existe en ninguna doc (hoy opera vía fallback a `ADMIN_USERNAME`, no seteada en Vercel) | `README.md:102-107`; `SETUP_GUIDE.md:189-194`; `lib/auth.ts:16-17`; `app/api/auth/login/route.ts:26-27`; `lib/auth-utils.ts:106`; `vercel env ls` | Agregar ADMIN_USERNAME/ADMIN_PASSWORD a ambas listas de deploy; documentar ADMIN_EMAIL y su fallback |
| A8-10 | 🟢 | **`FROM_EMAIL` con default de sandbox Resend** (`'onboarding@resend.dev'`): si la var faltara, los emails de producción saldrían desde el sender sandbox (Resend restringe sandbox a destinatarios de la propia cuenta → fallos de entrega silenciosos). Latente — var seteada en Vercel | `lib/resend.ts:9`; `vercel env ls` | Mismo tratamiento que A8-06: fallar ruidoso en producción en vez de degradar a sandbox |
| A8-11 | 🟢 | **`start_url: '/admin'` en el manifest:** la PWA instalada abre el panel admin, no la landing pública. Coherente si los únicos que instalan la PWA son hosts/admins — dejar constancia de que es una decisión, no un accidente | `app/manifest.ts:8` | Confirmar intención; si la PWA es para invitados, cambiar a `/` |
| A8-12 | 🟢 | **Íconos `purpose: maskable` con esquinas redondeadas propias:** los generadores dibujan borderRadius (38px en 192, 100px en 512) con esquinas transparentes; al aplicarles la máscara del launcher Android el recorte adicional puede mostrar artefactos en las esquinas (los maskable deben ser full-bleed con safe zone) | `app/manifest.ts:18,30` (maskable); `app/icon-192/route.tsx:16`; `app/icon-512/route.tsx:16` | Versión full-bleed (sin borderRadius) para los assets declarados maskable |

**Conteo: 0🔴 · 9🟡 · 3🟢**

## 6. Hallazgos fuera de scope

> Anotar factualmente con `archivo:línea` y referir a la auditoría dueña. No profundizar aquí.

| # | Constatación factual | Evidencia | Dueña |
|---|----------------------|-----------|-------|
| 1 | Check de `CRON_SECRET` condicional a que la var exista; si no está definida, el endpoint no valida nada. El cotejo del header real ya fue hecho por A1 (Vercel manda `Authorization: Bearer`; rama `x-vercel-cron-secret` muerta = A1-16). Constatación de ítem 10, sin evaluación | `app/api/cron/send-reminders/route.ts:22-37`; `vercel env ls` (var presente) | **Fase S** |
| 2 | Fallback `'default-secret'` para `CANCEL_TOKEN_SECRET` en 2 sitios. Constatación factual únicamente | `lib/queries.ts:189`, `lib/firestore.ts:187` | **Fase S** |
| 3 | `lib/firestore.ts` es módulo muerto (CERO imports en app/lib/scripts, ítem 16) + `firebase-admin@^12` en `dependencies` instalándose en cada build + scripts legacy (`scripts/add-demo-data.ts`, `scripts/migrate-firebase-to-neon.ts`) + **las 4 vars legacy (`GOOGLE_CLOUD_*`, `FIRESTORE_COLLECTION_NAME`) siguen seteadas en Vercel producción** (residuo post-migración) | grep ítem 16; `package.json:20`; `vercel env ls` | **A5** |
| 4 | Artefactos de prueba versionados en root: `prod-og-test.bin`, `test-og-dynamic.png`, `test-og-result.bin`, `test-og.png` (confirmado con `git ls-files`). **Corrección al candidato pre-registrado:** `tsconfig.tsbuildinfo` NO está versionado (no aparece en `git ls-files`) | `git ls-files` (4 matches) | **A5** |
| 5 | `/api/debug-home` — endpoint de debug vivo y desplegado en producción (expone `homeEventId`, datos básicos del evento y `legacyId` del config estático); aparece en el route manifest del build | `app/api/debug-home/route.ts:1-20`; build output (ruta listada) | **A5** (código muerto/debug); si aplica exposición → **Fase S** |
| 6 | `app/api/cron/send-reminders/route.ts:14` importa `event-config.json` estático del root — ya auditado por A1: el fallback silencioso al config estático es **A1-08** 🟡 (no duplicar) | `app/api/cron/send-reminders/route.ts:14`; `01_emails_recordatorios.md` A1-08 | **A1** (cerrado) |

## 7. Cierre

1. ✅ Todos los ítems 1-16 tienen resultado con evidencia; los 2 únicos sub-checks no ejecutables quedaron ⏭️ NOT RUN con razón explícita (ítem 9: estado de Fluid Compute no inspeccionable por CLI/MCP; ítem 13: verificación directa de DB sin `DATABASE_URL` local y con `vercel env pull` prohibido — resuelto por vía indirecta).
2. ✅ Sección 5 completada (PRE-4 → A8-01; hallazgos A8-01…A8-12) y sección 6 llena (6 constataciones con referencia cruzada).
3. Actualización de la fila A8 del INDEX y commit/push: los ejecuta el **orquestador** (esta sesión operó bajo regla de no-git y sin tocar `00_INDEX.md`).
4. Mensaje de commit sugerido: `audit: A8 build-config-deploy — 0🔴 9🟡 3🟢`.

**Conteo final: 0🔴 · 9🟡 · 3🟢**
