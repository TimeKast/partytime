# A3 — 🎪 Eventos y settings

> **Estado:** ✅ completada · **Owner/Sesión:** fable5-675614-a3 · **Inicio:** 2026-07-09 11:02 · **SHA auditado:** `61f74e5`
> **SHA de referencia del framework:** `bcc7f1e`
>
> ⚠️ **Antes de empezar:** seguir el **Protocolo por sesión (lock atómico)** de `docs/audits/00_INDEX.md` — `git pull --ff-only`, marcar A3 como 🔄 en la tabla del INDEX con owner/fecha/SHA, commit + push (el lock existe solo cuando el push tiene éxito). Auditoría **read-only**: prohibido editar código de la app; solo se escribe en este MD y en el INDEX.

---

## 1. Objetivo

Verificar que el ciclo de vida completo de un evento — creación, lectura pública por slug, actualización (dos rutas distintas), cambio de slug, activación/desactivación, borrado soft/hard y settings (theme, displayTitle, imágenes OG) — funciona correctamente y de forma consistente en todas las superficies que consumen esos datos (página pública, metadatos OG, home). Detectar además configuración legacy muerta o divergente (`event-config.json`, rutas OG duplicadas).

## 2. Contexto mínimo (para sesión fría)

- **Multi-evento por slug:** cada evento vive en la tabla `events` (`lib/schema.ts:14` — `slug` varchar unique) y se sirve en `/{slug}` vía `app/[slug]/page.tsx` (client component que hace fetch a `/api/events/[slug]`). Los metadatos OG se generan server-side en `app/[slug]/layout.tsx`.
- **Theme JSONB:** `events.theme` guarda `{primaryColor, secondaryColor, accentColor, backgroundColor, textColor}`. Se consume en la página pública, en emails (`lib/email-template.ts:64`) y —en teoría— en imágenes OG.
- **`event-config.json` es herencia de la era single-event** (evento "rooftop-party-andras-oct2024" de 2024, pre-migración a Neon). Sigue importado en **15+ archivos** como fallback. `scripts/create-legacy-event.ts` insertó ese evento en DB con `id = slug = eventConfig.event.id` (líneas 53-54), lo cual hace que varios fallbacks "funcionen de casualidad". **Hallazgo de esta sesión:** el slug de ese evento fue renombrado después a `andrreas` (verificado en producción: `id=rooftop-party-andras-oct2024`, `slug=andrreas`), así que hoy los fallbacks funcionan por el fallback-por-ID, no por el insert original.
- ⚠️ **Gotcha central:** `rsvps.eventId` (text) almacena el **slug** del evento, no el UUID (`lib/schema.ts:82`, `app/api/rsvp/route.ts:49`). Por eso `updateEventSlug` (`lib/queries.ts:345`) debe reescribir `rsvps.eventId` al cambiar el slug — si esa reescritura falla o es parcial, los RSVPs quedan huérfanos (invisibles para el panel y para los reminders del evento). Mismo riesgo con `deleteEvent`: los RSVPs del evento borrado **no se tocan**.
- **Dos rutas OG coexisten:** `app/api/og/[slug]/route.ts` (ImageResponse nodejs, 107 líneas) y `app/api/og-image/[slug]/route.ts` (proxy + compresión sharp + fallback SVG, 294 líneas). Además existe `app/opengraph-image.tsx` (convención file-based de Next para `/opengraph-image`). Parte de esta auditoría es determinar cuál se usa realmente y cuál sobra.
- **Historia relevante:** `43cf503` + `392428f` introdujeron `displayTitle` (título de la invitación separado del nombre interno; vacío = no se muestra título). `d3f0547` cambió el comportamiento de `rsvpClosed`: ahora se muestra la info del evento con un mensaje, en vez de una lock page.

## 3. Scope

**Dentro:**
- `app/api/events/route.ts` (GET lista / POST crear)
- `app/api/events/[slug]/route.ts` (GET / PUT / DELETE, incluye cambio de slug y `renameOgImages`)
- `app/api/event-settings/route.ts` y `app/api/admin/event-settings/update/route.ts`
- `app/[slug]/page.tsx`, `app/[slug]/layout.tsx`, `app/page.tsx` (home redirect + metadata)
- `lib/queries.ts` funciones de evento: `createEvent:207`, `getEventBySlug:223`, `getEventBySlugWithSettings:247`, `getEventById:277`, `getAllEvents:291`, `updateEvent:308`, `deleteEvent:326`, `updateEventSlug:345`, `getAppSetting:413`
- `event-config.json`, `lib/config.ts`, `scripts/create-legacy-event.ts`
- Rutas OG: `app/api/og/[slug]/route.ts`, `app/api/og-image/[slug]/route.ts`, `app/opengraph-image.tsx`

**Fuera (no duplicar; anotar en "Hallazgos fuera de scope" si aparece algo):**
- Contenido/envío de emails y reminders → **A1** (aquí solo se verifica que el *theme/datos de evento* lleguen bien al template).
- Panel admin (`app/admin/page.tsx`) y sus flujos UI → **A4**.
- Ciclo de vida RSVP (validación server-side de `rsvpClosed` al enviar RSVP, capacidad, etc.) → **A2**.
- Protección de endpoints y modelo de auth → **Fase S**.

## 4. Checklist ejecutable

> Regla del INDEX: **todo ítem requiere evidencia aunque PASE** (`archivo:línea` o comando + output). Ítem no ejecutado → `⏭️ NOT RUN` + razón.
>
> **Método runtime:** verificación de producción limitada a GETs de solo lectura (curl a páginas/API públicas — sin crear datos ni disparar emails). Sin `DATABASE_URL` local (solo existe `.env.example`), las queries directas a DB quedan NOT RUN.

### 4.1 Routing público por slug

| # | Verificación | Cómo verificar | Resultado | Evidencia |
|---|--------------|----------------|-----------|-----------|
| 1 | Evento inexistente → la API responde 404 | Leer `app/api/events/[slug]/route.ts:107-111` (devuelve 404 JSON cuando `getEventBySlug` retorna null). Opcional: `curl -i https://party.timekast.mx/api/events/no-existe-xyz` | ✅ PASA | `app/api/events/[slug]/route.ts:107-111` devuelve `{success:false}` con status 404. Runtime: `curl -i https://party.timekast.mx/api/events/no-existe-xyz-audit-a3` → `HTTP/2 404`, `content-type: application/json` |
| 2 | Evento inexistente → la **página** muestra estado "no encontrado", pero ¿con qué status HTTP? `app/[slug]/page.tsx` es client component: renderiza el error en `page.tsx:75-97` con **HTTP 200**, y aunque importa `notFound` de `next/navigation` (`page.tsx:4`) **nunca lo llama**. Confirmar si eso es aceptable (SEO/soft-404) o hallazgo | `curl -sI https://party.timekast.mx/no-existe-xyz \| head -1` (esperar 200, no 404) + grep `notFound` en `app/[slug]/page.tsx` (import sin uso) | ⚠️ Hallazgo → **A3-12** 🟢 | Runtime: `curl -sI https://party.timekast.mx/no-existe-xyz-audit-a3` → `HTTP/2 200`. `grep -n notFound app/[slug]/page.tsx` → solo `page.tsx:4` (import, cero llamadas). Estado de error client-side en `page.tsx:75-97` |
| 3 | Evento con `isActive=false` (incluye soft-deleted) → se muestra lock page "Las inscripciones para este evento están cerradas" con el `title` interno | `app/[slug]/page.tsx:100-112`. Nótese la **conflación semántica**: soft-delete (`deleteEvent` → `isActive:false`, `lib/queries.ts:332-335`) y "evento inactivo" muestran lo mismo; contrastar con el mensaje configurable de `rsvpClosed` (ítem 4) | ⚠️ Hallazgo → **A3-10** 🟡 | `app/[slug]/page.tsx:100-112` (lock page con `event.title` y texto fijo "Las inscripciones… cerradas"); `lib/queries.ts:332-335` (soft-delete = `isActive:false`). Un evento "borrado" y uno "pausado" son públicamente indistinguibles y muestran el título |
| 4 | `rsvpClosed=true` → se muestra la **info completa del evento** (fecha, hora, lugar, detalles) con `rsvpClosedMessage`, NO una lock page, y el botón de RSVP desaparece | `app/[slug]/page.tsx:244-265` (ternario `event.rsvpClosed`); verificar contra el diff de `d3f0547` (`git show d3f0547 -- app/[slug]/page.tsx`) | ✅ PASA | `app/[slug]/page.tsx:244` (`{event.rsvpClosed ? '🎉' : 'RSVP INDISPENSABLE'}`), `:247-252` (mensaje en vez de botón); `git show d3f0547` confirma la remoción del early-return lock page (bloque `- if (event.rsvpClosed)` eliminado) y el ternario nuevo |
| 5 | Evento **pasado**: ¿existe alguna lógica que lo detecte? `events.date` es texto libre estilo "SÁBADO, 29 NOV" (`event-config.json:6`, `lib/schema.ts`), no comparable con `now()`. Confirmar que NADA en la página pública ni en `/api/events/[slug]` oculta o cierra eventos pasados — el único mecanismo es el flag manual `rsvpClosed`/`isActive` | `grep -rn "new Date\|Date.parse" app/[slug]/ app/api/events/` (esperar cero lógica de comparación de fecha del evento); decidir si es hallazgo o diseño aceptado | ✅ Confirmado (diseño actual; ya registrado en A2) | `grep -rn "new Date\|Date.parse" app/[slug]/ app/api/events/` → solo `app/api/events/route.ts:153-154` (timestamps de mock, no comparación). `lib/schema.ts:18` (`date: text`). Cero lógica de evento pasado. Hallazgo ya registrado como **A2-H14** — no se duplica |
| 6 | `getEventBySlug` tiene **fallback por ID** (`lib/queries.ts:234-240`): la URL pública `/{uuid}` también resuelve el evento. Documentar el comportamiento y sus efectos colaterales: (a) `createEvent:211` rechaza slugs que coincidan con un `id` existente; (b) dos URLs distintas sirven el mismo evento | Leer `lib/queries.ts:223-241` y `lib/queries.ts:207-214`; opcional: probar `/{event.id}` de un evento real | ✅ Documentado (con notas) | `lib/queries.ts:227-240` (intento por slug, luego por id). Runtime: `curl /api/events/8a56d835-c441-4581-84d0-ca96b1de016b` → `success:true, slug:40vueltas` (dos URLs sirven el mismo evento). Efecto (a): `lib/queries.ts:210-214` — un slug igual al id de otro evento se rechaza con mensaje engañoso (→ **A3-15**). Efecto extra: el evento legacy vive hoy con `id=rooftop-party-andras-oct2024` y `slug=andrreas`, así que ambos paths resuelven |

### 4.2 Home (`/`)

| # | Verificación | Cómo verificar | Resultado | Evidencia |
|---|--------------|----------------|-----------|-----------|
| 7 | `home_event_id` guarda el **UUID** del evento (admin: `app/admin/page.tsx:2308` pasa `evt.id`; `:211` lo persiste) y `app/page.tsx:125` lo resuelve con `getEventById` — tipos consistentes. Verificar que no haya ningún camino que guarde un slug ahí | `grep -rn "home_event_id" app lib` y revisar cada writer (`app/api/admin/settings/route.ts:82` no valida el valor — cualquier string se guarda) | ✅ PASA (con nota → A4) | `grep -rn home_event_id` → único writer UI: `app/admin/page.tsx:211` (`setAsHome` con `evt.id`, botón en `:2308`). Reader: `app/page.tsx:121-125` (`getEventById`). Consistente hoy — runtime: `curl -I /` → `307 → /patchwrk-260815`. PERO `app/api/admin/settings/route.ts:69-79` persiste cualquier `id/value` sin validar existencia (→ fuera de scope, A4); si se guardara un slug, `getEventById` da null y el home cae al legacy silenciosamente (→ **A3-07**) |
| 8 | Cadena de fallbacks del home (`app/page.tsx:116-154`): sin `home_event_id` → `getEventById(eventConfig.event.id)` (funciona solo porque el evento legacy tiene `id` = ese string, `scripts/create-legacy-event.ts:53-54`) → último recurso `redirect('/' + eventConfig.event.id)` **sin verificar existencia** → si el evento legacy no está en DB, el visitante aterriza en "Evento no encontrado". Evaluar si este last-resort tiene sentido hoy | Leer `app/page.tsx:136-153` + `scripts/create-legacy-event.ts:44-55`; opcional (si hay DATABASE_URL): `SELECT id, slug FROM events WHERE id = 'rooftop-party-andras-oct2024'` | ⚠️ Hallazgo → **A3-07** 🟡 | `app/page.tsx:137-141` (fallback a `getEventById('rooftop-party-andras-oct2024')`), `:152-153` (last-resort `redirect` sin verificación). El evento legacy SÍ existe (runtime: `/api/events/andrreas` → `id=rooftop-party-andras-oct2024`, `slug=andrreas`, verificado vía API pública en lugar de SQL), pero su slug ya NO es el id — el last-resort aterrizaría en `/rooftop-party-andras-oct2024`, que resuelve solo por el fallback-por-ID (ítem 6). Si el legacy se borrara: "Evento no encontrado" |
| 9 | Metadata del home: usa `getEventBySlugWithSettings(homeEventId)` (`app/page.tsx:16`) donde `homeEventId` es un **UUID** — funciona solo por el fallback-por-ID de `getEventBySlug` (ítem 6). Verificar coherencia y que el OG del home apunte a `/api/og-image/{slug}` **sin** el cache-buster `?v=5` que sí usa el layout de evento (ítem 15) | `app/page.tsx:14-16,53` vs `app/[slug]/layout.tsx:33` | ✅ PASA (con nota → A3-09) | `app/page.tsx:14-16` (resuelve UUID vía `getEventBySlugWithSettings` → `getEventBySlug` fallback-por-ID `lib/queries.ts:234-240` — funciona); `app/page.tsx:53` (`/api/og-image/${event.slug}` **sin** `?v`) vs `app/[slug]/layout.tsx:33` (`?v=5`). Incoherencia del cache-buster → **A3-09** |

### 4.3 displayTitle y rutas de actualización divergentes

| # | Verificación | Cómo verificar | Resultado | Evidencia |
|---|--------------|----------------|-----------|-----------|
| 10 | `displayTitle` vacío → **no se renderiza ningún `<h1>`** en la invitación (el diseño asume título dentro de la imagen de fondo). Confirmar que no cae de vuelta a `title` (el comentario de `lib/schema.ts:16` dice "if empty, uses title" — **el comentario miente**, el fix `392428f` cambió a "empty means no title") | `app/[slug]/page.tsx:147-160` (`{event.displayTitle && ...}`) + `git show 392428f` + comentario desactualizado en `lib/schema.ts:16` y `types/event.ts:13` | ⚠️ Hallazgo → **A3-14** 🟢 | `app/[slug]/page.tsx:148-160` (`{event.displayTitle && (<h1>…`) — sin fallback a `title`. `git show 392428f` = "displayTitle now only shows when set - empty means no title". Comentarios mentirosos: `lib/schema.ts:16` y `types/event.ts:13` ("if empty, uses title") |
| 11 | `displayTitle` viaja completo: `/api/events/[slug]` GET lo incluye (spread `...event` en `route.ts:76-77`), `/api/event-settings` GET lo expone (`route.ts:57`), y `admin/event-settings/update` lo persiste (`route.ts:93`, `?? ''`) | Leer las tres rutas; opcional: crear/editar en staging y verificar round-trip | ✅ PASA | `app/api/events/[slug]/route.ts:76-77` (spread `...event` — runtime: la respuesta de `/api/events/40vueltas` incluye `displayTitle`); `app/api/event-settings/route.ts:57` (`displayTitle: event.displayTitle \|\| ''`); `app/api/admin/event-settings/update/route.ts:93` (`updates.displayTitle = body.displayTitle ?? ''`) |
| 12 | **Divergencia entre las DOS rutas de update:** `PUT /api/events/[slug]` (`route.ts:209-227`) **no** soporta `displayTitle`, `ogImageUrl`, `priceCurrency`, `requirePlusOneName`, `hostPhone` ni `emailConfig`; `POST /api/admin/event-settings/update` (`route.ts:92-137`) **no** soporta `isActive` ni `contact/host*`. Dos vocabularios parciales sobre la misma tabla → mapear qué UI usa cada una (A4) y si algún campo es in-actualizable o se pisa | Comparar campo a campo `app/api/events/[slug]/route.ts:211-227` vs `app/api/admin/event-settings/update/route.ts:70-137`; documentar la matriz de campos | ⚠️ Hallazgo → **A3-05** 🟡 | Matriz (ver detalle bajo la tabla). PUT `route.ts:211-227`: sin `displayTitle/ogImageUrl/priceCurrency/requirePlusOneName/hostPhone/emailConfig`. update `route.ts:92-137`: sin `isActive/hostName/hostEmail/hostPhone`. UI: el panel usa PUT **solo** para `newSlug` (`app/admin/page.tsx:739-744`) y update para todo lo demás (`:843`) ⇒ `hostName/hostEmail/hostPhone` in-editables desde el panel y `hostPhone` in-actualizable por CUALQUIER ruta de update |
| 13 | El full-update de `event-settings/update` **fuerza** `theme.backgroundColor='#1a0033'` y `textColor='#ffffff'` hardcodeados (`route.ts:110-111`), pisando cualquier valor previo del JSONB. ¿Esos dos campos se usan en algún lado o son peso muerto del theme? | `grep -rn "backgroundColor\|textColor" app lib --include="*.ts*" \| grep -v node_modules` y rastrear consumo real | ⚠️ Hallazgo → **A3-06** 🟡 | `app/api/admin/event-settings/update/route.ts:106-112` (bg/text hardcodeados). Consumo real (grep): `theme.backgroundColor` SÍ alimenta los 5 senders de email (`app/api/rsvp/route.ts:96`, `cron/send-reminders/route.ts:132`, `send-bulk-reminder:91`, `send-email:68`, `send-bulk-email:55`) y `app/layout.tsx:35` usa el del config estático como `themeColor`. `textColor`: cero consumidores fuera del formateo del API (`events/[slug]/route.ts:97`) y defaults del schema — peso muerto. Valores custom (posibles vía `POST /api/events` `route.ts:126` `theme: body.theme` o `PUT` `route.ts:222`) se pisan en cualquier guardado de settings |

**Matriz de campos ítem 12** (columnas de `lib/schema.ts:13-71` vs las tres rutas de escritura):

| Campo | POST /api/events (crear, `route.ts:112-137`) | PUT /api/events/[slug] (`route.ts:211-227`) | POST event-settings/update full (`route.ts:92-137`) |
|---|---|---|---|
| title, subtitle, date, time, location, details | ✅ | ✅ | ✅ |
| displayTitle | ❌ | ❌ | ✅ `:93` |
| priceEnabled/Amount | ✅ | ✅ | ✅ |
| priceCurrency | ✅ `:122` | ❌ | ✅ `:101` |
| capacityEnabled/Limit | ✅ | ✅ | ✅ |
| backgroundImageUrl | ✅ | ✅ | ✅ |
| ogImageUrl | ❌ | ❌ | ✅ `:105` |
| theme | ✅ completo (`:126`) | ✅ completo (`:222`) | ⚠️ solo 3 colores; bg/text pisados (`:106-112`) |
| hostName / hostEmail | ✅ | ✅ `:223-224` | ❌ |
| hostPhone | ✅ `:135` | ❌ | ❌ (in-actualizable) |
| isActive | ✅ | ✅ `:225` | ❌ |
| rsvpClosed / rsvpClosedMessage | ❌ | ✅ `:226-227` | ✅ `:116-117` |
| requirePlusOneName | ❌ | ❌ | ✅ `:114` |
| emailConfig (confirmation/reminder*) | ❌ | ❌ | ✅ `:121-137` |
| slug | ✅ | ✅ vía `newSlug` (`:182-205`) | ❌ |

### 4.4 event-config.json y lib/config.ts — ¿legacy vivo o muerto?

| # | Verificación | Cómo verificar | Resultado | Evidencia |
|---|--------------|----------------|-----------|-----------|
| 14 | Censo de importadores de `event-config.json` (hay **15+**). Clasificar cada uno: (a) fallback alcanzable, (b) fallback muerto, (c) default de `eventId` cuando el request no lo trae — el caso (c) es el peligroso | `grep -rn "event-config" --include="*.ts" --include="*.tsx" app lib scripts` y revisar cada uso en contexto | ✅ Censo completo (ver tabla bajo esta) | `grep -rn event-config` → 16 importadores + script. Clasificación campo a campo abajo. Los (c) peligrosos ya registrados: `rsvp:38` → A2-H15, `stats:27` → A1-OOS-05, `event-settings:32` → **A3-08** |
| 15 | `app/api/event-settings/route.ts:97-140`: si el evento no existe devuelve `success:true` con `source:'config'` y **los datos del evento legacy de 2024** (título "Party Time!", fecha "SÁBADO, 29 NOV"). Un consumidor que no revise `source` cree que el evento existe. Evaluar si algún caller depende de esto o si debería ser 404 | Leer el bloque completo + `grep -rn "event-settings" app/admin` para ver cómo lo consume el panel (cruzar con A4) | ⚠️ Hallazgo → **A3-08** 🟡 (y descubrimiento colateral → **A3-01** 🔴) | `app/api/event-settings/route.ts:98-140` (`success:true, source:'config'`, datos 2024). Único caller: `app/admin/page.tsx:284-318` (`loadEventConfig`) — **no revisa `source`** y llenaría el form con datos 2024. Colateral crítico descubierto aquí: la rama "database" (`route.ts:52-94`) **omite `rsvpClosed`/`rsvpClosedMessage`** del objeto settings, pero el admin los lee de ahí (`page.tsx:317-318`) → **A3-01** |
| 16 | `lib/config.ts` (`getEventConfig`, `getStaticEventConfig`) — **cero importadores** fuera de sí mismo; además `getEventConfig` hace fetch relativo (`/api/event-settings`) que solo funcionaría client-side y menciona Firestore (era pre-migración). Confirmar que es código muerto | `grep -rn "lib/config\|getEventConfig\|getStaticEventConfig" app components lib --include="*.ts*"` (esperar solo `lib/config.ts`) → cruzar con A5 | ✅ Confirmado muerto → OOS A5 | `grep -rn "lib/config\|getEventConfig\|getStaticEventConfig" app lib scripts` → únicos matches: `lib/config.ts:7,53` (las propias definiciones). Fetch relativo `lib/config.ts:10` y comentario Firestore `:5,:42`. Registrado en "Hallazgos fuera de scope" |
| 17 | `scripts/create-legacy-event.ts`: one-shot ya ejecutado (el evento existe en DB con `id`=`slug`=string legacy). ¿Debe permanecer en el repo? Documentar su rol como explicación de por qué los fallbacks por ID funcionan | Leer script completo; si hay DB: `SELECT id, slug, is_active FROM events WHERE slug='rooftop-party-andras-oct2024'` | ✅ Documentado (verificación por API pública en vez de SQL) | `scripts/create-legacy-event.ts:52-54` (INSERT con `id`=`slug`=`eventConfig.event.id`), `:27-37` (idempotente: sale si ya existe). Runtime read-only: `curl /api/events/andrreas` → `id=rooftop-party-andras-oct2024`, `slug=andrreas`, `isActive=true` — el evento existe pero su **slug fue renombrado** post-creación; hoy los fallbacks por el string legacy funcionan solo gracias al fallback-por-ID de `getEventBySlug` (`lib/queries.ts:234-240`). Remoción del script → cruzar con A5 |

**Censo ítem 14 — importadores de `event-config.json`:**

| Importador | Uso | Clase |
|---|---|---|
| `app/page.tsx:2` | default de home (`:15,:137,:153`) + metadata fallback con datos 2024 (`:21-46,:85-112`) | (a)+(c) → A3-07 |
| `app/layout.tsx:3` | `themeColor` global = `eventConfig.theme.backgroundColor` (`:35`) — siempre activo | (a) legacy vivo |
| `app/opengraph-image.tsx:2` | imagen OG de `/` con datos 2024 hardcodeados (`:13-18`) | (a) alcanzable vía fallback del home → A3-07 |
| `app/admin/components/LoginForm.tsx:5` | **import sin ningún uso** (cero referencias en el archivo) | (b) muerto → OOS A5 |
| `app/admin/page.tsx:10` | estado inicial del `configForm` con datos 2024 (`:38-49`) antes del load | (a) transitorio |
| `app/api/event-settings/route.ts:5` | default `eventId` (`:32`) + fallback de datos (`:98-140`) | (c) → A3-08 |
| `app/api/rsvp/route.ts:2` | default `eventId = eventConfig.event.id` (`:38,:157`) | (c) → **A2-H15** (ver nota ítem 27) |
| `app/api/stats/route.ts:6` | default `eventIdOrSlug` (`:27`) | (c) → **A1-OOS-05** |
| `app/api/admin/send-email/route.ts:8`, `send-bulk-email:8`, `send-bulk-reminder:15`, `cron/send-reminders:14` | fallbacks por campo (theme/hostEmail legacy) | (a) → **A1-08 / A1-14** |
| `app/api/admin/add-demo-data/route.ts:5` | demo data apuntada al evento legacy (`:13-55`) | (c) herramienta admin → A4 |
| `app/api/debug-home/route.ts:4` | expone `legacyId` informativo (`:20`) | (a) debug → OOS A5/Fase S |
| `app/api/og/[slug]/route.ts:2` | defaults de ruta muerta (`:14-18`) | (b) → OOS A5 |
| `lib/config.ts:1` | módulo muerto completo | (b) → OOS A5 |
| `lib/email-template.ts:6` | fallback si `eventData` ausente (`:52-65`) | (a) → **A1-14** |
| `scripts/create-legacy-event.ts:12` | one-shot ya ejecutado | (b) → OOS A5 |

### 4.5 Theme JSONB — consistencia entre superficies

| # | Verificación | Cómo verificar | Resultado | Evidencia |
|---|--------------|----------------|-----------|-----------|
| 18 | Página pública aplica el theme del evento en todos los elementos (título neon, subtítulo, card de info, botón RSVP, overlay) con defaults por campo en la API (`app/api/events/[slug]/route.ts:46-53,92-98`) | `app/[slug]/page.tsx:114,128-265` — buscar colores hardcodeados que ignoren el theme (p.ej. el link "Volver al inicio" usa `#00FFFF` fijo, `page.tsx:87`) | ✅ PASA (con notas) | Theme aplicado: overlay `page.tsx:134`, título `:153-154`, subtítulo `:170-171`, card `:186-188`, precio `:221`, capacidad `:229`, título RSVP `:243`, botón `:260`. Defaults por campo en API `app/api/events/[slug]/route.ts:92-98`. Hardcodes solo en estados sin evento cargado (error `:87` `#00FFFF`) y en la lock page `isActive=false` (`:100-112`, sin theme aunque el evento SÍ está cargado) — menor, se anota dentro de A3-10 |
| 19 | Emails: `lib/email-template.ts:64` usa `event.theme \|\| eventConfig.theme` — el theme del evento llega al template **solo si el caller lo pasa** en `eventData`. Verificar qué callers pasan theme y cuáles caen al theme legacy (detalle de contenido → A1; aquí solo la plomería del dato) | `grep -rn "generateEmailTemplate\|eventData" app/api --include="*.ts" \| head` y revisar 1-2 callers | ✅ PASA (plomería correcta) | La función real es `generateConfirmationEmail` (`lib/email-template.ts:42`; `generateEmailTemplate` no existe). Los 5 callers construyen `eventData.theme` desde el evento con defaults por campo: `app/api/rsvp/route.ts:85-97`, `cron/send-reminders/route.ts:116-137`, `send-bulk-reminder:80-96`, `send-email:57-74`, `send-bulk-email:40-61`. El fallback global (`email-template.ts:52-65`) solo aplica si `eventData` no llega — ese caso ya es **A1-08/A1-14** |
| 20 | Imágenes OG **ignoran el theme por completo**: `og-image` SVG fallback hardcodea `#ff6b9d/#00f5ff/#b8b8b8` (`app/api/og-image/[slug]/route.ts:96-99`), `og/[slug]` hardcodea lo mismo (`route.ts:55-84`), `opengraph-image.tsx` ídem (`:44-84`). ¿Divergencia aceptada o hallazgo de consistencia? | Leer los tres archivos; comparar colores con `DEFAULT_THEME` de `app/api/events/[slug]/route.ts:47-53` (ni siquiera coinciden: `#FF1493` vs `#ff6b9d`) | ⚠️ Hallazgo → **A3-13** 🟢 | `app/api/og-image/[slug]/route.ts:96-99` (`#ff6b9d/#00f5ff/#b8b8b8`); `app/api/og/[slug]/route.ts:58,66,76,83` (ídem); `app/opengraph-image.tsx:48,58,70,79` (ídem). Ninguno lee `event.theme` (getEventBySlugWithSettings ni siquiera lo devuelve — `lib/queries.ts:247-272`). Paleta OG ≠ `DEFAULT_THEME` (`#FF1493/#00FFFF/#FFD700`, `events/[slug]/route.ts:47-53`) |

### 4.6 Las DOS rutas OG

| # | Verificación | Cómo verificar | Resultado | Evidencia |
|---|--------------|----------------|-----------|-----------|
| 21 | **¿Cuál usan los metadatos reales?** `/api/og-image/{slug}` es la única referenciada: `app/[slug]/layout.tsx:33` (con `?v=5`) y `app/page.tsx:53` (sin `?v`). `/api/og/[slug]` tiene **cero referencias** en el código — quedó como fallback histórico. Candidata a eliminación → cruzar con A5 | Repetir el grep + leer historia git de ambas rutas | ✅ Confirmado: solo `og-image` se usa; `og/` muerta → OOS A5 | `grep -rn "api/og" app lib \| grep -v og-image \| grep -v "app/api/og/"` → exit 1 (cero referencias). Referencias a og-image: `app/[slug]/layout.tsx:33`, `app/page.tsx:53`. Historia: `og/[slug]` nació en `c0a5d2a`/`9efcbe8` ("preferir backgroundImageUrl y fallback /api/og") y quedó huérfana cuando og-image la sustituyó (historia og-image: `c22bb70`…`3545dff`) |
| 22 | Divergencia funcional entre ambas: `og/[slug]` genera SIEMPRE una tarjeta ImageResponse con datos del evento (sin imagen custom); `og-image/[slug]` hace: (1) busca `og-{slug}.png/jpg` en `/public` vía self-fetch HTTP, (2) proxy de `ogImageUrl`/`backgroundImageUrl` con compresión sharp <280KB y rechazo de imágenes verticales (ratio <1.2), (3) fallback SVG. Documentar que si alguien "restaurara" `og/`, perdería todo el pipeline de WhatsApp | Leer ambos archivos completos; `ls public/og-*` (hoy: `og-40vueltas.jpg`, `og-andrreas.png`, `og-carrillo-fest.png`) | ✅ Documentado | `og/[slug]/route.ts:11-106` (solo ImageResponse con datos, sin pipeline). `og-image/[slug]/route.ts`: paso 1 self-fetch `og-{slug}.{png,jpg}` `:119-177`; paso 2 proxy `ogImageUrl \|\| backgroundImageUrl` con compresión (`:14-28`, target 280KB `:10`) y rechazo vertical ratio<1.2 (`:12,:269-278`); paso 3 SVG `:78-109`. `ls public/og-*` → `og-40vueltas.jpg` (158KB), `og-andrreas.png` (2.3MB — se recomprime en cada request al superar 280KB, `:159-164`), `og-carrillo-fest.png` (228KB) |
| 23 | `app/opengraph-image.tsx` (runtime edge): solo alcanzable como imagen de `/` cuando el home cae al fallback estático (`app/page.tsx:33,44,98,110`) y sirve datos **hardcodeados del evento 2024**. ¿Tercer generador OG redundante? | Leer archivo + los tres bloques de metadata de `app/page.tsx`; cruzar con A5 | ✅ Confirmado: tercer generador, solo datos 2024 | `app/opengraph-image.tsx:12-18` (datos 100% de `eventConfig`, comentario `:13` lo admite). Referenciado solo en las ramas fallback/error del metadata del home: `app/page.tsx:32-33,44` (evento no encontrado) y `:98-99,110` (catch). Redundante con el SVG fallback de og-image → parte de A3-07; remoción → cruzar con A5 |
| 24 | Cache-buster `?v=5` **hardcodeado** en `app/[slug]/layout.tsx:33` ("fuerza invalidación cuando se cambia la imagen OG") — requiere editar código y deployar para invalidar caché de WhatsApp; el home ni lo usa. Evaluar coherencia del mecanismo | Leer comentario `layout.tsx:31-33` y `app/page.tsx:52-53` | ⚠️ Hallazgo → **A3-09** 🟡 | `app/[slug]/layout.tsx:31-33` (comentario + `?v=5` literal); `app/page.tsx:52-53` (sin `?v`). El admin permite cambiar `ogImage.url` en runtime (`event-settings/update route.ts:105`) pero el buster solo cambia con un deploy — el host cambia la imagen y WhatsApp sigue mostrando la vieja |

### 4.7 updateEventSlug — integridad de RSVPs

| # | Verificación | Cómo verificar | Resultado | Evidencia |
|---|--------------|----------------|-----------|-----------|
| 25 | `updateEventSlug` **SÍ** actualiza `rsvps.eventId` del slug viejo al nuevo (`lib/queries.ts:390-392`). PERO: los pasos 5 y 6 son **dos statements sin transacción** (driver neon-http). Si el paso 6 falla, el evento ya tiene slug nuevo y TODOS sus RSVPs quedan huérfanos apuntando al slug viejo | Leer `lib/queries.ts:345-404`; verificar que `lib/db.ts` usa `drizzle-orm/neon-http` (sin `db.transaction`); documentar la ventana de fallo | ⚠️ Hallazgo → **A3-03** 🟡 (relacionado A1-12) | `lib/queries.ts:384-387` (update `events.slug`) y `:390-392` (update `rsvps.eventId`) — statements separados. `lib/db.ts:2` (`drizzle-orm/neon-http`; neon-http no soporta transacciones interactivas). Ventana: fallo entre :387 y :392 → RSVPs con `eventId=oldSlug` invisibles para `getRSVPsByEvent(newSlug)` y para `getConfirmedRSVPsForReminder` (`lib/queries.ts:503-514`) |
| 26 | El conteo `updatedRsvps` devuelto es **aproximado**: cuenta con un `SELECT` posterior de todos los RSVPs con el slug nuevo, no las filas afectadas — si ya existieran RSVPs con `eventId = newSlug`, se contarían y **se mezclarían silenciosamente** con los del evento renombrado | Leer `:389-403` + razonar contra el escenario del ítem 29 (slug reciclado) | ⚠️ Hallazgo → **A3-17** 🟢 | `lib/queries.ts:394-402` (comentario "We'll count manually" + SELECT por `eventId=newSlug`). El paso 4 (`:373-381`) impide renombrar HACIA un slug de un evento vivo, pero NO detecta RSVPs huérfanos preexistentes con ese slug (de un evento hard-borrado) — se contarían y quedarían absorbidos. El admin muestra ese número como "N RSVP(s) actualizados" (`app/admin/page.tsx:751-752`) |
| 27 | RSVPs cuyo `eventId` guardó el **UUID** en vez del slug NO se migran en el cambio de slug (el `where` es solo por `oldSlug`). ¿Existen filas así en producción? | Si hay DB: `SELECT event_id, count(*) FROM rsvps GROUP BY event_id` y cruzar contra `SELECT slug FROM events`. Sin DB: `⏭️ NOT RUN` + razón | ⏭️ NOT RUN (parte DB) + traza de código | **Razón NOT RUN:** no hay `DATABASE_URL` local (`ls .env*` → solo `.env.example`) y la verificación runtime permitida (vercel env ls / MCP lectura) no incluye queries a la DB de producción. **Traza:** el `where` es solo `eq(rsvps.eventId, oldSlug)` (`lib/queries.ts:392`) — cualquier fila keyed por UUID o por un slug ya renombrado no se migra. Vector activo hoy: `POST /api/rsvp` sin `eventSlug` escribe `eventId='rooftop-party-andras-oct2024'` (`app/api/rsvp/route.ts:38`), que desde el rename a `andrreas` ya NO es el slug vigente → huérfano inmediato (raíz registrada como **A2-H15**; el ángulo post-rename se anota ahí, no se duplica) |
| 28 | `renameOgImages` (`app/api/events/[slug]/route.ts:15-44`) renombra `public/og-{oldSlug}.*` con `renameSync` en `process.cwd()` — en **Vercel el filesystem del deployment es de solo lectura/efímero**: el rename fallará o no persistirá, dejando `og-{oldSlug}.png` servido para el slug viejo y **nada** para el nuevo. Funciona solo en dev local | Leer `route.ts:15-44` + `og-image/route.ts:121-177`; verificar en logs de Vercel un cambio de slug real, o marcar NOT RUN con razón | ⚠️ Hallazgo → **A3-04** 🟡 (resuelto por traza; logs runtime inviables: retención ≈1h y no hubo cambio de slug en esa ventana) | `app/api/events/[slug]/route.ts:15-44`: `existsSync(join(process.cwd(),'public',…))`. En Vercel `public/` NO está en el FS del lambda — lo documenta el propio repo: `og-image/route.ts:117-118` ("En Vercel, los archivos de public/ no están accesibles via readFileSync, así que hacemos fetch desde la URL pública"). Consecuencia: `existsSync`→false para TODAS las extensiones → `renamed=[]`, `errors=[]` → **no-op silencioso**; el admin muestra "✅ Slug cambiado" sin advertencia (`app/admin/page.tsx:749-760` solo informa si hay renamed/errors). El CDN sigue sirviendo `og-{oldSlug}.png` y el paso 1 de og-image para el slug nuevo (`route.ts:121-122`, fetch de `og-{newSlug}.*`) → 404 → cae a `ogImageUrl`/fallback SVG |

### 4.8 deleteEvent y getAllEvents

| # | Verificación | Cómo verificar | Resultado | Evidencia |
|---|--------------|----------------|-----------|-----------|
| 29 | `deleteEvent` **hard** (`?hard=true`) borra solo la fila de `events` — los RSVPs del evento **quedan huérfanos** con `eventId = slug` (no hay FK). Peor: el slug queda libre; si se crea un evento nuevo con el mismo slug, **hereda los RSVPs del evento borrado** (aparecen en su panel y entran en cualquier envío bulk/reminder de ese slug → email a quien no corresponde) | Leer `lib/queries.ts:326-338` + `lib/schema.ts:80-84` (sin FK ni cascade); razonar el escenario slug-reciclado; si hay DB, buscar huérfanos (query del ítem 27) | ⚠️ Hallazgo → **A3-02** 🔴 | `lib/queries.ts:329-330` (hard delete solo `events`); `lib/schema.ts:83` (`eventId: text('event_id').notNull()` — sin FK/cascade); endpoint `app/api/events/[slug]/route.ts:282,301`. Escenario slug-reciclado (p.ej. evento anual re-creado): el evento nuevo hereda los RSVPs viejos vía `getRSVPsByEvent(slug)` (`lib/queries.ts:62-71`) y `getConfirmedRSVPsForReminder(slug)` (`:503-514`) → bulk/reminder a invitados del evento anterior. Efecto extra: `saveRSVP` dedup por email+slug (`:28-38`) bloquearía con 409 a un invitado nuevo que comparta email con uno del evento borrado. Búsqueda de huérfanos en DB: NOT RUN (misma razón ítem 27) |
| 30 | `deleteEvent` **soft** (default) = `isActive:false`. Efectos: página pública muestra lock page con el título (ítem 3); `getAllEvents(activeOnly=true)` lo excluye; el panel (sin `?active=true`) lo sigue listando. ¿El público debería ver el título de un evento "borrado"? ¿Hay forma de reactivarlo (PUT `isActive:true`)? | Leer las tres superficies; documentar el ciclo desactivar→reactivar | ⚠️ Hallazgo → **A3-10** 🟡 | Soft delete: `lib/queries.ts:332-335`. Público: lock page con título `app/[slug]/page.tsx:100-112`. Panel: lista todo y marca "(Inactivo)" (`app/admin/page.tsx:1252`). Reactivación posible por API (`PUT` con `isActive:true`, `app/api/events/[slug]/route.ts:225`) pero **el panel no tiene NINGÚN UI de desactivar/borrar/reactivar**: `grep -in "delete\|eliminar\|desactivar\|reactivar" app/admin/page.tsx` → cero llamadas (el único PUT del panel es el de slug, `:739-744`) — el ciclo completo es API-only |
| 31 | `getAllEvents(activeOnly)`: "activo" ≡ `isActive` — el mismo flag significa a la vez "soft-deleted" y "cerrado al público" (ítems 3 y 30). Además el filtro es **en JS post-query** (trae toda la tabla y filtra en memoria) en vez de un `where`. Verificar quiénes llaman con `activeOnly=true` y si la semántica es consistente | `grep -rn "getAllEvents" app lib --include="*.ts*"` y revisar cada caller | ⚠️ Hallazgo → **A3-16** 🟢 | `lib/queries.ts:294-301` (SELECT completo + `result.filter(e => e.isActive)` en JS). Callers: solo `app/api/events/route.ts:36-37` (expone `?active=true`; el panel llama sin el flag, `app/admin/page.tsx:175`). Duplicado legacy muerto: `lib/firestore.ts:445` → ya cubierto por **A1-OOS-02**/A5. Conflación semántica del flag → **A3-10** |

### 4.9 Creación y metadatos

| # | Verificación | Cómo verificar | Resultado | Evidencia |
|---|--------------|----------------|-----------|-----------|
| 32 | `POST /api/events` valida slug (`^[a-z0-9-]+$`) y title requerido, y `createEvent` rechaza slug duplicado con 409. Nota: el check de duplicado usa `getEventBySlug` que también matchea por **id** — un slug igual al UUID de otro evento se rechaza con mensaje engañoso. Verificar además la **carrera** check-then-insert (el unique de DB salva, pero el error no se traduce a 409) | Leer `app/api/events/route.ts:93-178` + `lib/queries.ts:207-221` + `lib/schema.ts:14` (unique) | ⚠️ Hallazgo → **A3-15** 🟢 | Validaciones: `route.ts:96-100` (requeridos), `:104-108` (regex). Dup check: `lib/queries.ts:210-214` vía `getEventBySlug` (matchea id, `:234-240`) → mensaje "Ya existe un evento con este slug" engañoso para colisión con id. 409 solo si `error.message` incluye 'Ya existe' (`route.ts:167-172`); en la carrera check-then-insert el unique de `lib/schema.ts:14` lanza un error de Postgres que NO contiene 'Ya existe' → 500 genérico en vez de 409 |
| 33 | `POST /api/events` NO acepta `displayTitle`, `ogImageUrl`, `rsvpClosed*`, `requirePlusOneName` ni `emailConfig` al crear — un evento recién creado depende de un segundo save vía `event-settings/update` para quedar completo. ¿El flujo del admin lo hace? (cruzar con A4) | Comparar `eventInput` (`route.ts:112-137`) contra columnas de `lib/schema.ts:13-66` | ✅ Confirmado (documentado en matriz ítem 12; flujo UI → A4) | `eventInput` `route.ts:112-137` omite `displayTitle/ogImageUrl/rsvpClosed/rsvpClosedMessage/requirePlusOneName/emailConfig` (defaults del schema aplican, `lib/schema.ts:16,37,63-71`). Peor: el form de creación del panel solo envía 7 campos (slug/title/subtitle/date/time/location/details — `app/admin/page.tsx:2348-2356`), ni siquiera `contact/theme` que el API sí acepta → `hostName/hostEmail/hostPhone=''` y, por A3-05, los host* nunca podrán editarse desde el panel. Flujo UI → OOS A4 |
| 34 | Metadatos del evento: `title` OG = `"${event.title} - ${event.subtitle}"` → con subtitle vacío queda `"Título - "` (guion colgante). `description` = `"${date} ${time} - ${location}"` → campos vacíos generan `" - "`. Verificar con un evento sin subtitle | Leer `layout.tsx:28-29`; opcional: `curl -s https://party.timekast.mx/{slug} \| grep -o '<title>[^<]*'` para un evento sin subtitle | ⚠️ Hallazgo → **A3-11** 🟡 | `app/[slug]/layout.tsx:28-29` (interpolación sin guardas). Confirmado en producción HOY: `curl -s https://party.timekast.mx/40vueltas \| grep -o '<title>[^<]*'` → `<title>40 Vueltas del Mundo - </title>` (subtitle vacío verificado vía `/api/events/40vueltas` → `subtitle:""`). Mismo patrón en el home (`app/page.tsx:49-50`) |
| 35 | El evento no encontrado en `generateMetadata` devuelve metadata "Evento no encontrado" (`layout.tsx:20-26`) pero la página igual renderiza client-side (ítem 2) — coherente entre sí; documentar | `app/[slug]/layout.tsx:17-26` | ✅ PASA | `app/[slug]/layout.tsx:20-25` (metadata "Evento no encontrado" server-side) coherente con el estado de error client-side `app/[slug]/page.tsx:75-97`. El único desajuste es el status 200 (soft-404), ya registrado como **A3-12** |

## 5. Hallazgos

> Formato: `A3-XX` · severidad (🔴/🟡/🟢 según rúbrica del INDEX) · evidencia `archivo:línea` obligatoria.

| ID | Sev | Título | Evidencia | Detalle |
|----|-----|--------|-----------|---------|
| A3-01 | 🔴 | **Guardar settings REABRE silenciosamente un RSVP cerrado.** `GET /api/event-settings` omite `rsvpClosed`/`rsvpClosedMessage` del objeto `settings`; el form del admin los lee de esa respuesta (siempre `undefined` → `false`) y el full-save los persiste con `?? false` | `app/api/event-settings/route.ts:52-94` (settings sin `rsvpClosed`); `app/admin/page.tsx:317-318` (lee `data.settings.rsvpClosed \|\| false`); `app/api/admin/event-settings/update/route.ts:116-117` (`rsvpClosed = body.rsvpClosed ?? false`) | Ciclo: host cierra RSVP (checkbox `page.tsx:1768` → save → DB `true`). Al recargar el form, el checkbox aparece SIEMPRE desmarcado; cualquier guardado posterior (editar fecha, texto, lo que sea) persiste `rsvpClosed=false` y resetea `rsvpClosedMessage` al default. El evento reabre sin que el host lo sepa → invitados pueden inscribirse y recibir confirmaciones cuando no corresponde. Riesgo real HOY: `40vueltas` está en producción con `rsvpClosed=true` (runtime `/api/events/40vueltas`). La única vía UI de cerrar RSVP es este mismo form (el PUT del panel solo maneja `newSlug`, `page.tsx:739-744`) |
| A3-02 | 🔴 | **Hard delete deja RSVPs huérfanos y un slug reciclado los hereda → emails a quien no corresponde.** `deleteEvent(hard)` borra solo la fila de `events`; sin FK, los RSVPs quedan keyed al slug liberado | `lib/queries.ts:329-330` (delete solo events); `lib/schema.ts:83` (sin FK/cascade); `app/api/events/[slug]/route.ts:282,301` (endpoint `?hard=true`); `lib/queries.ts:62-71,503-514` (lecturas por slug) | Escenario realista (evento anual re-creado con el mismo slug): el evento nuevo hereda TODOS los RSVPs del borrado — aparecen confirmados en su panel, entran en `send-bulk-email`/`send-bulk-reminder`/cron del slug, y el dedup de `saveRSVP` (`lib/queries.ts:28-38`) responde 409 a invitados nuevos cuyo email coincida con uno heredado. Cumple el criterio 🔴 del INDEX ("email enviado a quien no corresponde"). Mitiga hoy: no hay UI de delete en el panel (API-only, ver A3-10) |
| A3-03 | 🟡 | **`updateEventSlug`: dos updates sin transacción → ventana de orfandad permanente de RSVPs** (relacionado con A1-12, que registró la carrera con el cron sobre el mismo código) | `lib/queries.ts:384-392` (update events y update rsvps separados); `lib/db.ts:2` (`drizzle-orm/neon-http`, sin soporte de transacción interactiva) | Si el segundo update falla (timeout/red), el evento ya tiene slug nuevo y TODOS sus RSVPs apuntan al viejo: invisibles en el panel (`getRSVPsByEvent(newSlug)`) y excluidos de reminders (`getConfirmedRSVPsForReminder(newSlug)`), sin ningún error visible después. No hay mecanismo de reconciliación |
| A3-04 | 🟡 | **`renameOgImages` es un no-op silencioso en Vercel:** `public/` no existe en el FS del lambda, `existsSync` siempre da false y el admin recibe "éxito" sin advertencia | `app/api/events/[slug]/route.ts:15-44` (`existsSync`/`renameSync` sobre `process.cwd()/public`); `app/api/og-image/[slug]/route.ts:117-118` (el propio repo documenta que `public/` no es accesible en Vercel); `app/admin/page.tsx:749-760` (solo informa si hay renamed/errors — con arrays vacíos calla) | Tras un cambio de slug: el CDN sigue sirviendo `og-{oldSlug}.png` para la URL vieja y el slug nuevo pierde su imagen OG custom (el paso 1 de og-image busca `og-{newSlug}.*` → 404 → cae a `ogImageUrl`/fallback SVG). Funciona solo en dev local. Resuelto por traza de código (logs runtime inviables: retención ≈1h sin cambio de slug en ventana) |
| A3-05 | 🟡 | **Dos vocabularios parciales de update sobre `events`:** PUT `/api/events/[slug]` y POST `event-settings/update` soportan subconjuntos distintos; `hostPhone` es in-actualizable por cualquier ruta y `hostName/hostEmail` son in-editables desde el panel | Matriz completa en ítem 12: `app/api/events/[slug]/route.ts:211-227` vs `app/api/admin/event-settings/update/route.ts:92-137`; UI: `app/admin/page.tsx:739-744` (PUT solo newSlug) y `:843` (update para el resto); creación del panel sin contact: `page.tsx:2348-2356` | Consecuencia funcional: los eventos creados desde el panel quedan con `hostName/hostEmail/hostPhone=''` para siempre (sin UI que los edite), y los emails caen al `hostEmail` legacy `info@timekast.mx` de `event-config.json` (plomería del contenido → A1-08/A1-14). Cualquier campo nuevo obliga a decidir en cuál de los dos vocabularios vive — deuda que ya produjo A3-01 y A3-06 |
| A3-06 | 🟡 | **El full-save de settings pisa `theme.backgroundColor`/`textColor` con valores hardcodeados**, destruyendo valores custom que POST/PUT sí permiten guardar — y `backgroundColor` SÍ se consume en los 5 senders de email | `app/api/admin/event-settings/update/route.ts:106-112` (bg `'#1a0033'`/text `'#ffffff'` fijos); custom posible vía `app/api/events/route.ts:126` y `app/api/events/[slug]/route.ts:222`; consumo: `app/api/rsvp/route.ts:96`, `cron/send-reminders:132`, `send-bulk-reminder:91`, `send-email:68`, `send-bulk-email:55` | Un evento con `backgroundColor` custom (seteado vía API) pierde ese color en los emails en cuanto alguien guarda settings desde el panel. `textColor` no tiene consumidores reales (peso muerto del JSONB — limpieza → plan correctivo) |
| A3-07 | 🟡 | **La cadena de fallbacks del home aterriza en el evento legacy 2024:** `home_event_id` inválido → redirect silencioso a OTRO evento (el legacy); last-resort redirect sin verificar existencia; metadata/OG fallback con datos 2024 hardcodeados | `app/page.tsx:137-141` (fallback `getEventById(eventConfig.event.id)`), `:152-153` (redirect ciego a `/rooftop-party-andras-oct2024`), `:21-46,:85-112` (metadata 2024); `app/opengraph-image.tsx:12-18` (OG con datos 2024) | Hoy funciona (runtime: `/` → 307 → `/patchwrk-260815`), pero: (a) un `home_event_id` corrupto (el writer no valida — A4) manda al visitante al evento legacy sin error; (b) si el legacy se hard-borra, el last-resort produce "Evento no encontrado" como home; (c) el OG de `/` en fallback anuncia "Party Time! / SÁBADO, 29 NOV" de 2024 |
| A3-08 | 🟡 | **`GET /api/event-settings` responde `success:true` con datos del evento 2024 para eventos inexistentes** (`source:'config'`), y su único consumidor no revisa `source`; además sin `?eventId` el default es el evento legacy | `app/api/event-settings/route.ts:32` (default `eventConfig.event.id`), `:98-140` (fake success con datos 2024); consumidor: `app/admin/page.tsx:284-318` (`loadEventConfig` ignora `source`) | Si el evento seleccionado desaparece (hard delete concurrente) o el default legacy dejara de resolver, el form del admin se llena con "Party Time! / SÁBADO, 29 NOV / HAMBURGO 108" como si fueran datos reales del evento. Debería ser 404 (el save posterior sí daría 404, `update/route.ts:45-51`, dejando al host confundido) |
| A3-09 | 🟡 | **Mecanismo de invalidación de caché OG incoherente:** `?v=5` hardcodeado en el layout de evento (cambiarlo requiere editar código + deploy) y el home ni siquiera lo usa | `app/[slug]/layout.tsx:31-33` (comentario + literal `?v=5`); `app/page.tsx:52-53` (sin `?v`); la imagen OG es editable en runtime vía `event-settings/update/route.ts:105` | El host cambia la imagen OG desde el panel y WhatsApp sigue mostrando la anterior hasta que un dev incremente `v` y deploye. Para el home ni eso funciona. Fix natural (plan correctivo): derivar el buster de `event.updatedAt` o del hash de `ogImageUrl` |
| A3-10 | 🟡 | **`isActive` conflacta "soft-deleted", "inactivo" y "cerrado al público", con lock page que muestra el título del evento borrado y SIN UI para desactivar/reactivar/borrar** | `lib/queries.ts:332-335` (soft delete = `isActive:false`); `app/[slug]/page.tsx:100-112` (lock page pública con `event.title`, texto fijo que colisiona semánticamente con `rsvpClosed`); `app/api/events/[slug]/route.ts:225` (reactivación posible solo por API); grep en `app/admin/page.tsx` → cero UI de delete/desactivar (solo label "(Inactivo)" `:1252`) | Un evento "borrado" sigue siendo una página pública que revela su título; un evento "pausado" es indistinguible de uno borrado; y todo el ciclo desactivar→reactivar→borrar es API-only (curl a mano), lo que además reduce —pero no elimina— la exposición a A3-02. La lock page tampoco aplica el theme aunque el evento está cargado (`:100-112`, ítem 18) |
| A3-11 | 🟡 | **Metadatos OG con guion colgante en producción:** `title = "${title} - ${subtitle}"` y `description = "${date} ${time} - ${location}"` sin guardas para campos vacíos | `app/[slug]/layout.tsx:28-29`; `app/page.tsx:49-50` (mismo patrón). Runtime: `<title>40 Vueltas del Mundo - </title>` servido hoy por `https://party.timekast.mx/40vueltas` (subtitle vacío confirmado vía API) | Todo share de WhatsApp/Twitter de un evento sin subtitle (2 de los 4 eventos conocidos tienen subtitle vacío o lo tendrán) muestra el guion colgante; con date/time/location vacíos la description queda `" - "` |
| A3-12 | 🟢 | **Evento inexistente responde HTTP 200 (soft-404) y el import de `notFound` quedó sin uso** | `app/[slug]/page.tsx:4` (import sin llamada), `:75-97` (error client-side). Runtime: `curl -sI https://party.timekast.mx/no-existe-xyz-audit-a3` → `HTTP/2 200` | Coherente con la arquitectura client-fetch (el layout server-side sí pone metadata "Evento no encontrado", `layout.tsx:20-25`), pero scrapers/SEO ven 200. Aceptable para una app de invitaciones; se registra como mejora + limpieza del import |
| A3-13 | 🟢 | **Las 3 superficies OG ignoran `event.theme` y usan una paleta hardcodeada que ni coincide con `DEFAULT_THEME`** (`#ff6b9d/#00f5ff` vs `#FF1493/#00FFFF`) | `app/api/og-image/[slug]/route.ts:96-99`; `app/api/og/[slug]/route.ts:58-84`; `app/opengraph-image.tsx:44-84`; vs `app/api/events/[slug]/route.ts:47-53`. `getEventBySlugWithSettings` ni devuelve theme (`lib/queries.ts:247-272`) | El host configura colores del evento y la tarjeta OG del share sale siempre rosa/cian genérico. Divergencia de consistencia, no rompe flujo |
| A3-14 | 🟢 | **Comentarios mentirosos sobre `displayTitle`:** "if empty, uses title" en schema y types, cuando el fix `392428f` estableció "empty means no title" | `lib/schema.ts:16`; `types/event.ts:13`; comportamiento real `app/[slug]/page.tsx:148-160`; `git show 392428f` | Riesgo de que un dev "arregle" el supuesto bug reintroduciendo el fallback a `title` que `392428f` eliminó a propósito |
| A3-15 | 🟢 | **Creación: carrera check-then-insert responde 500 en vez de 409, y el dup-check por `getEventBySlug` da mensajes engañosos para colisiones con IDs** | `lib/queries.ts:210-214` (check previo), `lib/schema.ts:14` (unique salva la carrera); `app/api/events/route.ts:167-172` (409 solo si el mensaje contiene 'Ya existe' — el error del unique de Postgres no lo contiene) | Dos creates simultáneos del mismo slug: uno recibe 500 "Error al crear evento" en vez de 409. Un slug igual al `id` de otro evento se rechaza como "Ya existe un evento con este slug" (falso: existe un evento con ese *id*) |
| A3-16 | 🟢 | **`getAllEvents` trae toda la tabla y filtra `isActive` en JS** en vez de un `where` | `lib/queries.ts:294-301` (`result.filter(e => e.isActive)`); único caller `app/api/events/route.ts:36-37` | Con el volumen actual es inocuo; es deuda de consulta (y existe un duplicado legacy muerto en `lib/firestore.ts:445` → A5/A1-OOS-02) |
| A3-17 | 🟢 | **`updatedRsvps` del cambio de slug es un conteo aproximado post-hoc** que absorbería RSVPs huérfanos preexistentes con el slug nuevo | `lib/queries.ts:394-402` (SELECT por `eventId=newSlug` en vez de filas afectadas); el admin lo muestra como "N RSVP(s) actualizados" (`app/admin/page.tsx:751-752`) | Solo diverge si existen huérfanos con ese slug (escenario A3-02) — en ese caso además los mezcla silenciosamente con los del evento renombrado |

**Conteo: 2🔴 · 9🟡 · 6🟢**

## 6. Hallazgos fuera de scope

> Cosas detectadas durante A3 que pertenecen a otra auditoría — anotar factualmente con referencia cruzada, sin profundizar.

| ID | Auditoría dueña | Descripción | Evidencia |
|----|-----------------|-------------|-----------|
| A3-OOS-01 | A5 | `lib/config.ts` (getEventConfig/getStaticEventConfig) es código muerto: cero importadores fuera de sí mismo; fetch relativo solo-client y comentarios pre-migración (Firestore) | `grep -rn "lib/config\|getEventConfig\|getStaticEventConfig" app lib scripts` → solo `lib/config.ts:7,53` |
| A3-OOS-02 | A5 | `app/api/og/[slug]/route.ts` es ruta muerta: cero referencias en el código (los metadatos usan solo `/api/og-image/`) | `grep -rn "api/og" app lib \| grep -v og-image` → vacío; historia `9efcbe8`→sustituida |
| A3-OOS-03 | A5 | `app/admin/components/LoginForm.tsx` importa `event-config.json` sin usarlo (import muerto) | `LoginForm.tsx:5` — único match de `eventConfig` en el archivo |
| A3-OOS-04 | A5 | `scripts/create-legacy-event.ts` es one-shot ya ejecutado (evento en DB verificado vía API pública); candidato a archivar/remover con nota histórica — explica por qué los fallbacks-por-ID funcionan | `scripts/create-legacy-event.ts:52-54`; runtime `/api/events/andrreas` |
| A3-OOS-05 | A5 | `app/api/debug-home/route.ts` — endpoint de debug en producción; candidato a remoción (también nota Fase S abajo) | `app/api/debug-home/route.ts:8-22` |
| A3-OOS-06 | A4 | `POST /api/admin/settings` guarda cualquier `id/value` sin validar (p.ej. `home_event_id` inexistente o slug en vez de UUID) — raíz del riesgo A3-07(a) | `app/api/admin/settings/route.ts:69-79` |
| A3-OOS-07 | A4 | El panel no tiene UI para desactivar/reactivar/borrar eventos ni para editar `hostName/hostEmail/hostPhone`; el form de creación solo envía 7 campos (evento incompleto hasta el segundo save) | grep sin DELETE en `app/admin/page.tsx`; create form `:2348-2356` |
| A3-OOS-08 | A4 | El estado inicial del `configForm` del admin son los datos 2024 de `event-config.json` (visibles hasta que carga el evento real) y `loadEventConfig` no distingue `source:'config'` | `app/admin/page.tsx:38-49,284-318` |
| A3-OOS-09 | A2 | Enforcement server-side de `rsvpClosed` al recibir RSVP — ya registrado como **A2-H01** (aquí solo se constató que la página oculta el botón, ítem 4) | `docs/audits/02_rsvp_lifecycle.md` A2-H01 |
| A3-OOS-10 | A2 | El default de `POST /api/rsvp` sin `eventSlug` escribe `eventId='rooftop-party-andras-oct2024'`, que tras el rename del slug a `andrreas` ya no es slug vigente → RSVP huérfano inmediato — profundiza **A2-H15** con el ángulo post-rename | `app/api/rsvp/route.ts:38` + runtime `/api/events/andrreas` (id≠slug) |
| A3-OOS-11 | A6 | Falta FK/cascade entre `rsvps.eventId` y `events` (raíz estructural de A3-02/A3-03); `eventId` guarda slug mutable como clave de facto | `lib/schema.ts:82-83` |
| A3-OOS-12 | Fase S | Nota factual: `GET /api/events/[slug]` (público, sin auth) devuelve la fila completa del evento vía spread — incluye `hostEmail`, `hostPhone` y config interna de reminders. Y `GET /api/debug-home` no valida sesión. Sin profundizar aquí | `app/api/events/[slug]/route.ts:64-104` (spread `...event`); `app/api/debug-home/route.ts:8` |

## 7. Cierre

1. ✅ Todos los ítems del checklist (1-35) tienen Resultado + Evidencia `archivo:línea`; único NOT RUN parcial: la verificación en DB de los ítems 27/29 (sin `DATABASE_URL` local; resuelta la parte lógica por traza de código, y la existencia del evento legacy por API pública read-only).
2. ✅ Hallazgos consolidados: **2🔴 · 9🟡 · 6🟢** (A3-01…A3-17).
3. ✅ Fila A3 del INDEX → actualizar a ✅ con el conteo (lo hace el orquestador junto con el commit).
4. Commit + push: `audit: A3 eventos-settings — 2🔴 9🟡 6🟢`.

**Método runtime empleado:** exclusivamente GETs de solo lectura contra producción (`/api/events/{slug}`, `/{slug}`, `/` con `-I`) — sin creación de datos, sin POST/PUT/DELETE, sin ningún trigger de email. No se usó `vercel env pull` ni se leyeron valores de variables.
