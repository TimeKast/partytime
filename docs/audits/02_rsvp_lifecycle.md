# A2 — 🎟️ Ciclo de vida RSVP

> **Estado:** ✅ completada · **Owner/Sesión:** fable5-675614 · **Inicio:** 2026-07-09 10:39 · **SHA auditado:** `e73ca03`
> **SHA de referencia del framework:** `bcc7f1e`
> **Protocolo:** seguir al pie de la letra el "Protocolo por sesión (lock atómico)" y las "Reglas de evidencia" de `docs/audits/00_INDEX.md`. Read-only sobre el código de la app: solo se escribe en este MD y en el INDEX.

---

## 1. Objetivo

Verificar la corrección **funcional** del ciclo de vida completo de un RSVP — creación, consulta, edición, cancelación y re-confirmación — incluyendo: que `rsvpClosed` realmente cierre el periodo de RSVP en todos los caminos (no solo en UI), qué impide duplicados, si la capacidad (`capacityEnabled`/`capacityLimit`) se respeta, la consistencia de `plusOne`/`plusOneName` entre los tres caminos de escritura (create público, update público, edición admin), la validación funcional del `cancelToken`, el comportamiento de `emailHistory` al cambiar email, y si es posible hacer RSVP a eventos pasados.

**Esto NO es una auditoría de seguridad.** Todo se evalúa como corrección funcional (¿el flujo hace lo que el host y el invitado esperan?), no como superficie de ataque.

## 2. Contexto mínimo (para sesión fría)

- **Estados de un RSVP:** columna `rsvps.status` (`lib/schema.ts:93`, varchar 20, default `'confirmed'`). En la práctica solo existen dos valores escritos por el código: `'confirmed'` (`lib/queries.ts:51`, `app/api/rsvp/update/route.ts:47`, `app/admin/page.tsx:586`) y `'cancelled'` (`lib/queries.ts:123`, `app/admin/page.tsx:586`). No hay enum ni constraint en DB.
- **Ciclo esperado:** invitado crea RSVP desde el modal público (`app/components/RSVPModal.tsx` → `POST /api/rsvp`) → recibe email de confirmación (si `emailConfirmationEnabled`) con link `/cancel/{rsvpId}?token={cancelToken}` → desde esa página puede **editar** (`POST /api/rsvp/update`), **cancelar** (`POST /api/rsvp/cancel`) o, si está cancelado, **re-confirmar** (update con `reconfirm: true`). El admin puede editar/cancelar/reconfirmar desde el panel (`POST /api/admin/update-rsvp`).
- **cancelToken:** es **determinístico**, no aleatorio: `sha256("{rsvpId}-{email}-{CANCEL_TOKEN_SECRET}")` truncado a 32 hex (`lib/queries.ts:188-193`). La validación **recalcula** el token a partir de `rsvpId` + el email **actual** en DB (`lib/queries.ts:195-198`); la columna `rsvps.cancelToken` (`lib/schema.ts:103`) NO se lee al validar. Consecuencia funcional: el token de un link depende del email vigente y del secret — si cualquiera de los dos cambia, los links viejos dejan de funcionar.
- ⚠️ **Gotcha:** `rsvps.eventId` (text, `lib/schema.ts:83`) almacena el **slug** del evento, no el UUID (`app/api/rsvp/route.ts:49`). `getEventBySlug` acepta slug y hace fallback por id (`lib/queries.ts:223-241`).
- `events.date` es **texto libre** (`lib/schema.ts:18`), no timestamp — relevante para el ítem de eventos pasados.
- `POST /api/rsvp` tiene un **modo demo** (sin `DATABASE_URL`) que guarda en un array en memoria (`app/api/rsvp/route.ts:147-173`); en producción con Neon configurado ese camino no debería ejecutarse.

## 3. Scope

**Dentro:**
- `app/api/rsvp/route.ts` (POST crear, GET admin-list)
- `app/api/rsvp/get/route.ts`, `app/api/rsvp/update/route.ts`, `app/api/rsvp/cancel/route.ts`
- `app/api/admin/update-rsvp/route.ts` (solo su efecto sobre datos de RSVP)
- `app/cancel/[rsvpId]/page.tsx`, `app/components/RSVPModal.tsx`, sección RSVP de `app/[slug]/page.tsx`
- `lib/queries.ts` — `saveRSVP:17`, `getRSVPById:76`, `updateRSVP:90`, `cancelRSVP:108`, `recordEmailSent:133`, `getEventStats:167`, `generateCancelToken:188`, `validateCancelToken:195`
- `lib/schema.ts` — tabla `rsvps` y campos de `events` que gobiernan el ciclo (`rsvpClosed`, `capacityEnabled/Limit`, `requirePlusOneName`, `isActive`)

**Fuera (explícito):**
- Contenido, templates y puntos de envío de **emails** (confirmación/recordatorios) → **A1**. Aquí solo se audita *cuándo* se dispara/omite el email de confirmación y qué se registra en `emailHistory`, no el email en sí.
- **Protección/autenticación de endpoints**, robustez del token frente a adversarios, rate limiting → **Fase S**. El ítem de `cancelToken` aquí es funcional: "¿el flujo legítimo funciona y los links siguen siendo válidos cuando deben?".
- Panel admin en general (UI, permisos, otras funciones) → **A4**. Solo entra la consistencia de datos que `update-rsvp` escribe.
- Queries/schema en general (índices, tipos, migraciones) → **A6**.

## 4. Checklist ejecutable

> Cada ítem requiere evidencia (`archivo:línea` inspeccionado o comando + output), también al pasar. Ítems no ejecutables → `⏭️ NOT RUN` + razón. Los ítems marcados **[runtime]** idealmente se verifican con requests reales (curl contra dev local con `DATABASE_URL` de prueba o rama Neon); si no hay entorno, se resuelven por inspección de código y se anota el método en la evidencia.
>
> **Nota de método (aplica a todos los [runtime]):** no existe entorno de pruebas aislado (no hay `DATABASE_URL` de prueba ni rama de DB); ejecutar requests reales contra producción crearía RSVPs reales y podría disparar emails de confirmación a direcciones reales — violaría el mandato read-only y el objetivo "cero emails por error" de la auditoría. Todos los [runtime] se resolvieron por **traza exhaustiva de código** (archivo completo leído, no snippets) + greps; el entorno de producción se verificó de forma no intrusiva vía `vercel env ls` y runtime logs de Vercel (solo lectura).

### Grupo A — Flujo end-to-end básico

| # | Verificación | Cómo verificar | Resultado | Evidencia |
|---|--------------|----------------|-----------|-----------|
| A2-01 | **Crear**: `POST /api/rsvp` con datos válidos crea RSVP `status='confirmed'` con `eventId=slug`, y valida campos requeridos (name/email/phone) y formato de email. **[runtime]** | Inspeccionar `app/api/rsvp/route.ts:15-74` (validaciones :21-35, resolución de evento :46-65, `saveRSVP` :67-74) y `lib/queries.ts:43-56` (status `'confirmed'` :51). Confirmar que `eventId` guardado es el slug (:49). | ✅ PASA con notas (inspección) | Presencia name/email/phone `app/api/rsvp/route.ts:21-26`; regex de email :29-35; evento inexistente → 404 :60-63; `isActive` → 400 :52-58; `eventId = event.slug` :49 e insert `lib/queries.ts:50`; `status: 'confirmed'` `lib/queries.ts:51`. **Notas:** `phone` solo presencia, sin validación de formato (:21); email se inserta sin trim/lowercase (`lib/queries.ts:46` — alimenta A2-09); sin `eventSlug` en el body cae al evento estático de `event-config.json` (`route.ts:38`, id `rooftop-party-andras-oct2024`) **sin** verificar existencia ni `isActive` → **A2-H15** |
| A2-02 | **Consultar**: `GET /api/rsvp/get?rsvpId&token` devuelve el RSVP con token válido y 403/404 en los casos contrarios. Revisar QUÉ campos devuelve: el objeto de respuesta (`app/api/rsvp/get/route.ts:39-50`) incluye id, name, email, phone, plusOne, status, eventId — verificar si **falta `plusOneName`** y qué consecuencia tiene aguas abajo (ver A2-14). | Leer `app/api/rsvp/get/route.ts` completo (59 líneas). Contrastar con la interface `RSVPData` de `app/cancel/[rsvpId]/page.tsx:9-18` que sí declara `plusOneName`. | 🟡 FALLA parcial | Códigos correctos: 400 sin params `app/api/rsvp/get/route.ts:12-17`, 404 :22-27, 403 token inválido :32-37. **Confirmado: el objeto de respuesta :41-49 omite `plusOneName`** (solo id, name, email, phone, plusOne, status, eventId), mientras la interface `RSVPData` del consumidor sí lo declara (`app/cancel/[rsvpId]/page.tsx:15`) → tipo mentiroso + causa raíz de A2-14 → **A2-H07** |
| A2-03 | **Editar**: `POST /api/rsvp/update` con token válido actualiza name/email/phone/plusOne/plusOneName y responde el RSVP actualizado. **[runtime]** | Inspeccionar `app/api/rsvp/update/route.ts:4-57` (validación de campos :9, token contra email ACTUAL :26-34, `updateData` :37-43, `updateRSVP` :51) y `lib/queries.ts:90-103`. | ✅ PASA con hallazgo (inspección) | Requeridos rsvpId/token/name/email/phone `app/api/rsvp/update/route.ts:9-14`; 404 :19-24; token vs email ACTUAL :26-34; `updateData` con trim+null de plusOneName :37-43; `updateRSVP` retorna fila actualizada `lib/queries.ts:96-103` y se responde :53-57. **Hallazgos:** update NO valida formato de email (:9 solo presencia; create sí, `route.ts:29-35`) → **A2-H12**. Semántica full-overwrite: `plusOne` omitido → `false` → plusOneName `null` (:41-42); desde la UI siempre viaja payload completo (`app/cancel/[rsvpId]/page.tsx:135-144`), pero el contrato del endpoint no soporta update parcial |
| A2-04 | **Cancelar**: `POST /api/rsvp/cancel` con token válido pone `status='cancelled'` y responde éxito; token inválido → 403, RSVP inexistente → 404. **[runtime]** | Inspeccionar `app/api/rsvp/cancel/route.ts:4-47` y `lib/queries.ts:108-128` (validación :118, set cancelled :122-125). | ✅ PASA (inspección) | 400 sin params `app/api/rsvp/cancel/route.ts:9-14`; `cancelRSVP` valida token internamente `lib/queries.ts:118-120`, set `status='cancelled'` :122-125; mapeo de errores: 'Token inválido' → 403 `cancel/route.ts:28-33`, 'RSVP no encontrado' → 404 :35-40. Cancelar un RSVP ya cancelado es idempotente (no chequea status previo; re-escribe 'cancelled' — inocuo) |
| A2-05 | **Re-confirmar**: update con `reconfirm: true` sobre RSVP `cancelled` regresa a `confirmed`; sobre RSVP ya `confirmed` el flag se ignora (condición `reconfirm && currentRSVP.status === 'cancelled'`, `app/api/rsvp/update/route.ts:46-48`). La página `/cancel` manda `reconfirm` automáticamente cuando el status cargado es `cancelled` (`app/cancel/[rsvpId]/page.tsx:143`). **[runtime]** | Ejecutar (o trazar por código) la secuencia completa crear→cancelar→re-confirmar y registrar el status resultante en cada paso. | ✅ PASA (traza de código) | Secuencia trazada: crear → `'confirmed'` (`lib/queries.ts:51`) → cancelar → `'cancelled'` (`lib/queries.ts:122-125`) → `/cancel` carga status cancelled y manda `reconfirm: rsvpData?.status === 'cancelled'` (`app/cancel/[rsvpId]/page.tsx:143`) → `reconfirm && currentRSVP.status === 'cancelled'` → `'confirmed'` (`app/api/rsvp/update/route.ts:46-48`). Flag sobre RSVP ya confirmed: condición no aplica → ignorado ✅. El banner post-update tiene lógica invertida → ver A2-23 / **A2-H16** |

### Grupo B — rsvpClosed

| # | Verificación | Cómo verificar | Resultado | Evidencia |
|---|--------------|----------------|-----------|-----------|
| A2-06 | ¿`rsvpClosed=true` **bloquea la creación en la API**? `POST /api/rsvp` solo valida `event.isActive` (`app/api/rsvp/route.ts:52-58`); buscar cualquier chequeo de `rsvpClosed` en el endpoint y en `saveRSVP` (`lib/queries.ts:17-57`). `grep -n rsvpClosed app/api/rsvp/route.ts lib/queries.ts` — al cierre de este framework el único hit en queries es la lógica de reminders (:482). **[runtime]** | Inspección + (ideal) POST real a un evento con `rsvpClosed=true` y `isActive=true`, registrar si crea el RSVP (y si dispara email de confirmación, `app/api/rsvp/route.ts:77-137` — cruzar con A1). | 🔴 FALLA (inspección + grep; sin POST real: escribiría en prod) | `grep -n rsvpClosed app/api/rsvp/route.ts lib/queries.ts` → **único hit `lib/queries.ts:482`** (filtro de reminders). El POST valida solo `isActive` (`app/api/rsvp/route.ts:52-58`); `saveRSVP` (`lib/queries.ts:17-57`) tampoco chequea. Un POST a evento cerrado-pero-activo crea el RSVP (:67-74) y, con `emailConfirmationEnabled`, envía la confirmación (:77-137). Contraste: los reminders SÍ excluyen `rsvpClosed` (`lib/queries.ts:481-483`). Efecto email ya registrado como **A1-09**; el flujo → **A2-H01** |
| A2-07 | ¿`rsvpClosed=true` bloquea **edición y re-confirmación** en la API? `POST /api/rsvp/update` no carga el evento en ningún momento (`app/api/rsvp/update/route.ts` completo — sus únicos imports de queries son `updateRSVP, validateCancelToken, getRSVPById` :2); verificar que ni update ni reconfirm consultan `rsvpClosed`, `isActive` ni capacidad. | Inspección del archivo completo (67 líneas) + trazar `updateRSVP` (`lib/queries.ts:90-103`). | 🟡 FALLA | Archivo completo leído (67 líneas): imports de queries = `updateRSVP, validateCancelToken, getRSVPById` (`app/api/rsvp/update/route.ts:2`) — el evento jamás se carga; ni update ni reconfirm (:46-48) consultan `rsvpClosed`/`isActive`/capacidad; `updateRSVP` (`lib/queries.ts:90-103`) es un `.set(data)` ciego. Un invitado puede editar y **re-confirmar** en un evento cerrado o incluso desactivado → **A2-H04** |
| A2-08 | Comportamiento **UI** de `rsvpClosed`: la página pública oculta el botón y muestra `rsvpClosedMessage` (`app/[slug]/page.tsx:244-265`), pero el `RSVPModal` no re-verifica el estado al hacer submit (`app/components/RSVPModal.tsx:40-82`). ¿Qué pasa si el host cierra el RSVP mientras un invitado tiene el modal abierto (o la página cacheada)? Cruzar con A2-06: si la API tampoco bloquea, el RSVP entra. | Inspección de ambos componentes; documentar la ventana modal-abierto→submit. | 🔴 FALLA (ventana confirmada) | El evento se carga **una sola vez al montar** (`app/[slug]/page.tsx:20-54`, fetch `cache: 'no-store'` :24-26, dependencia solo `[slug]` :54); `rsvpClosed` solo oculta el CTA (:247-252). `RSVPModal.handleSubmit` (`app/components/RSVPModal.tsx:40-82`) postea directo sin re-verificar estado. **Ventana real:** invitado con la página/modal abiertos desde antes del cierre → submit → la API no bloquea (A2-06) → RSVP entra + email de confirmación. No requiere "POST directo": es el flujo normal de un usuario con pestaña abierta. Consolidado en **A2-H01** |

### Grupo C — Duplicados y capacidad

| # | Verificación | Cómo verificar | Resultado | Evidencia |
|---|--------------|----------------|-----------|-----------|
| A2-09 | **Anti-duplicados por email**: el único mecanismo es el SELECT previo en `saveRSVP` (`lib/queries.ts:28-38`) con match **exacto** de email (case-sensitive, sin trim) + `eventId`, que arroja error atrapado como 409 (`app/api/rsvp/route.ts:178-183`). Verificar: (a) `Foo@x.com` vs `foo@x.com` ¿crea duplicado?; (b) no existe unique constraint en DB (`lib/schema.ts:79-107` — confirmar también en `drizzle/`); (c) el check es check-then-insert sin transacción → dos requests simultáneos pueden duplicar (documentar como comportamiento, la corrección es del plan correctivo). **[runtime]** | Inspección + prueba con email en distinta capitalización. `grep -rn "unique" drizzle/ lib/schema.ts` para (b). | 🟡 FALLA (a)(b)(c) (inspección; sin prueba real: escribiría en prod) | (a) match exacto `eq(rsvps.email, ...)` + `eq(rsvps.eventId, ...)` (`lib/queries.ts:31-32`) — `=` de Postgres sobre text es case-sensitive y no hay trim/lowercase en todo el ciclo (`grep -rn toLowerCase app/api/rsvp lib/queries.ts` → 0 hits) → `Foo@x.com` vs `foo@x.com` SÍ crea duplicado (y recibiría 2 emails) → **A2-H05**. (b) `grep -rn unique drizzle/ lib/schema.ts`: solo `events_slug_unique`, `user_sessions_token_unique`, `users_email_unique`; snapshot `drizzle/meta/0000_snapshot.json`: `public.rsvps → uniqueConstraints: {} | indexes: {}` (verificado parseando el JSON). (c) SELECT :28-38 + INSERT :43-54 sin transacción (driver `drizzle-orm/neon-http`, `lib/db.ts:2` — HTTP sin soporte transaccional) → carrera duplica → **A2-H06** |
| A2-10 | **Duplicados por teléfono**: ¿existe algún chequeo por `phone`? (al cierre del framework: ninguno en `lib/queries.ts:28-38` ni en el endpoint). Mismo invitado con dos emails distintos y mismo teléfono → dos RSVPs. Documentar si esto es aceptado por diseño o hallazgo. | `grep -n "phone" lib/queries.ts app/api/rsvp/route.ts` + inspección. | ⚪ Documentado (sin hallazgo) | `grep -n phone lib/queries.ts app/api/rsvp/route.ts`: todos los hits son captura/insert (`route.ts:18,21,70,155`; `queries.ts:20,47,92`) — **ningún chequeo de duplicado por phone**. Mismo invitado con 2 emails → 2 RSVPs. Se documenta como diseño aceptado: la identidad del invitado es el email (el token, la confirmación y el dedup giran sobre email); dedup por teléfono introduciría falsos positivos (familiares que comparten número) |
| A2-11 | **Duplicado vs cancelado**: el check de `lib/queries.ts:28-38` NO filtra por status → un invitado que canceló y quiere volver a inscribirse desde la página pública recibe 409 con el mensaje "Ya confirmaste tu asistencia anteriormente" (`app/api/rsvp/route.ts:180`), que es engañoso (está cancelado, no confirmado). Su único camino es re-confirmar vía su link de cancelación (que quizá ya no conserva). Evaluar corrección del flujo y del mensaje. **[runtime]** | Trazar crear→cancelar→POST /api/rsvp de nuevo con el mismo email; registrar respuesta y mensaje mostrado por el modal (`app/components/RSVPModal.tsx:72-75, 269-273`). | 🔴 FALLA (traza de código) | Secuencia trazada: crear → cancelar (`status='cancelled'`) → nuevo POST mismo email: el SELECT (`lib/queries.ts:28-38`) **no filtra status**, encuentra el cancelado → throw :36-37 → 409 "Ya confirmaste tu asistencia anteriormente" (`app/api/rsvp/route.ts:178-183`) → el modal lo muestra tal cual (`app/components/RSVPModal.tsx:73-74, 269-273`). Mensaje **falso** (está cancelado) y flujo **bloqueado**: el único self-service es el link del email — que no existe si `emailConfirmationEnabled=false` (`route.ts:77` — el email nunca se envió) o si lo perdió. → **A2-H03** |
| A2-12 | **Capacidad**: ¿`capacityEnabled`/`capacityLimit` (`lib/schema.ts:29-30`) se aplican en ALGÚN punto del ciclo de creación/re-confirmación? Al cierre del framework, `grep -rn capacity app/api/rsvp lib/queries.ts` no arroja hits: el límite solo se **muestra** como texto en la página pública (`app/[slug]/page.tsx:227-233`) y se configura en admin. Verificar qué pasa al llegar al límite (¿se sigue aceptando?) y si `plusOne` contaría doble en algún conteo. | Inspección + grep; probar (o trazar) creación con cupo "lleno". | 🔴 FALLA | `grep -rn capacity app/api/rsvp lib/queries.ts` → **0 hits** (re-ejecutado; los únicos archivos con "capacity" son admin UI, event-settings/events endpoints, `[slug]/page.tsx`, schema y config). El límite solo se muestra: "⚠️ Cupo limitado: {N} personas" (`app/[slug]/page.tsx:227-233`). Ni create (`app/api/rsvp/route.ts:67-74`) ni reconfirm (`update/route.ts:46-48`) cuentan confirmados vs `capacityLimit` → con cupo "lleno" se sigue aceptando sin límite. `plusOne` no entra a ningún conteo de capacidad (no existe conteo) → un límite de 50 puede volverse 100 personas con +1s → **A2-H02** |
| A2-13 | **Conteo de asistentes**: `getEventStats` (`lib/queries.ts:167-182`) devuelve `totalConfirmed: allRsvps.length` — que en realidad es el TOTAL incluyendo cancelados — y NO suma acompañantes (`plusOne`). Verificar quién consume esta función (nota: existe una homónima en `lib/firestore.ts:159` — ¿código muerto? cruzar con A5) y si algún conteo mostrado al host cuenta `plusOne` como segunda persona. | `grep -rn "getEventStats" app lib` + inspección de cómo el admin calcula totales (`app/admin/page.tsx`, buscar `confirmed`/`filter`). | ✅ PASA (conteos activos correctos) | `grep -rn getEventStats app lib` → solo las **definiciones** (`lib/queries.ts:167`, `lib/firestore.ts:159`), **cero llamadores** → función muerta con campo mentiroso (`totalConfirmed: allRsvps.length` incluye cancelados, `lib/queries.ts:170-178`) pero sin impacto funcional → fuera de scope A5. Los conteos que el host SÍ ve se calculan aparte y **correctamente**: `confirmedRsvps = rsvps.filter(r => r.status === 'confirmed')` (`app/admin/page.tsx:1169`), `totalGuests: confirmedRsvps.length + confirmedRsvps.filter(r => r.plusOne).length` (:1175 — +1 cuenta como segunda persona), export texto :1019,:1041 y CSV :1118-1119 con la misma lógica |

### Grupo D — plusOne / plusOneName

| # | Verificación | Cómo verificar | Resultado | Evidencia |
|---|--------------|----------------|-----------|-----------|
| A2-14 | **Pérdida de plusOneName al editar vía /cancel**: `GET /api/rsvp/get` no devuelve `plusOneName` (`app/api/rsvp/get/route.ts:41-49`) → la página lo inicializa siempre vacío (`app/cancel/[rsvpId]/page.tsx:83`, `data.rsvp.plusOneName || ''` sobre un campo que no viene). En el submit se manda `plusOneName: plusOne ? plusOneName : ''` (:142) y el endpoint lo normaliza a `null` si viene vacío (`app/api/rsvp/update/route.ts:42`). Verificar la consecuencia: un invitado con +1 nombrado que actualiza cualquier dato (p.ej. teléfono) ¿**borra silenciosamente** el nombre de su acompañante? Considerar ambos casos: `requirePlusOneName=true` (campo visible pero vacío y required, :375-391) y `=false` (campo ni se muestra). **[runtime]** | Trazar el ciclo completo con un RSVP que tenga `plusOneName` en DB; confirmar el valor de la columna tras el update. | 🟡 FALLA (traza de código) | Cadena completa confirmada: GET omite `plusOneName` (`app/api/rsvp/get/route.ts:41-49`) → init `''` (`app/cancel/[rsvpId]/page.tsx:83`) → submit `plusOneName: plusOne ? plusOneName : ''` (:142) → update normaliza `''.trim() \|\| null` → `null` (`app/api/rsvp/update/route.ts:42`) → **columna borrada**. Caso `requirePlusOneName=true`: campo visible+required vacío (:375-391) → el invitado debe re-tipear (sin pérdida silenciosa, pero fricción y riesgo de inconsistencia). Caso `=false` **o fallo del fetch del evento** (catch :101-103 deja `eventData=null` → `eventData?.requirePlusOneName` undefined → campo NO se renderiza :375): submit manda `''` → **pérdida silenciosa** del nombre del acompañante al editar cualquier dato → **A2-H07** |
| A2-15 | **Consistencia de normalización entre los 3 caminos de escritura**: create (`app/api/rsvp/route.ts:72`) y update público (`app/api/rsvp/update/route.ts:42`) guardan `plusOne ? (plusOneName?.trim() \|\| null) : null`; el **admin** (`app/api/admin/update-rsvp/route.ts:61`) pasa `updates` (el `editForm` completo, `app/admin/page.tsx:648-686`, :671) **sin trim ni normalización** → puede persistir `plusOneName: ''` (string vacío en vez de null) o un nombre huérfano con `plusOne=false`. Verificar y documentar las combinaciones resultantes posibles en DB. | Inspección de los tres endpoints + del modal de edición admin (`app/admin/page.tsx:624-631` inicialización del form). | 🟡 FALLA | Público normaliza: create `app/api/rsvp/route.ts:72`, update `app/api/rsvp/update/route.ts:42` (`plusOne ? trim\|\|null : null`). Admin NO: `saveEdit` manda `updates: editForm` completo (`app/admin/page.tsx:669-672`) y el endpoint lo pasa verbatim a `updateRSVP` (`app/api/admin/update-rsvp/route.ts:61`). `editForm.plusOneName` init `rsvp.plusOneName \|\| ''` (:631); al desmarcar plusOne el JSX lo limpia a `''` (:2480) — **string vacío, no null, y sin trim**. Combinaciones posibles en DB vía admin: `plusOne=false + plusOneName=''`, `plusOne=true + plusOneName=''` (evento sin require), nombres con espacios ` X `. Nombre huérfano con plusOne=false NO es alcanzable desde la UI admin (el uncheck limpia), pero sí `''` donde público escribe `null` → queries/filtros divergentes. Además el camino admin no valida formato de email (:651 solo presencia). → **A2-H08** |
| A2-16 | **`requirePlusOneName` solo se valida en cliente**: el modal lo exige por JS (`app/components/RSVPModal.tsx:47-52`) y la página /cancel por atributo `required` condicionado a `eventData?.requirePlusOneName` (`app/cancel/[rsvpId]/page.tsx:375`), pero ni `POST /api/rsvp` ni `/api/rsvp/update` verifican el flag del evento (grep `requirePlusOneName` en `app/api/` — al cierre solo aparece en event-settings/events endpoints). Un POST directo (o un fallo de carga de `eventData` en /cancel, :101-103) permite `plusOne=true` sin nombre en eventos que lo requieren. | Inspección + grep. | 🟡 FALLA | `grep -rn requirePlusOneName app/api` → solo `event-settings/route.ts:91,137` y `admin/event-settings/update/route.ts:114` (config del flag) — **ningún hit en los endpoints de RSVP**. Cliente: modal valida por JS (`app/components/RSVPModal.tsx:47-52`); /cancel usa `required` condicionado (`app/cancel/[rsvpId]/page.tsx:375`) que desaparece si el fetch del evento falla (:101-103). El server acepta `plusOne=true` sin nombre en eventos que lo exigen (create `route.ts:72` y update `update/route.ts:42` lo normalizan a `null` sin consultar el flag) → **A2-H09** |

### Grupo E — cancelToken (funcional)

| # | Verificación | Cómo verificar | Resultado | Evidencia |
|---|--------------|----------------|-----------|-----------|
| A2-17 | **Validación funcional del token**: (a) el token del email se genera con el `rsvp.id` real (`app/api/rsvp/route.ts:104-105`) y la validación recalcula con id+email actual (`lib/queries.ts:195-198`) → el flujo legítimo funciona; (b) la **columna** `rsvps.cancelToken` se llena en `saveRSVP` con un token derivado de `crypto.randomUUID()` — un UUID que NO es el id real del RSVP (`lib/queries.ts:41` vs el id generado por `$defaultFn` en `lib/schema.ts:80`) → el valor almacenado nunca coincide con el token de los links y no se lee en ninguna validación. Confirmar que la columna es valor muerto/engañoso (`grep -rn "cancelToken" app lib --include="*.ts" --include="*.tsx"`); (c) un token de OTRO rsvp/email simplemente no valida (recalculo determinístico) — verificar que ambos endpoints (get/update/cancel) validan contra el email del RSVP correcto (`get:30`, `update:27`, `cancel → lib/queries.ts:118`). | Inspección + grep. NO evaluar fuerza criptográfica ni escenarios adversariales (Fase S). | (a)(c) ✅ · (b) 🟡 confirmado | (a) Email genera con id real post-insert: `generateCancelToken(rsvp.id, email)` (`app/api/rsvp/route.ts:104-105`); validación recalcula determinístico (`lib/queries.ts:195-198`) → flujo legítimo OK. (b) **Confirmado valor muerto/engañoso**: `saveRSVP` genera la columna con `generateCancelToken(crypto.randomUUID(), email)` (`lib/queries.ts:41`) — UUID fantasma distinto del id real que `$defaultFn` genera en el insert (`lib/schema.ts:80`) → jamás coincide con los links. Grep `cancelToken` en app/lib: todos los usos **generan al vuelo** (`send-email:85`, `send-bulk-email:106`, `send-bulk-reminder:125`, `cron:143`, `rsvp:104`); **nadie lee la columna** (`lib/firestore.ts:94-100` es legacy) → **A2-H10**. (c) Los tres caminos cargan el RSVP por id y validan contra SU email: `get/route.ts:20,30`, `update/route.ts:17,27`, `cancel → lib/queries.ts:111-118` ✅ |
| A2-18 | **Estabilidad de links**: el token depende de `CANCEL_TOKEN_SECRET` (fallback `'default-secret'`, `lib/queries.ts:189`) y del email vigente. Verificar funcionalmente: (a) ¿`CANCEL_TOKEN_SECRET` está definido en el entorno de producción (Vercel)? — si algún día se define/cambia habiendo links emitidos con el fallback, TODOS los links viejos mueren; (b) links en emails ya enviados ¿siguen siendo válidos hoy? **[runtime parcial]** | Revisar variables de entorno del proyecto en Vercel (o `.env*` local, sin volcar valores) y validar un link real si hay uno disponible. Si no hay acceso → NOT RUN con razón. | (a) ✅ verificado en prod · (b) ⏭️ NOT RUN parcial | (a) `vercel env ls` (proyecto timekast/partytime): **`CANCEL_TOKEN_SECRET` presente en Development/Preview/Production, creado hace 246d** — anterior incluso a `DATABASE_URL` (192d, migración a Neon) → todos los links de la era Neon se generaron con el mismo secret y no ha habido cambio (una re-creación mostraría fecha más reciente) → sin invalidación masiva. El fallback `'default-secret'` (`lib/queries.ts:189`) solo aplicaría sin la env — nota: dev local sin esa var genera tokens incompatibles con prod. (b) Validar un link real: **NOT RUN** — requeriría un link real de invitado o leer el valor del secret (prohibido volcar valores); runtime logs de prod (retención Hobby ~1h) sin tráfico a `/cancel` ni `/api/rsvp/get` en la ventana (query "cancel" → 0 hits). Evidencia indirecta de ciclo vivo: `GET /api/rsvp 200` × 11 del panel admin entre 15:32-16:00 UTC de hoy |
| A2-19 | **Cambio de email en update invalida el link en uso**: el token se valida contra el email ACTUAL (`app/api/rsvp/update/route.ts:26-27`). Tras un update que cambia el email: (a) el token del URL con el que el invitado está en la página deja de validar → recargar `/cancel/...` o pulsar "Cancelar mi Asistencia" (`app/cancel/[rsvpId]/page.tsx:168-205`) fallará con "Token inválido o expirado"; (b) NO se envía email al nuevo (ni al viejo) con el link regenerado — el update no dispara ningún envío (archivo completo `app/api/rsvp/update/route.ts`, sin imports de resend). Verificar la secuencia real y documentar cómo queda el invitado (¿sin ningún link válido en su poder?). **[runtime]** | Trazar/probar: crear → update cambiando email → intentar cancelar con el mismo token. Cruzar el (no-)envío con A1. | 🟡 FALLA (traza de código) | Secuencia trazada: (1) update con email nuevo valida contra email VIEJO (aún en DB) → pasa (`app/api/rsvp/update/route.ts:26-34`) y persiste el email nuevo (:37-51). (2) Cualquier acción posterior con el mismo token — "Cancelar mi Asistencia" (`app/cancel/[rsvpId]/page.tsx:168-205`, mismo `token` del URL :188-190) o recargar la página — recalcula con el email NUEVO (`lib/queries.ts:195-198`) → 403 "Token inválido o expirado". (3) Ningún email sale del update: imports del archivo completo = next/server + queries (`update/route.ts:1-2`), sin resend → **el invitado queda sin ningún link válido en su poder** (el link del email original apunta al token viejo). Único remedio: admin reenvía confirmación (`send-email` regenera token con el email vigente, `app/api/admin/send-email/route.ts:85-86`). Cruce A1: el no-envío en update no está registrado como punto de envío (correcto, no existe) → **A2-H11** |

### Grupo F — Email history, eventos pasados y estados de UI

| # | Verificación | Cómo verificar | Resultado | Evidencia |
|---|--------------|----------------|-----------|-----------|
| A2-20 | **emailHistory al cambiar email**: `recordEmailSent` (`lib/queries.ts:133-162`) solo appendea `{sentAt, type}` — no registra a QUÉ dirección se envió. Tras un cambio de email, el historial (`lib/schema.ts:97-100`) queda asociado al RSVP sin rastro del email original. El update público y el admin-update no escriben nada en `emailHistory`. Verificar y documentar si esto compromete alguna decisión del host (p.ej. "¿ya le llegó la confirmación?" tras un cambio de email la respuesta del historial es engañosa). | Inspección de `recordEmailSent` y de quién lo llama (`grep -rn "recordEmailSent" app lib`). | 🟡 FALLA (limitación confirmada) | `recordEmailSent` appendea solo `{sentAt, type}` (`lib/queries.ts:146-158`); schema del JSONB sin campo de dirección (`lib/schema.ts:97-100`). Callers (grep): `rsvp/route.ts:128` (confirmation), `cron/send-reminders:176` (reminder), `send-email:132`, `send-bulk-email:142`, `send-bulk-reminder:153` — ninguno registra destinatario; update público (`update/route.ts`) y admin-update (`update-rsvp/route.ts`) no escriben en emailHistory (sin llamadas). Consecuencia: tras un cambio de email, el host ve `emailSent`/historial "confirmation ✓" sobre un RSVP cuyo email actual **nunca recibió nada** — el filtro "📧 enviados" del admin (`app/admin/page.tsx:359,388,1176`) y la decisión de (no) reenviar se basan en dato engañoso → **A2-H13** |
| A2-21 | **Eventos pasados**: ¿se puede crear/editar/re-confirmar un RSVP para un evento cuya fecha ya pasó? `events.date` es texto libre (`lib/schema.ts:18`) y `POST /api/rsvp` solo valida `isActive` (`app/api/rsvp/route.ts:52-58`) — no hay comparación de fechas en ningún endpoint del ciclo (los únicos chequeos de fecha del repo están en el cron de reminders). Documentar el comportamiento y si el host tiene forma de cerrar el evento más allá de `rsvpClosed`/`isActive` manual. | Inspección + `grep -rn "new Date\|Date.now" app/api/rsvp/` + revisar cómo se compara fecha en `app/api/cron/send-reminders/route.ts` como contraste. | 🟡 FALLA (documentado) | `grep -rn "new Date\|Date.now" app/api/rsvp/` → solo el modo demo (`route.ts:152,158`). `events.date` es texto libre (`lib/schema.ts:18`) — imposible de comparar sin parseo, y nadie lo parsea. Contraste: el único chequeo de fecha del ciclo de reminders es sobre `reminderScheduledAt` (timestamp real, `lib/queries.ts:462-478`), nunca sobre `events.date` (A1-04 ya registró que el filtro de fecha pasada de reminders se perdió en `0e7ad21`). Resultado: **crear/editar/re-confirmar sigue funcionando indefinidamente para eventos pasados**; el único cierre es manual (`rsvpClosed` `lib/schema.ts:63` / `isActive` :60) — y A2-06/07 muestran que `rsvpClosed` ni siquiera cierra la API → **A2-H14** |
| A2-22 | **Estados y mensajes del modal RSVP**: recorrer los 3 estados (`idle/success/error`, `app/components/RSVPModal.tsx:37`) y verificar: (a) el mensaje de éxito "¡Confirmado!" (:145-162) se muestra igual haya o no email de confirmación habilitado — ¿promete algo que no ocurre?; (b) el 409 de duplicado se muestra con el texto del server (:73-74) — cruzar con A2-11 sobre lo engañoso del mensaje para cancelados; (c) el auto-cierre a los 2.5s (:67-71) y el reset del form; (d) error de conexión (:76-78). | Inspección + prueba manual en dev si hay entorno. | ✅ PASA con cruces (inspección; sin entorno dev con DB) | (a) Éxito "¡Confirmado! / Nos vemos en la fiesta" (`app/components/RSVPModal.tsx:159-161`) **no promete email** — correcto que sea idéntico con/sin `emailConfirmationEnabled` ✅. (b) 409 muestra el texto del server (:73-74 → :269-273) — el texto en sí es el problema (falso para cancelados), registrado en **A2-H03** (A2-11). (c) Auto-cierre 2.5s + reset completo del form + vuelta a idle (:67-71) ✅. (d) Catch de conexión con mensaje propio (:76-78) ✅. Estado `isSubmitting` deshabilita inputs y botón durante el POST (:42,:178,:278) — sin doble-submit desde la misma sesión de modal ✅ |
| A2-23 | **Estados y mensajes de la página /cancel**: recorrer loading (:207-215), link inválido (:66-70), cancelado-éxito (:220-236), warning de RSVP cancelado (:290-294), mensaje post-update que distingue reconfirmación de actualización (:302-308), botón dinámico (:404-406) y que "Cancelar mi Asistencia" solo aparece con status `confirmed` (:410-422). Verificar coherencia del estado local tras update (setRsvpData con la respuesta, :152) y tras reconfirm. | Inspección de `app/cancel/[rsvpId]/page.tsx` + prueba manual si hay entorno. | 🟢 FALLA parcial (inspección) | Loading :207-215 ✅; sin rsvpId/token → 'Link inválido' :66-70 + card de error :238-251 ✅; cancelado-éxito :220-236 ✅; warning cancelado :290-294 ✅; botón dinámico Reconfirmar/Actualizar :402-407 ✅; "Cancelar mi Asistencia" solo con `status==='confirmed'` :410-422 ✅; estado local tras update coherente: `setRsvpData(data.rsvp)` :152 con la fila fresca del server (incluye plusOneName y status reales) ✅. **PERO** el mensaje post-update :302-308 condiciona sobre el estado FINAL (`rsvpData?.status === 'confirmed'`), que tras cualquier update exitoso es siempre 'confirmed' → **siempre muestra "✅ ¡Asistencia reconfirmada!"**, incluso al solo cambiar el teléfono; la rama "Información actualizada correctamente" es inalcanzable; `wasReconfirmed` (:150) se calcula y no se usa para el mensaje → **A2-H16** |
| A2-24 | **Modo demo en producción**: confirmar que el branch demo de `POST /api/rsvp` (`app/api/rsvp/route.ts:147-173`, `mockRsvps` :13) es inalcanzable en producción (`isDatabaseConfigured`, `lib/db.ts`) y anotar el array module-level como candidato a A5 (código muerto en prod). | Inspección de `lib/db.ts` + `vercel env` conceptual (no volcar valores). | ✅ PASA (verificado en prod) | `isDatabaseConfigured() = !!databaseUrl && !!db` (`lib/db.ts:19-21`), evaluado a nivel de módulo desde `process.env.DATABASE_URL` (:6-16). `vercel env ls`: **`DATABASE_URL` presente en Development/Preview/Production** (creado 192d) → el branch demo del POST (`app/api/rsvp/route.ts:147-173`) y del GET (:236-246) más el array `mockRsvps` (:13) son inalcanzables en prod. Candidato a limpieza → fuera de scope A5 |

## 5. Hallazgos

> Registrar aquí SOLO hallazgos con evidencia `archivo:línea`. IDs secuenciales A2-Hxx. Severidad según rúbrica del INDEX (🔴 rompe flujo / email indebido · 🟡 edge case incorrecto o deuda que causará bugs · 🟢 limpieza).

| ID | Severidad | Descripción | Evidencia |
|----|-----------|-------------|-----------|
| A2-H01 | 🔴 | **`rsvpClosed` no cierra el RSVP en ningún camino de escritura — solo oculta el botón.** Creación: el POST valida solo `isActive`; único uso de `rsvpClosed` en el ciclo es el filtro de reminders. Ventana real sin "POST directo": la página carga el evento una sola vez al montar, así que un invitado con la pestaña/modal abiertos desde antes del cierre hace submit → el RSVP entra **y sale el email de confirmación** en un evento cerrado (efecto email = A1-09). | `app/api/rsvp/route.ts:52-58,67-74,77-137`; grep `rsvpClosed` → único hit `lib/queries.ts:482`; `app/[slug]/page.tsx:20-54,247-252`; `app/components/RSVPModal.tsx:40-82` |
| A2-H02 | 🔴 | **La capacidad (`capacityEnabled`/`capacityLimit`) no se aplica en ningún punto del ciclo** — solo se muestra como texto en la página pública y se configura en admin. Con el cupo lleno se siguen aceptando RSVPs (y +1s, que ni siquiera entran a un conteo) → overbooking de eventos con cupo/precio reales. | `grep -rn capacity app/api/rsvp lib/queries.ts` → 0 hits; `lib/schema.ts:29-30`; display `app/[slug]/page.tsx:227-233`; create sin conteo `app/api/rsvp/route.ts:67-74`; reconfirm sin conteo `app/api/rsvp/update/route.ts:46-48` |
| A2-H03 | 🔴 | **Un invitado cancelado no puede volver a inscribirse desde la página pública**: el check de duplicado no filtra `status`, responde 409 con el mensaje **falso** "Ya confirmaste tu asistencia anteriormente". Su único self-service es el link de cancelación, que no existe si `emailConfirmationEnabled=false` (nunca se envió) o si perdió el email → flujo legítimo bloqueado sin intervención del admin. | `lib/queries.ts:28-38`; `app/api/rsvp/route.ts:178-183`; `app/components/RSVPModal.tsx:73-74,269-273` |
| A2-H04 | 🟡 | **Update y re-confirmación son ciegos al evento**: `/api/rsvp/update` nunca carga el evento (imports solo de RSVP), no consulta `rsvpClosed`, `isActive` ni capacidad → se puede editar y re-confirmar asistencia en un evento cerrado o desactivado. | `app/api/rsvp/update/route.ts:2` (imports), :46-48; `lib/queries.ts:90-103` |
| A2-H05 | 🟡 | **Duplicados por capitalización/espacios de email**: el dedup usa match exacto (`=` case-sensitive de Postgres, sin trim/lowercase en todo el ciclo) → `Foo@x.com` y `foo@x.com` crean dos RSVPs y reciben dos confirmaciones. | `lib/queries.ts:31-32`; insert sin normalizar `lib/queries.ts:46`; `grep toLowerCase` en el ciclo → 0 hits |
| A2-H06 | 🟡 | **Dedup check-then-insert sin transacción ni unique constraint**: SELECT previo + INSERT separados sobre driver `neon-http` (sin transacciones); la tabla `rsvps` no tiene unique ni índices → dos submits simultáneos del mismo email duplican. | `lib/queries.ts:28-54`; `drizzle/meta/0000_snapshot.json` → `public.rsvps uniqueConstraints {} / indexes {}`; `lib/db.ts:2` (neon-http) |
| A2-H07 | 🟡 | **Pérdida silenciosa de `plusOneName` al editar vía /cancel**: `GET /api/rsvp/get` omite `plusOneName` en la respuesta (el tipo del consumidor sí lo declara) → la página lo inicializa `''` → el update lo normaliza a `null` → editar cualquier dato borra el nombre del acompañante cuando el campo no se renderiza (`requirePlusOneName=false`, o fallo del fetch del evento). Con `requirePlusOneName=true` obliga a re-tipear. | `app/api/rsvp/get/route.ts:41-49`; `app/cancel/[rsvpId]/page.tsx:15,83,101-103,142,375`; `app/api/rsvp/update/route.ts:42` |
| A2-H08 | 🟡 | **El camino admin escribe sin la normalización de los caminos públicos**: `update-rsvp` pasa `updates` (el `editForm` entero) verbatim a `updateRSVP` — persiste `plusOneName: ''` (string vacío donde público escribe `null`), sin trim, y sin validación de formato de email → datos divergentes según quién editó. | `app/api/admin/update-rsvp/route.ts:61`; `app/admin/page.tsx:631,651,669-672,2480`; contraste `app/api/rsvp/route.ts:72`, `app/api/rsvp/update/route.ts:42` |
| A2-H09 | 🟡 | **`requirePlusOneName` solo se valida en cliente**: ni `POST /api/rsvp` ni `/api/rsvp/update` consultan el flag del evento → `plusOne=true` sin nombre entra en eventos que lo exigen (POST directo, o /cancel cuando el fetch del evento falla y el `required` desaparece). | `app/components/RSVPModal.tsx:47-52`; `app/cancel/[rsvpId]/page.tsx:101-103,375`; `grep requirePlusOneName app/api` → solo event-settings (`event-settings/route.ts:91,137`, `admin/event-settings/update/route.ts:114`) |
| A2-H10 | 🟡 | **La columna `rsvps.cancelToken` almacena un token que jamás coincide con los links**: `saveRSVP` la genera con `crypto.randomUUID()` (un id fantasma, no el id real que `$defaultFn` crea en el insert) y ninguna validación la lee (todos los usos generan el token al vuelo) → dato muerto y engañoso; un dev futuro que "valide contra la columna" rompería todos los links. | `lib/queries.ts:41` vs `lib/schema.ts:80,103`; grep `cancelToken`: generación al vuelo en `send-email:85`, `send-bulk-email:106`, `send-bulk-reminder:125`, `cron:143`, `rsvp:104`; cero lecturas de la columna |
| A2-H11 | 🟡 | **Cambiar el email desde /cancel invalida el único link del invitado sin emitir uno nuevo**: el token se recalcula contra el email vigente, y el update no envía ningún email con el link regenerado → tras corregir su email, el invitado no puede volver a cancelar/editar (403 "Token inválido o expirado") hasta que un admin le reenvíe la confirmación. | `app/api/rsvp/update/route.ts:1-2,26-34`; `lib/queries.ts:195-198`; `app/cancel/[rsvpId]/page.tsx:168-205`; remedio admin `app/api/admin/send-email/route.ts:85-86` |
| A2-H12 | 🟡 | **`/api/rsvp/update` no valida formato de email** (solo presencia), mientras create sí usa regex → un email malformado persiste, invalida el link (token depende del email) y rompe envíos futuros a ese RSVP. | `app/api/rsvp/update/route.ts:9` vs `app/api/rsvp/route.ts:29-35` |
| A2-H13 | 🟡 | **`emailHistory` no registra la dirección destino** (`{sentAt, type}`): tras un cambio de email, el host ve "confirmation enviada ✓" en un RSVP cuyo email actual nunca recibió nada — el filtro "enviados" del admin y la decisión de reenviar se basan en dato engañoso. | `lib/queries.ts:146-158`; `lib/schema.ts:97-100`; consumo en filtros/stats `app/admin/page.tsx:359,388,1176` |
| A2-H14 | 🟡 | **El ciclo no tiene noción de fecha**: `events.date` es texto libre y ningún endpoint la compara → eventos pasados siguen aceptando crear/editar/re-confirmar indefinidamente; el único cierre es manual (`rsvpClosed`/`isActive`), y `rsvpClosed` ni siquiera cierra la API (A2-H01). | `lib/schema.ts:18,60,63`; `grep "new Date\|Date.now" app/api/rsvp/` → solo demo (:152,158); contraste reminders `lib/queries.ts:462-478` |
| A2-H15 | 🟡 | **`POST /api/rsvp` sin `eventSlug` cae al evento hardcodeado de `event-config.json`** (`rooftop-party-andras-oct2024`) sin verificar existencia ni `isActive` → RSVPs huérfanos posibles vía API directa a un evento legacy de 2024 (ninguna UI actual omite el slug, pero el contrato lo permite). | `app/api/rsvp/route.ts:37-38,46,67-74` (el check de evento solo corre `if (eventSlug)`); `event-config.json` (`event.id`) |
| A2-H16 | 🟢 | **El banner post-update de /cancel siempre dice "¡Asistencia reconfirmada!"**: condiciona sobre el estado final (siempre `'confirmed'` tras un update exitoso) en vez del estado previo → la rama "Información actualizada correctamente" es inalcanzable; `wasReconfirmed` se calcula y no se usa para el mensaje. | `app/cancel/[rsvpId]/page.tsx:150,302-308` |

**Conteo: 3🔴 · 12🟡 · 1🟢**

## 6. Hallazgos fuera de scope

> Anotar factualmente, con referencia cruzada a la auditoría dueña (A1, A4, A5, A6, A7, Fase S). No profundizar.

| Ref → | Descripción | Evidencia |
|-------|-------------|-----------|
| A1 | El efecto email de A2-H01 (confirmación enviada para evento con `rsvpClosed=true`) ya está registrado como **A1-09**; aquí se registra el flujo (no se duplica el efecto email). | `docs/audits/01_emails_recordatorios.md` A1-09, A1-OOS-04 |
| A5 | `getEventStats` (`lib/queries.ts:167-182`) sin llamadores en app/lib, con campo `totalConfirmed` mentiroso (incluye cancelados); homónima legacy en `lib/firestore.ts:159` → código muerto. | `grep -rn getEventStats app lib` → solo definiciones |
| A5 | Branch demo de `/api/rsvp` (POST :147-173, GET :236-246) + array module-level `mockRsvps` (:13): inalcanzable en prod (DATABASE_URL presente en Vercel) → código muerto en producción. | `app/api/rsvp/route.ts:13,147-173,236-246`; `lib/db.ts:19-21`; `vercel env ls` |
| A5/A4 | Endpoint `POST /api/admin/add-demo-data` siembra 7 RSVPs hardcodeados (incluye un email real de persona) al evento legacy de `event-config.json` — utilidad demo viva en producción. | `app/api/admin/add-demo-data/route.ts:7-57,76` |
| A6 | Tabla `rsvps` sin unique constraints ni índices de ningún tipo (ni siquiera por `eventId`, su columna de filtrado principal); `status` varchar sin enum/check; `events.date` texto libre. | `drizzle/meta/0000_snapshot.json` (`public.rsvps uniques {} / indexes {}`); `lib/schema.ts:18,93` |
| A3 | Semántica solapada de `isActive=false` vs `rsvpClosed=true`: la página muestra para `isActive=false` un mensaje genérico hardcodeado ("Las inscripciones para este evento están cerradas") mientras `rsvpClosed` tiene mensaje configurable (`rsvpClosedMessage`) — dos formas de "cerrar" con comportamientos distintos (una bloquea la API, la otra no). | `app/[slug]/page.tsx:100-110,247-252`; `app/api/rsvp/route.ts:52-58` |
| Fase S | El `cancelToken` es determinístico (sha256 truncado a 32 hex de `id-email-secret`) con fallback `'default-secret'` si falta la env; la robustez adversarial del esquema y del fallback se evalúa en Fase S (aquí solo se auditó el flujo legítimo). | `lib/queries.ts:188-193` |

## 7. Cierre

1. ✅ Todos los ítems A2-01…A2-24 tienen resultado con evidencia; único `⏭️ NOT RUN` parcial: A2-18(b) (validar un link real requeriría el valor del secret o un link de invitado; logs de prod con retención ~1h sin tráfico a /cancel).
2. ✅ Conteo consolidado: **3🔴 · 12🟡 · 1🟢**.
3. ✅ Fila A2 del `docs/audits/00_INDEX.md` actualizada a ✅ con el conteo.
4. ✅ Commit + push: `audit: A2 rsvp-lifecycle — 3🔴 12🟡 1🟢`.
