# 🧹 A5 — Código muerto y duplicado

> **Estado:** ✅ completada · **Owner/Sesión:** fable5-675614-a5 · **Inicio:** 2026-07-09 11:02 · **SHA auditado:** `61f74e5` (referencia del framework y de la pre-verificación: `bcc7f1e`; el working tree al ejecutar era `9b06ced`, que solo difiere de `61f74e5` en `docs/audits/00_INDEX.md` — código idéntico, verificado con `git diff --stat 61f74e5..HEAD`)
>
> **Protocolo:** seguir `docs/audits/00_INDEX.md` — `git pull --ff-only`, adquirir lock (marcar A5 🔄 en la tabla del INDEX + commit + push exitoso), ejecutar read-only (prohibido editar código de la app; solo se escribe en este MD y en el INDEX), evidencia obligatoria en TODOS los ítems (también los que pasan), cerrar con ✅ + conteo.

---

## 1. Objetivo

Identificar y documentar con evidencia todo el **código muerto** (archivos/deps/rutas sin ningún importador ni referencia), **duplicado** (rutas/archivos redundantes) y **lógica duplicada divergente** (bloques repetidos que ya divergieron entre sí) del repo.

⚠️ **Esta auditoría NO borra nada.** BORRAR/consolidar es del plan correctivo (post `99_CONSOLIDADO.md`), no de la auditoría. Aquí solo se produce el inventario con veredictos y evidencia reproducible.

## 2. Contexto mínimo (para sesión fría)

La app nació como **invitación single-event** ("Rooftop Party" de Andrreas, ver `event-config.json`) sobre **Firebase/Firestore** (y antes Cosmos DB, ver `DOCUMENTATION_UPDATE_REPORT.md`). Después migró en dos ejes:

1. **Single-event → multi-event**: eventos en tabla `events` con slug propio; `event-config.json` quedó como fallback legacy en muchos endpoints.
2. **Firebase → Neon PostgreSQL + Drizzle**: `lib/firestore.ts` fue reemplazado por `lib/queries.ts`/`lib/db.ts`; la migración se hizo con scripts one-shot (`scripts/migrate-firebase-to-neon.ts` etc.).

Por eso hay tres estratos de restos: (a) capa Firestore completa, (b) config/tipos de la era single-event, (c) artefactos de pruebas manuales de OG images y docs de fases ya cerradas. **Producción con datos y usuarios reales** — el veredicto "muerto" debe estar respaldado por grep de importadores, no por intuición.

Convención de veredictos: **muerto** = 0 referencias en código ejecutable · **vivo** = referenciado en runtime · **parcial** = referenciado solo por código muerto/one-shot, o vivo pero duplicado/divergente.

## 3. Scope

- **In:** repo completo — `app/`, `lib/`, `types/`, `scripts/`, root (MDs, artefactos, `package.json`, `event-config.json`). Archivos muertos, rutas duplicadas, deps sin uso, tipos divergentes, y duplicación de LÓGICA (bloques repetidos), no solo de archivos.
- **Out:** vulnerabilidades y protección de endpoints (**Fase S** — si aparece algo, va a "Hallazgos fuera de scope" sin profundizar) · corrección de flujos de email (A1) · queries/schema (A6) · UX (A7) · decidir/borrar (plan correctivo).

## 4. Checklist ejecutable

Todos los comandos se corren desde la raíz del repo. La columna **Evidencia pre-verificada** contiene el output real obtenido al crear este documento (SHA `bcc7f1e`, 2026-07-09); la sesión ejecutora debe **re-correr cada comando**, pegar/confirmar el output y marcar Resultado.

### 4.1 Capa Firestore

| # | Candidato | Cómo verificar (comando exacto) | Resultado | Evidencia pre-verificada | Veredicto preliminar |
|---|-----------|--------------------------------|-----------|--------------------------|----------------------|
| 1 | `lib/firestore.ts` (526 líneas) | `grep -rn "lib/firestore\|from './firestore'\|from \"@/lib/firestore\"" --include="*.ts" --include="*.tsx" app lib types scripts` | ✅ Confirmado (re-ejecutado): grep exit=1, **0 importadores**. Grep amplio de "firestore" solo pega en los 2 scripts one-shot (`scripts/add-demo-data.ts:23`, `scripts/migrate-firebase-to-neon.ts:47`). `wc -l` = 526. Throw a nivel de módulo verificado en `lib/firestore.ts:17-22`. Knip también lo lista en "Unused files". | **0 importadores.** Nada en `app/` ni `lib/` lo importa post-migración. Además lanza `throw` a nivel de módulo si faltan env vars GCP (`lib/firestore.ts:17-22`) — si algo lo importara, rompería el build en Vercel (donde esas vars ya no existen). | **Muerto** |
| 2 | dep `firebase-admin` (package.json) | `grep -rn "firebase-admin" --include="*.ts" --include="*.tsx" app lib types scripts` | ✅ Confirmado: exactamente 3 hits — `lib/firestore.ts:1`, `scripts/add-demo-data.ts:1`, `scripts/migrate-firebase-to-neon.ts:12`. Sigue en `dependencies` (`package.json:20`). Knip: "Unused dependencies: firebase-admin". | Solo 3 archivos: `lib/firestore.ts:1` (muerto, ítem 1), `scripts/migrate-firebase-to-neon.ts:12` y `scripts/add-demo-data.ts:1` (one-shots, ítems 12-13). **Cero uso en runtime de la app.** Sigue en `dependencies` inflando `node_modules` y el install de Vercel. | **Muerto** (en runtime) |
| 3 | `scripts/add-demo-data.ts` | `head -30 scripts/add-demo-data.ts` y `grep -rn "add-demo-data" app lib scripts *.md *.json` | ✅ Confirmado: `admin.firestore()` en `scripts/add-demo-data.ts:23`; primer RSVP demo con email real `joseassem@gmail.com` (`:29`). Única referencia externa: `COMMANDS.md:126` con `npx ts-node` (no instalado, ítem 8/19). | Usa `admin.firestore()` (`scripts/add-demo-data.ts:23`) — **apunta a Firestore, roto post-migración a Neon**. Única referencia externa: `COMMANDS.md:126` (`npx ts-node scripts/add-demo-data.ts`, doblemente stale: ts-node no está instalado, ver ítem 19). | **Muerto** |

### 4.2 Rutas API duplicadas / de debug

| # | Candidato | Cómo verificar | Resultado | Evidencia pre-verificada | Veredicto preliminar |
|---|-----------|----------------|-----------|--------------------------|----------------------|
| 4 | `app/api/og/[slug]/route.ts` (107 l) vs `app/api/og-image/[slug]/route.ts` (294 l) | `grep -rn "api/og" --include="*.ts" --include="*.tsx" app lib public` | ✅ Confirmado: únicos hits `app/page.tsx:53` y `app/[slug]/layout.tsx:33`, ambos hacia **og-image**; 0 hacia `/api/og/`. `wc -l`: 107 vs 294. Implementaciones verificadas: `og/` usa `ImageResponse` de `next/og` (`og/[slug]/route.ts:1`) y fallback a `event-config.json` (`:2,:14-15`); `og-image/` usa `sharp` con target <280KB para WhatsApp (`og-image/[slug]/route.ts:3,:10`). Nota: ambos comparten `getEventBySlugWithSettings` de `lib/queries`, así que `og/` mantiene VIVO parte del grafo aunque nadie lo llame. | Solo se referencia **og-image**: `app/page.tsx:53` (`/api/og-image/${event.slug}`) y `app/[slug]/layout.tsx:33` (`/api/og-image/${slug}?v=5`). **Nada apunta a `/api/og/`.** Son implementaciones distintas del mismo propósito: `og/` genera imagen sintética con `next/og` ImageResponse; `og-image/` sirve `ogImageUrl`/`backgroundImageUrl` del evento comprimido con `sharp` para WhatsApp. `og/` es la versión vieja superseded. | **Muerto** (duplicado) |
| 5 | `app/api/debug-home/route.ts` (22 l) + `scripts/debug-home.ts` (83 l) | `grep -rn "debug-home" app lib scripts *.md *.json` | ✅ Confirmado: grep exit=1 (0 referencias fuera de los propios archivos); `wc -l` = 22 y 83. Leído completo `app/api/debug-home/route.ts`: GET sin ningún check de sesión que devuelve `homeEventId`, slug/título del evento home y `legacyId` (`route.ts:9-22`) → nota factual en fuera de scope (FS-A5-a). | **0 referencias** a cualquiera de los dos (ni UI, ni docs, ni package.json). Es un endpoint de diagnóstico de la migración de `home_event_id` desplegado en prod. Nota: el endpoint responde sin auth (ver Hallazgos fuera de scope → Fase S). | **Muerto** |
| 6 | `app/api/admin/add-demo-data/route.ts` (101 l) | `grep -rn "add-demo-data" app --include="*.tsx" --include="*.ts"` (¿algún botón/fetch del admin lo llama?) | ✅ Confirmado: grep exit=1 → **0 callers** (ni fetch ni botón; el string no aparece en el contenido de ningún archivo de `app/`). Leído el route completo: auth super_admin en `route.ts:60-72`; 7 RSVPs demo con `eventId: eventConfig.event.id` (`route.ts:13,20,27...`) = `'rooftop-party-andras-oct2024'` (`event-config.json:3`) e email real `joseassem@gmail.com` (`route.ts:10`); inserta vía `saveRSVP` (`route.ts:76`) **sin disparar emails**. Nota: como `rsvps.eventId` guarda slug (gotcha A6), esos RSVPs quedarían ligados a un id que puede no ser el slug real del evento legacy. | **0 referencias desde la UI** (el único hit del repo es el propio route + `COMMANDS.md:126` que refiere al *script*, no al endpoint). Tiene auth super_admin (`route.ts:59-71`) pero inserta 7 RSVPs demo con `eventId: eventConfig.event.id` **hardcodeado al evento legacy** (`route.ts:13`) e incluye un email real (`joseassem@gmail.com`, `route.ts:10`). Endpoint de seed accesible en producción con datos reales. | **Muerto** (sin caller) — riesgo si se invoca |

### 4.3 Artefactos y docs en root

| # | Candidato | Cómo verificar | Resultado | Evidencia pre-verificada | Veredicto preliminar |
|---|-----------|----------------|-----------|--------------------------|----------------------|
| 7 | `test-og.png` (646 KB), `test-og-dynamic.png` (137 KB), `test-og-result.bin`, `prod-og-test.bin` (0 B), `setup.ps1` | `grep -rn "test-og\|prod-og\|setup.ps1" --include="*.ts" --include="*.tsx" --include="*.json" --include="*.md" app lib scripts public *.md *.json` | ✅ Confirmado: grep exit=1 (0 refs). `ls -la`: test-og.png 646.682 B, test-og-dynamic.png 137.505 B, test-og-result.bin 1.492 B, prod-og-test.bin 0 B, setup.ps1 2.041 B. `setup.ps1:8,21-24,48` verifica/pide `background.jpg` mientras el default actual es `/background.png` (`lib/schema.ts:33`). | **0 referencias** (grep exit=1). Son outputs de pruebas manuales `curl` de las OG images (git: `caf3a2e` "WhatsApp compatibility") y un script de setup Windows de la era inicial (`76b615f` Initial commit; verifica `background.jpg` que ya ni es el nombre actual — el código usa `/background.png`, `lib/schema.ts:33`). ~790 KB de binarios versionados en el repo. | **Muerto** |
| 8 | 11 MDs en root — vigencia | Comparar afirmaciones clave contra el código: `grep -n "ts-node\|Firestore\|Firebase\|Cosmos" *.md` y `grep -n '"ts-node"\|"tsx"' package.json` | ✅ Confirmado: `ts-node` en 7 líneas de 6 MDs (`ADMIN_GUIDE.md:105`, `CHECKLIST.md:25`, `COMMANDS.md:121,126`, `README.md:78`, `SETUP_GUIDE.md:110`, `START_HERE.md:50`); `package.json:39` solo instala `"tsx": "^4.20.6"` (no hay `ts-node`). Tabla 4.3.1 re-verificada afirmación por afirmación: README describe Neon+Drizzle (`README.md:3`); DEPLOYMENT_SUCCESS/PROJECT_SUMMARY son snapshots "Rooftop Party" (`DEPLOYMENT_SUCCESS.md:1`, `PROJECT_SUMMARY.md:1`); DOCUMENTATION_UPDATE_REPORT relata la corrección Cosmos→Neon (`:20-22,:88-92`); PROPUESTA_GESTION es propuesta comercial (`:1-6`); INDEX.md indexa los demás MDs (`INDEX.md:6-22`). | Ver desglose abajo (tabla 4.3.1). Claim transversal roto: **6 MDs instruyen `npx ts-node scripts/...` pero el repo instala `tsx`, no `ts-node`** (`package.json` devDependencies) → los comandos documentados fallan tal cual. | **Parcial** (ver 4.3.1) |

#### 4.3.1 Desglose de MDs root (evidencia pre-verificada)

| MD | Afirmación clave verificada | Estado propuesto |
|----|------------------------------|------------------|
| `README.md` | Describe Neon+Drizzle correctamente; pero `README.md:78` → `npx ts-node scripts/create-super-admin.ts` (ts-node no instalado) | **Vigente** (corregir ts-node→tsx) |
| `START_HERE.md` | Setup válido (Neon); `START_HERE.md:50` mismo bug ts-node | **Vigente** (corregir) |
| `SETUP_GUIDE.md` | Guía válida; `SETUP_GUIDE.md:110` mismo bug ts-node | **Vigente** (corregir) |
| `ADMIN_GUIDE.md` | Describe panel actual; `ADMIN_GUIDE.md:105` mismo bug ts-node | **Vigente** (corregir) |
| `COMMANDS.md` | `COMMANDS.md:121` ts-node; `COMMANDS.md:126` → `npx ts-node scripts/add-demo-data.ts` que además es el **script Firestore roto** (ítem 3) | **Parcial** — comandos rotos |
| `CHECKLIST.md` | `CHECKLIST.md:25` mismo bug ts-node; resto es checklist de un setup ya hecho | **Histórico** (archivar) |
| `INDEX.md` | Índice de los otros MDs; se vuelve stale si se archivan | **Parcial** (actualizar tras decidir) |
| `DEPLOYMENT_SUCCESS.md` | Guía de un deploy ya realizado | **Histórico** (archivar) |
| `PROJECT_SUMMARY.md` | Snapshot ASCII-art de un estado pasado del proyecto | **Histórico** (archivar) |
| `DOCUMENTATION_UPDATE_REPORT.md` | Reporte de la corrección de docs Cosmos→Neon (`:20`, `:90`) — evento puntual pasado | **Histórico** (archivar) |
| `PROPUESTA_GESTION.md` | Propuesta comercial/de gestión, no doc técnica del código | **Histórico** (archivar) |

### 4.4 Scripts one-shot

| # | Candidato | Cómo verificar | Resultado | Evidencia pre-verificada | Veredicto preliminar |
|---|-----------|----------------|-----------|--------------------------|----------------------|
| 9 | `scripts/migrate-firebase-to-neon.ts` | `git log --oneline --follow -- scripts/migrate-firebase-to-neon.ts \| head -3` | ✅ Confirmado: `821911f` "Remove deprecated migrateEventSettings…", `3a3deac` "feat: Multi-party system + Neon migration". Importa `firebase-admin` (`:12`) y lee colecciones Firestore (`:47,:56,:102`) — credenciales GCP ya no aplican. Knip: unused file. | `821911f`, `3a3deac` (feat: Multi-party system + Neon migration). Migración **ya ejecutada** — la app corre en Neon con datos reales. Requiere credenciales GCP que ya no aplican. | **Muerto** (one-shot ejecutado; valor solo histórico) |
| 10 | `scripts/fix-rsvp-event-link.ts` | `git log --oneline --follow -- scripts/fix-rsvp-event-link.ts \| head -3` | ✅ Confirmado: único commit `9fc2b5c`. 0 referencias en código/docs. Knip: unused file. | `9fc2b5c`. Fix puntual de vinculación RSVP↔evento, ya aplicado. | **Muerto** (one-shot) |
| 11 | `scripts/create-legacy-event.ts` | `git log --oneline --follow -- scripts/create-legacy-event.ts \| head -3` | ✅ Confirmado: único commit `9fc2b5c`. Autodescriptivo: "create the legacy event from event-config.json" (`:2,:12,:40`). Knip: unused file. | `9fc2b5c`. Creó el evento legacy desde `event-config.json` en la DB — ya existe. | **Muerto** (one-shot) |
| 12 | `scripts/create-super-admin.ts` (control: NO es candidato a muerto) | `grep -rn "create-super-admin" *.md` | ✅ Confirmado vivo (operativo): referenciado en `CHECKLIST.md:25`, `ADMIN_GUIDE.md:105`, `COMMANDS.md:121`, `SETUP_GUIDE.md:107,110`, `README.md:78,184`, `START_HERE.md:50`, `PROJECT_SUMMARY.md:140`. Matiz: knip lo lista como "unused file" porque nada lo importa — correcto para un script CLI; el veredicto Vivo se sostiene por su rol operativo, con el detalle de que TODAS las docs lo invocan con `ts-node` inexistente (ítem 8). | Referenciado en README/SETUP_GUIDE/ADMIN_GUIDE/CHECKLIST/START_HERE/COMMANDS como herramienta operativa vigente (crear admins). | **Vivo** |
| 13 | `scripts/debug-home.ts` | (cubierto en ítem 5) | ✅ Confirmado: 0 referencias (grep ítem 5). Origen: `git log --follow` → `1b13ae5` "UI refinement… fix home page 500 errors" (corrige el dato pre-verificado que lo atribuía a `9fc2b5c`; no cambia el veredicto). Knip: unused file. | 0 referencias; debug puntual de `home_event_id`. | **Muerto** |

### 4.5 Dependencias (package.json)

| # | Candidato | Cómo verificar | Resultado | Evidencia pre-verificada | Veredicto preliminar |
|---|-----------|----------------|-----------|--------------------------|----------------------|
| 14 | `image-size` | `grep -rln "image-size" --include="*.ts" --include="*.tsx" app lib types scripts` | ✅ Confirmado: grep exit=1, 0 imports. El parsing de dimensiones es manual con `buf.readUInt32BE/readUInt16BE` (`app/api/og-image/[slug]/route.ts:35-36,51-56`). En `dependencies` (`package.json:22`). | **0 imports.** (El parsing de dimensiones en `app/api/og-image/[slug]/route.ts` se hace a mano con buffers, no con esta lib.) Confirmado por depcheck (ítem 16). | **Muerto** |
| 15 | `jspdf`, `jspdf-autotable`, `sharp`, `xlsx`, `bcryptjs`, `@vercel/blob`, `react-international-phone`, `firebase-admin` | Por cada dep: `grep -rln "<dep>" --include="*.ts" --include="*.tsx" app lib types scripts` | ✅ Confirmado por-dep: `jspdf` → `app/admin/page.tsx:7` · `jspdf-autotable` → `app/admin/page.tsx:8` + `types/jspdf-autotable.d.ts` · `xlsx` → `app/admin/page.tsx:9` · `sharp` → `app/api/og-image/[slug]/route.ts:3` · `bcryptjs` → `lib/auth-utils.ts:1` + `scripts/create-super-admin.ts:11` · `@vercel/blob` → `app/api/admin/upload-image/route.ts:2` · `react-international-phone` → `app/components/RSVPModal.tsx:5-6`, `app/cancel/[rsvpId]/page.tsx:5-6`, `app/admin/page.tsx` → **vivas**. `firebase-admin` → muerta (ítem 2). | `jspdf`: `app/admin/page.tsx:7` · `jspdf-autotable`: `app/admin/page.tsx:8,1071` + `types/jspdf-autotable.d.ts` · `sharp`: `app/api/og-image/[slug]/route.ts:3` · `xlsx`: `app/admin/page.tsx:9` · `bcryptjs`: `lib/auth-utils.ts` + `scripts/create-super-admin.ts` · `@vercel/blob`: `app/api/admin/upload-image/route.ts` · `react-international-phone`: `app/cancel/[rsvpId]/page.tsx`, `app/admin/page.tsx`, `app/components/RSVPModal.tsx` → todas **vivas**. `firebase-admin` → **muerta** (ítem 2). | **Vivas** salvo firebase-admin |
| 16 | `npx depcheck` | `npx depcheck --json` | ✅ Re-ejecutado: `"dependencies":["image-size"]` · `"devDependencies":["@types/react-dom","tsx","typescript"]` (falsos positivos: los usa `next build`/el runner de scripts) · `"missing":{}`. Output idéntico al pre-verificado. | Corrió OK: `"dependencies":["image-size"]` (única dep de producción sin uso) · `"devDependencies":["@types/react-dom","tsx","typescript"]` → **falsos positivos** (typescript/`@types/*` los usa `next build`; `tsx` es el runner de los scripts vivos, p.ej. `npx tsx scripts/create-super-admin.ts`). `"missing":{}`. | Confirma ítem 14 |
| 17 | `@types/bcryptjs` en `dependencies` | `grep -n "@types/bcryptjs" package.json` | ✅ Confirmado: `package.json:16` — dentro del bloque `"dependencies"` (verificado con el bloque completo; `bcryptjs` runtime está en `:19`). Debe vivir en `devDependencies`. | Está en `dependencies` (package.json, bloque dependencies) — los types no se necesitan en runtime; debe vivir en `devDependencies`. | **Parcial** (mal ubicada) |

### 4.6 Tipos: triple definición de "Event"

| # | Candidato | Cómo verificar | Resultado | Evidencia pre-verificada | Veredicto preliminar |
|---|-----------|----------------|-----------|--------------------------|----------------------|
| 18 | `types/event.ts` vs `lib/schema.ts` ($inferSelect) vs `types/event-settings.ts` | `grep -rn "from '@/types/event'" app lib` · `grep -rn "from '@/lib/schema'\|from './schema'" app lib` · leer los 3 archivos y comparar campo por campo | ✅ Confirmado con **refinamiento** (ver nota bajo 4.6.1): importadores del `Event` nested: `app/admin/page.tsx:12`, `app/[slug]/page.tsx:8`, `lib/firestore.ts:363` (muerto); del `Event` Drizzle: `app/api/events/route.ts:6`, `lib/queries.ts:8`. Los 3 archivos leídos completos; tabla 4.6.1 verificada campo por campo contra `types/event.ts:19-52` y `lib/schema.ts:24-71` (`ogImageUrl` solo en `lib/schema.ts:37`). `npx tsc --noEmit` pasa limpio (exit=0): la mentira compila porque los payloads JSON entran como `any`. | Ver comparación 4.6.1. Importadores de `types/event.ts#Event`: `app/admin/page.tsx:12`, `app/[slug]/page.tsx:8`, `lib/firestore.ts:363` (muerto). Importadores del `Event` de Drizzle: `app/api/events/route.ts:6`, `lib/queries.ts:8`. **El mismo nombre `Event` designa dos shapes incompatibles a ambos lados del mismo fetch.** | **Parcial** (triple definición divergente) |
| 19 | `types/event-settings.ts` | `grep -rn "types/event-settings\|EventSettings\|EventConfig" --include="*.ts" --include="*.tsx" app lib scripts` | ✅ Confirmado: `EventSettings` solo lo importa `lib/firestore.ts:290` (muerto) y lo usa en `:297,:312,:322,:350`; `EventConfig` (tipo) 0 importadores — los hits en `app/admin/page.tsx:281,330,784,1589` son las funciones locales `loadEventConfig`/`saveEventConfig`; `lib/config.ts:17` solo lo menciona en comentario. Knip: `types/event-settings.ts` en "Unused files". | `EventSettings`: único importador es `lib/firestore.ts:290` (muerto, ítem 1). `EventConfig`: **0 importadores** (los hits `EventConfig` en `app/admin/page.tsx:281,330,784` son funciones locales `loadEventConfig`/`saveEventConfig`, no el tipo). `app/api/event-settings/route.ts` construye el shape a mano sin importar el tipo. | **Muerto** (arrastrado por firestore.ts) |

#### 4.6.1 Comparación campo por campo (pre-verificada)

| Concepto | `types/event.ts` (nested) | `lib/schema.ts` → `$inferSelect` (flat) | Divergencia |
|----------|---------------------------|------------------------------------------|-------------|
| Precio | `price: { enabled, amount, currency }` | `priceEnabled`, `priceAmount`, `priceCurrency` | shape distinto |
| Capacidad | `capacity: { enabled, limit }` | `capacityEnabled`, `capacityLimit` | shape distinto |
| Imagen fondo | `backgroundImage: { url, uploadedAt? }` | `backgroundImageUrl` | shape distinto |
| OG image | **no existe** | `ogImageUrl` (`lib/schema.ts:37`) | falta en types/event.ts |
| Contacto | `contact: { hostName, hostEmail, hostPhone? }` | `hostName`, `hostEmail`, `hostPhone` | shape distinto |
| Email cfg | `emailConfig: { confirmationEnabled, reminderEnabled, reminderScheduledAt, reminderSentAt }` | `emailConfirmationEnabled`, `reminderEnabled`, `reminderScheduledAt`, `reminderSentAt` | shape distinto |
| Fechas | `createdAt/updatedAt: string` | `timestamp` (Date) | tipo distinto |

**Consecuencia concreta:** `GET /api/events` devuelve filas Drizzle **flat** (`app/api/events/route.ts:42-48` solo agrega `accessRole`, no re-mapea), pero `app/admin/page.tsx:31` (`useState<Event[]>`) y `app/[slug]/page.tsx:15` las tipan con el `Event` **nested** de `types/event.ts` → el tipo miente; funciona solo porque los campos que esas páginas tocan (`slug`, `title`, `isActive`, `rsvpClosed`…) coinciden por casualidad en ambos shapes. Cualquier acceso a `event.price.enabled` compilaría y explotaría en runtime.

**Refinamiento de la sesión ejecutora (2026-07-09, re-verificado leyendo `app/api/events/[slug]/route.ts` completo):** la pre-verificación es exacta para `app/admin/page.tsx` (consume `GET /api/events` flat en `page.tsx:175` y lo tipa nested en `:31`), pero `app/[slug]/page.tsx` consume `GET /api/events/[slug]`, que SÍ re-mapea flat→nested (`formattedEvent`, `app/api/events/[slug]/route.ts:76-99`) — por eso los accesos nested `event.price?.enabled`/`event.capacity.limit`/`event.backgroundImage?.url` (`app/[slug]/page.tsx:128,219-230`) funcionan de verdad, no por coincidencia. El problema adicional que esto revela: **el mismo recurso devuelve dos shapes distintos según el verbo** — `GET /api/events/[slug]` responde nested+flat híbrido (spread `...event` + campos nested), mientras `PUT /api/events/[slug]` devuelve la fila Drizzle flat cruda (`finalEvent`, `route.ts:234-236`) y `GET /api/events` (lista) devuelve flat. El re-mapeo flat→nested existe UNA sola vez (en el GET por slug) en lugar de ser un serializador compartido — misma familia de duplicación divergente que el ítem 22.

### 4.7 Restos de la era single-event

| # | Candidato | Cómo verificar | Resultado | Evidencia pre-verificada | Veredicto preliminar |
|---|-----------|----------------|-----------|--------------------------|----------------------|
| 20 | `lib/config.ts` (55 l) | `grep -rn "from '@/lib/config'\|from '../lib/config'\|from './config'" --include="*.ts" --include="*.tsx" app lib scripts` | ✅ Confirmado: grep exit=1, 0 importadores; `wc -l` = 55; docstring "(que lee de Firestore)" en `lib/config.ts:5,9`. Knip: unused file. | **0 importadores** de `getEventConfig`/`getStaticEventConfig`. Su docstring aún dice "lee de Firestore" (`lib/config.ts:5,9`) — resto de la era single-event+Firestore. | **Muerto** |
| 21 | `event-config.json` | `grep -rn "event-config" --include="*.ts" --include="*.tsx" app lib scripts \| wc -l` y revisar cada sitio | ✅ Confirmado VIVO — 21 hits, 18 archivos con import. Desglose por estrato: **vivos** (13): `app/page.tsx:2`, `app/layout.tsx:3`, `app/opengraph-image.tsx:2`, `app/admin/page.tsx:10`, `app/api/event-settings/route.ts:5`, `app/api/rsvp/route.ts:2`, `app/api/stats/route.ts:6`, `app/api/cron/send-reminders/route.ts:14`, `app/api/admin/send-email/route.ts:8`, `send-bulk-email/route.ts:8`, `send-bulk-reminder/route.ts:15`, `lib/email-template.ts:6` y `app/admin/components/LoginForm.tsx:5` (este último importador es a su vez **código muerto**, ver ítem 26); **muertos/one-shot** (5): `app/api/debug-home/route.ts:4`, `app/api/og/[slug]/route.ts:2`, `app/api/admin/add-demo-data/route.ts:5`, `lib/config.ts:1`, `scripts/create-legacy-event.ts:12`. Deuda viva, no muerto. | **VIVO — 16 importadores** en runtime: fallback de eventId legacy (`app/api/rsvp/route.ts:38,157,209`, `app/api/stats/route.ts:27`, `app/api/event-settings/route.ts:32,102-127`), fallback de theme/hostEmail en los 5 endpoints de email, título/branding en `app/layout.tsx`, `app/page.tsx`, `app/opengraph-image.tsx`, `app/admin/components/LoginForm.tsx`, `lib/email-template.ts:6`. **No es código muerto** — es deuda viva: datos del evento de nov-2024 hardcodeados como default global multi-evento. | **Vivo** (deuda, no muerto) |

### 4.8 Duplicación de LÓGICA

| # | Candidato | Cómo verificar | Resultado | Evidencia pre-verificada | Veredicto preliminar |
|---|-----------|----------------|-----------|--------------------------|----------------------|
| 22 | Bloque "Build EventData" repetido en los 5 endpoints de email | `grep -n "Build EventData" app/api/rsvp/route.ts app/api/cron/send-reminders/route.ts app/api/admin/send-email/route.ts app/api/admin/send-bulk-email/route.ts app/api/admin/send-bulk-reminder/route.ts` y leer ±25 líneas de cada hit | ✅ Confirmado leyendo los 5 bloques completos: `rsvp/route.ts:81-102`, `cron/send-reminders/route.ts:115-137`, `send-email/route.ts:53-75`, `send-bulk-email/route.ts:38-61`, `send-bulk-reminder/route.ts:76-97`. Variante A (rsvp `:82`, cron `:116`, bulk-reminder `:77`): `theme = event.theme \|\| {}`, hex hardcodeados, `eventConfig.contact?.hostEmail` con `?.`, bg `'/background.png'`. Variante B (send-email `:54`, bulk-email `:41`): `theme = event.theme \|\| eventConfig.theme`, fallbacks `eventConfig.theme.*`, `eventConfig.contact.hostEmail` SIN `?.`, bg con `eventConfig.event.backgroundImage` extra. Hoy los valores coinciden (`event-config.json:17-22` = hex hardcodeados). Coincide con **A1-14** (allí el impacto en emails; aquí la duplicación). | 5 copias de ~22 líneas: `rsvp/route.ts:81-102`, `cron/send-reminders/route.ts:115-137`, `send-email/route.ts:53-75`, `send-bulk-email/route.ts:38-60`, `send-bulk-reminder/route.ts:76-97`. **Ya divergieron en el fallback de theme**: variante A (`rsvp`, `cron`, `bulk-reminder`): `theme = event.theme \|\| {}` + hex hardcodeados (`'#FF1493'`…); variante B (`send-email`, `send-bulk-email`): `theme = event.theme \|\| eventConfig.theme` + `eventConfig.theme.*` como fallback + `eventConfig.event.backgroundImage` extra en la cadena de backgroundImageUrl. Hoy los valores coinciden numéricamente, pero un cambio en `event-config.json` haría que el mismo evento mande emails con colores distintos según el endpoint. | **Parcial** (duplicado divergente) |
| 23 | Patrón de auth repetido (cookie `rp_session` + `validateSession`) | `grep -rln "rp_session" app/api \| sort` y `grep -rn "validateSession" app/api \| wc -l` | ✅ Confirmado: 17 rutas con `rp_session` (lista completa re-obtenida: add-demo-data, event-settings/update, reminder-status, send-bulk-email, send-bulk-reminder, send-email, settings, update-rsvp, users/[id]/events, users/[id], users, validate, event-settings, events/[slug], events, rsvp, stats) · 44 menciones de `validateSession` en `app/api` · `grep -rn "requireAuth\|withAuth" app lib` exit=1 → no existe helper. Dato factual adicional para Fase S: `app/api/admin/upload-image/route.ts` no aparece en la lista — su POST no valida sesión (leído `:15-40`), ver FS-A5-c. | 17 rutas repiten a mano el bloque cookie→token→`validateSession`→check de rol (44 menciones de `validateSession` en `app/api`). No hay helper tipo `requireAuth()`/middleware; cada copia puede divergir (esto ya mordió: hotfix `bcc7f1e` fue exactamente una ruta que se quedó atrás). Inventario factual aquí; el análisis de suficiencia de auth es **Fase S**. | **Parcial** (duplicación estructural) |
| 24 | Helpers repetidos en `app/admin/page.tsx` (2599 l) | `grep -n "fetch('/api/admin/event-settings/update'" app/admin/page.tsx` · `grep -n "'#FF1493'" app/admin/page.tsx app/api/event-settings/route.ts app/components/RSVPModal.tsx "app/cancel/[rsvpId]/page.tsx"` | ✅ Confirmado y ampliado: (a) POST a `event-settings/update` 3× (`app/admin/page.tsx:843,893,958`); (b) inventario COMPLETO de `#FF1493` como default de theme (`grep -rn "FF1493" app lib types`): `app/admin/page.tsx:52,304` · `app/api/event-settings/route.ts:79,125` · `app/components/RSVPModal.tsx:24` · `app/cancel/[rsvpId]/page.tsx:36` · `lib/schema.ts:47` · `app/api/events/[slug]/route.ts:48` (`DEFAULT_THEME`) · `app/api/events/route.ts:127` · `app/api/admin/event-settings/update/route.ts:107` · `app/api/rsvp/route.ts:93` · `app/api/cron/send-reminders/route.ts:129` · `app/api/admin/send-bulk-reminder/route.ts:88` · más `event-config.json:18` → **≥13 sitios de default de theme** (los hits en `app/icon-512`, `icon-192`, `apple-icon` son color de marca en iconos, uso distinto); (c) helpers de export inline: `stripEmojis` `page.tsx:1010`, `exportInformativeList` `:1017`, `exportExcelList` `:1117`; `wc -l` = 2599. | (a) El POST a `/api/admin/event-settings/update` está copiado 3 veces: `page.tsx:843` (guardar form), `:893` (auto-save tras subir background), `:958` (auto-save tras subir OG image). (b) El theme default `#FF1493…` está re-hardcodeado en ≥5 sitios: `app/admin/page.tsx:52,304`, `app/api/event-settings/route.ts:79,125`, `app/components/RSVPModal.tsx:24`, `app/cancel/[rsvpId]/page.tsx:36`, más `lib/schema.ts:47` (default del JSONB) y las 2 variantes del ítem 22 — una sola fuente de verdad no existe. (c) Helpers de export (`stripEmojis:1010`, `exportInformativeList:1017`, `exportExcelList:1117`) viven inline en el componente. | **Parcial** (duplicación intra/inter-archivo) |

### 4.9 Verificación adicional de la sesión ejecutora (knip + tsc, no pre-verificada)

Herramientas read-only adicionales corridas el 2026-07-09 sobre `61f74e5`: `npx knip --no-config-hints` (con un ERROR benigno al cargar `drizzle.config.ts` por falta de `DATABASE_URL` local — no afecta el análisis del grafo de imports) y `npx tsc --noEmit` (exit=0, repo typechecks limpio). Knip confirmó TODOS los archivos muertos de los ítems 1, 3, 9-11, 13, 19, 20 y las deps de los ítems 2, 14, y encontró además lo siguiente (cada hallazgo re-verificado a mano con grep/lectura, no aceptado a ciegas):

| # | Candidato | Cómo se verificó | Resultado | Veredicto |
|---|-----------|------------------|-----------|-----------|
| 25 | `lib/auth.ts` (42 l) — validador HTTP Basic `validateAdminAuth` con `ADMIN_USERNAME`/`ADMIN_PASSWORD` | Knip "Unused files" + `grep -rn "from '@/lib/auth'\|from './auth'\|from '../lib/auth'" app lib scripts types` → exit=1 | ✅ **0 importadores.** Es el modelo de auth ANTERIOR al actual de cookie `rp_session`+`validateSession` (`lib/auth-utils.ts`); nadie llama `validateAdminAuth` (`lib/auth.ts:4`). No estaba en la pre-verificación. | **Muerto** |
| 26 | `app/admin/components/LoginForm.tsx` (95 l) | Knip "Unused exports: LoginForm… default LoginForm.tsx:15" + `grep -rn "<LoginForm" app` → exit=1 + `grep -rn "LoginForm"` → único hit fuera del propio archivo es el barrel `app/admin/components/index.ts:7` + lectura de `app/login/page.tsx` | ✅ **Nunca se renderiza.** El login real está implementado inline en `app/login/page.tsx:7-20+` (fetch a `/api/auth/me`, form propio). El barrel lo re-exporta pero `app/admin/page.tsx:14` solo importa `StatsCards, UserManagement, ReminderStatusSection, type RSVP`. Además importa `event-config.json` (`LoginForm.tsx:5`), inflando el conteo de importadores "vivos" del ítem 21. | **Muerto** (componente + su export en el barrel) |
| 27 | Exports sin callers en módulos vivos | Knip "Unused exports/types" + re-verificación por símbolo: `grep -rn "cleanupExpiredSessions\|getEventStats\|updateUserPassword\|getUserAccessibleEventIds" app lib scripts` → cero call sites (único hit adicional: el `getEventStats` PROPIO de `lib/firestore.ts:159`, muerto) | ✅ Confirmados 6: `cleanupExpiredSessions` (`lib/auth-utils.ts:148`) · `getEventStats` (`lib/queries.ts:167`; ya señalado por A1-OOS-02 hacia A5) · `updateUserPassword` (`lib/user-queries.ts:107`) · `getUserAccessibleEventIds` (`lib/user-queries.ts:270`) · `AdminStats` (`app/admin/components/index.ts:25`) · `CreateEventInput`/`UpdateEventInput` (`types/event.ts:68,73` — solo los importa `lib/firestore.ts:363`, muerto). Falsos positivos de knip descartados con justificación: `tsx` (runner operativo de scripts), `scripts/create-super-admin.ts` (CLI vivo, ítem 12). | **Muertos** (exports) |

## 5. Hallazgos

> IDs `A5-XX`. Los siguientes quedan **pre-registrados** por la pre-verificación al crear este doc (SHA `bcc7f1e`); la sesión ejecutora los confirma, ajusta severidad si su re-verificación difiere, y añade los nuevos que encuentre. Evidencia detallada: ver el ítem del checklist citado.
>
> **Sesión ejecutora (fable5-675614-a5, 2026-07-09, SHA `61f74e5`):** A5-01…A5-18 re-verificados y CONFIRMADOS todos (severidades sin cambio; evidencia de A5-13 refinada y de A5-18 ampliada — ver ítems 18 y 24). Nuevos: A5-19, A5-20, A5-21 (sección 4.9).

| ID | Sev | Hallazgo | Evidencia | Ítem |
|----|-----|----------|-----------|------|
| A5-01 | 🟢 | `lib/firestore.ts` (526 l) muerto: 0 importadores post-migración a Neon | grep importadores = 0 | 1 |
| A5-02 | 🟢 | Dep `firebase-admin` muerta en runtime (solo la importan firestore.ts muerto y 2 scripts one-shot) | `grep -rn "firebase-admin"` → 3 archivos | 2 |
| A5-03 | 🟢 | `scripts/add-demo-data.ts` roto: escribe a Firestore inexistente | `scripts/add-demo-data.ts:23` | 3 |
| A5-04 | 🟢 | `app/api/og/[slug]/route.ts` duplicado muerto de `og-image` (0 referencias; la versión viva es `og-image`) | `app/page.tsx:53`, `app/[slug]/layout.tsx:33` | 4 |
| A5-05 | 🟢 | Endpoint + script `debug-home` muertos (0 refs), desplegados en prod | grep `debug-home` = 0 refs | 5 |
| A5-06 | 🟡 | `app/api/admin/add-demo-data/route.ts` sin ningún caller pero desplegado: un POST autenticado insertaría 7 RSVPs demo (incl. email real `joseassem@gmail.com`) en el evento legacy en producción | `route.ts:7-57` | 6 |
| A5-07 | 🟢 | ~790 KB de artefactos de prueba versionados (`test-og*.png`, `*-og-*.bin`) + `setup.ps1` obsoleto (verifica `background.jpg` que ya no existe) | grep = 0 refs | 7 |
| A5-08 | 🟢 | 6 MDs instruyen `npx ts-node` pero el repo instala `tsx` → comandos documentados fallan; `COMMANDS.md:126` además apunta al script Firestore roto | tabla 4.3.1 | 8 |
| A5-09 | 🟢 | 5 MDs root históricos a archivar (CHECKLIST, DEPLOYMENT_SUCCESS, PROJECT_SUMMARY, DOCUMENTATION_UPDATE_REPORT, PROPUESTA_GESTION); INDEX.md quedaría stale | tabla 4.3.1 | 8 |
| A5-10 | 🟢 | 4 scripts one-shot ya ejecutados (`migrate-firebase-to-neon`, `fix-rsvp-event-link`, `create-legacy-event`, `debug-home`) — candidatos a archivo/borrado | git log ítems 9-13 | 9-13 |
| A5-11 | 🟢 | Dep `image-size` sin un solo import (confirmado por depcheck) | grep = 0; depcheck | 14, 16 |
| A5-12 | 🟢 | `@types/bcryptjs` en `dependencies` en lugar de `devDependencies` | package.json | 17 |
| A5-13 | 🟡 | Triple definición divergente de `Event`. Refinado por la sesión: el tipo nested miente específicamente en `app/admin/page.tsx:31` (consume `GET /api/events` flat, `page.tsx:175`); `app/[slug]/page.tsx` sí recibe nested porque `GET /api/events/[slug]` re-mapea (`route.ts:76-99`) — pero ese re-mapeo existe UNA sola vez y el mismo recurso devuelve flat en `PUT` (`route.ts:234-236`) y en la lista → shapes inconsistentes por verbo/endpoint. A `types/event.ts` además le falta `ogImageUrl` (`lib/schema.ts:37`) | tabla 4.6.1 + refinamiento bajo 4.6.1 | 18 |
| A5-14 | 🟢 | `types/event-settings.ts` muerto por arrastre (EventSettings solo lo importa firestore.ts muerto; EventConfig 0 importadores) | grep ítem 19 | 19 |
| A5-15 | 🟢 | `lib/config.ts` muerto (0 importadores; docstring aún dice "lee de Firestore") | grep ítem 20 | 20 |
| A5-16 | 🟡 | Bloque "Build EventData" duplicado 5× con 2 variantes de fallback de theme ya divergentes → un mismo evento puede mandar emails con estilos distintos según el endpoint si cambia `event-config.json` (impacto en flujos de email registrado como **A1-14**; aquí la duplicación como deuda) | ítem 22 | 22 |
| A5-17 | 🟢 | Patrón auth cookie+`validateSession` copiado a mano en 17 rutas sin helper común (la divergencia de copias ya causó el hotfix `bcc7f1e`) | ítem 23 | 23 |
| A5-18 | 🟢 | Duplicación intra-repo: POST a `event-settings/update` 3× en `app/admin/page.tsx:843,893,958`; theme default `#FF1493` re-hardcodeado en **≥13 sitios** sin fuente única (inventario completo en ítem 24, incluye `DEFAULT_THEME` de `app/api/events/[slug]/route.ts:47-53` y los 5 bloques del ítem 22) | ítem 24 | 24 |
| A5-19 | 🟢 | `lib/auth.ts` (42 l) muerto: validador HTTP Basic (`validateAdminAuth`, env `ADMIN_USERNAME`/`ADMIN_PASSWORD`) del modelo de auth anterior; 0 importadores | `lib/auth.ts:4`; grep exit=1; knip unused file | 25 |
| A5-20 | 🟢 | `app/admin/components/LoginForm.tsx` (95 l) muerto: nunca se renderiza (`grep "<LoginForm"` = 0); el login real vive inline en `app/login/page.tsx`; solo lo re-exporta el barrel `app/admin/components/index.ts:7`; además importa `event-config.json` (`LoginForm.tsx:5`) inflando los "importadores vivos" del ítem 21 | ítem 26 | 26 |
| A5-21 | 🟢 | 6 exports muertos en módulos vivos: `cleanupExpiredSessions` (`lib/auth-utils.ts:148`), `getEventStats` (`lib/queries.ts:167`, ya apuntado por A1-OOS-02), `updateUserPassword` (`lib/user-queries.ts:107`), `getUserAccessibleEventIds` (`lib/user-queries.ts:270`), `AdminStats` (`app/admin/components/index.ts:25`), `CreateEventInput`/`UpdateEventInput` (`types/event.ts:68,73`, solo importados por firestore.ts muerto) | ítem 27 | 27 |

**Conteo final (sesión ejecutora): 0 🔴 · 3 🟡 · 18 🟢**

## 6. Hallazgos fuera de scope

| Ref | Destino | Nota factual (sin profundizar) |
|-----|---------|--------------------------------|
| FS-A5-a | **Fase S** | `app/api/debug-home/route.ts` responde **sin autenticación** y expone `homeEventId`, slug/título del evento home y el id legacy. Se registra el hecho; la evaluación es de Fase S. |
| FS-A5-b | **Fase S** | El inventario del ítem 23 (17 rutas con auth copiada a mano) es insumo directo para el barrido de protección de endpoints de Fase S. |
| FS-A5-c | **Fase S** | Detectado de paso al verificar el ítem 23: `app/api/admin/upload-image/route.ts` (POST, sube a Vercel Blob) no contiene `rp_session`/`validateSession`/`cookies` — única ruta bajo `/api/admin/` fuera del patrón de auth. Solo se registra el hecho; la evaluación es de Fase S. |
| FS-A5-d | **Fase S** | `lib/auth.ts` muerto (A5-19) referencia envs `ADMIN_USERNAME`/`ADMIN_PASSWORD` de un modelo de auth abandonado — insumo para la limpieza de secretos/envs de Fase S. |
| A3/A6 | **A3 Eventos/settings, A6 Datos** | `event-config.json` vivo como fallback global (ítem 21): decidir si el default multi-evento debe seguir siendo el evento de nov-2024 es de A3; el uso de `eventConfig.event.id` como eventId default en `rsvp/stats/event-settings` toca el gotcha slug-vs-UUID de A6. |
| A1 | **A1 Emails** | La divergencia de theme del ítem 22 puede producir emails visualmente inconsistentes; el impacto en los flujos de envío se evalúa en A1 (aquí solo la duplicación). |

## 7. Cierre (para la sesión ejecutora)

1. ✅ Ítems 1-24 (+ 25-27 añadidos en 4.9): TODOS con Resultado ✅ y evidencia `archivo:línea` u output re-ejecutado. Cero ⏭️ NOT RUN — ningún ítem requería runtime; todo se resolvió por grep/lectura/git log/depcheck/knip/tsc, sin ningún efecto en producción.
2. ✅ Hallazgos confirmados/ampliados: **Conteo final: 0 🔴 · 3 🟡 · 18 🟢** (A5-01…A5-18 confirmados sin cambio de severidad; A5-19/20/21 nuevos).
3. Fila A5 → ✅ en `docs/audits/00_INDEX.md` con conteo: **la maneja el orquestador** (esta sesión es read-only fuera de este MD).
4. Commit + push `audit: A5 codigo-muerto — 0🔴 3🟡 18🟢`: **lo maneja el orquestador**.
5. ✅ Nada fue borrado ni modificado fuera de este MD — la eliminación/consolidación sale del plan correctivo post-`99_CONSOLIDADO.md`.

**Resumen ejecutivo:** el repo arrastra ~1.070 líneas de código muerto verificado (capa Firestore completa: `lib/firestore.ts` 526 l + `lib/config.ts` 55 l + `lib/auth.ts` 42 l + `LoginForm.tsx` 95 l + `types/event-settings.ts` + ruta `og/` 107 l + `debug-home` 105 l + `add-demo-data` route 101 l), 2 deps de producción sin uso (`firebase-admin`, `image-size`), ~790 KB de binarios de prueba versionados, 6 scripts one-shot ya ejecutados, 6 exports muertos, y tres focos de duplicación divergente (EventData 5×, auth 17×, theme default ≥13×, shapes de `Event` inconsistentes por endpoint). Nada de esto rompe un flujo hoy (0 🔴), pero los tres 🟡 son deuda que ya mordió antes (hotfix `bcc7f1e`) o puede morder al primer cambio de `event-config.json`.
