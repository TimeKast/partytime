# A4 — 🛠️ Panel Admin

> **Estado:** ✅ completada · **Owner/Sesión:** fable5-675614-a4 · **Inicio:** 2026-07-09 11:02 · **SHA auditado:** `61f74e5`
> **SHA de referencia del framework:** `bcc7f1e`
>
> ⚠️ **Antes de ejecutar:** seguir el **Protocolo por sesión** de `00_INDEX.md` (lock atómico):
> `git pull --ff-only` → marcar A4 como 🔄 en la tabla del INDEX con owner/fecha/SHA → commit + push (el lock existe solo si el push tiene éxito) → ejecutar este MD → registrar evidencia → marcar ✅.
> **Read-only:** prohibido editar código de la app durante la auditoría. Solo se escribe en este MD y en `00_INDEX.md`.
> Reglas de evidencia y rúbrica 🔴/🟡/🟢: ver `00_INDEX.md` (anti checkbox-theater — todo ítem requiere evidencia `archivo:línea` o comando+output, también cuando PASA; lo no ejecutado → `⏭️ NOT RUN` + razón).

---

## 1. Objetivo

Verificar que el panel de administración (`/admin`) funciona correctamente de punta a punta: que cada acción de UI llega al endpoint correcto con el payload correcto, que el estado local se refresca tras cada mutación (sin UI stale), que las estadísticas y exports reflejan los datos reales, que la gestión multi-evento de usuarios/roles opera bien, y que los errores de red/API se comunican al admin en vez de fallar en silencio. Registrar además las oportunidades de descomposición del monolito (sin refactorizar).

## 2. Contexto mínimo (para sesión fría)

- El panel vive en **`app/admin/page.tsx` (2,599 líneas — monolito client-side)** con 4 tabs: `dashboard` (tabla de RSVPs + envíos + exports), `config` (settings del evento seleccionado), `eventos` (CRUD de eventos, solo super_admin) y `usuarios` (gestión de usuarios, solo super_admin).
- Es **multi-evento**: un selector global (slug del evento) controla qué RSVPs/config se muestran. El slug seleccionado se persiste en `localStorage` (`rp_selected_event`).
- **Roles:** `super_admin` global; los demás usuarios reciben asignaciones por evento con rol `manager` o `viewer` (`user_event_assignments`, `lib/user-queries.ts`). `viewer` = solo lectura (sin envío de emails, sin config).
- **Auth de entrada:** el panel valida sesión vía `GET /api/auth/me` y redirige a `/login` si no hay sesión (`app/admin/page.tsx:105-124`). Desde `bcc7f1e` los endpoints admin exigen cookie `rp_session` + `validateSession`.
- Componentes ya extraídos (fix H-008): `StatsCards`, `UserManagement`, `ReminderStatusSection`, `LoginForm` en `app/admin/components/` + barrel `index.ts` (donde vive el tipo `RSVP` del panel).
- **Exports:** PDF con jsPDF+autotable (emojis stripped en `be004ed`, ASCII en `f134f03`) y Excel con `xlsx`; ambos con soporte de nombre del +1 (`c57fd67`, `ad272fa`).
- ⚠️ Gotcha heredado: `rsvps.eventId` guarda el **slug**, no el UUID. Los endpoints admin resuelven slug→UUID para el check de permisos (p.ej. `app/api/admin/update-rsvp/route.ts:49-50`).

### 2.1 Inventario del monolito `app/admin/page.tsx` (mapeado en `bcc7f1e`)

| Bloque | Líneas | Qué hace | Endpoint(s) que llama |
|---|---|---|---|
| Estado + configForm | 16-102 | ~25 useState: tabs, eventos, filtros display/email, modales, uploads | — |
| checkAuth (mount) | 105-124 | Valida sesión, redirect a `/login` | `GET /api/auth/me` |
| loadRSVPs | 126-168 | Carga RSVPs del evento seleccionado; auto-ajusta filtro de email | `GET /api/rsvp?eventId=<slug>` |
| loadEvents | 173-185 | Lista de eventos (filtrada por acceso server-side) | `GET /api/events` |
| loadAppSettings / setAsHome | 188-227 | Lee/escribe `home_event_id` | `GET/POST /api/admin/settings` |
| Init selector + localStorage | 230-277 | Restaura `rp_selected_event`, fallback home/primero | — |
| loadEventConfig | 281-332 | Carga settings del evento al form | `GET /api/event-settings?eventId=<slug>` |
| Filtros display/email (effects) | 341-394 | Deriva `filteredRsvps` y `emailTargetRsvps` | — |
| isEventPast | 396-462 | Heurística de parsing de fecha textual para bloquear envíos a eventos pasados | — |
| sendEmail (individual) | 464-522 | Confirm dialog + envío 1:1; tipo según status/emailSent | `POST /api/admin/send-email` |
| sendBulkEmails | 524-582 | Confirm con desglose por tipo; envía IDs filtrados | `POST /api/admin/send-bulk-email` |
| toggleStatus | 584-621 | Confirmar/cancelar asistencia SIN email | `POST /api/admin/update-rsvp` |
| Modal edición RSVP | 623-689, 2435-2514 | Edita name/email/phone/plusOne/**plusOneName** (`ad272fa`) | `POST /api/admin/update-rsvp` |
| Modal edición slug | 691-781, 2521-2596 | Cambia slug con advertencias; actualiza selección local | `PUT /api/events/[slug]` |
| saveEventConfig | 783-866 | Guarda todo el configForm (precio, capacidad, tema, emailConfig, rsvpClosed…) | `POST /api/admin/event-settings/update` |
| handleImageUpload (bg) | 868-930 | Sube imagen de fondo + auto-save de settings | `POST /api/admin/upload-image` → `POST /api/admin/event-settings/update` |
| handleOgImageUpload | 932-995 | Igual, con flag `imageType: 'og'` | ídem |
| handleLogout | 997-1007 | Cierra sesión y redirige | `POST /api/auth/logout` |
| stripEmojis | 1009-1014 | Regex de limpieza para jsPDF (`be004ed`) | — |
| exportInformativeList (PDF) | 1016-1114 | PDF de confirmados con fila indentada por +1 (`c57fd67`, `49f6b31`) | — (client-side) |
| exportExcelList | 1116-1166 | Excel de confirmados con columna "Nombre +1" | — (client-side) |
| Cálculo de stats | 1168-1177 | total/confirmed/cancelled/plusOne/totalGuests/emailsSent (client-side) | — |
| Permisos por evento | 1179-1193 | `accessRole` del evento seleccionado; gate de tabs config/eventos | — |
| Header + selector global | 1205-1301 | Botones usuarios/eventos/logout; selector de evento | — |
| Tab dashboard (render) | 1322-1579 | StatsCards, filtros, botones export, tablas confirmados/cancelados | — |
| Tab config (render) | 1581-2219 | Form completo de settings + `<ReminderStatusSection>` (2205) | — |
| Tab eventos (render) | 2221-2433 | Lista de eventos + crear evento (form inline con fetch propio 2336-2371) | `POST /api/events` |
| Tab usuarios (render) | 2516-2519 | Delega en `<UserManagement events={events} />` | — |

### 2.2 Componentes y endpoints admin (superficie a auditar)

- `app/admin/components/UserManagement.tsx` (514 l) → `GET/POST /api/admin/users`, `PUT /api/admin/users/[id]`, `GET/POST/DELETE /api/admin/users/[id]/events`
- `app/admin/components/ReminderStatusSection.tsx` (309 l) → `GET /api/admin/reminder-status`, `POST /api/admin/send-bulk-reminder`
- `app/admin/components/StatsCards.tsx` (49 l) — presentacional puro
- `app/admin/components/LoginForm.tsx` (95 l) → `GET /api/admin/validate` (⚠️ verificar si algo lo importa)
- Rutas sin consumidor aparente en el panel (verificar): `POST /api/admin/add-demo-data`, `GET /api/admin/validate`, `GET /api/stats`, `GET /api/admin/users/[id]`, `DELETE /api/admin/users/[id]`
- Queries: `lib/user-queries.ts` (278 l, CRUD usuarios + asignaciones + `userHasEventAccess`), `lib/queries.ts` (`getEventStats:167`, `updateRSVP:90`, `saveRSVP:17`)

## 3. Scope

**Dentro:**
- Todo `app/admin/**` (page + 4 componentes + barrel).
- Endpoints que el panel consume: `update-rsvp`, `users*`, `settings`, `reminder-status`, `upload-image`, más el **contrato panel↔endpoint** de `send-email` / `send-bulk-email` / `send-bulk-reminder` (payload correcto, refresh tras éxito, manejo de 401/403).
- Endpoints admin huérfanos: `validate`, `add-demo-data`, `/api/stats`, `getEventStats`.
- Exports PDF/Excel, stats mostradas, gestión multi-evento de usuarios.

**Fuera:**
- **El contenido/lógica de los envíos de email en sí → A1** (templates, destinatarios del cron, dedupe). Aquí solo se verifica que el panel *llame bien* y refresque.
- **Protección de endpoints y modelo de sesiones → Fase S.** Si aparece un endpoint sin validación de sesión o un contrato que confía en datos del cliente, se anota factualmente en "Hallazgos fuera de scope" sin profundizar.
- Lógica de `/api/events/[slug]` (rename de slug, OG images en filesystem) → A3; aquí solo el lado panel (payload, advertencias del modal, refresh).
- Código muerto global → A5 (aquí solo se registra lo detectado en superficie admin, con cross-ref).

## 4. Checklist ejecutable

> Cada fila: marcar resultado (✅ pasa / ❌ falla / ⏭️ NOT RUN) y completar la columna evidencia con `archivo:línea` u output de comando. Las anclas listadas son el punto de partida del mapeo previo — **confirmarlas contra el working tree en el SHA auditado**. Donde el mapeo previo ya detectó un problema, está marcado como **[pre-análisis]**: confirmar y convertir en hallazgo A4-XX si aplica.

| # | Verificación | Cómo verificar | Resultado | Evidencia |
|---|---|---|---|---|
| 1 | Inventario de features del monolito (tabla §2.1) es fiel y completo: ningún bloque funcional sin mapear | Recorrer `app/admin/page.tsx` por secciones (0-900, 900-1800, 1800-2599) y contrastar contra §2.1 | ✅ | Archivo completo leído (2,599 l). Rangos de §2.1 confirmados: loadRSVPs `page.tsx:126-168`, isEventPast `399-462`, exports `1016-1166`, tab config `1581-2219`, tab eventos `2221-2433`, modales `2435-2514`/`2522-2596`. Únicos bloques no mapeados en §2.1 (menores): guards de loading/auth `page.tsx:1195-1203` y barra de tabs `page.tsx:1303-1319` |
| 2 | Entrada al panel: `checkAuth` usa `/api/auth/me` y redirige a `/login`; **`LoginForm.tsx` no lo importa nadie** (solo barrel `index.ts:7`) y su flujo Basic-auth (`LoginForm.tsx:28-35`) es incompatible con `/api/admin/validate` que valida cookie `rp_session` e ignora el header (`app/api/admin/validate/route.ts:11-22`). **[pre-análisis: componente muerto y, de usarse, roto]** | `grep -rn "LoginForm" app --include='*.tsx'` (excluir barrel); leer ambos archivos | ✅ (pre-análisis confirmado) | checkAuth vía `/api/auth/me` + redirect: `page.tsx:105-124`. `grep -rn "LoginForm" app` → solo su propio archivo + barrel `components/index.ts:7`; `page.tsx:14` importa solo StatsCards/UserManagement/ReminderStatusSection/RSVP. Flujo Basic-auth `LoginForm.tsx:28-35` (guarda credenciales en sessionStorage `:50`) vs `validate/route.ts:11-22` que solo valida cookie `rp_session` e ignora `Authorization` → de usarse daría 401 perpetuo sin cookie. → **A4-10** |
| 3 | Selector de eventos — init: el effect `page.tsx:231-236` setea `selectedEventId` desde localStorage **sin validar** que el slug exista en la lista/acceso del usuario; la validación `events.some(...)` de `page.tsx:260` solo corre si `!selectedEventId`, o sea nunca corrige el valor ya restaurado. **[pre-análisis: slug de evento borrado/renombrado/sin acceso deja el panel clavado con "Error al cargar datos"]** | Leer los dos useEffect (231-236 vs 254-277); trazar qué pasa con un slug stale | ❌ (confirmado) | Effect `page.tsx:231-236` restaura sin validar; la validación `events.some(...)` `page.tsx:260` vive en un effect que solo corre si `!selectedEventId` (`:256`) → nunca corrige el valor restaurado. Traza con slug stale: `GET /api/rsvp?eventId=<stale>` → no-super_admin recibe 403 (`app/api/rsvp/route.ts:220-225`) → throw → "Error al cargar datos" (`page.tsx:135-137,163-164`); super_admin → dashboard vacío; loadEventConfig cae al fallback legacy de `event-config.json` (`app/api/event-settings/route.ts:98-140`). Recuperable solo eligiendo otro evento a mano en el selector. → **A4-03** |
| 4 | loadRSVPs llega al endpoint correcto (`GET /api/rsvp?eventId=<slug>`, `page.tsx:133`) y se re-dispara al cambiar evento (`page.tsx:335-339`); todas las mutaciones del dashboard (sendEmail:513, sendBulkEmails:573, toggleStatus:612, saveEdit:680) refrescan con `loadRSVPs()` tras éxito (sin stale UI) | Leer los 4 handlers y confirmar el `await loadRSVPs()` en la rama success | ✅ | Fetch `page.tsx:133`; effect de recarga por cambio de evento `page.tsx:335-339`. `await loadRSVPs()` en rama success de los 4: sendEmail `page.tsx:513`, sendBulkEmails `:573`, toggleStatus `:612`, saveEdit `:680`. `loadRSVPs()` sin arg usa `selectedEventId` (`:129`) |
| 5 | Feedback al usuario: el estado `message` se escribe desde ~50 puntos pero **solo se renderiza dentro del tab dashboard** (`page.tsx:1409`, dentro del bloque `activeTab === 'dashboard'` 1322-1579). Acciones de config (`saveEventConfig:856-862`), eventos (crear:2361-2368, `setAsHome:216-223`) y slug (750-777) muestran su resultado… en un tab donde el usuario no está. **[pre-análisis: éxitos y errores invisibles fuera del dashboard]** | `grep -n "message &&" app/admin/page.tsx` (debe dar solo 1409); confirmar que los tabs config/eventos no renderizan `message` | ❌ (confirmado) | `grep -n "message &&" app/admin/page.tsx` → único match `1409:{message && <div...>}` , dentro del bloque `activeTab === 'dashboard'` (`page.tsx:1322-1579`). Setters invisibles fuera del dashboard: saveEventConfig `page.tsx:856-862` (tab config), setAsHome `:216-223` (tab eventos), crear evento `:2361-2368` (tab eventos), cambio de slug `:750-777` (modal en tab eventos), auto-save de uploads `:889-909,954-975` (tab config; solo `uploadError`/`uploadErrorOg` sí se renderizan `:1859-1863,1949-1953`). → **A4-02** |
| 6 | `isEventPast()` (`page.tsx:396-462`): la heurística "si la fecha cae >2 meses en el futuro, asumir año pasado" (`444-451`) marca como PASADO cualquier evento con fecha textual sin año a >2 meses vista (ej. "SÁBADO, 13 DIC" evaluado en julio) → deshabilita envío individual y masivo (`1397-1403`, `1433-1437`, `1514`). **[pre-análisis: bloquea envíos legítimos; falla "cerrado" (no manda de más) pero rompe el flujo del admin]** | Trazar la función con fecha a 3+ meses; verificar los `disabled={...isEventPast()}` | ❌ (confirmado 🔴) | Traza con hoy=2026-07-09 y fecha "SÁBADO, 13 DIC": day=13, month=11 → eventDate=13-dic-2026 > hoy+2meses (`page.tsx:446-448`) → year=2025 (`:449-450`) → `isPast=true` → bloquea. Guards en sendEmail `:467-470` y sendBulkEmails `:527-530`; `disabled` en bulk `:1397-1403`, individual confirmados `:1433-1437`, individual cancelados `:1514-1517`. Extras confirmados: fecha ISO se parsea como UTC midnight (`:423-427`) → en TZ México el evento queda "pasado" el propio día del evento; la regex `:431` ignora un año escrito ("13 DIC 2026" sufre el mismo bug). Falla "cerrado" (no manda de más). → **A4-01** |
| 7 | sendEmail individual: confirm dialog previo (480-485), payload a `/api/admin/send-email` con `rsvpId` (497-504). **[pre-análisis de contrato]:** el server usa `to: email` **del body** (`app/api/admin/send-email/route.ts:117`) y decide el tipo por `status`/`emailSent` **del body** (`:81-82`), aunque ya releyó el RSVP de DB para `plusOneName` (`:92-96`). Si la tabla del panel está stale (otro admin editó el email), el correo sale a la dirección vieja mostrada. Documentar como contrato mixto cliente/DB | Leer `send-email/route.ts:25-132` y `page.tsx:492-505`; evaluar severidad con la rúbrica (edge case de concurrencia entre admins) | ❌ (confirmado) | Confirm previo `page.tsx:480-485`; payload con `name/email/plusOne/emailSent/status` del estado local `page.tsx:497-504`. Server: `to: email` del body (`send-email/route.ts:117`), tipo decidido por `status`/`emailSent` del body (`:81-82`), pese a releer el RSVP de DB (`:38` y `:92-96`, solo para permisos y plusOneName). Extra funcional: el try/catch `:37-78` traga errores del lookup; con `rsvpId` inexistente el email SÍ se envía (`:115`) y luego `recordEmailSent` lanza (`lib/queries.ts:144`) → 500 al admin con email ya enviado (riesgo de reintento/duplicado). Severidad 🟡 (edge de concurrencia; destinatario sigue siendo dirección histórica del mismo invitado). → **A4-04**; aspecto de confianza en body → FS-ref-2 |
| 8 | sendBulkEmails: confirm con desglose por tipo (539-549), payload `{eventId: slug, rsvpIds}` (`page.tsx:563-566`); server exige manager y filtra los IDs contra los RSVPs del evento (`app/api/admin/send-bulk-email/route.ts:65-67,82`) — IDs de otro evento no se procesan | Leer ambos lados; confirmar refresh en 573 | ✅ | Confirm con desglose confirmación/recordatorio/re-invitación `page.tsx:539-549`; payload `page.tsx:563-566`. Server: gate manager `send-bulk-email/route.ts:64-69`; scoping `getRSVPsByEvent(eventSlug)` + `filter(r => rsvpIds.includes(r.id))` (`:79-82`) → IDs ajenos al evento se descartan. Refresh `await loadRSVPs()` `page.tsx:573` |
| 9 | toggleStatus (confirmar/cancelar sin email): manda solo `{status}` a update-rsvp (`page.tsx:602-605`); **capacity check:** confirmar que NI el admin NI el flujo público aplican `capacityLimit` server-side (`grep -n "capacity" app/api/rsvp/route.ts lib/queries.ts app/api/admin/update-rsvp/route.ts` → esperado: sin matches). El toggle de config dice "**Mostrar** cupo limitado" (`page.tsx:1723-1725`) → capacity es display-only por diseño. Anotar el resultado; el flujo público es de A2 (cross-ref) | Ejecutar el grep + leer update-rsvp/route.ts completo | ✅ | toggleStatus manda solo `{status}` (`page.tsx:602-605`). `grep -n "capacity" app/api/rsvp/route.ts lib/queries.ts app/api/admin/update-rsvp/route.ts` → **0 matches** (exit 1): capacity NO se aplica server-side en ningún flujo. Coherente con el toggle "**Mostrar** cupo limitado" (`page.tsx:1723-1725`) → display-only por diseño. Cross-ref A2 (fila A2-ref-1 de §6) |
| 10 | Modal de edición de RSVP incluye `plusOneName` (`ad272fa`): estado (80-86), input condicional (2484-2493), y al desmarcar +1 limpia el nombre (`page.tsx:2480`); el server lo persiste (`updateRSVP` acepta `plusOneName`, `lib/queries.ts:92`) | Leer modal (2435-2514) + saveEdit (648-689) | ✅ | editForm con plusOneName `page.tsx:80-86` (poblado en openEditModal `:624-633`); input condicional `:2484-2493`; al desmarcar +1 limpia el nombre `:2480`; saveEdit manda el editForm completo como `updates` (`:669-672`) y refresca (`:680`); `updateRSVP` acepta `plusOneName` en su Pick (`lib/queries.ts:92`) |
| 11 | Contrato de `update-rsvp`: la ruta pasa `body.updates` **directo** a `updateRSVP` sin whitelist runtime (`app/api/admin/update-rsvp/route.ts:23,61`); la restricción `Partial<Pick<...5 campos>>` (`lib/queries.ts:90-93`) es solo de tipos — en runtime cualquier columna de la tabla es asignable vía `.set(data)`. El panel solo manda los 5 campos del editForm. **[pre-análisis: firma engañosa / tipos mentirosos]** | Leer ambos archivos; confirmar qué manda el panel (editForm 80-86) vs qué aceptaría la ruta | ❌ (confirmado) | Ruta pasa `body.updates` directo: `update-rsvp/route.ts:22-23` → `updateRSVP(rsvpId, updates)` `:61`, sin whitelist runtime. La restricción `Partial<Pick<RSVP, 6 campos>>` (`lib/queries.ts:90-93`) es solo de tipos; en runtime `.set(data)` (`:96-98`) acepta cualquier columna (`emailSent`, `emailHistory`, `cancelToken`, `eventId`…). El panel manda 5 campos del editForm (`page.tsx:80-86,669-672`) o `{status}` (`:602-605`). → **A4-05** |
| 12 | Permisos de update-rsvp: resuelve slug→UUID (`route.ts:49-50`) y exige `manager` para no-super_admin (`:53-57`) — consistente con el gotcha slug-en-eventId | Leer route.ts:39-58 | ✅ | Relee el RSVP para obtener su eventId (slug) `update-rsvp/route.ts:39-46`; resuelve slug→UUID `:49-50`; gate manager para no-super_admin `:53-57`. Consistente con el gotcha |
| 13 | Stats: StatsCards recibe stats calculadas client-side (`page.tsx:1168-1177`): `plusOne` y `totalGuests` **solo de confirmados** (cancelados excluidos ✅), cada +1 suma exactamente 1 persona extra (`1175`; no duplica). `emailsSent` cuenta sobre TODOS los rsvps incl. cancelados (`1176`) — ¿coherente con la card "✉️ Emails"? | Leer 1168-1177 + `StatsCards.tsx:20-49`; razonar cada fórmula | ✅ | Fórmulas `page.tsx:1168-1177`: `plusOne` y `totalGuests` solo sobre confirmados (`:1169,1174-1175`; cada +1 suma exactamente 1); `cancelled` correcto (`:1173`). `emailsSent` sobre todos incl. cancelados (`:1176`) — la card dice solo "✉️ Emails" (`StatsCards.tsx:43-46`): coherente como conteo histórico de envíos (un cancelado con email de re-invitación cuenta). Ambigüedad menor de labeling, sin hallazgo |
| 14 | `getEventStats` (`lib/queries.ts:167-182`) y `GET /api/stats` (`app/api/stats/route.ts`) **no tienen consumidores** (el panel calcula sus stats localmente) y ambos exponen `totalConfirmed` que en realidad es el TOTAL incluyendo cancelados (`queries.ts:178`, `stats/route.ts:46`). **[pre-análisis: código muerto + nombre mentiroso duplicado → cross-ref A5/A6]** | `grep -rn "getEventStats\|/api/stats" app lib --include='*.ts*'` (excluir definiciones y `lib/firestore.ts` legacy) | ✅ (pre-análisis confirmado) | Grep ejecutado → únicos matches: definición `lib/queries.ts:167`, duplicado legacy `lib/firestore.ts:159`, y la propia ruta (`app/api/stats/route.ts:57`). Cero consumidores (el panel calcula stats client-side, `page.tsx:1168-1177`). `totalConfirmed` = total incl. cancelados: `lib/queries.ts:178` y `app/api/stats/route.ts:46`. → **A4-12** (cross-ref A5/A6) |
| 15 | UserManagement — flujo completo: crear usuario (default `viewer`, `users/route.ts:96-97`), activar/desactivar vía `PUT` `{isActive}` (`UserManagement.tsx:110-136`), asignar evento por **UUID** con rol manager/viewer (`:166-188` → `POST users/[id]/events`), quitar acceso (`:190-214` → `DELETE ?eventId=`); cada mutación refresca (`loadUsers`/`loadUserAssignments`). Re-asignar mismo evento hace upsert del rol (`lib/user-queries.ts:184-199`) — única forma de cambiar rol de asignación desde la UI | Leer componente completo + las 3 rutas de users; confirmar refresh tras cada éxito | ❌ (parcial) | Flujo OK: crear con default viewer (`users/route.ts:96-97,111`), toggle activo `PUT {isActive}` (`UserManagement.tsx:117-121`), asignar por UUID+rol (`:169-173`), quitar (`:197-199`); refresh tras cada éxito (`:99` loadUsers, `:127`, `:179` y `:205` loadUserAssignments). **PERO la premisa del upsert falla:** el dropdown excluye eventos ya asignados (`UserManagement.tsx:469-474` `filter(e => !userAssignments.some(...))`) → el upsert server-side (`lib/user-queries.ts:183-199`) es **inalcanzable desde la UI**; no existe forma directa de cambiar el rol de una asignación (workaround: quitar + re-asignar). → **A4-09** |
| 16 | UserManagement — restos: `handleChangeRole` (`UserManagement.tsx:138-164`) **no se referencia en el JSX** (sin UI para cambiar rol global); `GET /api/admin/users/[id]` y `DELETE /api/admin/users/[id]` no los llama nadie del panel (el toggle usa PUT). Selects por `document.getElementById` (`:485-489`) en vez de estado React. **[pre-análisis: dead code + endpoints huérfanos → cross-ref A5]** | `grep -n "handleChangeRole" UserManagement.tsx` (definición sin uso); grep de fetches DELETE/GET a users/[id] | ✅ (pre-análisis confirmado) | `grep -n "handleChangeRole"` → solo la definición `UserManagement.tsx:138` (bloque 138-164, sin referencia en JSX). Fetches del componente (inventario completo): GET users `:55`, POST users `:87`, PUT users/[id] `:117`, GET users/[id]/events `:71`, POST `:169`, DELETE users/[id]/events `:197` → `GET /api/admin/users/[id]` (`users/[id]/route.ts:12-64`) y `DELETE /api/admin/users/[id]` (`:137-183`) sin consumidor. Selects por `document.getElementById` `:485-489`. → **A4-11** (cross-ref A5) |
| 17 | Los 4 endpoints de users exigen `super_admin` (`users/route.ts:25-30,76-81`; `users/[id]/route.ts:29-34,87-92,154-159`; `users/[id]/events/route.ts:29-34,79-84,145-150`) y el tab usuarios solo se renderiza para super_admin (`page.tsx:2517`); DELETE de usuario tiene guard anti-auto-desactivación (`users/[id]/route.ts:162-167`) | Leer los gates citados | ✅ | Gates super_admin verificados: `users/route.ts:25-30` (GET) y `:76-81` (POST); `users/[id]/route.ts:29-34` (GET), `:87-92` (PUT), `:154-159` (DELETE); `users/[id]/events/route.ts:29-34` (GET), `:79-84` (POST), `:145-150` (DELETE). Guard anti-auto-desactivación `users/[id]/route.ts:162-167`. Tab usuarios solo se renderiza para super_admin `page.tsx:2517` (y botón header `:1213-1221`) |
| 18 | Gating de UI por rol: viewer no ve sección de envío de emails (`page.tsx:1379`) ni columna Acciones (`1418,1429`); tab config solo manager/super_admin (`1311-1318`) con effect que expulsa de tabs sin permiso (`1186-1193`); `accessRole` viene calculado del server (`app/api/events/route.ts:40-49`) y la lista de eventos ya llega filtrada para no-super_admin (`:43-45`) | Leer los puntos citados; trazar `canManageSelectedEvent`/`isReadOnly` (1180-1183) | ✅ | Viewer sin sección de envío `page.tsx:1379` (`!isReadOnly`); columna Acciones oculta en ambas tablas: th `:1418`/`:1499`, td `:1429-1457`/`:1510-1537`. Botón tab config solo `canManageSelectedEvent` `:1311-1318`; effect que expulsa de tabs sin permiso `:1186-1193`; render de config re-verifica `:1582` y eventos `:2222`. `accessRole` calculado server-side: filtro por asignaciones `events/route.ts:40-45`, super_admin como manager `:47-48`; `canManageSelectedEvent`/`isReadOnly` derivados `page.tsx:1180-1183` |
| 19 | Export PDF (`page.tsx:1016-1114`): `stripEmojis` aplicado a title/subtitle/date/time/location/nombres (`1028-1038,1052,1062`; `be004ed`); '+1' como `'Si (+1)'` ASCII (`1055`; `f134f03`); fila indentada con nombre del +1 (`1058-1068`; `c57fd67`); solo confirmados (`1019`). **[pre-análisis, ítems menores]:** casts `(rsvp as any).plusOneName` innecesarios (`1059,1062,1140` — el tipo `RSVP` ya lo declara, `components/index.ts:19`); filename derivado de subtitle sin sanear acentos/vacío (`1112` → `lista-invitados-.pdf` si subtitle vacío); footer con nº de página solo se escribe en la última página (`1101-1109`) | Leer el bloque completo; si hay entorno, generar un PDF con datos con emojis/acentos | ✅ (con menores confirmados) | stripEmojis aplicado: title `page.tsx:1028`, subtitle `:1032`, date/time `:1033`, location `:1038`, nombre `:1052`, nombre del +1 `:1062`; `'Si (+1)'` ASCII `:1055`; fila indentada del +1 `:1058-1068`; solo confirmados `:1019`. Menores confirmados: casts `(rsvp as any).plusOneName` innecesarios `:1059,1062,1140` (el tipo ya lo declara, `components/index.ts:19`); filename `:1112` sin sanear acentos ni subtitle vacío (`lista-invitados-.pdf`); footer `:1101-1109` es un único `doc.text` tras el render → nº de página solo en la última página. Generación runtime del PDF ⏭️ no ejecutada (auditoría read-only sin sesión de panel); resuelto por traza de código. → **A4-14** |
| 20 | Export Excel (`page.tsx:1116-1166`): columna "Nombre +1" (`1132,1140`), solo confirmados, sin strip (Excel soporta Unicode ✅). **Ambos exports usan `rsvps` completos, no `filteredRsvps`** (`1019,1118`) — los botones viven junto a los filtros de visualización (`1358-1374`): ¿comportamiento esperado o sorpresa para el admin? | Leer ambas funciones; decidir si registrar como 🟢 | ✅ (con 🟢) | Excel: columna "Nombre +1" `page.tsx:1132` + valor `:1140`, solo confirmados `:1118`, sin strip (Unicode OK en xlsx). Ambos exports parten de `rsvps` completos (`:1019`, `:1118`), ignorando `filteredRsvps` y búsqueda; los botones PDF/Excel viven dentro de la sección "🔍 Filtros de Visualización" (`:1358-1374`) → sorpresa razonable para el admin que filtró. Registrado como 🟢 en **A4-14** |
| 21 | ReminderStatusSection consume bien sus endpoints: `GET /api/admin/reminder-status?eventSlug=` (`ReminderStatusSection.tsx:45`) y `POST /api/admin/send-bulk-reminder` con `{eventSlug, rsvpIds}` (`:177-184`); tras envío recarga estado (`:190`). Desde `bcc7f1e` ambos exigen sesión (+ viewer para status, manager para envío: `reminder-status/route.ts:59-64`, `send-bulk-reminder/route.ts:69-74`): un 401/403 devuelve `{success:false, error}` que el componente muestra como texto (`:48-53,96-107` y `:191-195`) **sin romper el render** (aunque tampoco redirige a login) | Leer componente + ambas rutas; simular respuesta 401 mentalmente (JSON con error) | ✅ | GET con eventSlug `ReminderStatusSection.tsx:45`; POST `{eventSlug, rsvpIds}` `:177-184`; recarga tras envío `:190` (setTimeout 2s, seguro porque la respuesta llega al terminar todos los envíos). Gates desde `bcc7f1e`: viewer para status `reminder-status/route.ts:59-64` (+ sesión `:21-28`), manager para envío `send-bulk-reminder/route.ts:69-74` (+ sesión `:25-32`). 401/403 devuelven JSON `{success:false,error}` → el componente lo muestra como texto (`:48-53`→`:96-107` y `:191-195`→`:216-227`) sin romper; no redirige a login (cubierto en **A4-08**) |
| 22 | ReminderStatusSection — estado stale entre eventos: el componente se monta sin `key` (`page.tsx:2205`) y su `data` no se resetea cuando cambia `eventSlug` → tras expandir y cambiar de evento, muestra los invitados del evento ANTERIOR; un envío mandaría `rsvpIds` viejos con el `eventSlug` nuevo. El server los descarta por scoping (`send-bulk-reminder/route.ts:102-116`: "No encontrado en este evento") así que **no sale email equivocado**, pero el admin ve fallidos inexplicables. **[pre-análisis]** También: el confirm promete "60 segundos entre cada uno" (`ReminderStatusSection.tsx:171`) pero el delay real es 5s (`send-bulk-reminder/route.ts:158-161`) | Trazar el ciclo de vida del estado ante cambio de prop; comparar textos vs delay real | ❌ (confirmado) | Montado sin `key` (`page.tsx:2205`); `data`/`isExpanded` son useState locales (`ReminderStatusSection.tsx:31,34`) y solo se actualizan al hacer clic en "Ver Estado" (`:38-59`) — no hay effect que resetee al cambiar `eventSlug` → tras expandir en evento A y cambiar a B, la lista mostrada es de A. Envío usaría `{eventSlug: B, rsvpIds: de A}` (`:180-183`); el server los descarta por scoping (`send-bulk-reminder/route.ts:105-114` "No encontrado en este evento") → **no sale email equivocado**, pero el admin ve N fallidos inexplicables. Texto del confirm "60 segundos entre cada uno" (`:171`) vs delay real 5s (`send-bulk-reminder/route.ts:158-161`). → **A4-06** |
| 23 | upload-image: valida MIME (jpeg/png/webp/gif, `upload-image/route.ts:8-13,36-41`) y tamaño 10MB (`:5,44-49`) — coincide con lo prometido en la UI (`page.tsx:1848,1938`). **[pre-análisis]:** el panel manda `imageType: 'og'` (`page.tsx:941`) que la ruta **nunca lee** — OG y fondo se nombran igual `events/{slug}-{ts}.{ext}` (`:52-54`); el blob anterior **nunca se elimina** (no hay `del()`) → huérfanos acumulándose en Vercel Blob con cada subida. Tras subir hay auto-save con fallback a guardado manual si falla (`page.tsx:891-910` ✅) | Leer ruta completa + ambos handlers de upload | ✅ (con 🟢) | MIME whitelist `upload-image/route.ts:8-13` validado `:36-41`; 10MB `:5` validado `:44-49` — coincide con la UI (`page.tsx:1848,1938`; nota: el accept del input OG omite gif `:1926` pero el server sí lo acepta). Confirmado: la ruta solo lee `file` y `eventSlug` (`:17-19`) → `imageType: 'og'` (`page.tsx:941`) se ignora; OG y fondo comparten naming `events/{slug}-{ts}.{ext}` (`:52-54`); no hay `del()` en la ruta → cada subida deja huérfano el blob anterior. Auto-save con fallback verificado: bg `page.tsx:892-910`, OG `:957-975`. → **A4-13**. Sin `validateSession` (0 matches, única ruta admin así) → **FS-ref-1** |
| 24 | add-demo-data: exige super_admin (`add-demo-data/route.ts:68-71`), **ningún botón del panel lo llama** (grep sin matches en `app/admin/`), y de invocarse en producción crearía 7 RSVPs (6 `example.com` + 1 email real, `:7-57`) contra el eventId legacy hardcodeado de `event-config.json:3` (`"rooftop-party-andras-oct2024"`); `saveRSVP` no valida que el evento exista (`lib/queries.ts:17-48`) → filas huérfanas en la tabla `rsvps`. No envía emails (solo insert). **[pre-análisis: endpoint huérfano con capacidad de ensuciar datos prod]** | `grep -rn "add-demo-data" app --include='*.tsx'`; leer la ruta y `saveRSVP` | ✅ (pre-análisis confirmado) | `grep -rn "add-demo-data" app --include='*.tsx' --include='*.ts'` → **0 matches** (ningún consumidor). Gate super_admin `add-demo-data/route.ts:59-71`. De invocarse: 7 RSVPs (`:7-57`; 6 `example.com` + 1 email real `joseassem@gmail.com` `:10`) contra `eventConfig.event.id` = `"rooftop-party-andras-oct2024"` (`event-config.json:3`); `saveRSVP` no valida existencia del evento (`lib/queries.ts:17-57` — solo chequea email duplicado) → filas huérfanas en `rsvps` de prod. No envía emails (solo insert `:76`). → **A4-07** |
| 25 | Manejo de errores de fetch en el panel: mutaciones muestran mensaje (aunque ver ítem 5), pero `loadEvents` y `loadAppSettings` fallan **en silencio** (solo `console.error`, `page.tsx:182-184,197-199` → selector clavado en "Cargando eventos..."); ningún handler distingue 401 (sesión expirada) para redirigir a `/login` — el admin queda ante errores genéricos ("Error al cargar datos", `:163-164`) | Revisar todos los catch del page.tsx y componentes; listar cuáles son silenciosos | ❌ (confirmado) | Silenciosos (solo console.error): loadEvents `page.tsx:182-184`, loadAppSettings `:197-199`, loadEventConfig `:322-324` (y además sin rama para `!response.ok`, `:285`). Con mensaje: loadRSVPs genérico `:163-164`, mutaciones (`:515,575,614,682,777,858-862`), UserManagement (`UserManagement.tsx:60-63`), ReminderStatusSection (`:52,105`). **Ningún** handler distingue 401 de sesión expirada para redirigir a `/login` (checkAuth solo corre al montar, `page.tsx:105-124`); si events nunca carga, el selector queda en "Cargando eventos..." (`:1247-1249`). → **A4-08** |
| 26 | Modal de slug: payload correcto (`PUT /api/events/[slug]` con `{newSlug}`, `page.tsx:739-745`), validación de formato client-side (710-717), doble confirm con advertencias (725-731), y tras éxito actualiza selección local + recarga eventos (`764-769` ✅). La advertencia promete renombrar `og-{slug}.*` (`2570-2572`) — la implementación server usa `fs.renameSync` sobre `public/` (`app/api/events/[slug]/route.ts:15-40`): en Vercel el filesystem del deploy no es escribible/persistente → anotar factualmente y cross-ref **A3** (dueño de la ruta) | Leer modal + handler + cabecera de la ruta events/[slug] | ✅ | Payload `PUT /api/events/[slug]` con `{newSlug}` `page.tsx:739-745`; validación formato/longitud client-side `:710-717`; advertencia inline en modal `:2558-2575` + `window.confirm` `:725-731`. Tras éxito: actualiza selección local `:764-766` (persistida a localStorage por el effect `:239-243`), cierra modal y `loadEvents()` `:768-769`. Server exige super_admin para cambiar slug (`events/[slug]/route.ts:182-189`), consistente con que el modal solo es alcanzable desde el tab eventos (`page.tsx:2222`). La promesa de renombrar `og-{slug}.*` (`:2572`) depende de `renameOgImages` con `fs.renameSync` sobre `public/` (`events/[slug]/route.ts:15-44`) → inviable en Vercel; anotado factualmente en **A3-ref-1** (dueño A3) |
| 27 | Crear evento (tab eventos): form inline con fetch propio (`page.tsx:2336-2371`) → `POST /api/events` (solo super_admin server-side, `events/route.ts:88-89`), resetea el form y recarga la lista tras éxito (`2362-2363` ✅); su mensaje de resultado cae en el `message` invisible (ver ítem 5) | Leer el bloque; confirmar gate server | ✅ | Form inline con fetch propio `page.tsx:2336-2371` → `POST /api/events` con slug/title/subtitle/date/time/location/details (`:2348-2356`). Gate server super_admin `events/route.ts:88-91` + validación slug `:96-109`. Tras éxito: `form.reset()` + `loadEvents()` `:2362-2363`. Mensajes de éxito/error caen en `message` invisible en el tab eventos (`:2361,2365,2368`) → cubierto por **A4-02** |
| 28 | Higiene: console.logs con datos de invitados en el panel (`page.tsx:130,141-144,158` — dump completo de RSVPs a consola del browser; `425-459` logs de isEventPast) — registrar como limpieza 🟢 | `grep -n "console.log" app/admin/page.tsx \| wc -l` + revisar cuáles imprimen datos | ✅ (registrado 🟢) | `grep -c "console.log" app/admin/page.tsx` → **20**. Con datos de invitados: `page.tsx:141-144` (dump del payload completo de RSVPs: nombres/emails/teléfonos), `:130,158`; isEventPast ruidoso en cada render de botones `:425-459` (4 logs); saveEventConfig imprime el request body completo `:840-841,851-853`; `:507-509` response de send-email. → **A4-15** |
| 29 | Descomposición del monolito: registrar hallazgo 🟢 con propuesta de cortes (NO refactorizar). Cortes naturales según §2.1: (a) `useAdminEvents` hook (selector+localStorage+config load, 173-339); (b) `RsvpTable` + `RsvpFilters` (341-394, 1327-1577 — las dos tablas confirmados/cancelados son ~95% duplicadas entre sí, 1412-1490 vs 1493-1571); (c) `EventConfigForm` (783-995, 1581-2219); (d) `exports.ts` util (1009-1166); (e) `EventsManager` (2221-2433); (f) modales `EditRsvpModal`/`EditSlugModal` (2435-2596); (g) `isEventPast` → util compartida con tests | Validar los rangos contra el archivo; estimar líneas por corte | ✅ (registrado 🟢) | Rangos validados contra `61f74e5`. Cortes con estimación: (a) `useAdminEvents` `page.tsx:173-339` ~165 l; (b) `RsvpTable`+`RsvpFilters` `:341-394` + `:1327-1577` ~300 l — tablas confirmados `:1412-1490` vs cancelados `:1493-1571` ~95% duplicadas (difieren solo en título y botón toggle `:1449-1455` vs `:1529-1536`); (c) `EventConfigForm` `:783-995` + `:1581-2219` ~850 l; (d) `exports.ts` `:1009-1166` ~158 l; (e) `EventsManager` `:2221-2433` ~213 l; (f) modales `:623-781` + `:2435-2596` ~320 l; (g) `isEventPast` util con tests `:396-462` ~67 l. → **A4-16** |

## 5. Hallazgos (A4-XX)

> Formato: **A4-NN [🔴/🟡/🟢] título** — descripción + evidencia `archivo:línea`. Solo cuenta con evidencia. Los **[pre-análisis]** del checklist son candidatos: confirmarlos aquí con su ID definitivo al ejecutar.

| ID | Sev | Título | Evidencia | Notas |
|----|-----|--------|-----------|-------|
| A4-01 | 🔴 | `isEventPast` marca como pasados eventos futuros a >2 meses y bloquea todo envío manual | Heurística `page.tsx:444-451`; guards `:467-470,527-530`; `disabled` en `:1397-1403,1433-1437,1514-1517` | Con fecha textual sin año a >2 meses vista (ej. "SÁBADO, 13 DIC" evaluado en julio) asume año pasado → botones de envío individual y masivo deshabilitados para un evento legítimo. Extras: fecha ISO parseada como UTC (`:423-427`) marca el evento como pasado el propio día del evento en TZ México; la regex (`:431`) ignora el año aunque esté escrito. Falla "cerrado" (no manda de más) pero rompe el flujo del admin (ítem 6) |
| A4-02 | 🟡 | `message` solo se renderiza en el tab dashboard: éxitos y errores invisibles en config/eventos | Único render `page.tsx:1409` (bloque 1322-1579); setters invisibles: `:856-862` (saveEventConfig), `:216-223` (setAsHome), `:2361-2368` (crear evento), `:750-777` (slug), `:889-909,954-975` (auto-save uploads) | El admin guarda config o crea un evento y no ve confirmación ni error (ítems 5, 27) |
| A4-03 | 🟡 | El selector restaura el evento de localStorage sin validar existencia/acceso | `page.tsx:231-236` vs validación `:254-277` que solo corre si `!selectedEventId` (`:256,260`) | Slug borrado/renombrado/sin acceso → 403 en `GET /api/rsvp` (`app/api/rsvp/route.ts:220-225`) → "Error al cargar datos"; super_admin ve dashboard vacío y config con fallback legacy (`app/api/event-settings/route.ts:98-140`). Recuperable solo re-seleccionando a mano (ítem 3) |
| A4-04 | 🟡 | `send-email` con contrato mixto cliente/DB: destino y tipo de email salen del body, no del RSVP releído | `send-email/route.ts:117` (`to: email` del body), `:81-82` (tipo por body) vs relectura DB `:38,92-96`; payload del panel `page.tsx:497-504` | Tabla stale (otro admin editó email/status) → correo a la dirección vieja o tipo equivocado. Extra: try/catch `:37-78` traga fallos del lookup; con rsvpId inexistente el email se envía y `recordEmailSent` lanza después (`lib/queries.ts:144`) → 500 con email ya enviado, invita a reintentos duplicados (ítem 7; cross-ref FS-ref-2) |
| A4-05 | 🟡 | `update-rsvp` sin whitelist runtime: la restricción de campos es solo de tipos | `update-rsvp/route.ts:22-23,61` pasa `body.updates` directo; `lib/queries.ts:90-98` (`Partial<Pick<...>>` + `.set(data)`) | Firma engañosa/tipos mentirosos: en runtime cualquier columna de `rsvps` es asignable. El panel solo manda 5 campos (`page.tsx:80-86,669-672`) o `{status}` (`:602-605`) (ítem 11) |
| A4-06 | 🟡 | ReminderStatusSection muestra datos del evento anterior tras cambiar el selector + promete "60 segundos" con delay real de 5s | Sin `key` `page.tsx:2205`; estado no reseteado `ReminderStatusSection.tsx:31-38`; texto `:171` vs delay `send-bulk-reminder/route.ts:158-161` | Envío con IDs viejos + slug nuevo → server los descarta por scoping (`send-bulk-reminder/route.ts:105-114`), no sale email equivocado, pero el admin ve fallidos inexplicables (ítem 22) |
| A4-07 | 🟡 | `add-demo-data` huérfano apuntando a eventId legacy: ensuciaría datos de prod | Sin consumidores (grep 0 matches en `app/`); `add-demo-data/route.ts:7-57` (6 `example.com` + 1 email real); `event-config.json:3`; `saveRSVP` sin validar evento (`lib/queries.ts:17-57`) | Crearía 7 filas huérfanas en `rsvps` contra `"rooftop-party-andras-oct2024"`. No envía emails (ítem 24; cross-ref A5 como endpoint muerto) |
| A4-08 | 🟡 | Cargas silenciosas y cero manejo de sesión expirada en el panel | loadEvents `page.tsx:182-184`, loadAppSettings `:197-199`, loadEventConfig `:322-324` (+ sin rama `!ok` `:285`); ningún catch distingue 401; selector clavado `:1247-1249` | Sesión expirada a mitad de uso → errores genéricos o silencio en vez de redirect a `/login` (checkAuth solo corre al montar `:105-124`) (ítems 21, 25) |
| A4-09 | 🟡 | Imposible cambiar el rol de una asignación desde la UI: el upsert server-side es inalcanzable | Dropdown excluye eventos ya asignados `UserManagement.tsx:469-474`; upsert `lib/user-queries.ts:183-199` | Cambiar viewer↔manager exige quitar acceso y re-asignar (dos pasos, con ventana sin acceso); el upsert diseñado para esto nunca se ejecuta desde el panel (ítem 15) |
| A4-10 | 🟢 | `LoginForm` muerto y, de usarse, roto (Basic auth vs validación por cookie) | Solo exportado en barrel `components/index.ts:7`, nadie lo importa (`page.tsx:14`); `LoginForm.tsx:28-35,50` vs `validate/route.ts:11-22` | `/api/admin/validate` queda sin consumidor real. Cross-ref **A5** (ítem 2) |
| A4-11 | 🟢 | `handleChangeRole` muerto + `GET/DELETE /api/admin/users/[id]` huérfanos + selects por `getElementById` | `UserManagement.tsx:138-164` sin uso en JSX; rutas `users/[id]/route.ts:12-64,137-183` sin fetch en el panel; `UserManagement.tsx:485-489` | Cross-ref **A5** (ítem 16) |
| A4-12 | 🟢 | `getEventStats` y `GET /api/stats` sin consumidores, con `totalConfirmed` mentiroso y duplicado legacy | `lib/queries.ts:167-182` (`:178`), `app/api/stats/route.ts:45-49` (`:46`), duplicado `lib/firestore.ts:159` | `totalConfirmed` = total incl. cancelados. Cross-ref **A5/A6** (ítem 14) |
| A4-13 | 🟢 | upload-image: `imageType` ignorado, naming compartido OG/fondo y blobs huérfanos acumulándose | Panel manda `imageType` `page.tsx:941`; ruta solo lee file/eventSlug `upload-image/route.ts:17-19`; naming `:52-54`; sin `del()` en toda la ruta | Cada re-subida deja el blob anterior huérfano en Vercel Blob. Menor: accept del input OG omite gif (`page.tsx:1926`) pero el server lo acepta (ítem 23) |
| A4-14 | 🟢 | Exports: ignoran filtros activos, filename sin sanear, casts `as any` innecesarios y nº de página solo en la última página del PDF | `rsvps` vs `filteredRsvps` `page.tsx:1019,1118` (botones dentro de la sección de filtros `:1358-1374`); filename `:1112,1164`; casts `:1059,1062,1140` (tipo ya lo declara `components/index.ts:19`); footer `:1101-1109` | Ítems 19-20 |
| A4-15 | 🟢 | 20 console.log en el panel, incluyendo dump completo de datos de invitados a la consola del browser | `page.tsx:141-144` (payload completo de RSVPs), `:130,158`, isEventPast `:425-459`, request body de config `:840-841,851-853` | Limpieza (ítem 28) |
| A4-16 | 🟢 | Descomposición del monolito (2,599 l) — propuesta de cortes sin refactorizar | Cortes (a)-(g) con rangos validados en ítem 29; duplicación ~95% entre tablas `page.tsx:1412-1490` vs `:1493-1571` | ~1,900 líneas extraíbles en 7 módulos; prioridad: (c) EventConfigForm y (b) RsvpTable (ítem 29) |

**Conteo: 1🔴 · 8🟡 · 7🟢**

## 6. Hallazgos fuera de scope

> Anotación factual + cross-ref a la auditoría dueña. No profundizar aquí.
> **Todas las filas pre-registradas fueron verificadas contra el working tree en `61f74e5` durante esta ejecución** (evidencia actualizada donde cambió el anclaje).

| Ref | Observación factual | Evidencia | Dueño |
|-----|--------------------|-----------|-------|
| FS-ref-1 | ✔️ Verificado: `POST /api/admin/upload-image` es la única ruta bajo `/api/admin/` sin `validateSession` — grep por ruta: 0 matches en upload-image, ≥2 en las otras 12 | `app/api/admin/upload-image/route.ts:15-19` (entra directo a `formData`) vs p.ej. `update-rsvp/route.ts:8-19` | **Fase S** |
| FS-ref-2 | ✔️ Verificado: `send-email` usa `to: email` y `status`/`emailSent` provenientes del body del request en lugar de releer el RSVP que ya consulta | `app/api/admin/send-email/route.ts:26,81-82,117` vs `:38,92-96` | **Fase S / A1** (el aspecto funcional-stale se registra en A4-04, ítem 7) |
| A3-ref-1 | ✔️ Verificado: `renameOgImages` renombra archivos de `public/` con `fs.renameSync` — en Vercel el filesystem del deployment no es escribible; la advertencia del modal del panel promete un rename que no puede ocurrir en prod | `app/api/events/[slug]/route.ts:15-44` (invocado `:196-199`); `app/admin/page.tsx:2572` | **A3** |
| A1-ref-1 | ✔️ Verificado: `send-bulk-reminder` duerme 5s entre emails dentro del request (`maxDuration = 300`) → >~55 destinatarios exceden el límite; además `maxDuration 300` vs plan Hobby | `app/api/admin/send-bulk-reminder/route.ts:18,158-161` | **A1 / A8** |
| A2-ref-1 | ✔️ Verificado: `capacityLimit` no se aplica server-side en ningún flujo (tampoco el público): es display-only ("Mostrar cupo limitado") | grep `capacity` en `app/api/rsvp/route.ts`, `lib/queries.ts` y `app/api/admin/update-rsvp/route.ts` → 0 matches; `app/admin/page.tsx:1723-1725` | **A2** |
| A5-ref-1 | Ramas "modo demo" con storage en memoria (`mockEvents`/`mockRsvps`) solo alcanzables sin `DATABASE_URL` — código muerto en prod detectado al auditar los endpoints del panel | `app/api/events/route.ts:10-11,147-163`; `app/api/rsvp/route.ts:12-13,147-173,236-245` | **A5** |

## 7. Cierre

1. ✅ Los 29 ítems del checklist tienen resultado + evidencia `archivo:línea` o comando+output. Ningún ítem quedó ⬜; el único sub-paso no ejecutado (generación runtime de un PDF, ítem 19, opcional) se resolvió por traza de código y está anotado en su evidencia.
2. ✅ Hallazgos consolidados en §5: **A4-01…A4-16**.
3. Fila A4 del `00_INDEX.md`: la actualiza el orquestador (esta sesión es read-only fuera de este MD).
4. Commit + push: `audit: A4 admin-panel — 1🔴 8🟡 7🟢` (lo ejecuta el orquestador).

**Conteo final: 1🔴 · 8🟡 · 7🟢** (16 hallazgos)

**Método:** auditoría estática sobre `61f74e5` — lectura completa de `app/admin/page.tsx` (2,599 l), los 4 componentes + barrel, y las 15 rutas API consumidas/huérfanas (`rsvp`, `events`, `events/[slug]`, `event-settings`, `admin/{send-email, send-bulk-email, send-bulk-reminder, reminder-status, update-rsvp, event-settings/update, settings, upload-image, users, users/[id], users/[id]/events, validate, add-demo-data}`, `stats`), más `lib/queries.ts` y `lib/user-queries.ts` completos. Greps de consumidores para todo lo marcado como huérfano. Sin ejecución runtime con efectos (producción con datos reales); los ítems de comportamiento se resolvieron por traza de código, método anotado en cada evidencia.
