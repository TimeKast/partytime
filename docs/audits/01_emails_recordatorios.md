# A1 — 📧 Auditoría: Emails y recordatorios

> **Estado:** ✅ completada · **Owner/Sesión:** fable5-c37b36 · **Inicio:** 2026-07-09 10:10 · **Fin:** 2026-07-09
> **SHA framework de referencia:** `bcc7f1e` · **SHA auditado:** `9d9c7f2`
> **Hallazgos:** 2🔴 12🟡 5🟢 (A1-01…A1-19) + 5 fuera de scope

**⚠️ ANTES DE EMPEZAR — protocolo de `docs/audits/00_INDEX.md` (obligatorio):**

1. `git pull --ff-only`.
2. Marcar la fila A1 del INDEX como 🔄 con Owner, Inicio y SHA auditado. **Commit + push inmediato** — el lock solo existe cuando el push tiene éxito; si es rechazado → `git pull --ff-only` y re-seleccionar auditoría.
3. **Read-only:** durante la auditoría está prohibido editar código de la app. Solo se escribe en este MD y en `00_INDEX.md`.
4. **Evidencia obligatoria en TODOS los ítems, también cuando pasan:** `archivo:línea` inspeccionado o comando + output relevante. Ítem sin evidencia = auditoría no cerrable.
5. Ítems no ejecutables → `⏭️ NOT RUN` + razón explícita (p.ej. "requiere DATABASE_URL, no disponible").
6. Alcance: **corrección funcional únicamente** — ¿se envía el email correcto, a quien corresponde, cuando corresponde, exactamente una vez? La protección de endpoints/sesiones NO se audita aquí (Fase S); si aparece algo de esa naturaleza, va a "Hallazgos fuera de scope" sin profundizar.

---

## 1. Objetivo

Verificar que los **5 puntos de envío de email** de la app envían el email correcto, al destinatario correcto, en el momento correcto, **exactamente una vez** — y que lo registran de forma consistente. Esto incluye: que los recordatorios del cron salgan solo el día agendado y solo a confirmados de eventos vigentes; que ningún punto de envío incluya RSVPs cancelados cuando no corresponde; que los links de cancelación funcionen para cada destinatario; que los templates muestren los datos del evento correcto (no los del config estático); y que los 4 fixes recientes (`5894370`, `0e7ad21`, `cc195f8`, `bcc7f1e`) no hayan regresado ni dejado huecos. La app está en **producción con usuarios reales** — un email mal dirigido o duplicado es el peor escenario de esta auditoría.

## 2. Contexto mínimo (para sesión fría)

- **Los 5 puntos de envío:**
  1. **Confirmación automática al hacer RSVP** — `app/api/rsvp/route.ts` (POST, envío en :77–137, condicionado a `event.emailConfirmationEnabled`).
  2. **Recordatorios programados vía cron** — `app/api/cron/send-reminders/route.ts` (GET/POST).
  3. **Email individual manual desde admin** — `app/api/admin/send-email/route.ts` (confirmación / recordatorio / re-invitación según estado).
  4. **Envío masivo manual desde admin** — `app/api/admin/send-bulk-email/route.ts` (tipo por-RSVP según `status`/`emailSent`).
  5. **Recordatorio masivo manual desde admin** — `app/api/admin/send-bulk-reminder/route.ts` (siempre `isReminder: true`).
- **Gotcha crítico:** `rsvps.eventId` (columna text) almacena el **slug** del evento, NO el UUID (`app/api/rsvp/route.ts:49` asigna `eventId = event.slug`; insert en `lib/queries.ts:50`). Todo helper que filtre RSVPs "por evento" debe recibir slug.
- **Cron:** `vercel.json:2-7` → `/api/cron/send-reminders` cada 12h (`0 */12 * * *`). Vercel Cron corre en **UTC** → 00:00 y 12:00 UTC = **18:00 y 06:00 hora Ciudad de México (UTC-6)**. La ventana "hoy" del query se calcula en UTC (`lib/queries.ts:462-467`).
- **Registro de envíos:** `rsvps.emailHistory` (JSONB, `lib/schema.ts:97`) acumula `{sentAt, type}` y `rsvps.emailSent` (timestamp, `lib/schema.ts:96`) guarda el último envío — ambos escritos por `recordEmailSent` (`lib/queries.ts:133`). A nivel evento, `events.reminderSentAt` (`lib/schema.ts:71`) es el lock anti-duplicado del cron, escrito por `markReminderSent` (`lib/queries.ts:492`).
- **Estados de RSVP:** solo existen `'confirmed'` (default, `lib/schema.ts:93`) y `'cancelled'`. **No existe `'declined'`** en el schema ni en el código (verificar con grep — ítem 16).
- Cliente Resend instanciado a nivel de módulo (`lib/resend.ts:7`); `FROM_EMAIL` con fallback `onboarding@resend.dev` (`lib/resend.ts:9`).

## 3. Scope de archivos

| Archivo | Zona relevante |
|---------|----------------|
| `app/api/cron/send-reminders/route.ts` | completo (237 líneas) — auth :22-37, loop eventos :82-201, lock :110-113, envío :158-163, registro :176 |
| `app/api/rsvp/route.ts` | POST :15-190 (envío :77-137) · GET :193-254 (lectura, para PRE-3) |
| `app/api/admin/send-email/route.ts` | completo (147 líneas) — datos del body :26, eventData :35-78, envío :115-120, registro :131-132 |
| `app/api/admin/send-bulk-email/route.ts` | completo (171 líneas) — resolución evento :33-36, loop :100-154 |
| `app/api/admin/send-bulk-reminder/route.ts` | completo (186 líneas) — scoping :105-116, filtro confirmed :118-122 |
| `lib/queries.ts` | `saveRSVP` :17 · `getRSVPsByEvent` :62 · `getRSVPById` :76 · `recordEmailSent` :133 · `generateCancelToken` :188 · `getEventsWithPendingReminders` :459 · `markReminderSent` :492 · `getConfirmedRSVPsForReminder` :503 · `updateEventSlug` :345 (migración de `rsvps.eventId` al renombrar slug) |
| `lib/resend.ts` | completo (9 líneas) |
| `lib/email-template.ts` | completo (347 líneas) — fallback estático :52-62, textos :71-89, plusOne :257-276, footer :324-334 |
| `vercel.json` | crons :2-7 |
| `app/admin/page.tsx` | SOLO :826-831 (cómo se serializa `reminderScheduledAt` desde el datetime-local) — lectura puntual, no se audita el panel (A4) |
| `app/api/admin/event-settings/update/route.ts` | SOLO :126-135 (persistencia de `reminderScheduledAt`) — lectura puntual |

**Fuera de scope explícito:**

- **Protección de endpoints y sesiones** (validación de `CRON_SECRET`, `validateSession`, `userHasEventAccess`, quién puede llamar qué) → **Fase S**. Aquí solo se verifica la *corrección funcional* de a quién se envía. Si algo de auth parece roto, se anota en "Hallazgos fuera de scope" sin explotar.
- Ciclo de vida RSVP (crear/editar/cancelar/reconfirmar como flujo) → A2. Aquí solo importa qué RSVPs reciben email.
- Panel admin (UI, formularios) → A4. Solo se lee `admin/page.tsx:826-831` como fuente del timestamp.
- Código muerto/duplicado en general → A5 (pero la duplicación **entre los 5 puntos de envío** sí es de A1, ítems 26-27).
- `app/api/admin/reminder-status/route.ts` — no envía emails (solo lectura de estado); mencionado únicamente si aparece divergencia con los criterios de selección de los que sí envían.

## 4. REQUISITO DE COMPLETITUD — call-graph de los 5 puntos de envío

**Obligatorio antes del checklist:** trazar ruta→helper de CADA punto de envío leyendo el código (no de memoria): qué query selecciona destinatarios, qué filtra (status, evento, ids), qué se marca como enviado y **en qué orden respecto al `resend.emails.send()`**. Llenar la tabla con `archivo:línea` en cada celda. Una fila incompleta = auditoría no cerrable.

| Punto de envío | Quién selecciona destinatarios (query + archivo:línea) | Filtros aplicados (status/evento/ids) | Qué/cuándo se registra en emailHistory / reminderSentAt (¿antes o después del send?) | Riesgo de duplicado/omisión detectado |
|---|---|---|---|---|
| 1. Confirmación RSVP (`app/api/rsvp/route.ts`) | El propio RSVP recién insertado por `saveRSVP` (`lib/queries.ts:43-54`, llamado en `route.ts:67-74`); destinatario = `email` del body recién validado (:29-35) e insertado | Evento existe (:47-64) + `isActive` (:53-58) + `emailConfirmationEnabled` (:77) + dedup email+evento en `saveRSVP` (`queries.ts:28-37`). NO valida `rsvpClosed` (ítem 18) | `recordEmailSent(rsvp.id,'confirmation')` DESPUÉS de send exitoso (:126-128); fallo de email no aborta el RSVP (catch :133-136) | Si `recordEmailSent` falla tras send, el envío queda sin registro (catch :133-136 solo log). Bajo impacto: email único |
| 2. Cron recordatorios (`app/api/cron/send-reminders/route.ts`) | `getEventsWithPendingReminders` (`queries.ts:459-487`) elige eventos; `getConfirmedRSVPsForReminder(event.slug)` (`queries.ts:503-514`) elige RSVPs (:97) | Evento: `reminderEnabled`+`isActive`+`scheduledAt` dentro de HOY UTC+`reminderSentAt IS NULL` (:471-478) + `!rsvpClosed` post-query (:481-484). RSVP: `status='confirmed'` en SQL (:509-510). SIN filtro de fecha de evento pasada (ítem 3) | `markReminderSent(event.id)` ANTES del loop de envío (:110-113) y también con 0 RSVPs (:103-107); `recordEmailSent(rsvp.id,'reminder')` después de cada send exitoso (:174-177) | Duplicado: protegido por lock-first. Omisión: crash/timeout post-lock deja cola sin email sin reintento; ~59 emails máx por corrida (300s/5s, :17+:182) compartidos entre eventos (ítems 9-10) |
| 3. Admin individual (`app/api/admin/send-email/route.ts`) | **El body del request** (:26): `email` destino (:117), `name`, `plusOne`, `emailSent`, `status` — NO se lee el destinatario de DB. `getRSVPById` (:38, :92) solo para eventData/plusOneName | Ninguno sobre status: el tipo se deriva del body (:81-82 `isCancelled`/`isReminder`). Frontend manda el RSVP cargado en estado (`app/admin/page.tsx:497-504`) | `recordEmailSent(rsvpId, tipo)` DESPUÉS del send (:131-132); si lanza, el catch exterior (:140-146) devuelve 500 con el email YA enviado | Body stale → email a dirección vieja, tipo equivocado, cancel link inválido (token con email del body :85). 500 post-send invita a reintentar → duplicado (ítems 11/17/20) |
| 4. Admin bulk email (`app/api/admin/send-bulk-email/route.ts`) | `getRSVPsByEvent(eventSlug)` (:79) ∩ `rsvpIds` del body (:82); datos por-RSVP leídos de DB | Sin filtro de status — feature: cancelados reciben "Te extrañamos" (:103-104, tipo derivado de DB). Ids ajenos al evento quedan fuera por la intersección (:82). Frontend: filtros default `confirmed`+`not-sent` (`page.tsx:73-74`) y confirm con desglose (:539-547) | `recordEmailSent` DESPUÉS de cada send exitoso (:141-142); excepción → catch :149-153 cuenta failed++ | Sin dedup por emailHistory: N reenvíos posibles. Delay 100ms (:147) puede exceder rate limit de Resend. SIN `maxDuration` explícito → muerte a mitad de lista según plan (ítems 13/30) |
| 5. Admin bulk reminder (`app/api/admin/send-bulk-reminder/route.ts`) | `rsvpIds` del body cruzados contra `getRSVPsByEvent(event.slug)` vía Map (:105-111) | Id ajeno al evento → "No encontrado en este evento" + failed++ (:112-116, fix bcc7f1e); `status !== 'confirmed'` → skip (:118-122); siempre `isReminder: true` (:134) | `recordEmailSent(rsvp.id,'reminder')` DESPUÉS de cada send exitoso (:152-153); excepción → catch :163-167 failed++ | Sin dedup por historial en el endpoint (la UI lo mitiga: `needsManualReminder` se calcula por emailHistory en reminder-status :100-119). Delay 5s (:159-161) + maxDuration 300 (:18) → ~59 máx |

> Pista de arranque (verificar, no copiar): el punto 3 es el único cuyo **destinatario** (`email`) llega en el body del request en vez de leerse del RSVP en DB (`app/api/admin/send-email/route.ts:26`). El punto 2 es el único con lock a nivel evento **antes** del envío (`app/api/cron/send-reminders/route.ts:110-113`).

## 5. Checklist de verificaciones

Formato de resultado: 🟩 pasa · 🟥 falla (→ registrar hallazgo) · ⏭️ NOT RUN (+ razón). **Evidencia obligatoria en todos.**

### A. Regresión de los 4 fixes recientes

| # | Verificación | Cómo verificar (comando/paths) | Resultado | Evidencia |
|---|---|---|---|---|
| 1 | **cc195f8 vigente:** `markReminderSent(event.id)` se ejecuta ANTES del loop de envío del cron (lock anti-duplicado por timeout). Confirmar que ninguna edición posterior lo movió después del envío. | Read `app/api/cron/send-reminders/route.ts:103-113` y comparar con `git show cc195f8 -- app/api/cron/send-reminders/route.ts` | 🟩 | `app/api/cron/send-reminders/route.ts:110-113`: `markReminderSent(event.id)` ANTES del loop de envío (:140-191). `git show cc195f8` movió el lock al inicio y eliminó el `markReminderSent` del final; el código actual conserva exactamente ese patrón. Vigente |
| 2 | **0e7ad21 vigente:** la ventana del cron selecciona SOLO eventos con `reminderScheduledAt` dentro de HOY (`gte startOfDay` + `lte endOfDay` + `reminderSentAt IS NULL`), no "hoy o antes". | Read `lib/queries.ts:462-478`; `git show 0e7ad21 -- lib/queries.ts` | 🟩 | `lib/queries.ts:475-477`: `gte(reminderScheduledAt, startOfDay)` + `lte(reminderScheduledAt, endOfDay)` + `isNull(reminderSentAt)`, con `startOfDay`/`endOfDay` de HOY UTC (:462-467). Coincide con el diff de `0e7ad21` (que reemplazó `lte(scheduledAt, now)` por la ventana de día). Vigente |
| 3 | **¿5894370 sobrevivió al rewrite de 0e7ad21?** `5894370` añadió filtro de "evento con fecha ya pasada"; `0e7ad21` reescribió la función. El filtro post-query actual (`lib/queries.ts:481-484`) SOLO excluye `rsvpClosed` — verificar si quedó algún check contra `events.date`. Caso concreto: evento con `date` de la semana pasada pero `reminderScheduledAt` = hoy (p.ej. reprogramado y olvidado) → ¿recibe reminder? | `git show 5894370 -- lib/queries.ts` vs `git show 0e7ad21 -- lib/queries.ts` vs Read `lib/queries.ts:459-487` actual — diff de los tres estados | 🟥 | **NO sobrevivió.** `git show 5894370` añadió el filtro post-query `if (!event.date) return true; return event.date >= today`. `git show 0e7ad21` reescribió ese bloque y lo redujo a `if (event.rsvpClosed) return false; return true`. Actual `lib/queries.ts:481-484`: solo excluye `rsvpClosed` — no queda ningún check contra `events.date`. Evento con `date` pasada + `reminderScheduledAt` = hoy SÍ recibe reminder → **A1-04** |
| 4 | **bcc7f1e vigente (parte funcional del scoping):** en `send-bulk-reminder`, los `rsvpIds` del body se cruzan contra `getRSVPsByEvent(event.slug)` y los ids ajenos al evento se rechazan con error, no se procesan. | Read `app/api/admin/send-bulk-reminder/route.ts:105-116` | 🟩 | `send-bulk-reminder/route.ts:105-106`: `getRSVPsByEvent(event.slug)` → `Map` por id; :111-116: id no presente en el Map → error "No encontrado en este evento" + `failed++` + `continue` — ids ajenos al evento no se procesan. Vigente |

### B. Fechas, "hoy" y zona horaria

| # | Verificación | Cómo verificar | Resultado | Evidencia |
|---|---|---|---|---|
| 5 | **¿Qué día "es" para el cron?** La ventana se construye con `now.toISOString().split('T')[0]` → día **UTC** (`lib/queries.ts:462-467`). El admin captura `reminderScheduledAt` en un `datetime-local` (hora local del navegador, típicamente México UTC-6) y lo serializa con `.toISOString()` (`app/admin/page.tsx:826-827`, persistido en `app/api/admin/event-settings/update/route.ts:126-128`). Trazar el caso: recordatorio agendado para "hoy 20:00" hora México → se guarda como 02:00 UTC del día **siguiente** → la corrida de 00:00 UTC (18:00 MX) del día agendado NO lo ve, y la del día siguiente sí → ¿el reminder sale el día equivocado según el usuario? Documentar la matriz corrida-cron × hora-agendada. | Read de las 3 zonas citadas + razonamiento escrito de la matriz (00:00 UTC y 12:00 UTC vs horas MX 00:00–23:59) | 🟥 | Trazado: `page.tsx:826-827` serializa el datetime-local (hora MX) con `.toISOString()` → UTC; `update/route.ts:126-127` persiste; ventana del cron en UTC (`queries.ts:462-467`). Corridas: 00:00Z = **18:00 MX del día anterior**, 12:00Z = 06:00 MX. **Matriz:** agenda 00:00–17:59 MX del día D → se guarda en día UTC D → primera corrida elegible es 00:00Z del día D = 18:00 MX del día **D-1** → el reminder sale la TARDE ANTERIOR al día elegido (hasta 24h antes de la hora elegida). Agenda 18:00–23:59 MX día D → día UTC D+1 → corrida 00:00Z D+1 = 18:00 MX día D → sale el mismo día MX, hasta 6h antes. El caso del ítem (hoy 20:00 MX → 02:00Z D+1): la corrida 00:00Z D+1 = 18:00 MX de HOY sí lo ve → sale hoy 2h antes; el caso grave es la agenda diurna, que es sistemático → **A1-02**. Agravantes encontrados en el mismo trace: (1) al recargar, `page.tsx:311` mete la hora UTC (`toISOString().slice(0,16)`) en el datetime-local, que el navegador interpreta como local → cada ciclo cargar→guardar desplaza `reminderScheduledAt` **+6h** → **A1-03**; (2) `clearSentStatus` (`page.tsx:830-831`) compara `scheduledAt` contra `sentAt` — timestamps de naturaleza distinta, casi siempre ≠ → cualquier guardado del config tras un reminder enviado pone `reminderSentAt=NULL` (`update/route.ts:134-136`) y re-arma el cron → **A1-01** |
| 6 | **La hora agendada se ignora dentro del día:** la condición es `startOfDay <= scheduledAt <= endOfDay` sin `scheduledAt <= now` (`lib/queries.ts:475-476`). Un reminder agendado hoy a las 23:00 UTC sale en la corrida de 00:00 UTC (23h antes de lo agendado). ¿Es comportamiento aceptado/documentado o sorpresa para el host? | Read `lib/queries.ts:469-478`; buscar documentación/UI que comunique esto: `grep -rn "recordatorio" app/admin/page.tsx \| head` | 🟥 | `queries.ts:475-476` sin `lte(scheduledAt, now)`: la hora se ignora dentro del día UTC (agendado 23:00Z sale en la corrida de 00:00Z). No documentado: la UI pide "Fecha y hora del recordatorio:" (`app/admin/page.tsx:2139`, input datetime-local :2143-2144) y el hint :2156 promete "se enviará automáticamente a todos los confirmados **en la fecha programada**" — recolecta una hora que el sistema descarta, sin avisar del desfase de día (ítem 5). Sorpresa para el host → **A1-02** |
| 7 | **Silencio permanente si se pierde el día:** combinando #5/#6 — si ambas corridas de un día fallan (deploy caído, 401 del cron), `reminderScheduledAt` queda en el pasado y `gte(startOfDay)` lo excluye para siempre; `reminderSentAt` queda NULL sin reintento ni alerta. Confirmar que no existe mecanismo de recuperación. | Read `lib/queries.ts:459-487`; `grep -rn "reminderScheduledAt" app lib --include="*.ts"` buscando algún re-enqueue | 🟥 | Confirmado sin recuperación automática: `queries.ts:475` `gte(startOfDay)` excluye para siempre un `scheduledAt` de días anteriores; `grep -rn reminderScheduledAt app lib` no muestra ningún re-enqueue/retry/alerta (solo captura en admin, persistencia y la query del cron). `reminderSentAt` queda NULL sin aviso. Mitigación manual existente: `ReminderStatusSection` lista `needsManualReminder` y ofrece `send-bulk-reminder` (`app/admin/components/ReminderStatusSection.tsx:157-184`) — pero requiere que el admin entre a mirar; no hay alerta proactiva → **A1-06** |
| 8 | **Método real de invocación del cron:** Vercel Cron hace **GET**; el route soporta GET y POST (`app/api/cron/send-reminders/route.ts:19,234-236`) y `00_INDEX.md:59` dice "POST". Verificar qué header manda Vercel realmente cuando `CRON_SECRET` está seteado (documentación oficial: `Authorization: Bearer <CRON_SECRET>`) vs el header custom `x-vercel-cron-secret` que el código también acepta (:23-28). **Riesgo funcional:** si ninguna de las dos ramas matchea lo que Vercel envía, el cron devuelve 401 silenciosamente y NINGÚN recordatorio sale jamás. | Read :22-37; consultar docs Vercel Cron (`vercel.com/docs/cron-jobs`); si hay acceso al dashboard/logs de Vercel, verificar respuestas recientes del cron (200 vs 401) — si no hay acceso, marcar sub-verificación de logs como ⏭️ NOT RUN | 🟩 | Código `route.ts:22-37`: acepta `x-vercel-cron-secret == CRON_SECRET` (:23,:28) O `Authorization: Bearer CRON_SECRET` (:29). Docs oficiales (vercel.com/docs/cron-jobs/manage-cron-jobs, consultadas vía search_vercel_documentation): Vercel invoca con GET y manda `Authorization: Bearer <CRON_SECRET>` → la rama `isManualCall` es la que matchea. **Logs de producción verificados** (Vercel MCP, runtime logs proyecto `prj_q29a8...`): `GET /api/cron/send-reminders 200` a las 00:01 y 12:01 UTC con output `📧 [CRON] Found 0 events with pending reminders` — el cron corre y autentica OK; no hay 401. La rama `x-vercel-cron-secret` es código muerto con comentario engañoso ("Vercel's auto cron") → **A1-16** 🟢 |

### C. Idempotencia — orden envío vs registro

| # | Verificación | Cómo verificar | Resultado | Evidencia |
|---|---|---|---|---|
| 9 | **Cron, lado omisión del lock-first:** al marcar `reminderSentAt` ANTES de enviar (`app/api/cron/send-reminders/route.ts:110-113`), un crash/timeout después del lock deja RSVPs sin email y el evento nunca se reintenta (el fix cc195f8 cambió duplicados por omisiones silenciosas). Además, con `rsvps.length === 0` también se marca sent (:103-107) → invitados que confirmen más tarde ese mismo día ya no reciben el reminder agendado. Documentar ambos trade-offs como comportamiento actual. | Read :103-113 + :140-191 (dónde puede morir el proceso) | 🟥 | Documentado ambos trade-offs: (a) lock antes del envío `:110-113`; el proceso puede morir en el loop `:140-191` (send :158-163, `recordEmailSent` :176, delay 5s :182) → RSVPs restantes sin email y evento nunca reintentado (cc195f8 cambió duplicados por omisiones silenciosas — sin registro de a quiénes faltó); (b) `:103-107` con `rsvps.length === 0` también marca `reminderSentAt` → quien confirme más tarde ese mismo día ya no recibe el reminder agendado → **A1-07** |
| 10 | **Aritmética del timeout del cron:** `maxDuration = 300` (:17) y delay fijo de 5000ms por RSVP (:182) → ≈59 emails máximo por corrida, compartidos entre TODOS los eventos del día. Un evento con >55 confirmados se trunca a mitad de lista con el evento ya locked (#9) → cola de invitados sin reminder, sin registro de a quiénes faltó. Verificar la aritmética y si existe paginación/reanudación. | Read :17, :82, :140-191; calcular: 300s / 5s por email; `grep -n "maxDuration\|setTimeout" app/api/cron/send-reminders/route.ts` | 🟥 | `:17` `maxDuration = 300`; `:182` delay fijo 5000ms tras CADA email (incluido el último, a diferencia de bulk-reminder que lo salta :159). Aritmética: 300s / ~5.3s por ciclo (send+record+delay) ≈ **56-59 emails máx por corrida**, compartidos entre TODOS los eventos del día (loop :82). No existe paginación ni reanudación (grep confirma un solo setTimeout y ningún cursor). Evento >55 confirmados → truncado con el lock ya puesto (#9) → cola sin reminder ni registro → **A1-07** |
| 11 | **Orden send→record en los 5 puntos:** `recordEmailSent` se llama DESPUÉS de que Resend acepta, en los 5: rsvp `:126-128`, cron `:174-177`, send-email `:122-132`, bulk-email `:136-143`, bulk-reminder `:147-153`. Caso "Resend acepta pero DB falla": el email salió y no queda registro → `emailSent` queda viejo → un reenvío admin posterior tratará al RSVP como no-notificado (posible duplicado) y en send-email/bulk-email además cambia el TIPO calculado (`isReminder = !!emailSent`, `send-email:82`, `bulk-email:104`). Caso inverso (registro sin envío) no ocurre por el orden. Verificar los 5 sitios y qué pasa con la excepción de `recordEmailSent` en cada uno (¿aborta el loop? ¿cuenta como failed?). | Read los 5 puntos citados; en cada uno seguir el catch que envuelve `recordEmailSent` | 🟥 | Orden send→record correcto en los 5: rsvp `:119-128`, cron `:158-176`, send-email `:115-132`, bulk-email `:129-142`, bulk-reminder `:140-153` (registro solo si Resend no devolvió error). Excepción de `recordEmailSent`: rsvp → catch `:133-136` solo log (no aborta, OK); cron → catch `:183-190` **failed++** (el email SÍ salió pero se reporta como fallo, sin registro); bulk-email → catch `:149-153` failed++; bulk-reminder → catch `:163-167` failed++; **send-email → catch exterior `:140-146` devuelve 500 "Error al procesar solicitud" con el email YA enviado** → el admin reintenta → duplicado, y además el TIPO del siguiente envío cambia (`isReminder = !!emailSent`, send-email :82, bulk-email :104) → **A1-10** |
| 12 | **`recordEmailSent` no es atómico:** hace SELECT del historial y luego UPDATE con el array reconstruido (`lib/queries.ts:139-159`). Dos envíos concurrentes al mismo RSVP (p.ej. bulk-email del admin corriendo mientras el cron procesa el mismo evento) pueden perder una entrada del historial (last-write-wins). Verificar que no hay `FOR UPDATE`/jsonb append atómico y evaluar la ventana real de concurrencia. | Read `lib/queries.ts:133-162`; `grep -n "emailHistory" lib/queries.ts lib/schema.ts` | 🟥 | `lib/queries.ts:139-159`: SELECT del RSVP (:139-144) + UPDATE con array reconstruido en JS (:151-159). No hay `FOR UPDATE`, transacción, ni append jsonb atómico (`grep emailHistory` → solo :146,:154 y schema :97). Ventana real: bulk-email corre con delay 100ms (`send-bulk-email:147`) y puede solaparse con el cron (5s por email) sobre el mismo RSVP → last-write-wins pierde una entrada del historial (`emailSent` timestamp sobrevive, la entrada del array no) → **A1-10** |
| 13 | **Ningún punto de envío consulta `emailHistory` para deduplicar:** confirmar que el único mecanismo anti-duplicado es `reminderSentAt` a nivel EVENTO (cron) y que bulk-reminder/bulk-email/send-email pueden reenviar al mismo RSVP N veces sin advertencia (responsabilidad 100% del admin en UI). Documentar como comportamiento, hallazgo si contradice expectativa de producto. | `grep -n "emailHistory" app/api -r`; Read de los loops de bulk-email :100-154 y bulk-reminder :109-168 | 🟩 | Confirmado: `grep emailHistory app/api -r` → solo `reminder-status/route.ts:71-95` (lectura de estado, no envía). Ningún punto de ENVÍO consulta el historial para deduplicar; anti-duplicado real = `reminderSentAt` a nivel EVENTO (cron) + criterio del admin en UI. Los loops bulk-email `:100-154` y bulk-reminder `:109-168` reenvían al mismo RSVP tantas veces como se invoquen. Mitigación de producto existente: el botón de bulk-reminder de la UI se alimenta de `needsManualReminder`, que reminder-status calcula filtrando `emailHistory` por `type==='reminder'` (:77,:100-119) — el flujo guiado no reenvía a quien ya tiene reminder. Comportamiento documentado; la deuda de divergencia va en A1-14 |

### D. Selección de destinatarios — exclusión de cancelados en LOS 5 puntos

| # | Verificación | Cómo verificar | Resultado | Evidencia |
|---|---|---|---|---|
| 14 | **Cron excluye cancelados:** `getConfirmedRSVPsForReminder` filtra `status = 'confirmed'` en SQL (`lib/queries.ts:506-511`) y el cron la usa (:97). | Read ambos | 🟩 | `lib/queries.ts:506-511`: `and(eq(rsvps.eventId, eventSlug), eq(rsvps.status, 'confirmed'))` — filtro en SQL; cron la usa en `send-reminders/route.ts:97` con `event.slug`. Cancelados excluidos |
| 15 | **bulk-reminder excluye no-confirmados:** salta con error `No confirmado` (`app/api/admin/send-bulk-reminder/route.ts:118-122`). | Read :109-122 | 🟩 | `send-bulk-reminder/route.ts:118-122`: `if (rsvp.status !== 'confirmed')` → error `"${rsvp.email}: No confirmado"` + `failed++` + `continue`. No-confirmados nunca reciben el reminder |
| 16 | **bulk-email NO filtra status — es feature:** a cancelados les manda "Te extrañamos" (re-invitación) y a confirmados confirmación/recordatorio (`app/api/admin/send-bulk-email/route.ts:103-104,121-127`). Verificar: (a) que el tipo se deriva del `status` LEÍDO DE DB, no del body; (b) quién construye `rsvpIds` en el frontend y si puede seleccionar cancelados sin darse cuenta (`grep -rn "send-bulk-email" app/admin app/components --include="*.tsx"`). No existe status `declined` (`grep -rn "'declined'\|\"declined\"" lib app --include="*.ts" --include="*.tsx"` → esperado: 0 resultados; `lib/schema.ts:93` default `'confirmed'`). | Read + greps citados | 🟩 | (a) Tipo derivado de DB: `send-bulk-email/route.ts:103-104` usa `rsvp.status`/`rsvp.emailSent` del array leído por `getRSVPsByEvent` (:79), no del body — ✓. (b) Frontend: `rsvpIds` sale de `emailTargetRsvps` (`app/admin/page.tsx:565`), construido con filtros cuyos defaults son `'confirmed'` + `'not-sent'` (:73-74, useEffect :378-394) → seleccionar cancelados exige cambiar el filtro explícitamente, y el confirm desglosa el conteo por tipo incluyendo re-invitaciones (:539-547); además hay gate `isEventPast()` (:527). (c) `grep "'declined'"` en lib/app → **0 resultados**; `lib/schema.ts:93` default `'confirmed'`. Feature operando como se espera |
| 17 | **send-email individual: TODO viene del body.** `name`, `email`, `plusOne`, `emailSent`, `status` los manda el cliente (`app/api/admin/send-email/route.ts:26`) — el email se envía a `email` del body aunque en DB el RSVP tenga otra dirección o esté cancelado; el tipo (`isReminder`/`isCancelled`) también se deriva del body (:81-82), no de DB. Comparar con `getRSVPById` que SÍ se consulta (dos veces: :38 y :92) pero solo para eventData/plusOneName. ¿Puede el flujo normal del admin producir un envío con datos stale (RSVP editado en otra pestaña)? | Read :24-101; `grep -rn "send-email" app/admin --include="*.tsx"` para ver qué manda el frontend | 🟥 | Confirmado: `send-email/route.ts:26` toma `name, email, plusOne, emailSent, status` del body; el envío va a `email` del body (:117) y el tipo se deriva del body (:81-82). `getRSVPById` se llama dos veces (:38, :92) pero solo para eventData/permiso y plusOneName — nunca para el destinatario. El frontend (`app/admin/page.tsx:497-504`) manda el RSVP del estado cargado con `loadRSVPs`; si el RSVP se editó en otra pestaña (email corregido, status cambiado) el flujo normal envía a la **dirección vieja** con **tipo stale**, y el cancel link se genera con ese email stale (ítem 20) → **A1-05** |
| 18 | **Confirmación RSVP: gating del evento.** El POST valida `event.isActive` (`app/api/rsvp/route.ts:53-58`) pero NO `event.rsvpClosed` (`lib/schema.ts:63`) — ¿un evento con RSVP "cerrado" pero activo sigue aceptando RSVPs nuevos y enviando confirmaciones? Verificar si `rsvpClosed` se valida en otra capa (frontend/página) y si el email de confirmación puede salir para un evento cerrado. El flujo RSVP completo es A2 — aquí SOLO el efecto email; cruzar referencia. | Read `app/api/rsvp/route.ts:42-74`; `grep -rn "rsvpClosed" app/api lib --include="*.ts"` | 🟥 | `app/api/rsvp/route.ts:53-58` valida solo `event.isActive`; `grep rsvpClosed` en `app/api` → solo event-settings/update (:116-117) y events/[slug] (lectura/escritura del flag) — **ningún check en el POST de RSVP**. La única barrera es de UI: `app/[slug]/page.tsx:244-250` oculta el CTA y muestra `rsvpClosedMessage`. Un POST directo a un evento cerrado-pero-activo crea el RSVP y, con `emailConfirmationEnabled`, **envía la confirmación** (:77-128). Efecto email registrado como **A1-09**; el flujo RSVP completo se cruza a **A2** |
| 19 | **Confirmación solo si `emailConfirmationEnabled`:** el envío está gated por `eventForEmail && eventForEmail.emailConfirmationEnabled` (`app/api/rsvp/route.ts:77`). Verificar: (a) default del flag = false (`lib/schema.ts:68`); (b) si el RSVP llega SIN `eventSlug` (rama legacy, `eventForEmail` queda null, :38-39) nunca hay email — ¿esa rama legacy es alcanzable desde el frontend actual? | Read :37-77; `grep -rn "'/api/rsvp'" app --include="*.tsx" \| head` y revisar si algún caller omite eventSlug | 🟩 | (a) `lib/schema.ts:68` `emailConfirmationEnabled` default **false** ✓; gating en `rsvp/route.ts:77` (`eventForEmail && eventForEmail.emailConfirmationEnabled`). (b) Único caller del POST en el frontend: `app/components/RSVPModal.tsx:55-60` — siempre incluye `eventSlug` (`{ ...formData, eventSlug }`), prop poblada desde `app/[slug]/page.tsx:303` (`eventSlug={slug}`). La rama legacy sin `eventSlug` (route :38-39, cae a `eventConfig.event.id` y `eventForEmail=null` → sin email) solo es alcanzable por llamada directa al API, no desde el frontend actual |

### E. Links de cancelación (token + URL base)

| # | Verificación | Cómo verificar | Resultado | Evidencia |
|---|---|---|---|---|
| 20 | **Consistencia token↔validación por punto de envío:** el token es determinístico `sha256(rsvpId-email-secret)` (`lib/queries.ts:188-193`) y `cancelRSVP` lo valida contra `rsvp.email` de DB (`lib/queries.ts:118`). Verificar que cada punto genera el token con el email DE DB: cron `:143` (rsvp.email ✓?), bulk-email `:106`, bulk-reminder `:125`, rsvp `:104` (email recién insertado ✓?), **send-email `:85` usa `email` del body** — si difiere del email en DB, el link del email no valida → botón "Modificar o Cancelar" roto para ese invitado. | Read los 5 sitios + `lib/queries.ts:108-128,188-198` | 🟥 | Token determinístico `sha256(rsvpId-email-secret)` (`queries.ts:188-193`); validación SIEMPRE contra email de DB: `cancelRSVP` `queries.ts:118`, `/api/rsvp/get:30`, `/api/rsvp/update:27`. Generación por punto: cron `:143` `rsvp.email` de DB ✓; bulk-email `:106` `rsvp.email` de DB ✓; bulk-reminder `:125` `rsvp.email` de DB ✓; rsvp `:104` email recién insertado (= DB) ✓; **send-email `:85` usa `email` del BODY** — si difiere del email en DB (edición concurrente), el link enviado no valida → botón "Modificar o Cancelar" roto para ese invitado → **A1-05** |
| 21 | **URL base:** los 5 puntos construyen `${NEXT_PUBLIC_APP_URL \|\| 'http://localhost:3000'}/cancel/...` (rsvp :105, cron :144, send-email :86, bulk-email :107, bulk-reminder :126) y el template usa la misma env para `bgImageUrl` (`lib/email-template.ts:92-95`). Si la env falta en prod → links y fondo rotos apuntando a localhost. Verificar que la env existe en el deploy (si hay acceso a Vercel) o al menos que `grep -rn "NEXT_PUBLIC_APP_URL" .env* vercel.json` y el código no tienen divergencias de nombre. | Greps citados; acceso a Vercel env si disponible (si no → sub-check ⏭️ NOT RUN con razón) | 🟩 | Los 5 puntos + template usan la MISMA env (grep): rsvp `:105`, cron `:144`, send-email `:86`, bulk-email `:107`, bulk-reminder `:126`, template `lib/email-template.ts:92-95` (bgImageUrl). Sin divergencias de nombre; `.env.example:35` la documenta. **Verificado en deploy** (`vercel env ls`, proyecto timekast/partytime): `NEXT_PUBLIC_APP_URL` existe en Production/Preview/Development. Sub-check del VALOR: ⏭️ NOT RUN — el valor está encriptado en Vercel y no es inspeccionable desde CLI/MCP (solo presencia verificable) |
| 22 | **Columna `rsvps.cancelToken` mentirosa:** `saveRSVP` genera el token con un `crypto.randomUUID()` aleatorio ANTES del insert (`lib/queries.ts:41`), así que el valor guardado en la columna NO corresponde a `generateCancelToken(rsvp.id, email)` que usan todos los emails. Verificar si algún flujo LEE la columna `cancelToken` (si nadie la lee, es dato muerto/engañoso — 🟡/🟢; si alguien la lee para validar, es link roto — 🔴). | `grep -rn "cancelToken" app lib --include="*.ts" --include="*.tsx"` y clasificar cada uso lectura/escritura | 🟥 | Escrituras: `queries.ts:41` genera el token con `crypto.randomUUID()` (un UUID aleatorio, NO el id real del row, que lo genera el schema en el insert `lib/schema.ts:80`) y `:52` lo inserta — el valor guardado jamás corresponde a `generateCancelToken(rsvp.id, email)`. Lecturas: **ninguna** — grep muestra solo schema `:103`, el insert `:52` y el legacy muerto `lib/firestore.ts:65,95,100`. Nadie valida contra la columna (todos recomputan), así que no hay link roto: es **dato muerto y engañoso** → **A1-15** 🟢 (cross-ref A5/A6 para su remoción) |
| 23 | **Limpieza `=` duplicada e inconsistente:** send-email limpia la URL antes del template (`app/api/admin/send-email/route.ts:88-89`) y el template la vuelve a limpiar para el href (`lib/email-template.ts:68`) — pero verificar que TODOS los puntos pasan por `cleanCancelUrl` del template (sí, si todos usan `generateConfirmationEmail`). Confirmar que no hay ruta que meta la URL cruda en otro campo. | Read `lib/email-template.ts:67-68,303,316`; grep `cancelUrl` en los 5 routes | 🟩 | Template limpia con `cleanCancelUrl` (`email-template.ts:68`) y lo usa tanto en el href del botón (:303) como en el texto copiable (:316) — nunca la URL cruda. Los 5 puntos pasan `cancelUrl` a `generateConfirmationEmail` (rsvp :108-116, cron :147-155, send-email :93-101, bulk-email :109-117, bulk-reminder :129-137); no existe ruta que meta la URL sin pasar por el template. Redundancia: send-email `:88-89` limpia la misma URL antes de que el template la vuelva a limpiar → **A1-19** 🟢 |

### F. Contenido de templates

| # | Verificación | Cómo verificar | Resultado | Evidencia |
|---|---|---|---|---|
| 24 | **`displayTitle` vs `title`:** el schema tiene `displayTitle` "Title shown on invitation page (if empty, uses title)" (`lib/schema.ts:16`) y la página pública lo respeta (`app/[slug]/page.tsx:148-157`), pero TODOS los emails usan `event.title` (template `lib/email-template.ts:141,144` vía `EventData.title`, poblado con `event.title` en cron :118, rsvp :84, send-email :56, bulk-email :43, bulk-reminder :79; subjects con `event.title` en cron :161, bulk-reminder :143, etc.). ¿Divergencia intencional? Un host que puso un `displayTitle` distinto verá que el email muestra el título interno. Clasificar. | Read sitios citados; `grep -rn "displayTitle" lib/email-template.ts app/api` (esperado: 0 en emails) | 🟥 | `grep displayTitle lib/email-template.ts app/api` → 0 hits en rutas de email (solo event-settings lectura/escritura :57,:93). TODOS los emails usan `event.title`: template `:141,:144` (header) y `:106,:76,:81,:86,:331` vía `EventData.title`, poblado con `event.title` en cron `:118`, rsvp `:84`, send-email `:56`, bulk-email `:43`, bulk-reminder `:79`; subjects con `event.title`/`eventTitle` (cron `:161`, rsvp `:122`, send-email `:104`, bulk-email `:36,:120-127`, bulk-reminder `:143`). La página pública SÍ respeta `displayTitle` (`app/[slug]/page.tsx:148-157`). Host con displayTitle distinto → email muestra el título interno → **A1-13** |
| 25 | **`plusOneName` en los 5 puntos:** el template lo muestra si existe (`lib/email-template.ts:257-276`, fallback "+1 Confirmado" :272). Verificar por punto: cron `:150`, bulk-email `:112`, bulk-reminder `:132` (los tres con cast `(rsvp as any).plusOneName` — ¿por qué `any` si `RSVP` lo tipa? tipos mentirosos), send-email `:92-96` (segunda query `getRSVPById` SOLO para esto — duplicada con :38), y rsvp `:111` que usa `plusOneName` del body SIN trim mientras `saveRSVP` guardó la versión trimmed (:72) — divergencia cosmética entre email y DB. | Read los 6 sitios citados | 🟥 | Template muestra plusOneName con fallback "+1 Confirmado" (`email-template.ts:257-276`, :272). Por punto: cron `:150`, bulk-email `:112`, bulk-reminder `:132` — los tres con cast `(rsvp as any).plusOneName` **innecesario**: `RSVP` ya tipa `plusOneName` (`lib/schema.ts:90`) → tipos mentirosos; send-email `:92-96` hace una SEGUNDA `getRSVPById` solo para plusOneName (duplica la de :38); rsvp `:111` pasa `plusOneName` del body SIN trim mientras `saveRSVP` guardó la versión trimmed (route `:72`) → divergencia cosmética email vs DB → **A1-18** 🟢 |
| 26 | **Fallback silencioso a `event-config.json` (datos de OTRO evento):** el template usa config estático si `eventData` es undefined (`lib/email-template.ts:52-62`). Trazar cuándo llega undefined: send-email si `getRSVPById` falla o el evento no existe — el catch :76-78 solo hace `console.warn` y CONTINÚA enviando con datos estáticos; bulk-email si `getEventBySlug` devuelve null (:33-40, `eventData` queda undefined y `eventTitle` cae a `eventConfig.event.title` :36) — el envío procede. Resultado: invitado recibe email con título/fecha/lugar del evento estático equivocado. Cron y bulk-reminder en cambio 404ean/skipean sin evento. Clasificar severidad por punto. | Read `send-email:35-78,104`, `bulk-email:33-61`, template :52-62; comparar con `bulk-reminder:60-66` | 🟥 | Fallback estático confirmado: `email-template.ts:52-62` usa `event-config.json` si `eventData` es undefined — datos de OTRO evento ("Party Time!"/"ANDRREAS"/"SÁBADO, 29 NOV"/"HAMBURGO 108", `event-config.json:2-16`). Rutas donde llega undefined y el envío PROCEDE: **send-email** — catch `:76-78` solo `console.warn` y continúa (getRSVPById/getEventBySlug fallan), y también si `event` es null (:52, sin error alguno); **bulk-email** — `event` null → `eventData` undefined (:39-40) y `eventTitle` cae al estático (:36), el loop envía igual (:100-134). Escenario real: evento hard-deleted (`deleteEvent(id, true)`, `queries.ts:329-330`) con RSVPs vivos → invitados reciben email con título/fecha/lugar del evento equivocado. Cron (`:103-107` skip sin RSVPs; evento siempre existe por la query) y bulk-reminder (`:61-66` 404 sin evento) NO tienen esta ruta. Severidad: 🟡 en ambos puntos admin (edge poco probable, consecuencia visible al invitado) → **A1-08** |
| 27 | **Textos y subjects correctos por tipo:** confirmación / recordatorio / re-invitación producen greeting+badge+botón coherentes (`lib/email-template.ts:71-89,303`) y subjects consistentes entre puntos (`Confirmación -`/`Recordatorio -`/`Te extrañamos -`: rsvp :122, cron :161, send-email :104-112, bulk-email :119-127, bulk-reminder :143). Detalle: el footer dice "Este email fue enviado porque confirmaste tu asistencia" (:331) también en la re-invitación a alguien que CANCELÓ — texto engañoso menor. Verificar los 3 modos y el heurístico de género `name.endsWith('a')` (:76). | Read template :71-89, :294-317, :324-334 + subjects en los 5 routes | 🟩 | Los 3 modos coherentes en greeting/badge/botón: re-invitación `:73-78` (badge "Te extrañamos", botón verde "Reconfirmar Asistencia ✨" :303), recordatorio `:79-83` (badge "Recordatorio"), confirmación `:84-89` (sin badge). Subjects consistentes en los 5 puntos: `Confirmación -` (rsvp :122, send-email :111, bulk-email :126), `Recordatorio -` (cron :161, send-email :109, bulk-email :124, bulk-reminder :143), `Te extrañamos -` (send-email :107, bulk-email :122). Notas 🟢: footer `:330-332` "Este email fue enviado porque confirmaste tu asistencia" aparece TAMBIÉN en la re-invitación a quien canceló (texto engañoso menor); heurístico de género `name.endsWith('a')` `:76` falla con nombres como "Joshua"/"Nicolás" → **A1-17** |
| 28 | **`FROM_EMAIL` y remitente:** los 5 puntos envían `from: Party Time! <${FROM_EMAIL}>`; si `FROM_EMAIL` no está seteado cae a `onboarding@resend.dev` (`lib/resend.ts:9`) — dominio de pruebas de Resend que en producción puede fallar o caer a spam. Verificar env en deploy si hay acceso; si no, documentar el fallback como riesgo. | Read `lib/resend.ts`; grep `FROM_EMAIL` en los 5 routes; env de Vercel si accesible (si no → ⏭️ parcial) | 🟩 | `lib/resend.ts:9` fallback `onboarding@resend.dev`; los 5 puntos usan `FROM_EMAIL` (rsvp :120, cron :159, send-email :116, bulk-email :130, bulk-reminder :141) con el mismo formato `Party Time! <...>`. **Verificado en deploy** (`vercel env ls`): `FROM_EMAIL` existe en Production/Preview/Development (igual que `RESEND_API_KEY`). Valor encriptado no inspeccionable → el riesgo del fallback queda latente solo si la env se elimina |

### G. Duplicación/divergencia entre los 5 puntos + PRE-3

| # | Verificación | Cómo verificar | Resultado | Evidencia |
|---|---|---|---|---|
| 29 | **Construcción de `EventData` copiada 5 veces con defaults divergentes:** cron :116-137 y bulk-reminder :77-96 usan colores hardcoded (`#FF1493`...) y `eventConfig.contact?.hostEmail` opcional; send-email :52-73 y bulk-email :40-61 usan `eventConfig.theme.*` y `eventConfig.contact.hostEmail` sin `?.`; rsvp :82-101 otra copia. Mapear las 5 copias campo por campo y registrar divergencias (mismo evento puede verse distinto según qué punto envió). Candidato natural a helper compartido — registrar como hallazgo de deuda (la corrección es del plan correctivo, NO de esta sesión). | Read/diff de los 5 bloques; opcional: extraer a archivos temp y `diff` | 🟥 | 5 copias del bloque `EventData`, en DOS familias de defaults: **familia A** (cron `:116-137`, bulk-reminder `:77-96`, rsvp `:82-101`): colores hardcoded (`#FF1493`/`#00FFFF`/`#FFD700`/`#1a0033`), `hostEmail: event.hostEmail \|\| eventConfig.contact?.hostEmail` (con `?.`), bg fallback `'/background.png'`; **familia B** (send-email `:52-73`, bulk-email `:40-61`): `theme = event.theme \|\| eventConfig.theme`, colores fallback `eventConfig.theme.*`, `eventConfig.contact.hostEmail` SIN `?.` (crash si faltara la sección), bg `eventConfig.event.backgroundImage \|\| '/background.png'`. Hoy los VALORES coinciden (`event-config.json:17-22` = hardcoded) así que no hay divergencia visible, pero cualquier edición del JSON hace que el mismo evento se vea distinto según qué punto envió. Candidato a helper compartido → **A1-14** |
| 30 | **Delays y límites divergentes:** 5000ms/email en cron (:182) y bulk-reminder (:159-161) vs 100ms en bulk-email (:147); `maxDuration = 300` en cron (:17) y bulk-reminder (:18) pero AUSENTE en bulk-email y send-email — un bulk-email largo puede morir por el límite default de Vercel a mitad de lista sin resumen de a quién le llegó. Verificar los 4 valores y el default del plan Hobby. | Read los 4 routes (`grep -n "maxDuration\|setTimeout" app/api/admin/*.ts app/api/cron/send-reminders/route.ts`) | 🟥 | Delays: cron 5000ms `:182` (tras CADA email, incluido el último), bulk-reminder 5000ms `:159-161` (salta el último), **bulk-email 100ms `:147`**, send-email n/a (unitario). `maxDuration`: cron `:17`=300, bulk-reminder `:18`=300, **AUSENTE en bulk-email y send-email** (grep confirma) → quedan al default del plan. Sub-check del default efectivo: ⏭️ NOT RUN — depende de si el proyecto tiene Fluid Compute activo (Hobby: 10-60s clásico vs 300s fluid), no inspeccionable vía CLI/MCP disponible; en el peor caso un bulk-email largo muere a mitad de lista Sin resumen de enviados (la response nunca se emite). Riesgo adicional: 100ms entre sends ≈ hasta 10 req/s, por encima del rate limit documentado de Resend (2 req/s) → 429s marcados como failed a mitad de lista → **A1-14** |
| 31 | **Lógica `isReminder`/tipo duplicada y divergente:** send-email deriva de `body.emailSent`/`body.status` (:81-82) mientras bulk-email deriva de `rsvp.emailSent`/`rsvp.status` de DB (:103-104) — misma intención, fuentes distintas, resultados potencialmente distintos para el mismo RSVP. Documentar. | Read ambos bloques | 🟥 | send-email deriva `isCancelled`/`isReminder` de `body.status`/`body.emailSent` (`:81-82`) mientras bulk-email lo deriva de `rsvp.status`/`rsvp.emailSent` leídos de DB (`:103-104`); el frontend replica la lógica una tercera vez para el confirm (`page.tsx:472-473`). Mismo RSVP puede recibir tipo distinto según el botón usado si el estado del cliente está stale → **A1-05 / A1-14** |
| 32 | **PRE-3 (pre-anclado): `getRSVPsByEvent(eventId)` recibe slug.** La firma dice `eventId` (`lib/queries.ts:62`) pero la columna guarda slug (insert `lib/queries.ts:50` ← `app/api/rsvp/route.ts:49,67-74`). Verificar TODOS los call sites pasan slug y ninguno pasa UUID: `app/api/rsvp/route.ts:228` (resuelve `event?.slug \|\| eventIdOrSlug` en :217 ✓?), `send-bulk-email:79` (`eventSlug` de :35), `send-bulk-reminder:105` (`event.slug`), `reminder-status:67`, `stats:43`. Ojo con los fallbacks `event?.slug \|\| eventIdOrSlug`: si el evento no existe y el caller pasó UUID, se consulta RSVPs por UUID → 0 resultados silenciosos (omisión). Lo mismo para `getConfirmedRSVPsForReminder(eventSlug)` (:503, llamada cron :97 con `event.slug` ✓?) y `getEventStats` (:167). Registrar como hallazgo de firma mentirosa + casos de fallback peligroso. | `grep -rn "getRSVPsByEvent\|getConfirmedRSVPsForReminder\|getEventStats" app lib --include="*.ts"` y Read de cada call site con su resolución de slug | 🟥 | Firma `getRSVPsByEvent(eventId)` (`queries.ts:62`) pero la columna guarda SLUG (insert `:50` ← rsvp route `:49` `eventId = event.slug`). Call sites: rsvp GET `:228` con `eventSlug = event?.slug \|\| eventIdOrSlug` (:217 — fallback crudo); bulk-email `:79` con `:35` (mismo fallback crudo); bulk-reminder `:105` `event.slug` tras 404 ✓; reminder-status `:67` `event.slug` tras 404 ✓; stats `:43` con `:33` (fallback crudo). `getConfirmedRSVPsForReminder(eventSlug)` `:503`: cron `:97` con `event.slug` ✓. `getEventStats` (`queries.ts:167`): **cero callers** en app (grep — solo el legacy `lib/firestore.ts:159` define el suyo) → código muerto, cross-ref A5. Los fallbacks `\|\| eventIdOrSlug`: `getEventBySlug` ya intenta slug e id (`:223-241`), así que el fallback solo actúa con evento INEXISTENTE → consulta RSVPs por un valor que no es slug → 0 resultados silenciosos (omisión sin error). Firma mentirosa + fallback peligroso → **A1-11** |
| 33 | **Renombre de slug no rompe emails:** `updateEventSlug` migra `rsvps.eventId` al nuevo slug (`lib/queries.ts:384-398`). Verificar la ventana: si el cron corre ENTRE el update del evento (:384-387) y el update de los RSVPs (:390-392), `getConfirmedRSVPsForReminder(nuevoSlug)` devuelve 0 y el evento se marca sent sin enviar nada (cron :103-107). Documentar la carrera (probabilidad baja pero omisión permanente por #9). | Read `lib/queries.ts:345-404` + cron :97-107 | 🟥 | `updateEventSlug`: update del evento (`:384-387`) y update de los RSVPs (`:390-392`) son DOS awaits sin transacción (no hay `db.transaction` en el archivo). Carrera: cron entre ambos → `getConfirmedRSVPsForReminder(nuevoSlug)` (`:97`) devuelve 0 → `markReminderSent` con 0 RSVPs (cron `:103-107`) → evento marcado sent sin enviar nada, y por la ventana solo-hoy (#7) no hay reintento → **omisión permanente**. Probabilidad baja (ventana de ms, 2 corridas/día) pero consecuencia silenciosa → **A1-12** |

## 6. Hallazgos

> Registrar SOLO hallazgos con evidencia `archivo:línea`. Numerar A1-01, A1-02, ... Severidad según rúbrica del INDEX (🔴 email a quien no corresponde / cuando no corresponde o flujo roto · 🟡 edge case incorrecto o deuda que causará bugs · 🟢 limpieza/consistencia).

| ID | Severidad | Descripción | Evidencia (archivo:línea) | Ítem del checklist |
|----|-----------|-------------|---------------------------|--------------------|
| A1-01 | 🔴 | **`clearSentStatus` re-arma el reminder en cada guardado del config.** Compara `reminderScheduledAt` (nuevo) contra `reminderSentAt` (cuándo se ENVIÓ) — timestamps de naturaleza distinta, prácticamente siempre ≠ → tras un reminder enviado, CUALQUIER guardado del formulario de config (aunque solo se cambie el título) manda `clearSentStatus: true` y el server pone `reminderSentAt = NULL`. Si `reminderScheduledAt` sigue cayendo en el día UTC en curso, la siguiente corrida del cron **re-envía el reminder a todos los confirmados** (duplicado masivo, misma clase del incidente "40 vueltas de Mundo" que cc195f8 arregló por otra vía) | `app/admin/page.tsx:830-831` (comparación), `app/api/admin/event-settings/update/route.ts:134-136` (aplica NULL), `lib/queries.ts:471-478` (re-elegibilidad), `app/api/cron/send-reminders/route.ts:110-113` | 5 |
| A1-02 | 🔴 | **El reminder diurno sale la tarde MX anterior y la hora se ignora.** La ventana del cron es el día UTC (corridas 00:00Z = 18:00 MX del día anterior, 12:00Z = 06:00 MX) sin `lte(scheduledAt, now)`: toda agenda entre 00:00–17:59 hora MX se guarda en el día UTC correcto pero la primera corrida elegible es 18:00 MX del día ANTERIOR → email hasta 24h antes de lo elegido, sistemático para agendas diurnas. La UI pide "Fecha y hora" y promete envío "en la fecha programada" sin avisar nada de esto | `lib/queries.ts:462-476`, `app/admin/page.tsx:826-827` (serialización UTC), `:2139,:2143-2144,:2156` (UI), matriz completa en ítem 5 | 5, 6 |
| A1-03 | 🟡 | **Round-trip del datetime desplaza la agenda +6h por ciclo cargar→guardar.** Al cargar, la hora UTC almacenada se mete cruda en el `datetime-local` (`toISOString().slice(0,16)`), que el navegador interpreta como hora local; al guardar se re-convierte a UTC → cada visita al config que termine en "Guardar" corre el reminder +6h (para navegador UTC-6), pudiendo cruzarlo a otro día UTC (sale un día tarde o, combinado con A1-01, re-arma en día distinto). El admin además VE una hora que nunca eligió | `app/admin/page.tsx:311` (carga), `:826-827` (guardado), `app/api/event-settings/route.ts:87` (fuente ISO) | 5 |
| A1-04 | 🟡 | **Regresión del fix `5894370`:** el filtro "no mandar reminders a eventos con fecha pasada" (`event.date >= today`) fue eliminado por el rewrite de `0e7ad21`; hoy el post-filtro solo excluye `rsvpClosed`. Evento con `date` pasada pero `reminderScheduledAt` = hoy (reprogramado/olvidado) recibe reminder | `lib/queries.ts:481-484` actual vs `git show 5894370`/`git show 0e7ad21 -- lib/queries.ts` | 3 |
| A1-05 | 🟡 | **send-email individual opera 100% con datos del body (stale-prone).** Destinatario, tipo (`isReminder`/`isCancelled`) y token de cancelación se derivan del body que manda el frontend desde su estado cargado — un RSVP editado en otra pestaña produce: email a la dirección VIEJA, tipo equivocado, y cancel link generado con el email stale que NO valida contra DB → botón "Modificar o Cancelar" roto. bulk-email deriva lo mismo de DB (fuentes divergentes para la misma intención) | `app/api/admin/send-email/route.ts:26,81-82,85,117` vs `send-bulk-email/route.ts:103-104`; frontend `app/admin/page.tsx:497-504`; validación `lib/queries.ts:118` | 17, 20, 31 |
| A1-06 | 🟡 | **Reminder perdido = silencio permanente.** Si las dos corridas de un día fallan (deploy caído, error), `gte(startOfDay)` excluye el `reminderScheduledAt` pasado para siempre; `reminderSentAt` queda NULL sin reintento ni alerta. Existe recuperación manual (ReminderStatusSection → send-bulk-reminder) pero exige que el admin lo descubra por su cuenta | `lib/queries.ts:475-476`; grep `reminderScheduledAt` sin re-enqueue; mitigación `app/admin/components/ReminderStatusSection.tsx:157-184` | 7 |
| A1-07 | 🟡 | **Lock-first del cron: omisiones silenciosas y truncamiento a ~59 emails.** El evento se marca enviado ANTES de enviar (fix cc195f8): crash/timeout post-lock deja invitados sin reminder sin registro ni reanudación; con 0 RSVPs también se marca (confirmados posteriores del mismo día quedan fuera); `maxDuration=300` con delay fijo 5s → ~56-59 emails máximo por corrida compartidos entre TODOS los eventos del día, sin paginación | `app/api/cron/send-reminders/route.ts:103-107,110-113,17,182`, loop `:140-191` | 9, 10 |
| A1-08 | 🟡 | **Fallback silencioso a `event-config.json` manda datos de OTRO evento.** En send-email (catch que continúa; y `event` null sin error) y bulk-email (`event` null → eventData undefined + título estático), el email sale con título/fecha/lugar del config estático ("Party Time!"/"ANDRREAS"/"SÁBADO, 29 NOV"). Cron y bulk-reminder abortan correctamente en el caso equivalente | `lib/email-template.ts:52-62`, `app/api/admin/send-email/route.ts:76-78,52`, `app/api/admin/send-bulk-email/route.ts:33-40,36`, `event-config.json:2-16` | 26 |
| A1-09 | 🟡 | **POST /api/rsvp no valida `rsvpClosed` → confirmación por email para evento cerrado.** Solo `isActive` se verifica en el API; la única barrera de `rsvpClosed` es que la UI oculta el botón. Un POST directo a un evento cerrado-activo crea el RSVP y dispara la confirmación automática (flujo completo → A2; aquí el efecto email) | `app/api/rsvp/route.ts:53-58,77-128`; grep `rsvpClosed` sin hits en el POST; UI `app/[slug]/page.tsx:244-250` | 18 |
| A1-10 | 🟡 | **`recordEmailSent` no atómico y fallo post-send mal manejado.** (a) SELECT+UPDATE sin lock ni append atómico → envíos concurrentes al mismo RSVP pierden entradas de `emailHistory` (last-write-wins). (b) Si `recordEmailSent` lanza tras un send exitoso: send-email devuelve **500 con el email ya enviado** (reintento del admin → duplicado, y el tipo calculado cambia); cron/bulk-email/bulk-reminder lo cuentan como `failed` (métrica engañosa, envío sin registro → futuro tipo/dedup incorrecto) | `lib/queries.ts:139-159`; send-email `:132→:140-146`; cron `:183-190`; bulk-email `:149-153`; bulk-reminder `:163-167` | 11, 12 |
| A1-11 | 🟡 | **Firma mentirosa `getRSVPsByEvent(eventId)` + fallbacks `\|\| eventIdOrSlug` silenciosos.** El parámetro se llama `eventId` pero la columna guarda slug; 3 call sites (rsvp GET, bulk-email, stats) usan `event?.slug \|\| eventIdOrSlug`: con evento inexistente la query corre con un valor que no es slug → 0 resultados sin error (omisión silenciosa). `getEventStats` en queries.ts no tiene callers (muerto → A5) | `lib/queries.ts:62,50,167`; `app/api/rsvp/route.ts:217,228`; `app/api/admin/send-bulk-email/route.ts:35,79`; `app/api/stats/route.ts:33,43` | 32 |
| A1-12 | 🟡 | **`updateEventSlug` sin transacción → carrera con el cron = omisión permanente.** Evento y RSVPs se migran en dos updates separados; si el cron corre en medio, ve 0 confirmados y marca `reminderSentAt` sin enviar nada (sin reintento por A1-06). Probabilidad baja, consecuencia silenciosa | `lib/queries.ts:384-392` + `app/api/cron/send-reminders/route.ts:97,103-107` | 33 |
| A1-13 | 🟡 | **Los emails usan `event.title`, nunca `displayTitle`.** La página pública respeta `displayTitle` como título de cara al invitado; los 5 puntos de envío y todos los subjects usan el título interno → invitación y email muestran títulos distintos cuando el host configuró `displayTitle` | grep displayTitle → 0 hits en emails; `lib/email-template.ts:141,144`; subjects cron `:161`, rsvp `:122`, send-email `:104`, bulk-email `:36`, bulk-reminder `:143`; página `app/[slug]/page.tsx:148-157` | 24 |
| A1-14 | 🟡 | **Duplicación divergente entre los 5 puntos de envío.** Bloque `EventData` copiado 5× en dos familias de defaults (hardcoded vs `eventConfig.theme`, `contact?.` vs `contact.`); lógica de tipo `isReminder` triplicada (body vs DB vs frontend); delays 100ms (bulk-email — hasta 10 req/s, sobre el rate limit documentado de Resend de 2 req/s → 429s a mitad de lista) vs 5000ms (cron/bulk-reminder); `maxDuration` presente en 2 de 4 rutas que envían en loop. Candidato a helper compartido en el plan correctivo | Familias: cron `:116-137`, bulk-reminder `:77-96`, rsvp `:82-101` vs send-email `:52-73`, bulk-email `:40-61`; delays `send-bulk-email:147` vs cron `:182`/bulk-reminder `:159-161`; maxDuration cron `:17`, bulk-reminder `:18`, ausente en los otros dos | 13, 29, 30, 31 |
| A1-15 | 🟢 | **Columna `rsvps.cancelToken` muerta y engañosa:** se escribe con un token derivado de un UUID aleatorio (no el id real del row) y NADIE la lee — todos los flujos recomputan el token determinístico. Dato basura que sugiere una validación que no existe | `lib/queries.ts:41,52`; grep sin lectores (solo legacy `lib/firestore.ts:65,95,100`); `lib/schema.ts:103` | 22 |
| A1-16 | 🟢 | **Rama `x-vercel-cron-secret` muerta con comentario engañoso** ("Vercel's auto cron"): Vercel manda `Authorization: Bearer` (docs + logs de producción 200 verificados); el header custom no lo manda nadie | `app/api/cron/send-reminders/route.ts:22-29`; docs Vercel cron-jobs; runtime logs 200 (00:01/12:01 UTC) | 8 |
| A1-17 | 🟢 | **Textos menores del template:** footer "porque confirmaste tu asistencia" también en la re-invitación a quien CANCELÓ; heurístico de género `name.endsWith('a')` falla con nombres como "Joshua" | `lib/email-template.ts:330-332,76` | 27 |
| A1-18 | 🟢 | **plusOneName:** casts `(rsvp as any).plusOneName` innecesarios en 3 puntos (el tipo `RSVP` ya lo incluye — tipos mentirosos); segunda query `getRSVPById` en send-email solo para este campo; rsvp route lo manda al email sin trim vs DB trimmed | cron `:150`, bulk-email `:112`, bulk-reminder `:132`, `lib/schema.ts:90`; send-email `:38,92-96`; rsvp `:72` vs `:111` | 25 |
| A1-19 | 🟢 | **Limpieza de `=` duplicada:** send-email limpia la cancelUrl que el template vuelve a limpiar para todos | `app/api/admin/send-email/route.ts:88-89` + `lib/email-template.ts:68` | 23 |

### Hallazgos fuera de scope

> Cosas detectadas durante A1 que pertenecen a otra auditoría. NO profundizar ni duplicar — una línea + referencia cruzada. En particular, cualquier observación sobre validación de sesión, `CRON_SECRET` o permisos va aquí → **Fase S**.

| ID | Auditoría dueña | Descripción breve | Evidencia |
|----|-----------------|-------------------|-----------|
| A1-OOS-01 | Fase S | La validación del cron solo aplica si `CRON_SECRET` está seteado (`if (cronSecret)`) — con la env ausente el endpoint queda sin auth; además acepta dos headers distintos. Solo anotado, sin profundizar | `app/api/cron/send-reminders/route.ts:27-37` |
| A1-OOS-02 | A5 | `lib/firestore.ts` legacy (Firebase) con funciones paralelas a queries.ts (`getRSVPsByEvent` :109, `getEventStats` :159, manejo de `cancelToken` :65-100) aparentemente sin uso; `queries.getEventStats` (`lib/queries.ts:167`) sin callers | greps de call sites en ítems 22 y 32 |
| A1-OOS-03 | A4 | El confirm del botón de bulk-reminder promete "60 segundos entre cada uno"; el delay real del endpoint es 5s | `app/admin/components/ReminderStatusSection.tsx:171` vs `app/api/admin/send-bulk-reminder/route.ts:160` |
| A1-OOS-04 | A2 | POST /api/rsvp sin gate de `rsvpClosed` (el flujo RSVP completo, capacidad y estados es de A2; el efecto email quedó registrado aquí como A1-09) | `app/api/rsvp/route.ts:53-58` |
| A1-OOS-05 | A4 | GET /api/rsvp y /api/stats caen a `eventConfig.event.id` estático cuando no llega `eventId` — comparte raíz con A1-11 | `app/api/rsvp/route.ts:209`, `app/api/stats/route.ts:27` |

## 7. Cierre

1. Verificar que **todos** los ítems 1-33 tienen resultado + evidencia (o ⏭️ NOT RUN + razón) y que la tabla de call-graph (§4) está completa — si no, la auditoría NO se puede marcar ✅.
2. Actualizar este header: estado ✅, y la fila **A1** de `docs/audits/00_INDEX.md`: estado ✅ + conteo de hallazgos 🔴/🟡/🟢.
3. Commit + push: `audit: A1 emails-recordatorios — X🔴 Y🟡 Z🟢` (solo este MD y el INDEX — recordar: read-only sobre el código).
4. Los hallazgos NO se corrigen en esta sesión — van al plan correctivo post-consolidado (`99_CONSOLIDADO.md`).
