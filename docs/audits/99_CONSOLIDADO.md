# 📊 Reporte Consolidado de Auditoría — Party Time!

> **Estado:** ✅ **AUDITORÍA COMPLETA** — A1–A8 + Fase S consolidadas. El proyecto queda "auditado al 100%" (condición del INDEX cumplida: fila Fase S ✅).
> **Regla de cierre:** cumplida — la fila "Fase S" del INDEX está ✅ (`09_fase_s.md`, 5🔴 14🟡 8🟢).
> **Consolidado:** 2026-07-09 · fuente: secciones "Hallazgos" de `01`–`08_*.md` (SHA `9d9c7f2`/`e73ca03`/`61f74e5`) + `09_fase_s.md` (SHA `55a15d4`).
> **Totales globales (post-dedup):** A1–A8 = 8🔴 56🟡 42🟢 · Fase S = 5🔴 14🟡 8🟢 (post-review Codex: FS-02 degradado a 🟢, FS-27 agregado 🟡) · **GRAN TOTAL = 13🔴 70🟡 50🟢**. PRE-1/PRE-2 verificados y cerrados en Fase S.

---

## Hallazgos consolidados

**Reglas de dedup aplicadas:** hallazgos con el MISMO root cause registrados por varias auditorías se fusionan en UNA fila (celda ID lista todos los IDs; severidad = la más alta del grupo). Hallazgos relacionados pero con root cause distinto quedan separados y cruzados en la descripción. Todo ID de las 8 auditorías aparece exactamente una vez (128 IDs originales → 106 filas). Las entradas "fuera de scope" (`Ax-OOS-*`, `FS-ref-*`) no tienen fila propia: son referencias cruzadas ya absorbidas por el hallazgo dueño o diferidas a Fase S.

> ⚠️ Nota de conteo: el MD de A6 auto-reporta `1🔴 9🟡 6🟢`, pero su tabla contiene **1🔴 10🟡 5🟢** (A6-09 es 🟡). Este consolidado usa las severidades reales de las tablas.

### 🔴 Críticos

| ID | Auditoría | Severidad | Descripción | Evidencia | Estado |
|----|-----------|-----------|-------------|-----------|--------|
| A1-01 | A1 | 🔴 | `clearSentStatus` re-arma el reminder en cada guardado del config: compara `reminderScheduledAt` contra `reminderSentAt` (timestamps de naturaleza distinta, casi siempre ≠) → tras un reminder enviado, CUALQUIER guardado del form pone `reminderSentAt=NULL` y el cron **re-envía a todos los confirmados** (duplicado masivo). | `app/admin/page.tsx:830-831`; `app/api/admin/event-settings/update/route.ts:134-136`; `lib/queries.ts:471-478` | ✅ corregido en PR #7 (3fc972f) |
| A1-02 + A6-06 | A1, A6 | 🔴 | Root cause común: la query del cron selecciona por **día UTC sin cota `<= now`** (`gte startOfDay`/`lte endOfDay`). Reminders agendados 00:00–17:59 hora MX salen a las 18:00 MX del día ANTERIOR (hasta 24h antes de lo elegido) y la hora se ignora; la UI promete envío "en la fecha programada". Agravante directo: A1-03. | `lib/queries.ts:457,462-478`; `app/admin/page.tsx:826-827,2139-2156`; `vercel.json:2-7` (corridas 00:00/12:00 UTC) | ✅ corregido en PR #7 (3fc972f) |
| A2-H01 + A1-09 | A2, A1 | 🔴 | Root cause común: `POST /api/rsvp` **no valida `rsvpClosed`** (solo `isActive`; único uso de `rsvpClosed` en el ciclo es el filtro de reminders) — la única barrera es que la UI oculta el botón. Invitado con la pestaña/modal abiertos desde antes del cierre (o POST directo) crea el RSVP **y dispara el email de confirmación** en un evento cerrado. | `app/api/rsvp/route.ts:52-58,67-137`; grep `rsvpClosed` → único hit `lib/queries.ts:482`; `app/[slug]/page.tsx:247-252` | ✅ corregido en PR #6 (4b72bdb) |
| A2-H02 | A2 | 🔴 | La capacidad (`capacityEnabled`/`capacityLimit`) **no se aplica en ningún punto del ciclo** (0 hits de `capacity` en `app/api/rsvp` y `lib/queries.ts`): solo se muestra como texto en la página pública. Con cupo lleno se siguen aceptando RSVPs y +1s (que ni entran a un conteo) → overbooking. | `lib/schema.ts:29-30`; `app/[slug]/page.tsx:227-233`; `app/api/rsvp/route.ts:67-74`; `app/api/rsvp/update/route.ts:46-48` | ✅ corregido en PR #9 (6b3991d) + PR #10 (d1b2c83) — trigger `rsvps_capacity_check` aplicado en prod 2026-07-10 |
| A2-H03 | A2 | 🔴 | Un invitado **cancelado no puede re-inscribirse** desde la página pública: el dedup no filtra `status` → 409 con mensaje **falso** "Ya confirmaste tu asistencia anteriormente". Sin link de cancelación (nunca emitido si `emailConfirmationEnabled=false`, o perdido) el flujo queda bloqueado sin admin. | `lib/queries.ts:28-38`; `app/api/rsvp/route.ts:178-183`; `app/components/RSVPModal.tsx:73-74,269-273` | ✅ corregido en PR #8 (5b4098c) |
| A3-01 | A3 | 🔴 | Guardar settings **REABRE silenciosamente un RSVP cerrado**: `GET /api/event-settings` omite `rsvpClosed`/`rsvpClosedMessage` → el form los carga siempre `false` y cualquier guardado posterior persiste `rsvpClosed=false` y resetea el mensaje. Riesgo activo HOY: `40vueltas` en prod con `rsvpClosed=true`. | `app/api/event-settings/route.ts:52-94`; `app/admin/page.tsx:317-318`; `app/api/admin/event-settings/update/route.ts:116-117` | ✅ corregido en PR #4 (7390bca) |
| A3-02 + A6-09 | A3, A6 | 🔴 | Root cause común: `deleteEvent(hard)` **borra solo la fila de `events`** y no hay FK/cascade → los RSVPs quedan huérfanos keyed al slug liberado; un slug reciclado (evento anual re-creado) **hereda todos los RSVPs del borrado** — entran a bulk emails/cron del evento nuevo (email a quien no corresponde) y el dedup 409ea a invitados nuevos. Mitiga hoy: sin UI de delete (API-only, ver A3-10). | `lib/queries.ts:326-338`; `lib/schema.ts:83` (sin FK); `app/api/events/[slug]/route.ts:276-301`; lecturas por slug `lib/queries.ts:62-71,503-514` | ✅ corregido en PR #11 (823b086) — FK `rsvps_event_id_events_slug_fk` (ON UPDATE CASCADE / ON DELETE RESTRICT) aplicado en prod 2026-07-10 + hard-delete atómico vía db.batch |
| A4-01 | A4 | 🔴 | `isEventPast` marca como pasados **eventos futuros a >2 meses** (fecha textual sin año → asume año pasado) y el propio día del evento en TZ MX (parse UTC) → **bloquea todos los botones de envío manual** del admin para eventos legítimos. | `app/admin/page.tsx:423-459` (heurística); guards `:467-470,527-530,1397-1403,1433-1437,1514-1517` | ✅ corregido en PR #5 (0760b50) |

### 🟡 Importantes

| ID | Auditoría | Severidad | Descripción | Evidencia | Estado |
|----|-----------|-----------|-------------|-----------|--------|
| A1-03 | A1 | 🟡 | Round-trip del datetime **desplaza la agenda +6h por ciclo cargar→guardar**: al cargar se mete la hora UTC cruda en el `datetime-local` (interpretada como local) y al guardar se re-convierte a UTC — puede cruzar el reminder a otro día UTC. Agravante de A1-01/A1-02. | `app/admin/page.tsx:311,826-827`; `app/api/event-settings/route.ts:87` | ✅ corregido en PR #7 (3fc972f) |
| A1-04 | A1 | 🟡 | **Regresión del fix `5894370`**: el filtro "no reminders a eventos con fecha pasada" fue eliminado por el rewrite de `0e7ad21`; hoy el post-filtro solo excluye `rsvpClosed` → evento con `date` pasada y `reminderScheduledAt`=hoy recibe reminder. | `lib/queries.ts:481-484` vs `git show 5894370`/`0e7ad21 -- lib/queries.ts` | ⬜ pendiente de plan correctivo |
| A1-05 + A4-04 | A1, A4 | 🟡 | Root cause común: `send-email` individual opera **100% con datos del body** (destinatario, tipo `isReminder`/`isCancelled`, token) en vez de releer el RSVP que ya consulta → tabla stale = email a la dirección VIEJA, tipo equivocado y cancel link que no valida contra DB; el 500 post-send invita a reintentos duplicados. bulk-email deriva lo mismo de DB (fuentes divergentes). | `app/api/admin/send-email/route.ts:26,81-82,85,117` vs `:38,92-96`; `app/admin/page.tsx:497-504` | ✅ corregido en PR #2 (3274b56) |
| A1-06 | A1 | 🟡 | Reminder perdido = **silencio permanente**: si las dos corridas de un día fallan, `gte(startOfDay)` excluye el `scheduledAt` pasado para siempre; sin reintento ni alerta (recuperación manual existe pero exige que el admin la descubra). | `lib/queries.ts:475-476`; `app/admin/components/ReminderStatusSection.tsx:157-184` | ⬜ pendiente de plan correctivo |
| A1-07 | A1 | 🟡 | Lock-first del cron: `markReminderSent` ANTES de enviar → crash/timeout post-lock deja invitados sin reminder **sin registro ni reanudación**; con 0 RSVPs también marca sent; `maxDuration=300` + delay fijo 5s → **~56-59 emails máx por corrida** compartidos entre todos los eventos, sin paginación. | `app/api/cron/send-reminders/route.ts:17,103-113,140-191,182` | ⬜ pendiente de plan correctivo |
| A1-08 | A1 | 🟡 | Fallback silencioso a `event-config.json`: en send-email (catch que continúa) y bulk-email (evento null), el email **SALE con título/fecha/lugar del evento estático de 2024** (datos de OTRO evento). Cron y bulk-reminder sí abortan. | `lib/email-template.ts:52-62`; `send-email/route.ts:52,76-78`; `send-bulk-email/route.ts:33-40`; `event-config.json:2-16` | ✅ corregido en PR #2 (3274b56) |
| A1-10 + A6-12 | A1, A6 | 🟡 | Root cause común: `recordEmailSent` es **read-modify-write sin atomicidad** (SELECT + UPDATE con array reconstruido) → envíos concurrentes pierden entradas de `emailHistory` (last-write-wins) y `reminder-status` decide reenvíos con ese historial → recordatorio manual duplicado. Además, fallo post-send: send-email devuelve **500 con el email ya enviado** (reintento → duplicado); los bulk lo cuentan como `failed` (métrica engañosa). | `lib/queries.ts:139-160`; `send-email:132→140-146`; `reminder-status/route.ts:77-114` | ⬜ pendiente de plan correctivo |
| A1-11 + A6-04 | A1, A6 | 🟡 | Root cause común (**confirma PRE-3**): "eventId" significa slug o UUID según la tabla y las firmas no lo dicen — `getRSVPsByEvent(eventId)`/`getEventStats(eventId)` reciben slug; `user-queries` usa UUID. 3 call sites con fallback `event?.slug \|\| eventIdOrSlug`: con evento inexistente la query corre con un no-slug → **0 resultados silenciosos** (omisión sin error). | `lib/queries.ts:50,62,167`; `app/api/rsvp/route.ts:217,228`; `send-bulk-email:35,79`; `app/api/stats/route.ts:33,43`; `lib/user-queries.ts:175-243` | ⬜ pendiente de plan correctivo |
| A1-12 + A3-03 + A6-07 + A3-17 | A1, A3, A6 | 🟡 | Root cause común: `updateEventSlug` migra evento y RSVPs en **dos UPDATEs sin transacción** (driver neon-http sin tx): fallo del segundo → RSVPs huérfanos permanentes e invisibles en panel/reminders; carrera con el cron → evento marcado sent sin enviar nada (omisión permanente, sin reintento por A1-06); y el conteo `updatedRsvps` es un SELECT post-hoc que absorbería huérfanos preexistentes. | `lib/queries.ts:384-402`; `lib/db.ts:2`; cron `:97,103-107`; `app/admin/page.tsx:751-752` | ⬜ pendiente de plan correctivo |
| A1-13 | A1 | 🟡 | Los emails usan `event.title`, **nunca `displayTitle`** (que la página pública sí respeta) → invitación y email muestran títulos distintos cuando el host configuró `displayTitle`. | grep `displayTitle` → 0 hits en emails; `lib/email-template.ts:141,144`; `app/[slug]/page.tsx:148-157` | ✅ corregido en PR #2 (3274b56) |
| A1-14 + A5-16 | A1, A5 | 🟡 | Root cause común: **bloque `EventData` copiado 5× en dos familias de defaults divergentes** (hardcoded vs `eventConfig.theme`, `contact?.` vs `contact.`); lógica de tipo `isReminder` triplicada (body vs DB vs frontend); delays 100ms (bulk-email → hasta 10 req/s > rate limit Resend 2 req/s) vs 5000ms; `maxDuration` en solo 2 de 4 rutas → mismo evento puede enviar emails distintos según el endpoint. Candidato a helper compartido. | cron `:116-137`, bulk-reminder `:77-96`, rsvp `:82-101` vs send-email `:52-73`, bulk-email `:40-61`; `send-bulk-email:147` | ⬜ pendiente de plan correctivo |
| A1-15 + A2-H10 + A6-10 | A1, A2, A6 | 🟡 | Root cause común: la columna `rsvps.cancelToken` almacena un token generado con `crypto.randomUUID()` (**id fantasma, no el id real del row**) y NADIE la lee — todos los flujos recomputan el token determinístico → dato muerto/engañoso; un dev futuro que "valide contra la columna" rompería todos los links. | `lib/queries.ts:41,52` vs `lib/schema.ts:80,103`; grep `cancelToken`: 0 lectores (solo legacy `lib/firestore.ts`) | 🔶 parcial en PR #8 (5b4098c) — escritura muerta removida; falta dropear la columna |
| A2-H04 | A2 | 🟡 | **Update y re-confirmación ciegos al evento**: `/api/rsvp/update` nunca carga el evento — no consulta `rsvpClosed`, `isActive` ni capacidad → se puede editar y re-confirmar asistencia en un evento cerrado o desactivado. Relacionado con A2-H01. | `app/api/rsvp/update/route.ts:2` (imports), `:46-48`; `lib/queries.ts:90-103` | ✅ corregido en PR #12 (764e478) — guard `isSeatAddingChange` (cierre/inactivo bloquean solo cambios que añaden asientos); capacidad ya cubierta por el trigger de PR #9 |
| A2-H05 + A2-H06 + A6-11 | A2, A6 | 🟡 | Root cause común: **dedup de RSVP frágil** — match exacto case-sensitive sin trim/lowercase (`Foo@x.com` vs `foo@x.com` duplica y recibe 2 confirmaciones) + check-then-insert sin transacción ni UNIQUE (tabla `rsvps` sin constraints ni índices) → requests concurrentes duplican. | `lib/queries.ts:28-54`; `drizzle/meta/0000_snapshot.json` (`rsvps` uniques/indexes `{}`); `lib/db.ts:2` (neon-http) | ✅ corregido en PR #8 (5b4098c) |
| A2-H07 | A2 | 🟡 | **Pérdida silenciosa de `plusOneName` al editar vía /cancel**: `GET /api/rsvp/get` omite el campo (el tipo del consumidor sí lo declara) → la página inicializa `''` → el update lo normaliza a `null` → editar cualquier dato borra el nombre del acompañante cuando el campo no se renderiza. Materializada por A7-H04. | `app/api/rsvp/get/route.ts:41-49`; `app/cancel/[rsvpId]/page.tsx:15,83,101-103,142,375`; `app/api/rsvp/update/route.ts:42` | ⬜ pendiente de plan correctivo |
| A2-H08 | A2 | 🟡 | El camino admin escribe **sin la normalización de los caminos públicos**: `update-rsvp` pasa el `editForm` verbatim — persiste `plusOneName: ''` (donde público escribe `null`), sin trim y sin validación de formato de email → datos divergentes según quién editó. | `app/api/admin/update-rsvp/route.ts:61`; `app/admin/page.tsx:631,651,669-672,2480` | ⬜ pendiente de plan correctivo |
| A2-H09 | A2 | 🟡 | `requirePlusOneName` **solo se valida en cliente**: ni create ni update consultan el flag del evento → `plusOne=true` sin nombre entra en eventos que lo exigen (POST directo, o /cancel con fetch del evento fallido). | grep `requirePlusOneName app/api` → solo event-settings; `RSVPModal.tsx:47-52`; `cancel:375` | ⬜ pendiente de plan correctivo |
| A2-H11 | A2 | 🟡 | Cambiar el email desde /cancel **invalida el único link del invitado sin emitir uno nuevo** (el token se recalcula contra el email vigente y el update no envía nada) → 403 en toda acción posterior hasta que un admin reenvíe la confirmación. | `app/api/rsvp/update/route.ts:1-2,26-34`; `lib/queries.ts:195-198`; remedio `send-email/route.ts:85-86` | ⬜ pendiente de plan correctivo |
| A2-H12 | A2 | 🟡 | `/api/rsvp/update` **no valida formato de email** (solo presencia; create sí usa regex) → un email malformado persiste, invalida el link (token depende del email) y rompe envíos futuros. | `app/api/rsvp/update/route.ts:9` vs `app/api/rsvp/route.ts:29-35` | ⬜ pendiente de plan correctivo |
| A2-H13 | A2 | 🟡 | `emailHistory` **no registra la dirección destino** (`{sentAt, type}`): tras un cambio de email el host ve "confirmation ✓" sobre un RSVP cuyo email actual nunca recibió nada — el filtro "enviados" del admin y la decisión de reenviar se basan en dato engañoso. | `lib/queries.ts:146-158`; `lib/schema.ts:97-100`; `app/admin/page.tsx:359,388,1176` | ⬜ pendiente de plan correctivo |
| A2-H14 | A2 | 🟡 | **El ciclo no tiene noción de fecha**: `events.date` es texto libre y ningún endpoint la compara → eventos pasados aceptan crear/editar/re-confirmar indefinidamente; el único cierre es manual (`rsvpClosed`/`isActive`), y `rsvpClosed` ni siquiera cierra la API (A2-H01). | `lib/schema.ts:18,60,63`; grep `new Date\|Date.now` en `app/api/rsvp/` → solo demo | ⬜ pendiente de plan correctivo |
| A2-H15 | A2 | 🟡 | `POST /api/rsvp` sin `eventSlug` **cae al evento hardcodeado de `event-config.json`** (`rooftop-party-andras-oct2024`) sin verificar existencia ni `isActive` → RSVPs huérfanos vía API directa (post-rename ese id ya ni es slug vigente). | `app/api/rsvp/route.ts:37-38,46,67-74`; `event-config.json:3` | ✅ corregido en PR #6 (4b72bdb) |
| A3-04 | A3 | 🟡 | `renameOgImages` es un **no-op silencioso en Vercel** (`public/` no existe en el FS del lambda → `existsSync` siempre false): tras un cambio de slug, el CDN sirve la OG vieja, el slug nuevo pierde su imagen custom y el admin recibe "éxito". | `app/api/events/[slug]/route.ts:15-44`; `app/api/og-image/[slug]/route.ts:117-118`; `app/admin/page.tsx:749-760` | ⬜ pendiente de plan correctivo |
| A3-05 | A3 | 🟡 | **Dos vocabularios parciales de update sobre `events`** (PUT `/api/events/[slug]` vs `event-settings/update`): `hostPhone` in-actualizable por cualquier ruta; `hostName/hostEmail` in-editables desde el panel → eventos creados del panel quedan sin contacto para siempre y los emails caen al `hostEmail` legacy `info@timekast.mx`. Deuda que ya produjo A3-01 y A3-06. | `app/api/events/[slug]/route.ts:211-227` vs `app/api/admin/event-settings/update/route.ts:92-137`; create sin contact `page.tsx:2348-2356` | ⬜ pendiente de plan correctivo |
| A3-06 | A3 | 🟡 | El full-save de settings **pisa `theme.backgroundColor`/`textColor` con hardcodes** (`#1a0033`/`#ffffff`), destruyendo customs que POST/PUT sí permiten guardar — y `backgroundColor` SÍ se consume en los 5 senders de email. Relacionado con A3-01 (mismo full-save). | `app/api/admin/event-settings/update/route.ts:106-112`; consumo en rsvp `:96`, cron `:132`, bulk-reminder `:91`, send-email `:68`, bulk-email `:55` | ✅ corregido en PR #4 (7390bca) |
| A3-07 | A3 | 🟡 | La cadena de fallbacks del home **aterriza en el evento legacy 2024**: `home_event_id` inválido → redirect silencioso al legacy; last-resort redirect sin verificar existencia; metadata/OG fallback con datos 2024 hardcodeados. Raíz del writer sin validación: A3-OOS-06 (→ A4). | `app/page.tsx:21-46,85-112,137-153`; `app/opengraph-image.tsx:12-18` | ⬜ pendiente de plan correctivo |
| A3-08 | A3 | 🟡 | `GET /api/event-settings` responde **`success:true` con datos del evento 2024 para eventos inexistentes** (`source:'config'`) y su único consumidor no revisa `source`; sin `?eventId` el default es el legacy — debería ser 404. | `app/api/event-settings/route.ts:32,98-140`; `app/admin/page.tsx:284-318` | ⬜ pendiente de plan correctivo |
| A3-09 | A3 | 🟡 | **Invalidación de caché OG incoherente**: `?v=5` hardcodeado en el layout de evento (cambiarlo exige editar código + deploy) y el home ni lo usa → el host cambia la imagen OG desde el panel y WhatsApp sigue mostrando la anterior. | `app/[slug]/layout.tsx:31-33`; `app/page.tsx:52-53`; editable en runtime `event-settings/update:105` | ⬜ pendiente de plan correctivo |
| A3-10 | A3 | 🟡 | `isActive` **conflacta "soft-deleted", "inactivo" y "cerrado al público"**, con lock page pública que muestra el título del evento "borrado" y SIN UI para desactivar/reactivar/borrar (ciclo API-only, lo que hoy mitiga A3-02). | `lib/queries.ts:332-335`; `app/[slug]/page.tsx:100-112`; grep sin UI de delete en `app/admin/page.tsx` | ⬜ pendiente de plan correctivo |
| A3-11 | A3 | 🟡 | **Metadatos OG con guion colgante en producción**: `"${title} - ${subtitle}"` y `"${date} ${time} - ${location}"` sin guardas para vacíos — `<title>40 Vueltas del Mundo - </title>` servido hoy. | `app/[slug]/layout.tsx:28-29`; `app/page.tsx:49-50`; runtime `party.timekast.mx/40vueltas` | ⬜ pendiente de plan correctivo |
| A4-02 | A4 | 🟡 | `message` **solo se renderiza en el tab dashboard**: éxitos y errores de guardar config, crear evento, cambiar slug y auto-save de uploads son invisibles para el admin. | único render `page.tsx:1409`; setters invisibles `:216-223,750-777,856-862,889-909,954-975,2361-2368` | ⬜ pendiente de plan correctivo |
| A4-03 | A4 | 🟡 | El selector **restaura el evento de localStorage sin validar existencia/acceso** (la validación solo corre si `!selectedEventId`) → slug borrado/renombrado/sin acceso = "Error al cargar datos", recuperable solo re-seleccionando a mano. | `page.tsx:231-236` vs `:254-277`; `app/api/rsvp/route.ts:220-225` | ⬜ pendiente de plan correctivo |
| A4-05 | A4 | 🟡 | `update-rsvp` **sin whitelist runtime**: la restricción de campos es solo de tipos (`Partial<Pick<...>>` + `.set(data)`) — en runtime cualquier columna de `rsvps` es asignable vía `body.updates`. | `app/api/admin/update-rsvp/route.ts:22-23,61`; `lib/queries.ts:90-98` | ✅ corregido en PR #2 (3274b56) |
| A4-06 | A4 | 🟡 | ReminderStatusSection **muestra datos del evento anterior** tras cambiar el selector (sin `key` ni reset de estado) y su confirm promete "60 segundos" con delay real de 5s → el admin ve fallidos inexplicables (el scoping server-side evita el email equivocado). | `page.tsx:2205`; `ReminderStatusSection.tsx:31-38,171` vs `send-bulk-reminder/route.ts:158-161` | ⬜ pendiente de plan correctivo |
| A4-07 + A5-06 | A4, A5 | 🟡 | Root cause común: `POST /api/admin/add-demo-data` **sin ningún caller pero desplegado en prod** — un POST insertaría 7 RSVPs demo hardcodeados (6 `example.com` + 1 email real de persona) contra el eventId legacy → filas huérfanas en producción. No envía emails. | `app/api/admin/add-demo-data/route.ts:7-57,76`; grep 0 consumidores; `saveRSVP` sin validar evento `lib/queries.ts:17-57` | ⬜ pendiente de plan correctivo |
| A4-08 | A4 | 🟡 | **Cargas silenciosas y cero manejo de sesión expirada** en el panel: ningún catch distingue 401 (checkAuth solo corre al montar) → errores genéricos o silencio en vez de redirect a `/login`. | `page.tsx:105-124,182-184,197-199,285,322-324` | ⬜ pendiente de plan correctivo |
| A4-09 | A4 | 🟡 | **Imposible cambiar el rol de una asignación desde la UI**: el dropdown excluye eventos ya asignados, así que el upsert server-side diseñado para esto es inalcanzable → quitar acceso y re-asignar en dos pasos (con ventana sin acceso). Relacionado con A4-11 (código muerto asociado). | `UserManagement.tsx:469-474`; `lib/user-queries.ts:183-199` | ⬜ pendiente de plan correctivo |
| A5-13 + A6-02 | A5, A6 | 🟡 | Root cause común: el tipo `Event` (`types/event.ts`) **miente — una sola interface para 3 payloads distintos** (row Drizzle plano, formattedEvent nested, settings) con shapes inconsistentes por verbo/endpoint; le falta `ogImageUrl`; `emailConfig`/`contact` no los construye quien el tipo dice; nullabilidad divergente del schema. | `types/event.ts` vs `lib/schema.ts:17-37`; `app/api/events/[slug]/route.ts:76-99,234-236`; `app/admin/page.tsx:31,175` | ⬜ pendiente de plan correctivo |
| A6-01 + A8-07 | A6, A8 | 🟡 | Root cause común: **migraciones drizzle desincronizadas del estado real** — journal con 1 sola entrada, `0001_add_rsvp_closed.sql` fuera del journal, 5 columnas de prod sin SQL ni snapshot (aplicadas con `db:push`) → un `drizzle-kit generate` futuro regenera ALTERs ya aplicados y una DB nueva desde migraciones queda incompleta. Elegir un solo mecanismo (push XOR migraciones). | `drizzle/meta/_journal.json`; `drizzle/meta/0000_snapshot.json`; `package.json:10-12`; repro `drizzle-kit generate` (A6 ítem 1b) | ⬜ pendiente de plan correctivo |
| A6-05 | A6 | 🟡 | **JSONB sin validación runtime**: el PUT escribe `theme` verbatim sin normalizar (vs la versión normalizada de event-settings/update) y 16 casts vestigiales (`theme as any`, `emailHistory as Array`, `(rsvp as any).plusOneName`) anulan los tipos que el schema ya declara. Raíz server de A7-H05; casts plusOneName también en A1-18. | `app/api/events/[slug]/route.ts:209,222`; `app/api/events/route.ts:126`; `lib/schema.ts:40-52,97-100` | ⬜ pendiente de plan correctivo |
| A6-14 | A6 | 🟡 | **Contrato real de `events.id`**: `text` con ids legacy no-UUID, fallback débil `Math.random` en la generación, y `getEventBySlug` acepta slug o id sin decirlo (≥8 endpoints dependen implícitamente; `getEventById` sin fallback simétrico) — el arquetipo que ya produjo orfandad de RSVPs. | `lib/schema.ts:4-9,13`; `lib/queries.ts:234-240,277-286`; `event-config.json:3` | ⬜ pendiente de plan correctivo |
| A7-H01 | A7 | 🟡 | **Todo fallo de carga del evento se presenta como "Evento no encontrado… ya no está disponible"** (500, 503 y errores de red igual), sin distinción ni reintento; los strings de error específicos se setean pero jamás se renderizan → el invitado concluye que el evento se canceló. | `app/[slug]/page.tsx:28-35,43-45` vs `:75-97`; códigos `app/api/events/[slug]/route.ts:108-125` | ⬜ pendiente de plan correctivo |
| A7-H02 | A7 | 🟡 | **Texto blanco sobre card blanca en /cancel**: "Cargando..." y "Tu asistencia ha sido cancelada exitosamente." invisibles (los `<p>` sin clase heredan `color:#fff` del body sobre `.card` blanca) — runtime prod confirmado. | `cancel:211,226`; `cancel.module.css:10-18`; `globals.css:6,26-29` | ⬜ pendiente de plan correctivo |
| A7-H03 | A7 | 🟡 | **El modal se cierra con click en overlay o ✕ durante el envío en curso** (el POST sigue en background) → el invitado queda confirmado sin saberlo (email "inesperado") o reintenta y recibe el 409 confuso. | `RSVPModal.tsx:55-81,105,119-125`; 409 `app/api/rsvp/route.ts:178-183` | ⬜ pendiente de plan correctivo |
| A7-H04 | A7 | 🟡 | **Fallo silencioso del fetch del evento en /cancel degrada el formulario sin aviso**: título genérico, theme default y `requirePlusOneName=false` → el campo del acompañante no se renderiza y el update borra `plusOneName` (materializa A2-H07/A2-H09; el server no compensa). | `cancel:87-103,218,254,259,375-391`; `app/api/rsvp/update/route.ts:42` | ⬜ pendiente de plan correctivo |
| A7-H05 | A7 | 🟡 | **Contraste no garantizado con themes dinámicos**: el `textShadow` inline del theme reemplaza el borde multi-capa (solo sobrevive stroke 2px), overlay derivado del propio `primaryColor`, `modalSubtitle` sin protección — sin validación/clamp de contraste al guardar (raíz server: PUT acepta theme verbatim → A6-05). | `app/[slug]/page.tsx:131-136,152-172`; `page.module.css:105-153`; `RSVPModal.tsx:116,139` | ⬜ pendiente de plan correctivo |
| A7-H06 | A7 | 🟡 | **Accesibilidad básica del modal ausente**: sin `role="dialog"`/`aria-modal`, sin foco inicial/focus trap/Escape, ✕ sin `aria-label`; labels de teléfono huérfanos (htmlFor sin id) en modal y /cancel. | `RSVPModal.tsx:100-125,206-221`; `cancel:342-355` | ⬜ pendiente de plan correctivo |
| A7-H07 | A7 | 🟡 | `maximumScale: 1` **bloquea el zoom del invitado** (WCAG 1.4.4; Chrome Android lo respeta); `themeColor` del navegador sale del JSON estático, no del theme del evento. | `app/layout.tsx:31-36`; `event-config.json:21` | ⬜ pendiente de plan correctivo |
| A8-01 | A8 | 🟡 | (**PRE-4 confirmado**) Build local/CI frío **FALLA sin `RESEND_API_KEY`**: `new Resend()` a nivel de módulo lanza "Missing API key" durante "Collecting page data"; ningún MD documenta el requisito (asimetría con `lib/db.ts`, que degrada con warn). No rompe deploys en Vercel. | `lib/resend.ts:3-7`; repro A8 ítem 2 (exit 1); contraste `lib/db.ts:8-16` | ✅ corregido en PR #1 (778f9ed) |
| A8-02 | A8 | 🟡 | **ESLint no configurado**: `npm run lint` = `next lint` sin ESLint instalado → prompt interactivo (inusable en CI); cero lint gate en un repo con auto-deploy a producción on push a main. | `package.json:9,33-41`; `grep -c '"eslint' package-lock.json` → 0 | ✅ corregido en PR #1 (778f9ed) |
| A8-03 | A8 | 🟡 | **Cero tests automatizados**: sin script `test`, sin framework, sin directorio de tests — en producción real con auto-deploy y sin CI. | `package.json:5-13`; greps exit 1 | ✅ corregido en PR #1 (778f9ed) |
| A8-04 | A8 | 🟡 | **Cron cada 12h fuera de spec del plan Hobby** ("once per day"; expresiones más frecuentes "will fail during deployment"): hoy corre 2×/día pero fuera de garantía documentada — un cambio de enforcement puede rechazar deploys o degradar el schedule sin aviso. Precisión Hobby ±59 min. | `vercel.json:2-7`; doc `cron-jobs/usage-and-pricing`; logs 00:01/12:01 UTC (A1 ítem 8) | ⬜ pendiente de plan correctivo |
| A8-05 | A8 | 🟡 | **Dos vars para la URL pública con fallbacks opuestos**: `NEXT_PUBLIC_APP_URL` (emails, fallback localhost) vs `NEXT_PUBLIC_BASE_URL` (metadata/OG, fallback dominio hardcodeado, NO documentada NI seteada en Vercel) → 4 sitios dependen de un dominio en código. | `app/page.tsx:11`; `app/layout.tsx:5`; `app/[slug]/layout.tsx:15`; `og-image/[slug]/route.ts:119` | ⬜ pendiente de plan correctivo |
| A8-06 | A8 | 🟡 | **Default `http://localhost:3000` en los cancelUrl que viajan dentro de emails reales** (6 sitios): si la var faltara/se renombrara, TODOS los emails de prod saldrían con links a localhost — modo de fallo silencioso (el build no avisa). Latente: var seteada hoy. | A8 ítem 6 (6 sitios); `vercel env ls` | ⬜ pendiente de plan correctivo |
| A8-08 | A8 | 🟡 | **`.env.example` incompleto y engañoso**: faltan `CRON_SECRET` y `BLOB_READ_WRITE_TOKEN` (requerida por upload-image, no documentada en NINGÚN lado); `RESEND_API_KEY` etiquetada "OPCIONAL" pese a romper el build (A8-01). | `.env.example:17,21`; greps exit 1; `vercel env ls` | ✅ corregido en PR #1 (778f9ed) |
| A8-09 | A8 | 🟡 | **Checklists de deploy omiten las credenciales admin**: README/SETUP listan 6 vars sin `ADMIN_USERNAME`/`ADMIN_PASSWORD` (login de un deploy fresco no funciona); `ADMIN_EMAIL` no existe en ninguna doc. | `README.md:102-107`; `SETUP_GUIDE.md:189-194`; `app/api/auth/login/route.ts:26-27`; `lib/auth-utils.ts:106` | ✅ corregido en PR #1 (778f9ed) |

### 🟢 Mejoras / limpieza

| ID | Auditoría | Severidad | Descripción | Evidencia | Estado |
|----|-----------|-----------|-------------|-----------|--------|
| A1-16 | A1 | 🟢 | Rama `x-vercel-cron-secret` **muerta con comentario engañoso** ("Vercel's auto cron"): Vercel manda `Authorization: Bearer` (docs + logs de prod 200 verificados). | `app/api/cron/send-reminders/route.ts:22-29` | ⬜ pendiente de plan correctivo |
| A1-17 | A1 | 🟢 | Textos menores del template: footer "porque confirmaste tu asistencia" también en la re-invitación a quien CANCELÓ; heurístico de género `name.endsWith('a')` falla con "Joshua"/"Nicolás". | `lib/email-template.ts:76,330-332` | ⬜ pendiente de plan correctivo |
| A1-18 | A1 | 🟢 | plusOneName: casts `(rsvp as any)` innecesarios en 3 puntos (el tipo ya lo incluye), segunda query `getRSVPById` en send-email solo para este campo, y envío sin trim vs DB trimmed. Cross-ref A6-05 (casts). | cron `:150`, bulk-email `:112`, bulk-reminder `:132`; `send-email:38,92-96`; rsvp `:72` vs `:111` | ⬜ pendiente de plan correctivo |
| A1-19 | A1 | 🟢 | Limpieza de `=` duplicada: send-email limpia la cancelUrl que el template vuelve a limpiar para todos los puntos. | `send-email/route.ts:88-89` + `lib/email-template.ts:68` | ⬜ pendiente de plan correctivo |
| A2-H16 | A2 | 🟢 | El banner post-update de /cancel **siempre dice "¡Asistencia reconfirmada!"** (condiciona sobre el estado final, siempre `confirmed`): la rama "Información actualizada correctamente" es inalcanzable; `wasReconfirmed` se calcula y no se usa. | `app/cancel/[rsvpId]/page.tsx:150,302-308` | ⬜ pendiente de plan correctivo |
| A3-12 | A3 | 🟢 | Evento inexistente responde **HTTP 200 (soft-404)** y el import de `notFound` quedó sin uso (también anotado en A7-H11). | `app/[slug]/page.tsx:4,75-97`; runtime `curl -I` → 200 | ⬜ pendiente de plan correctivo |
| A3-13 | A3 | 🟢 | Las 3 superficies OG **ignoran `event.theme`** y usan una paleta hardcodeada que ni coincide con `DEFAULT_THEME` → la tarjeta de share sale siempre rosa/cian genérico. | `og-image/[slug]/route.ts:96-99`; `og/[slug]/route.ts:58-84`; `app/opengraph-image.tsx:44-84` | ⬜ pendiente de plan correctivo |
| A3-14 | A3 | 🟢 | **Comentarios mentirosos sobre `displayTitle`** ("if empty, uses title") vs comportamiento real post-`392428f` ("empty means no title") — invita a reintroducir el bug que ese fix eliminó. | `lib/schema.ts:16`; `types/event.ts:13`; `app/[slug]/page.tsx:148-160` | ⬜ pendiente de plan correctivo |
| A3-15 | A3 | 🟢 | Creación de evento: carrera check-then-insert responde **500 en vez de 409** (el error del unique de Postgres no matchea 'Ya existe'); colisión slug-vs-id con mensaje engañoso. | `lib/queries.ts:210-214`; `app/api/events/route.ts:167-172` | ⬜ pendiente de plan correctivo |
| A3-16 + A6-15 | A3, A6 | 🟢 | Root cause común: **trabajo en app que corresponde a SQL** — `getAllEvents` trae toda la tabla y filtra `isActive` en JS; N+1 en `getUserEventAssignments`; `rsvpClosed` filtrado en JS post-query en el cron. Volumen actual bajo → deuda. | `lib/queries.ts:294-302,481-486`; `lib/user-queries.ts:152-168` | ⬜ pendiente de plan correctivo |
| A4-10 + A5-20 | A4, A5 | 🟢 | Root cause común: `LoginForm.tsx` (95 l) **muerto** — nunca se renderiza (login real inline en `app/login/page.tsx`); de usarse estaría roto (Basic auth vs validación por cookie); deja `/api/admin/validate` sin consumidor real e importa `event-config.json` sin usarlo. | `LoginForm.tsx:5,28-35,50`; `components/index.ts:7`; `validate/route.ts:11-22` | ⬜ pendiente de plan correctivo |
| A4-11 | A4 | 🟢 | `handleChangeRole` muerto + `GET/DELETE /api/admin/users/[id]` huérfanos + selects por `getElementById`. Relacionado con A4-09 (la UI que lo usaría no existe). | `UserManagement.tsx:138-164,485-489`; `users/[id]/route.ts:12-64,137-183` | ⬜ pendiente de plan correctivo |
| A4-12 + A6-13 | A4, A6 | 🟢 | Root cause común: `getEventStats` y `GET /api/stats` **sin consumidores**, con `totalConfirmed` mentiroso (= total incluyendo cancelados) y duplicado legacy en firestore.ts. También listado dentro de A5-21 (exports muertos). | `lib/queries.ts:167-182`; `app/api/stats/route.ts:45-49`; `lib/firestore.ts:159` | ⬜ pendiente de plan correctivo |
| A4-13 | A4 | 🟢 | upload-image: `imageType` ignorado por el server, naming compartido OG/fondo, y cada re-subida deja el **blob anterior huérfano** en Vercel Blob (sin `del()`); accept del input OG omite gif que el server acepta. | `upload-image/route.ts:17-19,52-54`; `page.tsx:941,1926` | ⬜ pendiente de plan correctivo |
| A4-14 | A4 | 🟢 | Exports: **ignoran los filtros activos** (exportan `rsvps`, no `filteredRsvps`, con botones dentro de la sección de filtros), filename sin sanear, casts `as any` innecesarios, nº de página solo en la última página del PDF. | `page.tsx:1019,1059-1164,1101-1109,1358-1374` | ⬜ pendiente de plan correctivo |
| A4-15 | A4 | 🟢 | **20 console.log en el panel**, incluyendo dump completo de datos de invitados a la consola del browser. | `page.tsx:130,141-144,158,425-459,840-853` | ⬜ pendiente de plan correctivo |
| A4-16 | A4 | 🟢 | **Monolito `app/admin/page.tsx` (2,599 l)**: ~1,900 líneas extraíbles en 7 módulos; duplicación ~95% entre las dos tablas de RSVPs. Propuesta de cortes en A4 ítem 29 (sin refactorizar aquí). | `page.tsx:1412-1490` vs `:1493-1571` | ⬜ pendiente de plan correctivo |
| A5-01 | A5 | 🟢 | `lib/firestore.ts` (526 l) **muerto**: 0 importadores post-migración a Neon (homónimas de queries.ts incluidas). | grep importadores = 0; A5 ítem 1 | ⬜ pendiente de plan correctivo |
| A5-02 | A5 | 🟢 | Dep **`firebase-admin` muerta en runtime** (solo la importan firestore.ts muerto y 2 scripts one-shot), instalándose en cada build; además las 4 vars legacy `GOOGLE_CLOUD_*`/`FIRESTORE_COLLECTION_NAME` siguen seteadas en Vercel (A8 OOS-3). | `package.json:20`; grep → 3 archivos | ⬜ pendiente de plan correctivo |
| A5-03 | A5 | 🟢 | `scripts/add-demo-data.ts` **roto**: escribe a Firestore inexistente (distinto del endpoint API — ver fila A4-07 + A5-06). | `scripts/add-demo-data.ts:23` | ⬜ pendiente de plan correctivo |
| A5-04 | A5 | 🟢 | `app/api/og/[slug]/route.ts` **duplicado muerto** de og-image (0 referencias; la versión viva es `og-image`). | grep `api/og` sin og-image → vacío | ⬜ pendiente de plan correctivo |
| A5-05 | A5 | 🟢 | Endpoint + script **`debug-home` muertos** (0 refs), desplegados en prod (la exposición sin auth se evalúa en Fase S — FS-A5-a). | `app/api/debug-home/route.ts:8-22`; grep = 0 refs | ✅ corregido en PR #2 (3274b56) |
| A5-07 | A5 | 🟢 | **~790 KB de artefactos de prueba versionados** (`test-og*.png`, `*-og-*.bin`) + `setup.ps1` obsoleto (verifica `background.jpg` inexistente). | `git ls-files` (4 matches); A5 ítem 7 | ⬜ pendiente de plan correctivo |
| A5-08 | A5 | 🟢 | 6 MDs instruyen `npx ts-node` pero el repo instala `tsx` → **comandos documentados fallan**; `COMMANDS.md:126` apunta al script Firestore roto. | tabla 4.3.1 de A5 | ⬜ pendiente de plan correctivo |
| A5-09 | A5 | 🟢 | 5 MDs root históricos a archivar (CHECKLIST, DEPLOYMENT_SUCCESS, PROJECT_SUMMARY, DOCUMENTATION_UPDATE_REPORT, PROPUESTA_GESTION); INDEX.md quedaría stale. | tabla 4.3.1 de A5 | ⬜ pendiente de plan correctivo |
| A5-10 | A5 | 🟢 | 4 scripts one-shot ya ejecutados (`migrate-firebase-to-neon`, `fix-rsvp-event-link`, `create-legacy-event`, `debug-home`) — candidatos a archivo/borrado. | git log A5 ítems 9-13 | ⬜ pendiente de plan correctivo |
| A5-11 | A5 | 🟢 | Dep `image-size` **sin un solo import** (confirmado por depcheck). | grep = 0; depcheck | ⬜ pendiente de plan correctivo |
| A5-12 | A5 | 🟢 | `@types/bcryptjs` en `dependencies` en lugar de `devDependencies`. | `package.json` | ⬜ pendiente de plan correctivo |
| A5-14 + A5-15 + A6-03 | A5, A6 | 🟢 | Root cause común: **`types/event-settings.ts` y `lib/config.ts` muertos post-migración** (shape Firestore legacy; único importador el firestore.ts muerto; docstring aún dice "lee de Firestore"). Eliminar junto con `lib/firestore.ts` (A5-01). | `types/event-settings.ts`; `lib/config.ts:7,53`; `lib/firestore.ts:290` | ⬜ pendiente de plan correctivo |
| A5-17 | A5 | 🟢 | Patrón auth cookie+`validateSession` **copiado a mano en 17 rutas** sin helper común — la divergencia de copias ya causó el hotfix `bcc7f1e`. Inventario = insumo directo de Fase S (FS-A5-b). | A5 ítem 23 | ⬜ pendiente de plan correctivo |
| A5-18 | A5 | 🟢 | Duplicación intra-repo: POST a `event-settings/update` 3× en el panel; theme default `#FF1493` **re-hardcodeado en ≥13 sitios** sin fuente única. | `page.tsx:843,893,958`; inventario A5 ítem 24 | ⬜ pendiente de plan correctivo |
| A5-19 | A5 | 🟢 | `lib/auth.ts` (42 l) **muerto**: validador HTTP Basic (envs `ADMIN_USERNAME`/`ADMIN_PASSWORD`) del modelo de auth anterior; 0 importadores. Insumo de limpieza de envs para Fase S (FS-A5-d). | `lib/auth.ts:4`; grep exit=1; knip | ⬜ pendiente de plan correctivo |
| A5-21 | A5 | 🟢 | **6 exports muertos en módulos vivos**: `cleanupExpiredSessions`, `getEventStats` (consolidado aparte como fila A4-12 + A6-13), `updateUserPassword`, `getUserAccessibleEventIds`, `AdminStats`, `CreateEventInput`/`UpdateEventInput`. | `lib/auth-utils.ts:148`; `lib/queries.ts:167`; `lib/user-queries.ts:107,270`; `types/event.ts:68,73` | ⬜ pendiente de plan correctivo |
| A6-08 | A6 | 🟢 | **Cero índices explícitos para los queries calientes** (`rsvps(event_id)`, `rsvps(event_id,status)`, etc.); un UNIQUE `(event_id, lower(email))` arreglaría también el dedup (fila A2-H05+A2-H06+A6-11). Volumen actual bajo → deuda. | grep INDEX `drizzle/` → 0; `lib/schema.ts:12-154` | ⬜ pendiente de plan correctivo |
| A6-16 | A6 | 🟢 | `assignEventToUser` **sin UNIQUE `(user_id,event_id)` ni validación de existencia del evento** (check-then-insert); `userHasEventAccess` toma el primer row sin ORDER BY → rol efectivo no determinista ante duplicados. Solo super_admin opera esto. | `lib/user-queries.ts:184-243`; `app/api/admin/users/[id]/events/route.ts:86-103` | ⬜ pendiente de plan correctivo |
| A7-H08 | A7 | 🟢 | **Español inconsistente**: dropdown de países en inglés, `confirm()` nativo, copy +1 divergente, "asistir a el evento", copy fijo de "fiesta" para todo tipo de evento. | `RSVPModal.tsx:161,209-221,235`; `cancel:174,218,227-229,370` | ⬜ pendiente de plan correctivo |
| A7-H09 | A7 | 🟢 | Detalles de animación/theme: **CTA principal invisible ~1–1.6 s** por `delay:1`, `exit` muerto en el campo +1 (sin AnimatePresence envolvente), glow dorado hardcoded bajo `accentColor` dinámico. | `page.tsx:237-243`; `RSVPModal.tsx:240-247` | ⬜ pendiente de plan correctivo |
| A7-H10 | A7 | 🟢 | **Touch targets secundarios <44px**: ✕ del modal 40×40, country selector 85×36, link "← Volver al inicio" 132×21 (los primarios sí cumplen). | runtime prod a 375px; `RSVPModal.module.css:39-40,131-143` | ⬜ pendiente de plan correctivo |
| A7-H11 | A7 | 🟢 | **Residuales del frontend público**: import `notFound` muerto (=A3-12), console.log de Home en cada request de prod (+ logs de `getAppSetting`), `theme.backgroundColor`/`textColor` letra muerta en el frontend (configurables sin efecto visible). | `app/[slug]/page.tsx:4`; `app/page.tsx:122-152`; `lib/queries.ts:415,425`; `types/event.ts:36-37` | ⬜ pendiente de plan correctivo |
| A8-10 | A8 | 🟢 | `FROM_EMAIL` con **default de sandbox Resend** (`onboarding@resend.dev`): si la var faltara, emails de prod desde sender sandbox → fallos de entrega silenciosos. Latente (var seteada). | `lib/resend.ts:9`; `vercel env ls` | ⬜ pendiente de plan correctivo |
| A8-11 | A8 | 🟢 | `start_url: '/admin'` en el manifest: la **PWA instalada abre el panel admin**, no la landing pública — confirmar que es decisión, no accidente. | `app/manifest.ts:8` | ⬜ pendiente de plan correctivo |
| A8-12 | A8 | 🟢 | Íconos `purpose: maskable` **con esquinas redondeadas propias** → el recorte del launcher Android puede mostrar artefactos (los maskable deben ser full-bleed con safe zone). | `app/manifest.ts:18,30`; `app/icon-192/route.tsx:16`; `app/icon-512/route.tsx:16` | ⬜ pendiente de plan correctivo |

**Conteo global post-dedup: 8🔴 · 56🟡 · 42🟢** (128 hallazgos originales en 106 filas)

## Hallazgos pre-registrados (previos a la ejecución de A1–A8)

Detectados durante el diseño del framework (adversarial review del plan, 2026-07-09). Framing factual; Fase S los verifica y busca patrones similares en el resto de endpoints.

| ID | Severidad | Descripción | Evidencia | Estado |
|----|-----------|-------------|-----------|--------|
| PRE-1 | 🔴 | `send-bulk-reminder` aceptaba requests **sin validar sesión** y disparaba envíos masivos; el loop además cargaba RSVPs por ID sin verificar que pertenecieran al evento (posible envío cross-evento) | Handler sin check: `app/api/admin/send-bulk-reminder/route.ts:17-41` (pre-fix) + loop de envío `:79-138` (pre-fix) | ✅ **VERIFICADO Y CERRADO** en Fase S (`09_fase_s.md`): trace de orden-de-llamada confirma 401/404/403 + scoping vía `getRSVPsByEvent` (Map, `:105-116`) precediendo a los sinks en `:140/:153` |
| PRE-2 | 🔴 | `reminder-status` devolvía nombre, email, teléfono, status y emailHistory de todos los RSVPs de un evento **sin validar sesión** | Handler sin check: `app/api/admin/reminder-status/route.ts:13-35` (pre-fix) + respuesta con datos `:72-98` (pre-fix) | ✅ **VERIFICADO Y CERRADO** en Fase S: 401 (`:22,:27`) / 404 (`:52-57`) / 403 `userHasEventAccess('viewer')` (`:60,:62`) preceden al mapeo PII en `:70` |
| PRE-3 | 🟡 | `getRSVPsByEvent(eventId)` declara `eventId` pero los callers le pasan el **slug** — funciona porque `rsvps.eventId` almacena el slug, pero la firma es engañosa y invita a bugs | `lib/queries.ts:62` vs `app/api/rsvp/route.ts:49,227` | ✅ **Confirmado y ampliado** por A1 (ítem 32 → **A1-11**) y A6 (→ **A6-04**): además de la firma mentirosa, 3 call sites con fallback `\|\| eventIdOrSlug` producen 0 resultados silenciosos con evento inexistente. Consolidado en la fila «A1-11 + A6-04»; fix → plan correctivo |
| PRE-4 | 🟢 | Build local falla sin `RESEND_API_KEY` porque `lib/resend.ts` instancia el cliente a nivel de módulo (en Vercel pasa por la env var) | `lib/resend.ts:1-9`; repro: `npm run build` sin env | ✅ **Confirmado** por A8 (ítem 2, reproducido con error exacto) y **promovido a A8-01** 🟡 (ningún MD documenta el requisito; asimetría con `lib/db.ts`). Fila propia «A8-01» en la tabla consolidada; fix → plan correctivo |

## Hallazgos Fase S (seguridad de endpoints/sesiones)

Detalle completo, evidencia y calibración de severidad en `09_fase_s.md`. Resumen (26 hallazgos post-dedup; los 4 subagentes reportaron upload-image, cron y cancel-token por triplicado — fusionados):

| ID | Sev | Descripción | Evidencia | Estado |
|----|-----|-------------|-----------|--------|
| FS-01 | 🔴 | `upload-image` POST **sin autenticación** — cualquiera sube 10MB a Vercel Blob público | `app/api/admin/upload-image/route.ts:15-60` | ✅ corregido en PR #2 (3274b56) |
| FS-02 | 🟢 | `upload-image` `eventSlug` sin sanitizar en key del blob (higiene). **Degradado 🔴→🟢 por post-review Codex:** overwrite/traversal no se sostiene (key con `Date.now()`, `allowOverwrite:false` por defecto) | `app/api/admin/upload-image/route.ts:52-59` | ✅ corregido en PR #2 (3274b56) |
| FS-03 | 🔴 | Cron `send-reminders` **fail-open** si falta `CRON_SECRET` (validación envuelta en `if(cronSecret)`) → envío masivo no autorizado | `app/api/cron/send-reminders/route.ts:27-37` | ✅ corregido en PR #3 (82a35fd) |
| FS-04 | 🔴 | **Contraseña super-admin hardcodeada** `'dave1511'` para `info@timekast.mx`, commiteada | `scripts/create-super-admin.ts:27-28` | ✅ corregido — rotación ops + código PR #2 (3274b56) |
| FS-05 | 🔴 | `CANCEL_TOKEN_SECRET` default público `'default-secret'` → cancel-tokens **forjables** (IDOR ver/editar/cancelar RSVP ajeno + PII) | `lib/queries.ts:189`; `lib/firestore.ts:187` | ⬜ plan correctivo + **verificar env en Vercel** |
| FS-06 | 🔴 | `send-email` **omite el check de permiso** con `rsvpId` inexistente (check anidado en `if(rsvp)`) + `to` body-controlado → cualquier sesión (incl. viewer) envía email a dirección arbitraria | `app/api/admin/send-email/route.ts:39-50,117` | ✅ corregido en PR #2 (3274b56) |
| FS-07 | 🟡 | Sesión `super_admin_env` sobrevive a rotación de credenciales; no revocable/auditable | `lib/auth-utils.ts:105-119`; `login:70-81` | ⬜ plan correctivo |
| FS-08 | 🟡 | Login **sin rate limiting/lockout** (agrava FS-04) | `app/api/auth/login/route.ts` | ✅ corregido en PR #3 (82a35fd) |
| FS-09 | 🟡 | `POST /api/rsvp` público sin rate limit/anti-automation/cap → abuso de cuota Resend + DB | `app/api/rsvp/route.ts:15-146` | ⬜ plan correctivo |
| FS-10 | 🟡 | Dup-check de RSVP no atómico (sin unique constraint `(email,eventId)`) → duplicados + doble email | `lib/queries.ts:28-38`; `lib/schema.ts:79-107` | ⬜ plan correctivo (cruza A2/A6) |
| FS-11 | 🟡 | Capacidad del evento nunca aplicada (columnas existen, no se consultan) | `lib/schema.ts:29-30`; `lib/queries.ts:17-57` | ✅ corregido en PR #9 (6b3991d) + PR #10 (d1b2c83) (**= A2-H02**) — trigger aplicado en prod 2026-07-10 |
| FS-12 | 🟡 | Sin anti-CSRF en mutaciones admin (solo `sameSite:lax`) | `lib/auth-utils.ts:176` | ⬜ plan correctivo |
| FS-13 | 🟡 | `og-image/[slug]` fetch server-side de URL de BD → SSRF acotado | `app/api/og-image/[slug]/route.ts:227-228` | ⬜ plan correctivo |
| FS-14 | 🟡 | `GET /admin/settings` legible por cualquier sesión (rol laxo vs POST super_admin) | `app/api/admin/settings/route.ts:23-26` | ⬜ plan correctivo |
| FS-15 | 🟡 | `PUT /admin/users/[id]` sin guard de auto-degradación / último super_admin | `app/api/admin/users/[id]/route.ts:100-111` | ⬜ plan correctivo |
| FS-16 | 🟡 | Enumeración de usuarios por status code (403 "desactivada") + timing (bcrypt solo si existe) | `app/api/auth/login/route.ts:37,46-51` | ✅ corregido en PR #3 (82a35fd) |
| FS-17 | 🟡 | Comparaciones de secretos no constant-time (cron, cancel-token, super-admin pwd) | `login:54-55`; `cron:28-29`; `queries.ts:197` | 🔶 parcial en PR #3 (82a35fd) — falta constant-time en cancel-token y login |
| FS-18 | 🟡 | `upload-image` valida MIME por `file.type` (spoofeable), no magic-bytes | `app/api/admin/upload-image/route.ts:36-41` | ✅ corregido en PR #2 (3274b56) |
| FS-21 | 🟡 | Sin invalidación de sesiones previas al login / sin "cerrar todas las sesiones" | `login:70-81`; `logout:18` | ⬜ plan correctivo |
| FS-27 | 🟡 | `update-rsvp` **mass-assignment**: `updates` del body sin allowlist → `updateRSVP` (firma `Pick` mentirosa, borrada en runtime) escribe `eventId`/`emailHistory`/`cancelToken` → manager mueve/corrompe RSVPs entre eventos. **Detectado por post-review Codex** | `app/api/admin/update-rsvp/route.ts:22-23,61`; `lib/queries.ts:90-99` | ✅ corregido en PR #2 (3274b56) (cruza A5-13 + A6-02) |
| FS-19 | 🟢 | `debug-home` GET sin auth expone config (sin PII) | `app/api/debug-home/route.ts:8-22` | ✅ corregido en PR #2 (3274b56) |
| FS-20 | 🟢 | `cleanupExpiredSessions` nunca invocada + retorno `0` falso | `lib/auth-utils.ts:148-155` | ⬜ plan correctivo |
| FS-22 | 🟢 | `name` sin escape HTML en cuerpo de email (content injection) | `app/api/rsvp/route.ts:108-116` | ⬜ plan correctivo |
| FS-23 | 🟢 | Columna `cancelToken` dead data (token aleatorio guardado nunca validado) | `lib/queries.ts:41,195-198` | 🔶 parcial en PR #8 (5b4098c) — escritura removida; la columna sigue (**cruza A1-15 + A2-H10 + A6-10**) |
| FS-24 | 🟢 | Cookie `secure` solo con `NODE_ENV==='production'` (mitigado en Vercel) | `lib/auth-utils.ts:175,195` | ⬜ plan correctivo |
| FS-25 | 🟢 | `event?.id \|\| slug` enmascara 404 como 403 (fail-closed, confuso) | `stats:32`; `rsvp:216`; `send-email:42` | ⬜ plan correctivo (**cruza PRE-3 / A1-11 + A6-04**) |
| FS-26 | 🟢 | Cancel-token sin expiración (pese a UI "expirado") | `lib/queries.ts:188-198` | ⬜ plan correctivo |

**Positivo (defensa central sólida):** scoping por evento (`slug→event.id` + `userHasEventAccess` con rol correcto) consistente en los 10 endpoints scoped; gestión de usuarios exige super_admin uniformemente; **sin IDOR clásico** ni escalada horizontal; queries 100% Drizzle parametrizado (sin SQLi); `.env*` fuera de git.

**⚠️ Acciones operacionales (NO son código, no esperan al plan correctivo):** rotar `dave1511` (FS-04); verificar `CRON_SECRET` y `CANCEL_TOKEN_SECRET` seteados en Vercel (FS-03/FS-05); `upload-image` es anónimo AHORA (FS-01/02).

## Priorización

Orden = severidad × frecuencia del flujo afectado. Los 🔴 de flujos vivos (emails indebidos, RSVP, settings que se auto-revierten) primero. La ejecución de fixes NO es de este ciclo (ver "Plan correctivo").

### P0-SEC — seguridad crítica (los 5 🔴 de Fase S, + exposiciones vivas)

Preceden en urgencia operacional incluso a los P0 funcionales por ser explotables sin ser usuario del flujo:
1. **FS-04** — contraseña de admin `dave1511` commiteada → **rotar de inmediato** (acción ops).
2. **FS-03 / FS-05** — verificar `CRON_SECRET` y `CANCEL_TOKEN_SECRET` en Vercel; si faltan, cron y cancel-tokens están abiertos AHORA. Fix de código: fail-closed.
3. **FS-01** — `upload-image` anónimo (subida pública sin sesión), explotable sin credenciales.
4. **FS-06** — envío de email a destinatario arbitrario por cualquier usuario del panel.

### P0 — críticos en flujos vivos (los 8 🔴)

1. **A1-01** — duplicado masivo de reminders: CUALQUIER guardado del config tras un reminder enviado lo re-arma (trigger cotidiano del panel; misma clase del incidente "40 vueltas").
2. **A1-02 + A6-06** — los reminders diurnos salen hasta 24h antes / "la tarde MX anterior", sistemático; la hora elegida se ignora. Agravante directo: A1-03 (P1).
3. **A3-01** — guardar settings reabre silenciosamente el RSVP cerrado (riesgo activo HOY: `40vueltas` en prod con `rsvpClosed=true`) → habilita RSVPs + confirmaciones indebidas.
4. **A2-H01 + A1-09** — `rsvpClosed` no cierra la API: RSVP + email de confirmación en evento cerrado con solo tener la pestaña abierta desde antes del cierre.
5. **A2-H03** — invitado cancelado bloqueado con mensaje falso, sin remedio self-service (flujo público roto).
6. **A2-H02** — capacidad no aplicada en ningún camino → overbooking de eventos con cupo/precio reales.
7. **A4-01** — `isEventPast` bloquea todos los envíos manuales para eventos legítimos (rompe la operación del admin, incluida la mitigación manual de A1-06).
8. **A3-02 + A6-09** — hard delete deja RSVPs huérfanos que un slug reciclado hereda (emails a quien no corresponde); mitigado hoy por ser API-only (sin UI de delete, A3-10).

### P1 — 🟡 en flujos vivos (emails, RSVP, operación del panel)

- **Cadena de reminders (agrava P0-1/2):** A1-03 (round-trip +6h), A1-04 (regresión: reminder a evento con fecha pasada), A1-06 (silencio permanente sin reintento), A1-07 (lock-first: omisiones + truncamiento ~59 emails), A8-04 (cron 2×/día fuera de spec Hobby — el schedule puede degradarse sin aviso).
- **Envíos desde admin:** A1-05 + A4-04 (send-email con body stale → dirección vieja/tipo/link rotos), A1-08 (email con datos de OTRO evento por fallback estático), A1-10 + A6-12 (recordEmailSent no atómico + 500 post-send → duplicados), A4-02 (éxitos/errores invisibles), A4-06 (ReminderStatusSection stale + "60s" falso), A2-H13 (emailHistory sin dirección → decisiones de reenvío engañosas), A1-13 (emails ignoran displayTitle).
- **Ciclo RSVP del invitado:** A2-H05 + A2-H06 + A6-11 (duplicados por capitalización/carrera → 2 confirmaciones), A2-H07 y A7-H04 (pérdida silenciosa de plusOneName), A2-H11 (cambio de email invalida el único link), A2-H04 (update/reconfirm ciegos al evento), A2-H12 (update sin validar email), A7-H01 (todo error = "evento no existe"), A7-H02 (texto invisible en /cancel), A7-H03 (modal cerrable durante submit → 409 confuso), A7-H05 (contraste no garantizado).
- **Settings/eventos:** A3-06 (full-save pisa colores que consumen los 5 senders), A3-05 (host contact ineditable → emails con contacto legacy), A3-04 (rename OG no-op con "éxito" falso), A3-09 (caché OG no invalidable desde el panel), A3-10 (isActive conflacta 3 estados, sin UI de ciclo de vida), A3-11 (OG con guion colgante en prod), A4-03 (selector restaura evento inválido), A4-08 (sesión expirada sin manejo).

### P2 — 🟡 estructurales/edge + toda la limpieza 🟢

- **Deuda estructural y edge cases (🟡):** A1-11 + A6-04 (PRE-3: firmas slug/UUID), A1-12 + A3-03 + A6-07 + A3-17 (updateEventSlug sin transacción), A1-14 + A5-16 (EventData 5×), A1-15 + A2-H10 + A6-10 (columna cancelToken basura), A5-13 + A6-02 (tipo Event mentiroso), A6-01 + A8-07 (migraciones desincronizadas), A6-05 (JSONB sin validación + casts), A6-14 (contrato de events.id), A2-H08, A2-H09, A2-H14, A2-H15, A3-07, A3-08, A4-05, A4-09, A4-07 + A5-06 (add-demo-data), A7-H06, A7-H07 (accesibilidad), A8-01 (PRE-4), A8-02, A8-03, A8-05, A8-06, A8-08, A8-09 (build/CI/docs).
- **Limpieza (los 42 🟢):** bloque A5 completo (capa Firestore muerta A5-01/02 y A5-14+A5-15+A6-03, scripts/artefactos/docs A5-03/07/08/09/10, deps A5-11/12, duplicación A5-17/18, muertos A5-19, A4-10+A5-20, A5-21, A4-11, A4-12+A6-13, A5-04/05), residuales de frontend/panel/OG/manifest (A1-16…19, A2-H16, A3-12…15, A3-16+A6-15, A4-13…16, A6-08, A6-16, A7-H08…H11, A8-10…12).

## Plan correctivo

📋 **`10_plan_correctivo.md`** — plan de remediación completo (2026-07-09), pasado por adversarial review de Codex (7 findings de secuenciación/deploy incorporados). Estructura: Fase 0 ops (rotar `dave1511`, envs Vercel, backup, branch protection) → B0 gate de CI → **B0.5 fundación de datos obligatoria** (contrato slug/id + journal de migraciones + decisión de driver, gate = diff-vacío) → Fase 2 seguridad (B3a helper → B1 endpoints → B2 cron/token → B3b sesión) → Fase 3 P0 funcional (B4–B8, con capacidad atómica y rollout de reminders con envío OFF) → Fases 4–5 P1/P2/limpieza. Regla dura de colisión: un batch por superarchivo (`lib/queries.ts`, `app/admin/page.tsx`) por ola. **Ejecución batch-por-batch, cada uno con su propio ciclo de review** — aún NO ejecutado.

## Registro de cierre

- [x] A1–A8 completadas (ver INDEX)
- [ ] Fase S ejecutada y ✅ (BLOQUEANTE)
- [x] Hallazgos consolidados y priorizados
- [ ] Plan correctivo creado y revisado
- Fecha de cierre: —
