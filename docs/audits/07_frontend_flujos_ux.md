# 🎨 A7 — Frontend, flujos y UX

> **Estado:** ✅ completada · **Owner/Sesión:** fable5-675614-a7 · **Inicio:** 2026-07-09 11:02 · **SHA auditado:** `61f74e5` (SHA de referencia del framework: `bcc7f1e`)
>
> **Protocolo:** seguir `docs/audits/00_INDEX.md` — lock atómico (marcar 🔄 en la tabla del INDEX + commit + push antes de empezar; el lock existe solo cuando el push tiene éxito), read-only sobre código de la app, reglas de evidencia anti checkbox-theater, y rúbrica de severidad 🔴/🟡/🟢. **Todo ítem requiere evidencia `archivo:línea` o comando+output, también cuando PASA.** Ítems no ejecutables → `⏭️ NOT RUN` + razón.

---

## 1. Objetivo

Verificar que el frontend **público** (el que ven los invitados reales, mayoritariamente en móvil) funciona de punta a punta sin callejones sin salida: cada fetch tiene manejo de error visible, cada página tiene estados loading/error coherentes, los textos son consistentes en español, el contraste es legible con cualquier theme que configure el organizador, la accesibilidad básica está cubierta, y las animaciones no bloquean ni degradan la interacción. Producción con invitados reales: un flujo roto = un invitado que no confirma.

## 2. Contexto mínimo (para sesión fría)

**Flujo del invitado:** recibe un link con slug (WhatsApp normalmente) → ve la invitación en `/{slug}` → abre el modal RSVP y confirma → recibe email de confirmación con un link de gestión → con ese link (`/cancel/{rsvpId}?token=...`) puede editar sus datos o cancelar/reconfirmar. El **theme JSONB por evento** (`primaryColor`, `secondaryColor`, `accentColor`, + `backgroundColor`/`textColor` en el tipo) pinta colores vía estilos inline sobre las clases CSS estáticas.

**Mapa de archivos del scope:**

| Pieza | Archivo | Notas |
|---|---|---|
| Página pública del evento | `app/[slug]/page.tsx` (311 l, client) | fetch a `/api/events/{slug}`, estados loading/error/inactivo, theme inline |
| Metadata OG del evento | `app/[slug]/layout.tsx` | `generateMetadata` server-side, imagen vía `/api/og-image/{slug}?v=5` |
| Modal RSVP | `app/components/RSVPModal.tsx` (297 l) + `RSVPModal.module.css` | POST `/api/rsvp`, PhoneInput de `react-international-phone` |
| Cancelar/editar | `app/cancel/[rsvpId]/page.tsx` (434 l) + `cancel.module.css` | 4 fetches; card blanca en CSS pero card principal re-pintada oscura inline |
| Home (redirect) | `app/page.tsx` (154 l, server) | resuelve `home_event_id` → redirect a `/{slug}` con fallbacks encadenados |
| Login | `app/login/page.tsx` (159 l) + `login.module.css` | frontera con panel admin (el panel en sí es A4) |
| Layout raíz | `app/layout.tsx` + `app/globals.css` | `lang="es"`, viewport, `overflow-x: hidden` global |
| Estilos página pública | `app/page.module.css` | clamp() para títulos, breakpoints 374/480/768/1024 |

**Trail de referencia del flujo end-to-end** (verificarlo/completarlo es el ítem E1): `app/page.tsx:121-153` (redirect por `home_event_id` con 3 niveles de fallback) → `app/[slug]/page.tsx:24` (fetch evento) → `:100-112` (gate `isActive`) → `:247-265` (gate `rsvpClosed` / botón CONFIRMAR) → `RSVPModal.tsx:55-61` (POST con `eventSlug`) → `:66-71` (éxito, autocierre 2.5s) → email (A1) contiene link a `/cancel/{rsvpId}?token=` → `app/cancel/[rsvpId]/page.tsx:74` (GET rsvp) → `:87` (GET evento — ojo: `rsvp.eventId` almacena el **slug**, gotcha del INDEX) → `:130` (update/reconfirm) / `:182` (cancel).

**Commit relevante:** `731125e` ("fix text contrast") — arregló contraste **solo dentro del form principal** de la cancel page (labels/spans a `#ffffff` sobre card oscura inline) y agregó el campo `plusOneName` al flujo de update. Revisar qué NO cubrió (ítem E12).

## 3. Scope

**Dentro:** todo el frontend público listado arriba (páginas, componentes, CSS modules, globals, layouts, metadata OG desde la óptica de UX del link compartido), más la página de login como frontera.

**Fuera:**
- Panel admin (`app/admin/**`) → **A4**.
- Lógica de las rutas API que estos fetches consumen (validaciones, respuestas, side-effects, emails) → **A2** (RSVP) / **A3** (eventos/settings) / **A1** (emails).
- Modelo de auth, tokens y protección de endpoints → **Fase S** (si aparece algo de esa naturaleza, va a "Hallazgos fuera de scope" sin profundizar).
- Código muerto/duplicado como tema sistemático → **A5** (cross-ref puntual permitida).

## 4. Checklist

### Parte A — Estática (siempre ejecutable, solo lectura de código)

| # | Verificación | Cómo verificar | Resultado | Evidencia |
|---|---|---|---|---|
| E1 | Flujo invitado end-to-end trazado paso a paso con `archivo:línea`, sin huecos ni estados inalcanzables | Seguir el trail de referencia de la sección 2 y documentarlo completo, incluyendo las 3 ramas de la página pública (loading / error / `!isActive` / `rsvpClosed`) y las 4 pantallas de la cancel page (loading / cancelled / sin-rsvp / form) | ✅ PASA con notas | Trail completo sin estados inalcanzables. **Home:** `app/page.tsx:116-154` — `getAppSetting('home_event_id')` `:121` → `getEventById` `:125` → `redirect('/{slug}')` `:128`, fallback sin slug `redirect('/{id}')` `:132`, fallback config `:137-141`, last-resort `:153`; todos los caminos terminan en `redirect()`; rethrow de `NEXT_REDIRECT` correcto `:144-147`. **Invitación** `app/[slug]/page.tsx`: fetch `:24-26`; ramas loading `:57-72`, error/`!event` `:75-97`, `!isActive` `:100-112`, render principal `:116-309`; gate `rsvpClosed` `:247-252` (mensaje) vs botón CONFIRMAR `:254-264` → abre modal `:256`. **Modal** `RSVPModal.tsx`: validación cliente +1 `:47-52` → POST `:55-61` con `eventSlug` → éxito `:65-71` (autocierre 2.5 s) / error API `:72-75` / error red `:76-78`. **Email** (A1): cancelUrl generada en `app/api/rsvp/route.ts:105`. **Cancel** `app/cancel/[rsvpId]/page.tsx`: guard `!rsvpId ⋁ !token` `:66-70` → GET rsvp `:74` → GET evento anidado `:87` (`eventId`=slug; funciona porque `getEventBySlug` también resuelve por id, `lib/queries.ts:234-240`) → 4 pantallas: loading `:207-215`, cancelled `:220-236`, sin-rsvp `:238-251`, form `:261-433`; update `:130`, cancel `:182` con `confirm()` `:174`. Notas: estado `error` con string nunca renderizado (ver E2); prop `isOpen` del modal redundante (`RSVPModal.tsx:97` — el padre ya renderiza condicional `app/[slug]/page.tsx:299`). |
| E2 | Fetch `/api/events/{slug}` (`app/[slug]/page.tsx:24-48`): fallo visible al usuario | Notar que `setError('Error al cargar el evento')` (`:32`) y `'Error de conexión'` (`:45`) se setean pero el render de error (`:75-97`) muestra SIEMPRE "Evento no encontrado" sin distinguir 404 de error de red/500 — ¿el string de error se usa en algún lado? | 🟡 FALLA parcial | Confirmado: el string de `error` NO se usa en ningún render — `app/[slug]/page.tsx:75-97` muestra texto fijo "Evento no encontrado / El evento que buscas no existe o ya no está disponible" `:80-82` para 404, 500, 503 (DB no configurada → `app/api/events/[slug]/route.ts:112-117`) y error de red `:43-45` por igual. Estados `'Error al cargar el evento'` `:32` y `'Error de conexión'` `:45` son código muerto de facto. Sin botón de reintento. → **A7-H01** |
| E3 | Fetch POST `/api/rsvp` (`RSVPModal.tsx:55-81`): fallo visible | Confirmar que error de API muestra `data.error` y error de red muestra "Error de conexión…" (`:74,78`) en `styles.errorMessage`, y que `isSubmitting` se libera en `finally` | ✅ PASA | `response.ok` chequeado `RSVPModal.tsx:65`; error API muestra `data.error ⋁ fallback` `:73-74`; error de red "Error de conexión. Por favor intenta de nuevo." `:77-78`; render en `styles.errorMessage` `:269-273` (contraste propio: `RSVPModal.module.css:302-310`); `isSubmitting` liberado en `finally` `:79-81`. Nota: el 409 de duplicado muestra el mensaje del server "Ya confirmaste tu asistencia anteriormente" — falso para cancelados (ya registrado como A2-H03, cross-ref). |
| E4 | Fetch `/api/rsvp/get` (`app/cancel/[rsvpId]/page.tsx:74`): fallo visible | Verificar ramas `data.error` (`:105`) y catch (`:108`), y la pantalla sin-rsvp (`:238-250`); notar que no valida `response.ok` antes de `.json()` | ✅ PASA con nota | Rama `data.error` `:104-105`, catch → `'Error de conexión'` `:107-108`; pantalla sin-rsvp muestra `❌ {error ⋁ 'No se encontró el RSVP'}` `:238-251`. Sin `response.ok` antes de `.json()` `:74-75` — benigno: la ruta siempre responde JSON (`app/api/rsvp/get/route.ts:12-57`) y un error de infra no-JSON lanza en `.json()` → cae al catch → mensaje visible. **Runtime (producción, GET idempotente):** `/cancel/audit-a7-nonexistent?token=x` → "❌ RSVP no encontrado" visible en rojo `rgb(220,38,38)`. Nota cross-ref: la respuesta omite `plusOneName` (`app/api/rsvp/get/route.ts:41-49`) → A2-H07. |
| E5 | Fetch `/api/events/{eventId}` anidado (`app/cancel/[rsvpId]/page.tsx:87-103`): fallo **silencioso** — evaluar consecuencias | El catch solo hace `console.error` (`:102`); con `eventData=null` el título queda "Evento", theme default, y `requirePlusOneName` queda `false` → el campo "Nombre del Acompañante" (`:375`) NO se renderiza aunque el evento lo exija. Documentar si la validación server-side (A2) lo compensa — sin profundizar, cross-ref | 🟡 FALLA | Confirmado: catch solo `console.error` `:101-103`; además la rama `success=false` deja `eventData=null` sin ningún aviso `:89-100`. Consecuencias: título "Evento"/"el evento" `:218,254`, theme default `:259`, `requirePlusOneName` efectivo `false` → campo del acompañante no se renderiza `:375-391` → el submit manda `plusOneName: ''` `:142` que el server normaliza a `null` (`app/api/rsvp/update/route.ts:42`) **sin** validar el flag del evento (`/api/rsvp/update` nunca carga el evento — ya registrado A2-H09/A2-H07, no se compensa server-side). → **A7-H04** |
| E6 | Fetch `/api/rsvp/update` (`:130`) y `/api/rsvp/cancel` (`:182`): fallo visible | Verificar mensajes en `styles.error` y que `setRsvpData(data.rsvp)` (`:152`) no pueda dejar `rsvpData` en `null`/`undefined` si la API responde `success` sin `rsvp` (la API sí lo devuelve: `app/api/rsvp/update/route.ts:54-56` — confirmar) | ✅ PASA con notas | Update: `data.success` `:149`, error visible `:158-159` y catch `:161-162` en `styles.error` (`cancel:296-300`, contraste propio `cancel.module.css:59-66`); `saving` liberado en `finally` `:163-165`. Confirmado que la API siempre incluye `rsvp` en éxito: `app/api/rsvp/update/route.ts:53-57` con `updateRSVP` que devuelve el row o lanza (`lib/queries.ts:96-102`) → `rsvpData` no puede quedar null tras éxito. Cancel: éxito `:195-196`, error `:197-198`, catch `:200-201`. Nota cross-ref: el banner post-update siempre dice "¡Asistencia reconfirmada!" (`cancel:302-307` condiciona sobre estado final) — ya registrado A2-H16. |
| E7 | Fetches de login `/api/auth/me` (`app/login/page.tsx:20`) y `/api/auth/login` (`:40`): fallo visible o silencio intencional documentado | `checkAuth` traga el error a propósito (queda en login — OK); login muestra `data.error` o "Error de conexión…" (`:51,54`) | ✅ PASA | `checkAuth`: catch vacío intencional `app/login/page.tsx:25-27` ("Not authenticated, stay on login page") + `finally` libera `checkingAuth` `:27-29` — silencio correcto: el fallback es la propia pantalla de login. Login: `data.error ⋁ 'Error al iniciar sesión'` `:50-52`, catch `'Error de conexión. Intenta de nuevo.'` `:53-54`, render visible `:80-85` (`login.module.css:97-107`), `isLoading` en `finally` `:55-57`. |
| E8 | Inventario completo de fetches del frontend público: ninguno fuera de E2–E7 | `grep -rn "fetch(" app --include="*.tsx" \| grep -v "app/admin"` y confirmar que la lista coincide (7 fetches en páginas públicas + 2 en login) | ✅ PASA | Output del grep: **8 fetches** — 6 públicos: `app/[slug]/page.tsx:24`, `RSVPModal.tsx:55`, `cancel:74`, `cancel:87`, `cancel:130`, `cancel:182`; 2 login: `app/login/page.tsx:20,40`. Todos cubiertos por E2–E7. (La cifra "7+2" del checklist era imprecisa; el inventario real es 6+2.) |
| E9 | Estados loading/empty/error por página pública | `/[slug]`: loading `:57-72`, error `:75-97`, inactivo `:100-112` · cancel: loading `:207-215`, cancelled `:220-236`, sin-rsvp `:238-250` · login: `checkingAuth` `:60-66` · home: server redirect (sin UI propia — verificar que todos los caminos terminan en `redirect()`) | ✅ PASA con cross-ref E12 | `/[slug]`: loading `app/[slug]/page.tsx:57-72`, error `:75-97`, inactivo `:100-112` ("Las inscripciones para este evento están cerradas" con el título del evento). Cancel: loading `cancel:207-215`, cancelled `:220-236`, sin-rsvp `:238-251` — pero loading y cancelled tienen texto invisible (ver E12). Login: spinner `checkingAuth` `app/login/page.tsx:60-66`. Home: los 4 caminos terminan en `redirect()` (`app/page.tsx:128,132,141,153`); el catch re-lanza `NEXT_REDIRECT` `:144-147` y ningún error escapa (todo envuelto en try `:119-149`). No existen `app/error.tsx` ni `app/not-found.tsx` custom (`ls app/`) — aceptable porque las páginas client manejan sus propios errores. |
| E10 | Home: cadena de fallbacks de redirect no lleva a un 404 de UX | `app/page.tsx:129-133` redirige a `/{event.id}` si el evento no tiene slug, y `:153` a `/{eventConfig.event.id}` como último recurso — pero `/api/events/[slug]/route.ts:69-70` solo busca **por slug** (`getEventBySlug`). ¿Esos fallbacks aterrizan en "Evento no encontrado"? Trazar y documentar | ✅ PASA (premisa del checklist incorrecta) | `getEventBySlug` NO busca solo por slug: tiene fallback por id (`lib/queries.ts:223-241`, fallback `:234-240`), y `events.id` es `text` (`lib/schema.ts:13`) → sin error de tipo. Por tanto `/{event.id}` (`app/page.tsx:132`) resuelve bien vía `/api/events/{id}`. El last-resort `/{eventConfig.event.id}` (`:153`, id `rooftop-party-andras-oct2024`, `event-config.json:3`) solo se alcanza si la DB falla o el evento default no existe; si la DB está caída el fetch de la página destino también falla → mismo estado "Evento no encontrado" de todas formas (la calidad de ese mensaje es A7-H01). Runtime: `GET https://party.timekast.mx/` → 307 a `/patchwrk-260815` (camino 1 operativo en producción). |
| E11 | Textos visibles al usuario en español consistente | Recorrer strings de las 4 páginas + modal. Puntos a revisar: dropdown de países de `react-international-phone` (nombres de país en inglés — "United States", etc.); `confirm()` nativo (`cancel/[rsvpId]/page.tsx:174`) con botones en idioma del navegador; copy divergente "¿Vienes con +1?" (`RSVPModal.tsx:235`) vs "Asistiré con acompañante (+1)" (`cancel:370`); "Email" como préstamo aceptado | 🟢 FALLA menor | Confirmados: (a) dropdown de países en inglés — `PhoneInput` sin prop `countries`/locale ES (`RSVPModal.tsx:209-221`, `cancel:343-355`; `react-international-phone@4.6.0` usa nombres ingleses por default, `package.json:28`); (b) `confirm()` nativo `cancel:174` — mensaje en español pero botones OK/Cancel en el idioma del navegador; (c) copy +1 divergente `RSVPModal.tsx:235` vs `cancel:370`; (d) gramática con fallback: "Lamentamos que no puedas asistir a {eventTitle}" con `eventTitle='el evento'` → "asistir a el evento" (`cancel:218,227-229`); (e) hardcodeados no configurables: "Nos vemos en la fiesta" (`RSVPModal.tsx:161`), "RSVP INDISPENSABLE" (`app/[slug]/page.tsx:244`) — raros en eventos no-fiesta; (f) "Email" como préstamo: consistente en modal/cancel/login ✓. → **A7-H08** |
| E12 | Contraste estático en cancel page — qué dejó fuera `731125e` | La card principal del form se pinta oscura inline con `color:#ffffff` (`cancel:262-270`), pero las pantallas **loading** (`:211 <p>Cargando...</p>`) y **cancelled** (`:226 <p>Tu asistencia ha sido cancelada...</p>`) usan `.card` blanca (`cancel.module.css:10-11`) con `<p>` sin color propio, que hereda `color:#ffffff` del body (`globals.css:26-29`) → ¿texto blanco sobre card blanca? Verificar herencia CSS y reportar | 🟡 FALLA confirmada | Sí: texto blanco sobre card blanca. `.card` define `background: white` sin `color` (`cancel.module.css:10-18`); `body` define `color: #ffffff` (`globals.css:26-29`, var `:6`). Los `<p>` sin clase heredan blanco: "Cargando..." (`cancel:211`) y "Tu asistencia ha sido cancelada exitosamente." (`cancel:226`) son **invisibles**. En la pantalla cancelled sobreviven el `<h1>` (`.card h1` color `#333`, `cancel.module.css:20-24`) y el `.subtext` (`#666`, `:314-318`). **Runtime producción:** `getComputedStyle(.card).color = rgb(255,255,255)` con `backgroundColor = rgb(255,255,255)` — probado en `/cancel/...` real. `731125e` solo pintó inline el form principal (`cancel:262-270,312,327,342,359,370,377`). → **A7-H02** |
| E13 | Contraste con themes dinámicos: colores claros sobre fondo claro | Título/subtítulo reciben `color` + `textShadow` inline del theme (`app/[slug]/page.tsx:152-155,169-172`) que **reemplazan** el text-shadow con borde negro de `page.module.css:105-121` (el `-webkit-text-stroke: 2px #000` de la clase sobrevive — confirmar); el overlay se deriva del mismo `primaryColor` (`page.tsx:134`), así que no garantiza contraste; `modalSubtitle` usa `secondaryColor` sobre fondo oscuro fijo (`RSVPModal.tsx:139`) → ilegible si el organizador elige un secundario oscuro. ¿Existe alguna validación/clamp de contraste en frontend o al guardar el theme (cross-ref A3)? | 🟡 FALLA confirmada | **Runtime producción** (evento real con theme naranja): `titleShadow` = solo glow del theme (el borde negro multi-capa de `page.module.css:105-121` fue reemplazado por el inline `page.tsx:154`), y `-webkit-text-stroke: 2px rgb(0,0,0)` sí sobrevive (`page.module.css:121,153,232`) — es la ÚNICA garantía de contraste del título/subtítulo. Overlay derivado del propio `primaryColor` con alpha `10`/`30` (`page.tsx:131-136`) → no garantiza contraste sobre imagen clara. `modalSubtitle` con `color: secondaryColor` (`RSVPModal.tsx:137-139`) sobre fondo modal oscuro fijo `rgba(26,0,51,0.98)` (`:116`) → secundario oscuro = ilegible, sin stroke ni shadow. Sin validación/clamp en frontend; al guardar, el PUT acepta `body.theme` verbatim (`app/api/events/[slug]/route.ts:222`) — cross-ref A3 (ver fuera de scope). → **A7-H05** |
| E14 | Coherencia theme dinámico vs sombras hardcodeadas | `rsvpTitle` toma `color: accentColor` inline (`page.tsx:243`) pero conserva glow dorado hardcoded (`page.module.css:230-231`); campos `theme.backgroundColor`/`textColor` existen en el tipo (`types/event.ts:35-41`) — ¿se usan en la página pública o son letra muerta? (cross-ref A5 si es lo segundo) | 🟢 FALLA menor | Confirmado: `rsvpTitle` con `color: accentColor` inline (`page.tsx:243`) conserva glow dorado hardcoded (`page.module.css:230-231`) → accent no-dorado produce glow incongruente (el stroke negro `:232` mantiene legibilidad). `theme.backgroundColor`/`textColor`: existen en el tipo (`types/event.ts:36-37`), la API los devuelve (`app/api/events/[slug]/route.ts:96-97`), pero grep en página pública/modal/cancel = 0 usos; el único uso de `backgroundColor` es el estático `eventConfig.theme.backgroundColor` en `app/layout.tsx:35` → letra muerta en frontend público, cross-ref A5. → **A7-H09**, **A7-H11** |
| E15 | Accesibilidad: labels de inputs | Modal: `htmlFor="name"/"email"/"plusOneName"` apuntan a ids reales (`RSVPModal.tsx:166-263`) ✓ pero `htmlFor="phone"` (`:206`) apunta a un input que `PhoneInput` renderiza **sin** `id="phone"` — label huérfano (mismo patrón en `cancel:342`). Login: `htmlFor` correctos (`login/page.tsx:88,105`). Documentar cada uno | 🟡 FALLA parcial | Modal: `name` `:166→:171` ✓, `email` `:186→:191` ✓, `plusOneName` `:248→:253` ✓; `htmlFor="phone"` `:206` huérfano — `PhoneInput` `:209-221` no recibe id. **Runtime producción:** input tel renderizado sin id (`phoneId: "NO-ID"`, `label[for="phone"]` sin match). Mismo patrón `cancel:342` → PhoneInput `:343-355` sin id. Checkboxes correctos por label envolvente: `RSVPModal.tsx:225-236`, `cancel:359-371`, `login:122-133`. Login `htmlFor` correctos: `email` `:88→:92`, `password` `:105→:109`. → parte de **A7-H06** |
| E16 | Accesibilidad: semántica del modal | `RSVPModal.tsx:100-125`: ¿`role="dialog"`/`aria-modal`? ¿focus trap / foco inicial? ¿cierre con Escape? ¿`aria-label` en el botón ✕ (`:119-125`)? ¿click en overlay cierra incluso con `isSubmitting=true` (`:105`) perdiendo el estado del envío en curso? | 🟡 FALLA | Sin `role="dialog"` ni `aria-modal` (`RSVPModal.tsx:100-118`; runtime: ambos "none"). Sin focus trap ni foco inicial (el componente no tiene ningún `useEffect` — solo `useState`, `:3`; runtime: `document.activeElement = BODY` tras abrir). Sin handler de Escape (0 listeners de teclado en el archivo). Botón ✕ sin `aria-label` (`:119-125`; runtime "none") — solo el carácter "✕". Overlay `onClick={onClose}` incondicional (`:105`) y ✕ no deshabilitado (`:121`) → cierran durante `isSubmitting=true`: el POST sigue en background; si tuvo éxito el invitado no ve confirmación y al reintentar recibe 409 "Ya confirmaste tu asistencia anteriormente". → **A7-H03**, **A7-H06** |
| E17 | Accesibilidad: imágenes | La página pública usa solo background-images CSS decorativas (`page.tsx:124-137`) — sin `<img>` que requiera `alt`; OG images llevan `alt` en metadata (`app/[slug]/layout.tsx:52`). Confirmar con `grep -n "<img\|<Image" app/[slug] app/components app/cancel app/login` | ✅ PASA | Grep `<img ⋁ <Image` sobre `app/[slug]`, `app/components`, `app/cancel`, `app/login` → **0 hits**. Fondo solo como background-image CSS decorativa (`app/[slug]/page.tsx:124-137`). OG image con `alt: event.title` (`app/[slug]/layout.tsx:46-53`). Emojis decorativos (🎉, ✨, 😢, 🔒) sin `aria-hidden` — menor, no bloquea. |
| E18 | Inputs con font-size ≥16px (evitar zoom automático de iOS Safari) | `RSVPModal.module.css:97` (1rem), `:153` (1rem phone), `cancel.module.css:101` (16px), `:146` (16px phone), `login.module.css:132` (1rem). Verificar TODOS los inputs, incluido el buscador del dropdown de países si existe | ✅ PASA | Modal: `.input` 1rem (`RSVPModal.module.css:95-104`), `.phoneInputField` 1rem !important (`:150-161`) — **runtime producción: 16px computados** en name/email/tel. Cancel: inputs 16px (`cancel.module.css:94-104`), phone 16px (`:143-153`). Login: 1rem (`login.module.css:125-135`); sin override de font-size raíz (`globals.css` no toca `html{font-size}`) → 1rem = 16px. Dropdown de países SIN input de búsqueda (runtime: 0 inputs dentro del dropdown; `react-international-phone@4.6.0` no renderiza search box — es type-to-jump). |
| E19 | Viewport y zoom | `app/layout.tsx:31-36`: `maximumScale: 1` bloquea pinch-zoom del invitado (WCAG 1.4.4 / usabilidad móvil) — evaluar; `themeColor` sale de `event-config.json` estático, no del theme del evento | 🟡 FALLA parcial | Confirmado `maximumScale: 1` (`app/layout.tsx:31-36`) → sin `user-scalable=no` pero maxScale 1 impide ampliar: iOS Safari lo ignora desde iOS 10 (mitigado de facto), Chrome Android lo respeta salvo el ajuste de accesibilidad → invitados con baja visión en Android no pueden hacer zoom (WCAG 1.4.4). `themeColor: eventConfig.theme.backgroundColor` estático (`:35`, `event-config.json:21` `#1a0033`) — no refleja el theme del evento activo; solo estético. → **A7-H07** |
| E20 | Responsive en código: breakpoints y unidades sospechosas | `page.module.css:298-359` (374/480/768/1024 + clamp para títulos); `html,body { overflow-x: hidden }` (`globals.css:17-18`) puede estar enmascarando overflow real — buscar anchos fijos: `grep -n "px" app/page.module.css app/components/RSVPModal.module.css` y evaluar `min-width:85px` del country selector, `max-width` de cards, modal `max-height: 90vh` (`RSVPModal.module.css:23`) con teclado en pantalla | ✅ PASA con notas | Breakpoints 374/480/768/1024 + clamp de títulos (`page.module.css:99,132,298-359`). **Runtime producción a 375×812:** `scrollWidth = 375` en invitación, con modal abierto y en cancel → el `overflow-x: hidden` global (`globals.css:15-18`) no está enmascarando overflow hoy. Anchos fijos evaluados: `min-width:85px` country selector (`RSVPModal.module.css:143`; runtime 85×36, cabe), modal `max-width:450px` (`:22`), cards `max-width:500px` (`cancel.module.css:15`), login `420px` (`login.module.css:41`) — todos con `width:100%` + padding del contenedor. `max-height:90vh` + `overflow-y:auto` (`RSVPModal.module.css:23-24`): con teclado iOS el vh no se encoge → el sistema scrollea el input enfocado; riesgo menor (ver D4). |
| E21 | framer-motion: ¿animaciones bloquean interacción o desplazan layout? | (a) CTA principal invisible ~1s: `initial opacity:0` + `delay:1` (`page.tsx:237-240`); (b) sparkles `repeat:Infinity` con `pointer-events:none` (`page.module.css:287` ✓); (c) `exit` en el motion.div de plusOneName (`RSVPModal.tsx:243-245`) sin `AnimatePresence` envolvente → exit nunca corre, y `height:0→auto` empuja el layout del form; (d) animaciones usan transform/opacity (no reflow) — confirmar | 🟢 FALLA menor | (a) Confirmado: la sección RSVP entra con `initial opacity:0, scale:0.9` + `delay:1, duration:0.6` (`page.tsx:237-241`) → el CTA (único objetivo de la página) es invisible ~1–1.6 s después del render, sumado a la carga de red; durante opacity 0 el botón ya es clickeable pero invisible. (b) Sparkles infinitos con `pointer-events:none` y `position:absolute` (`page.module.css:281-289`) ✓ no bloquean ni desplazan. (c) Confirmado: `exit` en el motion.div de plusOneName (`RSVPModal.tsx:240-247`) sin `AnimatePresence` dentro del modal (el único `AnimatePresence` envuelve al modal completo, `app/[slug]/page.tsx:298`) → exit muerto, desaparición instantánea (cosmético); `height:0→auto` empuja el form hacia abajo — reveal intencional. (d) Resto de animaciones solo opacity/transform (y, scale, rotate: `page.tsx:61-63,141-144,164-167,180-183,270-293`) — sin reflow. → **A7-H09** |
| E22 | Limpieza de efectos y timers | `setTimeout` de 2.5s que cierra el modal (`RSVPModal.tsx:67-71`) sin cleanup si se desmonta antes; `useEffect` de carga sin abort/cancel al desmontar (`page.tsx:20-54`, `cancel:65-115`) — ¿riesgo real de setState tras unmount o benigno en React 18? Documentar | ✅ PASA (benigno, documentado) | `setTimeout` 2.5 s sin cleanup (`RSVPModal.tsx:66-71`): si el invitado cierra con ✕ tras el éxito, el timer dispara `onClose()` redundante (no-op: `isModalOpen` ya false) + setStates sobre componente desmontado — en React 18 es no-op silencioso (sin warning ni leak). Fetches sin `AbortController` (`app/[slug]/page.tsx:20-54`, `cancel:65-115`): setState tras unmount igualmente benigno; carrera por cambio de `slug`/`rsvpId` en vivo no ocurre en la práctica (params estables por navegación). Deuda cosmética, sin hallazgo con impacto. |
| E23 | Elementos residuales / imports muertos en el frontend público | `notFound` importado y no usado (`app/[slug]/page.tsx:4`); `console.log` de debug en producción (`app/page.tsx:122,126,137,140,152`); ícono ⚙️ de admin en la invitación pública (`page.tsx:119-121`, `opacity:0.15`) — confirmar si es intencional ("ícono secreto", `page.module.css:12-31`) y si su touch target diminuto es deliberado. Cross-ref A5 lo que aplique | 🟢 FALLA menor | Confirmados: `notFound` importado y jamás llamado (`app/[slug]/page.tsx:4`, única aparición en el archivo — grep). `console.log/warn` en Home server en CADA request de producción (`app/page.tsx:122,126,131,137,140,152`) + mismos logs en `getAppSetting` (`lib/queries.ts:415,425`) — ruido de runtime logs, cross-ref A5. Ícono ⚙️ admin en la invitación pública (`page.tsx:119-121`) — intencional ("Ícono secreto de admin", `page.module.css:12-31`), runtime 22×29 px, opacity 0.15: visible para invitados curiosos, touch target diminuto deliberado (lleva a /admin → login; frontera A4/Fase S). → **A7-H10**, cross-ref A5. |

### Parte B — Dinámica (marcar `⏭️ NOT RUN` + razón si la sesión no tiene env vars / DB local; el repo solo trae `.env.example`)

> **Método:** sin `DATABASE_URL` local (solo existe `.env.example` — `ls .env*`) y con prohibición de efectos en producción, la parte dinámica se ejecutó **read-only contra producción** (`https://party.timekast.mx`, evento real `/patchwrk-260815`) con Chrome DevTools MCP: viewport emulado 375×812×2 (mobile, touch), solo GETs idempotentes y clicks client-side (abrir modal); **cero** submits/POSTs/creación de datos. Lo no verificable sin crear/mutar datos quedó `⏭️ NOT RUN` con su razón.

| # | Verificación | Cómo verificar | Resultado | Evidencia |
|---|---|---|---|---|
| D1 | Levantar dev server | `RESEND_API_KEY=re_dummy npm run dev` (requiere además `DATABASE_URL` válida — Neon o branch local). Registrar comando y puerto | ⏭️ NOT RUN (sustituido) | Sin `DATABASE_URL` local (`ls .env*` → solo `.env.example`) y apuntar un dev server a la DB de producción está prohibido por las reglas de esta auditoría. Sustituido por verificación runtime read-only contra producción (ver nota de método arriba); todas las mediciones D2/D3 provienen de ahí. |
| D2 | Sin scroll horizontal a 375px | Preview tools móvil 375×812 en `/{slug}` de un evento real de prueba: `document.documentElement.scrollWidth <= 375` en las 3 páginas públicas (invitación, modal abierto, cancel) | ✅ PASA (runtime producción) | Con viewport emulado 375×812: `scrollWidth = 375 = innerWidth` en (a) invitación `/patchwrk-260815`, (b) misma página con modal RSVP abierto, (c) `/cancel/<id-inexistente>?token=x` (pantalla sin-rsvp) y (d) página de error de slug inexistente. Las pantallas form/cancelled de cancel no son alcanzables sin RSVP real; su CSS acota a `max-width:500px; width:100%` (`cancel.module.css:15-16`). |
| D3 | Touch targets ≥44px | Inspeccionar bounding box de: botón ✕ del modal (CSS declara 40×40 — `RSVPModal.module.css:39-40`), checkbox +1 (con su label padded), botón CONFIRMAR, links "Volver al inicio", country selector del PhoneInput | 🟡 FALLA parcial (runtime producción) | Bounding boxes medidos a 375px: CONFIRMAR ASISTENCIA **295×80** ✓; submit del modal **291×50** ✓; checkbox +1 con label padded **291×52** ✓ (checkbox solo 24×24 pero el label entero es clickeable, `RSVPModal.module.css:203-213`). Por debajo de 44px: ✕ del modal **40×40** (`RSVPModal.module.css:39-40` — marginal), country selector **85×36** (alto), input tel **36** de alto, link "← Volver al inicio" de la página de error **132×21**, ícono admin **22×29** (secreto deliberado, E23). → **A7-H10** |
| D4 | Modal usable con teclado en pantalla | A 375px con modal abierto: enfocar cada input, verificar que el input activo queda visible (modal `max-height:90vh` + `overflow-y:auto`), que no hay zoom automático de iOS (E18), y que el dropdown de países es scrolleable y cerrable | ⏭️ NOT RUN (parcial por traza) | El teclado en pantalla de iOS/Android no es emulable vía CDP desktop. Verificado lo estático/medible: inputs 16px computados (E18, sin auto-zoom), modal `max-height:90vh` + `overflow-y:auto` (`RSVPModal.module.css:23-24`), dropdown de países renderizado dentro del modal scrolleable y sin buscador (runtime E18). La convivencia real con el teclado (vh que no se encoge) requiere dispositivo físico — pendiente fuera de esta sesión. |
| D5 | Recorrido RSVP completo en móvil | Flujo feliz: abrir invitación → modal → llenar → enviar → mensaje de éxito → autocierre. Y flujo de error: apagar red o forzar 500 y confirmar que el usuario ve un mensaje accionable (contraste con E2/E3) | ⏭️ NOT RUN | Enviar el form crearía un RSVP real en producción y dispararía email de confirmación (prohibido). Verificado hasta la frontera segura: invitación → click CONFIRMAR → modal abierto con todos los campos (runtime, sin submit). El resto queda cubierto por traza: éxito/autocierre `RSVPModal.tsx:65-71`, error visible `:72-78` (E3); el flujo de error de carga es E2 (mensaje NO accionable — A7-H01). |
| D6 | Recorrido cancel/editar en móvil | Con un RSVP de prueba y su token: cargar `/cancel/{id}?token=`, editar datos, reconfirmar desde estado cancelado, cancelar. Verificar legibilidad real de las pantallas loading y cancelled (E12) y el `confirm()` nativo | ⏭️ NOT RUN | Requiere un RSVP real con token: update/cancel mutarían datos de producción (prohibido). Cubierto por traza E4–E6 + runtime read-only de la pantalla sin-rsvp (mensaje de error visible). La legibilidad de loading/cancelled quedó **probada** sin necesidad de RSVP: computed styles en producción confirman `.card` blanca con color heredado blanco (E12 / A7-H02). `confirm()` nativo: solo verificable por código (`cancel:174`). |
| D7 | Contraste con theme adverso | Crear/editar un evento de prueba con `primaryColor`/`secondaryColor` claros (p.ej. `#FFFFFF`/`#FFFF99`) sobre imagen clara y capturar la invitación y el modal: ¿título, subtítulo y subtitle del modal legibles? (contraste con E13) | ⏭️ NOT RUN | Crear/editar eventos en producción está prohibido (y no hay DB local). El mecanismo quedó verificado en runtime sobre el evento real: el inline del theme reemplaza el text-shadow de borde negro (queda solo glow del color) y sobrevive únicamente el stroke 2px (E13) → con un theme claro sobre imagen clara la legibilidad depende de ese único stroke; `modalSubtitle` no tiene ni eso (`RSVPModal.tsx:139`). Análisis completo en E13 / A7-H05. |

## 5. Hallazgos

> Formato: `A7-XX · <severidad 🔴/🟡/🟢> · <título corto>` — descripción, evidencia `archivo:línea`, impacto en el invitado. Un hallazgo sin evidencia no cuenta.

| ID | Sev | Título | Evidencia | Detalle / impacto |
|----|-----|--------|-----------|-------------------|
| A7-H01 | 🟡 | **Todo fallo de carga del evento se presenta como "Evento no encontrado… ya no está disponible"** — 500, 503 (DB no configurada) y errores de red muestran el mismo texto afirmativamente falso, sin distinción ni botón de reintento; los strings de error específicos se setean pero jamás se renderizan (estado muerto) | `app/[slug]/page.tsx:28-35,43-45` (set) vs `:75-97` (render fijo `:80-82`); códigos de la API `app/api/events/[slug]/route.ts:108-125` | Durante un incidente transitorio (API caída, red móvil inestable — típico en browser embebido de WhatsApp), el invitado lee que el evento "no existe o ya no está disponible" y razonablemente concluye que se canceló → no reintenta, no confirma. |
| A7-H02 | 🟡 | **Texto blanco sobre card blanca en /cancel: "Cargando..." y "Tu asistencia ha sido cancelada exitosamente." invisibles** — lo que `731125e` no cubrió: pintó inline solo el form principal; las pantallas loading y cancelled usan `.card` blanca cuyos `<p>` sin clase heredan `color:#ffffff` del body | `cancel:211,226` (los `<p>`), `cancel.module.css:10-18` (card blanca sin color), `globals.css:6,26-29` (body blanco); runtime producción: `.card` computed `color:rgb(255,255,255)` sobre `background:rgb(255,255,255)` | Loading: card en blanco (breve). Cancelled: el mensaje central de la confirmación de cancelación es invisible — el invitado depende del `<h1>` "RSVP Cancelado" (que sí se ve) para saber que funcionó. |
| A7-H03 | 🟡 | **El modal se cierra con click en overlay o ✕ durante el envío en curso** — `onClick={onClose}` incondicional con `isSubmitting=true`; el POST continúa en background y si tiene éxito el invitado nunca ve la confirmación; al reintentar recibe 409 "Ya confirmaste tu asistencia anteriormente" | `RSVPModal.tsx:105` (overlay), `:119-125` (✕ sin disabled), `:55-81` (fetch no abortado); 409 en `app/api/rsvp/route.ts:178-183` | Un tap accidental fuera del modal (fácil en móvil) durante los ~1-3 s del envío deja al invitado sin feedback: quedó confirmado sin saberlo, con email de confirmación "inesperado", o reintenta y ve un error confuso. |
| A7-H04 | 🟡 | **Fallo silencioso del fetch del evento en /cancel degrada el formulario sin ningún aviso** — catch solo `console.error` (y la rama `success=false` tampoco avisa): título genérico "Evento", theme default, y `requirePlusOneName` queda `false` → el campo obligatorio del acompañante no se renderiza y el update borra `plusOneName` sin que el server lo impida | `cancel:87-103` (silencio), `:218,254,259` (degradación), `:375-391` (campo condicionado), `:142` + `app/api/rsvp/update/route.ts:42` (wipe a null) | El invitado edita p.ej. su teléfono en una carga degradada y pierde el nombre de su acompañante sin enterarse; el host pierde el dato para la logística. Server no compensa (cross-ref A2-H07/A2-H09). |
| A7-H05 | 🟡 | **Contraste no garantizado con themes dinámicos** — el `textShadow` inline del theme reemplaza el borde negro multi-capa del CSS (solo sobrevive el `-webkit-text-stroke: 2px`), el overlay se deriva del propio `primaryColor` con alpha bajo, y `modalSubtitle` pinta `secondaryColor` sobre fondo oscuro fijo sin stroke ni shadow; no existe validación/clamp de contraste ni en frontend ni al guardar el theme | `app/[slug]/page.tsx:152-155,169-172` (inline), `page.module.css:105-121,137-153` (shadow reemplazado, stroke sobrevive — runtime producción confirmado), `page.tsx:131-136` (overlay), `RSVPModal.tsx:116,139` (subtitle); PUT acepta theme verbatim `app/api/events/[slug]/route.ts:222` | Un organizador que elige un secundario oscuro deja "Necesitamos tus datos para el RSVP" ilegible; colores claros sobre imagen clara dejan título/subtítulo colgando de un único stroke de 2px. El invitado no puede leer la invitación. Validación al guardar → A3 (fuera de scope). |
| A7-H06 | 🟡 | **Accesibilidad básica del modal ausente + labels de teléfono huérfanos** — sin `role="dialog"`/`aria-modal`, sin foco inicial ni focus trap, sin cierre por Escape, ✕ sin `aria-label`; `htmlFor="phone"` apunta a un input sin id tanto en el modal como en /cancel | `RSVPModal.tsx:100-125` (semántica; runtime: role/aria/aria-label "none", activeElement=BODY), `:206,209-221` y `cancel:342,343-355` (labels huérfanos; runtime: input tel sin id) | Invitados con lector de pantalla no reciben anuncio de diálogo ni etiqueta del campo teléfono ni del botón de cierre; usuarios de teclado no pueden cerrar con Escape ni quedan enfocados dentro del modal. |
| A7-H07 | 🟡 | **`maximumScale: 1` bloquea el zoom del invitado** (WCAG 1.4.4) — iOS Safari lo ignora desde iOS 10, pero Chrome Android lo respeta salvo override de accesibilidad; además `themeColor` del navegador sale del JSON estático, no del theme del evento | `app/layout.tsx:31-36`; `event-config.json:21` | Invitados con baja visión en Android no pueden ampliar texto de la invitación (tamaños chicos en labels de 0.95rem y textos secundarios). |
| A7-H08 | 🟢 | **Español inconsistente en piezas de terceros y copy** — dropdown de países en inglés ("United States"…), `confirm()` nativo con botones del navegador, copy +1 divergente entre modal y cancel, "asistir a el evento" con el fallback (contracción "al"), "Nos vemos en la fiesta" y "RSVP INDISPENSABLE" hardcodeados para todo tipo de evento | `RSVPModal.tsx:209-221,235,161`, `cancel:174,370,218,227-229`, `app/[slug]/page.tsx:244`; `package.json:28` | Fricción menor de idioma/tono; para eventos no-fiesta (boda, corporativo) el copy fijo desentona. Sin bloqueo del flujo. |
| A7-H09 | 🟢 | **Detalles de animación/theme:** CTA principal invisible ~1–1.6 s por `delay:1` en su entrada (es EL botón de la página); `exit` muerto en el campo +1 (sin `AnimatePresence` envolvente dentro del form); glow dorado hardcoded bajo `accentColor` dinámico | `page.tsx:237-241` (CTA), `RSVPModal.tsx:240-247` + único AnimatePresence en `app/[slug]/page.tsx:298` (exit muerto), `page.tsx:243` + `page.module.css:230-231` (glow) | Percepción de lentitud en el primer paint del CTA; desaparición abrupta del campo +1; glow incongruente con themes no dorados. Cosmético. |
| A7-H10 | 🟢 | **Touch targets secundarios <44px** — ✕ del modal 40×40 (marginal), country selector 85×36, link "← Volver al inicio" 132×21, ícono admin 22×29 (deliberadamente secreto) | Runtime producción a 375px (bounding boxes); CSS: `RSVPModal.module.css:39-40,131-143`, `app/[slug]/page.tsx:84-93`, `page.module.css:12-31` | Los elementos primarios (CONFIRMAR 295×80, submit 291×50, checkbox+label 291×52) sí cumplen; los secundarios exigen precisión en móvil. |
| A7-H11 | 🟢 | **Residuales del frontend público:** import `notFound` muerto; `console.log` de debug en Home en cada request de producción (+ logs de `getAppSetting`); `theme.backgroundColor`/`textColor` letra muerta en el frontend público (el tipo y la API los cargan, nadie los pinta) | `app/[slug]/page.tsx:4`; `app/page.tsx:122,126,131,137,140,152` + `lib/queries.ts:415,425`; `types/event.ts:36-37` + `app/api/events/[slug]/route.ts:96-97` vs grep 0 usos en páginas públicas (solo `app/layout.tsx:35` con el JSON estático) | Ruido en runtime logs (retención ~1h en Hobby se llena de logs de Home) y campos de theme que el organizador puede configurar sin efecto visible. Cross-ref A5. |

**Conteo: 0🔴 · 7🟡 · 4🟢**

## 6. Hallazgos fuera de scope

> Cosas detectadas durante A7 que pertenecen a otra auditoría. Anotar factualmente + cross-ref; no profundizar ni duplicar. Candidatos probables según el checklist: validación server-side de `plusOneName` (→ A2, ver E5), validación de theme al guardarlo (→ A3, ver E13), imports/campos muertos y console.logs (→ A5, ver E14/E23), y cualquier observación sobre tokens/params de la cancel page (→ Fase S, solo anotar).

| ID | Pertenece a | Descripción | Evidencia |
|----|-------------|-------------|-----------|
| A7-OOS-01 | A2 (ya registrados) | Confirmados desde el frontend, sin re-descubrir: `GET /api/rsvp/get` omite `plusOneName` (=A2-H07), `requirePlusOneName` solo se valida en cliente (=A2-H09), `rsvpClosed`/capacidad sin gate en escritura (=A2-H01/H02), banner post-update siempre "reconfirmada" (=A2-H16), 409 con mensaje falso para cancelados (=A2-H03) | `app/api/rsvp/get/route.ts:41-49`; `app/api/rsvp/update/route.ts` (sin carga de evento); `app/api/rsvp/route.ts:53-58,178-183`; `cancel:302-307` |
| A7-OOS-02 | A3 | El PUT de eventos acepta `body.theme` verbatim sin validar shape ni colores (raíz server de A7-H05: nada impide guardar un theme ilegible o malformado que la página pública interpola directo en estilos inline) | `app/api/events/[slug]/route.ts:222` |
| A7-OOS-03 | A5 | `event-config.json` con los datos del evento de 2024 sigue vivo como fallback runtime en Home (redirect y metadata) y como `siteName` OG de todos los eventos; comparte raíz con A2-H15/A1-OOS-05 | `app/page.tsx:15,21-27,137-153`; `app/page.tsx:63` (`siteName: eventConfig.event.title`) |
| A7-OOS-04 | Fase S | Anotación factual, sin profundizar: el link de gestión viaja con `rsvpId`+`token` en query string de un GET y `/api/rsvp/get` responde datos personales con ese par; el modelo/formato del token es materia de Fase S | `app/cancel/[rsvpId]/page.tsx:74`; `app/api/rsvp/get/route.ts:39-50` |

## 7. Cierre

1. ✅ Todos los ítems E1–E23 tienen resultado + evidencia `archivo:línea` (incluida evidencia runtime read-only de producción donde aplicó); D1, D4–D7 quedaron `⏭️ NOT RUN` con razón explícita (crear/mutar datos o enviar emails en producción está prohibido; sin DB local) y D2–D3 se ejecutaron read-only contra producción.
2. Conteo de hallazgos: **0🔴 · 7🟡 · 4🟢** (A7-H01…A7-H11). El flujo feliz del invitado funciona de punta a punta (verificado por traza + runtime en el evento real de producción); los hallazgos son degradaciones de error-handling, contraste, accesibilidad y consistencia — ninguno rompe el camino principal en operación normal.
3. Actualizar la fila **A7** de `docs/audits/00_INDEX.md` a ✅ con Owner, fechas, SHA auditado y conteo 🔴/🟡/🟢 *(lo ejecuta el orquestador — esta sesión es read-only fuera de este archivo)*.
4. Commit + push: `audit: A7 frontend-ux — 0🔴 7🟡 4🟢` *(lo ejecuta el orquestador)*.
